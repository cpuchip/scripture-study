# Local Vite dev-server (npm run dev) talks to the compose stack at 8083.
# Production builds (npm run build) pick up .env.production or fall back to
# the JS-side default of `/api`.
VITE_API_BASE_URL=http://localhost:8083/api
