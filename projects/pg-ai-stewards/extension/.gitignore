# pgrx + Rust artifacts
target/
**/*.rs.bk
Cargo.lock

# pgrx-managed Postgres data (when running outside Docker)
.pgrx/

# Local secrets (provider API keys for Ollama / LM Studio / OpenCode Go etc.)
# .env.example is the committed shape reference; .env holds real values.
.env
.env.local

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
