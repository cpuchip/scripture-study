# pgrx + Rust artifacts
target/
**/*.rs.bk
Cargo.lock

# pgrx-managed Postgres data (when running outside Docker)
.pgrx/

# Local secrets (provider API keys for Ollama / LM Studio / OpenCode Go etc.)
# .env.example is the committed shape reference; .env (and any variant) holds real values.
.env
.env.*
!.env.example
!.env.*.example

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
