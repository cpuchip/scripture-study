---
date: 2026-05-29
title: 1828 UX 1-2 punch — always-visible unified search (the neglected thread)
workstream: WS7
session_type: ux-design + dev
status: shipped local + browser-verified; production deploy is Michael's Dokploy action
---

# 1828 UX punch

## Context — set substrate down, picked up the neglected thread

After closing the pg-ai-stewards arc (J.8–J.12 + docs + competitive research),
Michael set the substrate down and picked up **1828**, the one ratified cycle
thread untouched since the 2026-05-23 Sabbath. (The teaching thread had advanced
hugely via the scripture-book this week; substrate ate a lot of calendar — so
1828 was the gap. Reconciled the book-as-teaching framing first: the book isn't
a 4th thread, it's the teaching thread in paperback form, which the Sabbath
itself named as "the harvest.")

Within 1828-finish, three sub-items: Phase 6 Thummim cache sync (depends on the
just-set-down substrate), webster-v2 MCP (biggest, fresh build), UX 1-2 punch
(self-contained). Chose the UX punch — right size, no substrate re-open.

## Design pass (it was flagged "with the ux agent")

Grounded in the actual frontend first. Diagnosis: search was a *destination*
(buried in /word + /verse views), not a tool; the three modes (study/explore/
present) are invisible/emergent. Ratified: unified smart search input (not a
Cmd-K palette, not a scope toggle), modes implicit + connective actions later,
ship the search bar now.

## Shipped + browser-verified

`SearchBar.vue` in the header (hidden on /present), intent detection: reference
→ Verse Explorer canon params; word → word-detail. `/` and Cmd/Ctrl-K focus.
Neighboring fix: VerseExplorer's query-watch now re-fetches the passage (guarded
by `loadedKey`) — fixes same-page ref search AND a latent back/forward bug.

Verified in a real browser per 1828's rule (rebuilt frontend container vs. live
backend, Playwright): "1 ne 3:7" → dropdown → Enter → correct URL → verse
loaded; "charity" → "Look up" suggestion. Build clean.

Commit `3c7b5c8`. Spec: `projects/1828-illuminated/.spec/proposals/ux-1-2-punch.md`.

## Carry-forward (1828 finish, still open)

- **Full-text scripture search** — backend `/api/scripture/search` exists, no
  frontend results surface; the unified search handles ref+word, free-text
  falls through to word. Next build.
- **Connective actions (D-UX2)** — "study this" / "present this" on verses/words.
- **Phase 6 Thummim cache sync** — re-opens the substrate; do when bridging them.
- **webster-v2 MCP server** — the biggest remaining 1828 item; fresh build.
- **Production deploy** — Michael's Dokploy action (1828 is part of the workspace
  repo; no auto-deploy on commit). Local verification is done.

## Note on pace

This capped an enormous session (J.8 → J.12, models catalog, 3 projects, Opus
4.8 harness review, gemini enablement + spend cap + budget surfacing, docs pass,
competitive research, and this). The UX punch was deliberately the bounded,
self-contained pick for the back half of a long day — but the day was long. The
Sabbath's "less but better" question is still the live one.
