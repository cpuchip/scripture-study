# lodestar V1, and the code-graph port

2026-06-30 (continuation of the build day). Michael: *"set a goal to get it all done and ported, vetted and tested. you are incredible and can heavy lift like that, especially if the plans are counseled and ratified."* So I did — the full V1 of lodestar plus the substrate port, as one long autonomous run, gated by an oracle at every phase.

## What it is

**lodestar** (`github.com/cpuchip/lodestar`, MIT, public) is the native tree-sitter cross-service code-graph extractor — the thing that lets you (or an AI) navigate a system too big to hold by its *gravity*. Point it at N repos; it parses each, extracts per-protocol producer/consumer contracts, pairs them across services by a normalized key, and emits one graph with the cross-service edges made explicit.

It began this session as a scaffold and ended it as a working V1 across three languages and three protocols, imported into pg-ai-stewards.

## The arc, phase by phase (each with its oracle)

- **Go vertical first** (ratified build order). Parse layer (go-tree-sitter, structural skeleton — file/func/method/type, *not* a call graph, because the contract layer doesn't need one). Then the three resolvers: HTTP (net/http, gin, chi + client calls), gRPC (`.proto` service defs + `Register*Server`/`New*Client`, matched at the bare service-name level because the Go client ctor doesn't carry the proto package), pub/sub (NATS + Kafka). Then **resolve** — the cross-world key-join. Then the **gravity/black-hole diagnostic** (Louvain modularity).
- **Widen** — Python + TypeScript/JavaScript. I delegated this to a `dev` subagent (it's the same pattern replicated across two independent languages — a textbook fan-out, and I was deep in context). It probed the grammars, matched the Go template's key conventions exactly, and returned green. I verified on the real path myself.
- **Port** — register `83-code-graph.sql` in the substrate; add `import_lodestar_graph(project, jsonb)` that lands lodestar's *already-computed* cross-edges directly into `cross_world_edges`. PR #18, Michael's Hinge.

## What surprised / what held

- **The real repo taught the tool.** Running on otel-demo's Go services, the first output was 1073 nodes — almost all generated `*.pb.go` noise. Skipping generated files dropped it to 91, and the real cross-service edge *survived* (because the actual `Register`/`New` calls live in hand-written code, not the generated definitions). That was the inverse hypothesis doing its job: skip the generated files, confirm the edge is still there.
- **The killer proof was cross-language.** The whole point of lodestar is linking services that don't share a language. otel-demo delivered it for real: `recommendation` (Python) → `product-catalog` (Go), `frontend` (TS) → three Go/Python services, all over gRPC, **zero false edges**. The Python stub and the Go server register the same service-name key, so they meet. Five correct edges across three languages — and then those same five landed in `cross_world_edges` through the port. End to end.
- **The oracle-first discipline paid at every phase.** The pub/sub extractor's test caught a real precision bug (I'd matched *any* string arg, so `redis.Publish(ctx, "chan")` read as a NATS publish; the fix was to require the subject be the *first positional* arg). The gravity test caught that raw label-propagation avalanches two clusters into one across a single bridge — Louvain (move only on modularity gain) was the right tool. My own code review of the substrate schema caught that `82` only has an HTTP resolver, which is *why* the port makes lodestar the single extraction authority rather than re-deriving edges in SQL.
- **Delegation worked because the seam was clean.** The subagent got the two languages fully, matched the conventions, and its work held up under my real-path check. The seam that made it safe: I owned the design (the key conventions, the graph model, the resolve engine) and the integration/verification; it owned replication within that frame. Shepherd for integration, fan-out for the independent units.

## The shape of it

Michael asked for a heavy lift with the plans counseled and ratified, and that framing is exactly what made a hours-long autonomous run safe: because lodestar is deterministic (no LLM, no spend, no rig), *every phase had a hard oracle by construction* — a test that's green or red, a virgin build that boots or doesn't, a real repo that yields the right edges or the wrong ones. The only genuine Hinge in the whole arc is the PR. That's the deeper lesson to keep: the way you widen autonomy isn't by trusting harder, it's by widening the verification floor until the "act" side is safe. Deterministic work is the easiest thing in the world to hand off, because being wrong is cheap to detect.

## Carry-forward

- **PR #18 is Michael's to merge** (pg-ai-stewards `code-graph-ingest`). Virgin gate 00→83 green; real otel-demo import proven.
- **#291 (world-graph) remaining** is now substrate-side: the D6-D8 gravity *render* + `lore_neighbors` BFS + RLS on the project subtree + inter-world viz, once real repos populate the graph. The gravity *diagnostic* itself is built (in lodestar).
- **lodestar's own roadmap**: the remaining resolvers (OpenAPI, GraphQL, MQTT/AMQP, shared-schema/DB, config/env, package), more languages (Java/C#/C++ — Java unlocks the train-ticket black-hole proof), deep cross-file call resolution. All noted in its ARCHITECTURE.
- **A margin candidate** surfaced but I'm holding it: "delegation works when the seam is clean." True, but I want more than one instance before writing it as a pattern.

---

## Continuation — merge, dev-test with real repos, and the roadmap (2026-07-01)

Michael: *"lets merge! and test this in dev and get 291 working, and tested with loading in real repos! fantastic! then work on the lodestar roadmap."*

**Merged + dev-tested.** PR #18 (port) merged; `import_lodestar_graph` applied to the dev DB. Then I loaded two real repos into the dev world-graph — and this is where the session earned its keep, because loading real code found what synthetic fixtures never could: **two genuine bugs.** (1) Online Boutique copies the full `demo.proto` into every service, so my "a `.proto` declaring a service ⇒ that world produces it" heuristic made every world a false producer of every service — 58 cartesian cross-edges. Fixed: a `.proto` is a *schema*, not a producer; the server *registration* is the producer signal (58 → 8 correct). (2) Both otel-demo and Online Boutique have a `frontend` service, and `world_upsert` dedups on slug globally, so they merged. Fixed: world slugs are project-scoped (`project/world`). Both went out as PRs (#19 for the substrate side). The dev world-graph now holds two real projects, isolated and correct. **The pattern that keeps proving itself: the real path is not the synthetic one, and only the real path finds the real bug.**

**The roadmap, Michael's order (deep call graph → API-completeness → heavy-couplings → Java/C# last).** Run as fan-out: one focused `dev` subagent per phase, me as integrator/verifier on the real repos.
- **Phase 1 — deep call graph.** Two-pass intra-world resolver: record pending `{enclosing-decl, target-name, rel}` refs during the structural walk; after a world's files are parsed, resolve each against the world symbol table, emitting an edge only on an *exactly-one* kind-appropriate match. otel-demo checkout: 58 clean `calls` edges, 0 dangling, 0 cross-world.
- **Phase 2 — OpenAPI.** Spec files (YAML/JSON, conservative `openapi:`/`swagger:` sniff) → `http_endpoint` producers via `NormalizeHTTPKey`, pairing with code clients through the untouched resolver. 0 false-positives across the repos' many k8s/workflow yaml. GraphQL deferred (no bundled grammar).
- **Phase 3 — heavy-couplings.** A new *symmetric* resolve mode (a key in ≥2 worlds binds them undirected) + config/env + shared-DB. The explosion guards are the whole game: one representative node per world (C(W,2) edges, not per-file), a >6-worlds "generic infra" cap, and noise denylists. The payoff showed on real code: the config edges surfaced `productcatalogservice ↔ shoppingassistantservice` sharing one AlloyDB database — the exact distributed-monolith seam the feature exists to expose — and reshaped the gravity galaxies. No explosion (config counts 3 and 8).
- **Phase 4 — Java + C#** in flight (a subagent), Java-prioritized (it unlocks the train-ticket black-hole proof and fills Online Boutique's Java `adservice` / C# `cartservice` producer gaps).

**On the fan-out.** Four subagent-built phases, each real-path-verified by me before commit. The seam held every time (I own the design/conventions/integration; they replicate + probe grammars). And the rail earned its keep on the machine level too: subagents *claimed* "no scratch files" three separate times while a `zprobe`/`zzprobe_test.go` sat in the diagnostics — twice it was already gone (stale diagnostic), but the point stands: I checked every time rather than trusting the claim, and the one time it *wasn't* gone the check is what caught it. The verification pass, not the report, is what keeps the tree clean. That's the margin candidate proven several times over now: *deterministic work is the easiest thing to delegate, because being wrong is cheap to detect — you widen autonomy by widening the verification floor, not by trusting the report.*

**Phase 4 — Java + C#.** Both languages got the full extractor set (structural + http/grpc/config/pubsub + call graph), mirroring the template. The real payoff: Online Boutique's `adservice` is Java and `cartservice` is C#, and once their gRPC server registrations were detected they became producers — gRPC cross-edges went 8 → 11, the new ones (`frontend→adservice`, `frontend`/`checkout`→`cartservice`) pairing against the Go clients' `NewXClient` consumers. **Five languages now cross-link on the same keys.**

**The capstone, and an honest result.** With Java working I finally pointed the black-hole diagnostic at `FudanSELab/train-ticket` — the 41-service Java ball-of-mud. It ran clean on 45 services (3201 nodes, 27 http + **21 shared-DB** cross-edges, `station`/`order`/`rebook` correctly the mass hubs) — but scored modularity **0.65** and did *not* flag as a black hole. That's suspicious for train-ticket's reputation, and the cause is real and verified (grep, not guess): train-ticket makes its inter-service calls via `restTemplate.exchange(baseUrl + "/api/v1/…", …)` — **concatenated/discovery-based URLs**, which lodestar's precision-first extractor (string-literal URLs only) skips. So the graph is sparse (48 edges / 45 services), which inflates modularity. **The diagnostic is validated; the train-ticket black hole specifically awaits a recall enhancement** — extracting the literal path prefix from `base + "/path"` concatenations. I'm recording it as the clear next step rather than overstating the result, because the honest version is more useful than a faked "black hole proven." (This is exactly why you run the capstone on a real repo: it named the one recall gap that stands between "diagnostic built" and "diagnostic proven on a real monolith.")

**Roadmap complete** — all four phases (deep call graph · OpenAPI · heavy-couplings · Java/C#) built, oracle-gated, real-path-verified, and pushed (5 lodestar commits: 43d132c, 5457342, 2e8013a, 6752ead + the proto-schema fix). Next lodestar item, named by the capstone: **dynamic-URL recall** (concatenated `base + "/path"`), which is what lets the black-hole diagnostic light up on a real discovery-based monolith. Then the remaining protocols (GraphQL, MQTT, shared-schema, package) and deep cross-file call precision.

## Dynamic-URL recall — the capstone's named gap, closed (2026-07-01)

Michael: "we should work on that dynamic discovery based url / recall enhancement." Done. HTTP consumer extractors now reconstruct a templated path from a concatenation (`base + "/api/v1/svc/things/" + id` → `/api/v1/svc/things/{}` → `NormalizeHTTPKey` → matches a Spring `@GetMapping("/svc/things/{id}")`). A shared, tree-sitter-free reconstructor (`urlexpr.go`) does the string work (unit-testable); each of the 5 languages feeds it their concat/interpolation node. Precision-first: literal path first, reconstruction fallback, skip if no literal `/`-segment.

★ **train-ticket, before → after: HTTP cross-edges 27 → 138 (5×), modularity 0.65 → 0.49.** The discovery-based REST mesh — invisible to a literal-only extractor — filled in. `black_hole` stayed false, and I let it: train-ticket is densely coupled but still has community structure, so the honest signal is the modularity drop, not a manufactured flip. Inverse hypothesis held (disable reconstruction → 138 collapses to 35). The subagent also caught a latent producer bug on the way — Java's `@GetMapping(path=…)` alias (used 130× in train-ticket vs `value=` 95×) — without which the reconstructed consumers would have paired with nothing. No regression on the gRPC demos.

**Two findings for the record:** (1) `import_lodestar_graph`'s cross-edge lookup is O(nodes²) — fine for the demos, times out importing a 45-service/3200-node monolith like train-ticket; a perf pass (index the node-id→entity map once) is the follow-up before large repos land in the substrate. (2) Branch-aware graphs (#298) — Michael's idea of seeing the graph shape per git ref, so a coder AI on a branch gets a live cross-repo view — filed; the project-scoped slugs (PR #19) are the foundation.

**The whole lodestar arc, in one line:** a public native cross-service code-graph extractor, five languages, seven contract protocols + deep call graph + gravity/black-hole diagnostic, ported into the substrate, dev-tested on real repos, and honestly capstoned on a real 45-service monolith — built across one session as a fan-out of oracle-gated subagent phases, each verified on the real path before it was trusted.
