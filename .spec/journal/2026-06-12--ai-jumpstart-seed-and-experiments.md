# ai-jumpstart — the book's companion kit, built and cold-tested (2026-06-12)

Michael's morning council ("what have we learned, Charlie Brown? do we practice the
book?") became a build commission by noon: a model-agnostic starter kit any reader can
point their AI at, so the book's practices install themselves. He granted gh repo
creation (public, MIT) and bold testing ("you could even test it out!").

## Shipped

- **github.com/cpuchip/ai-jumpstart** (public, MIT) — name picked from his list
  (jumpstart = cables on a dead battery, then it runs on its own). Cloned to
  `projects/ai-jumpstart/`.
- **The kit:** `AGENTS.md` (first-session setup + standing disciplines; lean enough to
  sit in any model's context), `PRACTICES.md` (the nine + coda as checklists),
  `CYCLE.md` (the eleven steps, practice-forward, honest provenance), `templates/`
  (intent, covenant, journal-entry, active). v0.1 `7dee643`.
- **Cold-model experiments** (`experiments/ai-jumpstart/`, findings.md): role-played
  the human with the voice profile. Sonnet (2 turns): PASSED the core gate — asked six
  questions, built the four working files with checkable intent criteria, journaled
  unprompted, no premature code; drift = assumed the user was "Michael" from the book
  credit. Gemini (1 turn): asked four sharp questions + volunteered bounds; drift =
  pre-proposed "premium micro-animations" to a human who said "nothing fancy."
- **v0.1.1** (`382784f`): both findings folded into the seed ("ask, then stop";
  "stated constraints are bounds, not modesty to upgrade"; ask names, don't assume).
  Plus a leaked hook-cache cleanup (`f1ecf4b`).

## Also this morning

- The Ben-Test scorecard of our own practice (answer: ~8/10 genuinely practiced; the
  two soft spots — retro cadence, Sabbath — are exactly the book's own confessions).
- The Part-3 candidate list (lanes, verify-the-edition, hand-off-the-night, council
  the new model in, voice-with-evidence, critic-at-every-gate) — counseled to DEFER to
  a second edition/companion; the book stays closed.
- Voice profile sharpened with Michael's keyboard insight (no em-dash key; his
  connectives are `-`, `:`, `…`, `()`).

## Carry forward

- Experiments queued: Gemini turn-2 continuation; a true cold room OUTSIDE the
  workspace (MCP config leaked in as `becoming-mcp.log`); a GPT-class run; the
  substrate/chattercode Docker path; the session-2 memory-read test (the real
  portability proof).
- Michael's gates pending: the book's appendix/QR pointing readers at the kit (his
  call, book repo); P6 seven-vs-eight games; optional P2 Oct-5 counsel enrichment;
  voice-corpora commit decision.
- 18GB old-repo sort still queued (forkirk arrived nested under old-code/astrotreks/).
