---
date: 2026-06-08
title: §7 finished — persona/room facets + working tags; CT2 self-context complete
workstream: pg-ai-stewards
mode: dev
tags: [ct2, section7, durable-memory, working-tags, persona-host, complete]
---

# §7 core finished

Michael: "Lets finish out 7." Did 7c (persona/room facets) + 7d (working tags),
verified, on top of the morning's 7a/7a2/7b. CT2 self-context-management core
(§§1–6 + §7 core) is now complete.

## 7c — persona + room facets (the headline case)

- SQL (`ct2-7c-...`, 12a0b47): `session_facets` map + `set_session_facets()`;
  `dispatch_facets` merges persona+room; remember/forget defaults + owner are now
  persona-aware (persona > agent_family > session) — a chat persona's notes
  default to `{persona:self}`, not the too-coarse `{agent_family:persona}` shared
  by all roleplay personas.
- persona-host (Go): `Cognition.SetSessionFacets` + a best-effort call in
  `takeTurn` after a session is established. Rebuilt + reconnected to every room.
- Verified (SQL): a `{persona:starlet}` + `{room:10-forward}` note routes to
  Starlet but not the librarian's note or another room's; remember defaults to
  `{persona:starlet}`. The live `set_session_facets` write fires on the next real
  persona turn (organic).

## 7d — working tags (§7.4)

- `messages.context_tags[]` + `sessions.working_tag` + a stamp trigger
  (auto-tags new messages with the session's active tag — set once, keep working).
- `context_set_tag`/`clear_tag` + batch `context_fold/mute/expand/pin_tag` (one
  circuit-breaker event — the whole tagged set shares one cooldown, so a sweep
  isn't penalized as thrash). Pressure line echoes the working tag.
- Verified: set_tag → new messages auto-stamped; fold_tag → both compressed +
  locked together, the untagged message untouched; expand → restored; clear →
  stamping stops.

## Capstone

With all of §7 applied and nothing active (no notes, no tag, tools-off),
`compose_messages` is byte-identical to baseline (c21b449e). The whole stack is
inert until an agent opts in — the §6 safety property, end to end. Ledger clean
(222 files, 0 drift); soak resumed.

## What §7 gives us

Any substrate agent with `context_tools_enabled` can now keep durable, audience-
routed working knowledge (remember/forget across global/session/agent_family/
kind/pipeline/persona/room), fold/mute/pin individual messages or whole tagged
tasks, and see its context pressure + working tag — all backward-compatible and
gated. The Hermes self-curation loop, on Postgres. This is also the natural
substrate for the **Jarvis** idea Michael raised (pg-ai-stewards as the brain +
durable memory; an audio HMI front-end) — a durable, faceted memory the assistant
curates is exactly what a long-lived personal assistant needs.

## Carry-forward
- §7.3 self-editable base prompt: task #138, its own gated pass (propose→ratify,
  off by default).
- CT2.4 A/B (task #136): does agent-driven context management earn its keep.
- Organic: confirm the live session_facets write on the next persona turn.
- Root unpushed (Michael pushes): the §7 commits + memory.
- NEXT CONVERSATION: the Jarvis plan (pg-ai-stewards DB/brain + audio HMI).
