# Becoming Server Configuration
# Copy this file to .env and fill in real values
# .env is gitignored — never commit real secrets

# Database path (SQLite file) or PostgreSQL connection string
# SQLite:     BECOMING_DB=./becoming.db
# PostgreSQL: BECOMING_DB=postgres://becoming:secret@localhost:5432/becoming?sslmode=disable
BECOMING_DB=./becoming.db

# Server port
BECOMING_PORT=8080

# Session secret for cookie signing
# Generate with: openssl rand -hex 32
BECOMING_SESSION_SECRET=change-me-to-a-random-string

# Base URL for the app (used for OAuth callbacks and CORS)
BECOMING_BASE_URL=http://localhost:8080

# Google OAuth (optional — leave empty to disable Google sign-in)
# Set up at https://console.cloud.google.com/
# Add authorized redirect URI: {BASE_URL}/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback

# TLS (optional — for local HTTPS with Google OAuth)
# Generate certs: mkcert localhost 127.0.0.1 ::1
# Or use start-ssl.ps1 which handles this automatically
# TLS_CERT=.certs/localhost.pem
# TLS_KEY=.certs/localhost-key.pem

# Path to scripture files for memorization references
# BECOMING_SCRIPTURES=../../gospel-library/eng/scriptures

# API token for MCP server / external tools
# Generated from the app's Profile > API Tokens page
# BECOMING_API_TOKEN=bec_your_token_here
