---
lane: pg-ai-stewards
session_id: 7ea7faa4-688a-451a-ac68-b7ea662d4b81
status: active
started: 2026-06-11T22:00:16
last_active: 2026-06-27T11:05:21
---

## Working on (latest)
- **★★ 2026-06-26 PM(14) — A2A / OPEN ENGINE PHASE 1 BUILT, virgin-smoke-GREEN, LIVE-PROVEN over MCP *and* HTTP → PR #12 (Michael's merge-Hinge).** "Pedal to the metal" — and it went smoothly because the substrate was already ~80% of the engine. **`69-a2a-engine.sql`:** `a2a_agents` registry (generalizes lanes; identity + capabilities + delivery + D&C-121 scope wall) · `agent_notes` (the migrated NOTES inbox — the `.mind/sessions` v0, in the DB) · work_items `+= a2a_assignee/a2a_owner/a2a_question` + `origin='a2a'` · the inert **`a2a-handoff`** holding pipeline (never bgworker-dispatched; waits for its claim) · verbs **`a2a_register/submit/inbox/claim/needs_input/answer/receipt/note/note_clear`** (the escalation claim-lock generalized; 7 model-facing as tool_defs, `a2a_*` broadcast to all non-watchman) · the **drive-the-engine** core skill. **Surfaces:** MCP tools (`cmd/stewards-mcp/a2a.go`, with `A2A_MIRROR_DIR` best-effort `.mind/sessions` file-fallback) **+ REST mirror** (`cmd/stewards-ui/api/a2a.go`, `/api/a2a/*`) so agy can drive it via curl. **+ Claude skill** `.claude/skills/drive-the-engine/`. **Oracle:** `tests/virgin-smoke.sql` **OK 58** — the WHOLE loop on a virgin boot (register→submit→inbox→claim[atomic lock]→needs_input→answer→receipt→done + notes); **chain 00→69 green on a fresh image.** **Live-proven** on the dev substrate over BOTH surfaces, end to end, zero copy-paste (2nd claim → claimed:false; owner gets the receipt). **★ the live-apply caught a real bug the consolidated-`04` read alone missed:** `work_items.intent_id` is NOT NULL (added by `09`) → my direct INSERT failed → fixed by resolving the configured `default_intent_slug` intent like `work_item_create` does (+ dropped the stale 6-arg overload so re-apply leaves one signature). `go build`+`vet` clean (UI+MCP); MCP server registers all A2A tools without panic. Branch `a2a-open-engine`; spec status bumped (Phase-1 BUILT); journal `2026-06-26-a2a-open-engine-phase-1.md`; active.md line added. **Root skill committed, NOT pushed (Michael pushes root).** **NEXT:** the say-hello handshake with agy (first real cross-agent drive — register agy, submit, let it claim+receipt; needs agy reachable via MCP or `/api/a2a`) → Phase 2 (Agent Card + JSON-RPC + token auth) → Phase 3 (external + reachable deploy). **CI on PR #12 running the same virgin-smoke I ran locally — confirm terminal-green before calling it landed.**
- **★★ 2026-06-26 PM(13) — A2A / "OPEN ENGINE" SPEC WRITTEN + COUNCILLED + RATIFIED (`.spec/proposals/a2a-open-engine.md`).** Michael's new arc: make pg-ai-stewards the engine ALL his agents link to — Claude sessions + agy + personas + garrison + (later) other people's agents — so the human stops being "the hallway." Sparked by Nate B Jones's "Open Engine" video (`QSK4vf_ZTRA`, full transcript in `yt/`) + Google's A2A standard (Agent Card at `/.well-known/agent-card.json` + stateful Task lifecycle + cross-org credential trust; complements MCP [vertical tool] with horizontal agent↔agent). **★ THE REFRAME: pg-ai-stewards already IS Open Engine — the engine, not Nate's Linear-bolt-on.** Mapping near-1:1; **the claim-lock already exists** = `work_items.escalation_claimed_by` + `work_item_escalation_claim/resolve` (generalize "stronger model rescues a stuck task" → "any registered agent claims an assigned task"). **The `.mind/sessions/` inbox was our v0 A2A** (async/pull/persistent — "leave notes while you're busy, pick up when ready"); this generalizes what worked. ONE new primitive = the **externally-executed work_item** (assigned-not-dispatched; waits for an external agent to claim→work-in-its-env→post artifact+receipt). **RATIFIED (council):** v1 = MY AGENTS first (me+agy+personas, local; external+deploy = Phase 3) · **build in OSS CORE** (registry seeds/tokens in overlay) · **reuse work_items** (one system of record) · native-first (A2A Agent-Card+JSON-RPC wrapper = Phase 2) · **lane = agent identity** · **inbox MIGRATES to substrate as TWO panes — notes + todos — with a FILE FALLBACK** (substrate=truth, mirror-write to `.mind/sessions/` so a substrate-down agent reads stale-but-functional files). Governance = the scope IS the D&C 121 wall (invited laborers under walls = Zion). **NEXT = Phase 1 build:** agent registry (generalizes lanes) + the assigned work_item + `a2a_submit/inbox/claim/needs_input/receipt` (MCP + minimal `/api/a2a`) + the "drive-the-engine" skill + Nate's say-hello smoke test → **acceptance = my session writes a task, agy claims+does+receipts it, I see it done, ZERO copy-paste.** Spec has full provenance + the council §3 tensions (ephemeral-sessions-don't-heartbeat → pull-on-engage; simplicity-is-the-feature).
- **★ 2026-06-26 PM(12) — Rigor-on-the-doc-builder EXPERIMENT (honest null result) + model-fallback HARDENING (shipped).** Michael: "does rigor build a document up piece by piece like our studies?" Finding: **rigor is CHAT-ONLY** — it reaches an agent only via the chat's per-session toggle (autoloaded nowhere); a delegated doc-build runs WITHOUT it. The doc builder DOES build piece-by-piece (research-write = context_gather→gather→**build**→critique; the `research` agent + doc_create/append_section/patch/finalize) — study-shaped. Wired rigor in (autoload research-rigor onto `research`) + ran a controlled **before/after** over vivint (research-write, same billing question). **★ HONEST NULL RESULT (Ben Test):** BOTH docs strong + heavily cited — rigor-OFF actually had MORE inline citations (16 vs 8); rigor-ON's only clear edge = explicitly correcting the fee-vs-loan framing ("not a fee, it's a loan payoff"). The `research` builder is ALREADY grounded (cites by default) + qwen is capable → rigor's marginal value on the strong research pipeline is SMALL (opposite of the CHAT, where rigor-off fabricates). **DECISION: do NOT ship always-on rigor on the doc builder** (cost > marginal gain); reverted the dev autoload; if ever wanted on docs = carry-through (rigor only when the chat asked). Experiment earned its keep by proving the builder didn't need it. **★★ MODEL-FALLBACK HARDENING SHIPPED (`5bbb3da`, PR #11 5th commit, `68-model-fallback-hardening.sql`) — Michael's explicit ask after gemma went offline for games:** `diagnose_failure` (re-authors 32) learns the pulled-model 404 ("no slot serves model" / "model not found") → **transient** → the existing alias failover (steward_tick) walks to a live member instead of HARD-FAILING (the gemma-404 that killed his Star Trek pipeline-gather); + the local MoE pair are **mutual fallbacks** (gemma↔qwen on ingest/research-local/reason/critic, local-before-paid). Proven: the exact gemma-404 now classifies transient (was 'unknown'); genuine tool errors still tool_error. **virgin-smoke OK 57; fresh-image 00→68 GREEN.** (Exposed `ingest` primary was gemma pri-0 = the hidden 404 source.) **★★★ PR #11 MERGED to main (Michael "lets push this up"; rebase → `277718c`→`2673e3e`, 5 commits preserved, branch deleted, local synced; **main CI GREEN after a re-run** — first run failed on a transient crates.io download [HTTP2 framing blip in `cargo install cargo-pgrx`], NOT our diff [identical tree to the green branch run]; `gh run rerun --failed` → success).** The whole rigor arc is now ON MAIN: v2 contract+critic+cap+force-final + the model-fallback resilience fix. DEFERRED resilience follow-up: liveness-aware resolution (don't pick an offline model as primary — needs live roster check; the failover catches it reactively for now).
- **★ 2026-06-26 PM(11) — PR #11 RIGOR v2 + the vetting that paid off.** Michael opened **PR #11** (his account, branch `feat/rigor-mode-v2-verify-pass`) = Rigor Mode v2, sharper than my spec: reframes provenance→**FIDELITY** (a citation that RESOLVES ≠ one that SUPPORTS). `66-rigor-verify.sql` = §1 research-rigor v2 contract (verify-before-ship: re-read each cited source, never single-record→population, never subset→population-stat) + §2 trajectory-critic grounding→**fidelity rubric** (the standing gate; catches over-generalization even with a resolving citation) + §3 work-item-chat→auto_critique_families. 3 layers (contract/critic-in-core/oracle-in-overlay). SOAKED: reviewed (the critic UPDATE hits a real `stewards.agents` row — not a no-op; it's a HUMAN-authored grader change in the STRICTER direction, eval-gaming-safe), added **OK 55** (the missing smoke assert — closed the lib.rs+Dockerfile+smoke triad), fresh-image virgin-smoke 00→66 GREEN. **VETTED LIVE both layers:** L1 contract refused 2 designed fidelity traps over vivint (population-% w/ no denominator; geography not in corpus); L2 critic caught a SYNTHETIC bad trajectory (grounding **0.0/fail**, named subset→population + fabricated Texas anecdote) and passed the clean one (0.9). **★ THEN Michael hit a real bug: his Star Trek rigor chat DIED** — debug (Agans): work-item-chat `steps=12` tool-loop cap, hit mid-pagination (rigor v2 "re-read every source" × big docs paginated at `page_in_tool_result_cap_chars=4000` → doc_get×14/result_read×9/doc_search×7), **0 final answers = SILENT DEATH** (no force-final for interactive chats — the soft/hard-cap force-final in `chat_post_internal` is gated to PIPELINE stages only). **FIX: raised work-item-chat steps 12→40** (seed in 45; `b3178ad` on the PR). Inverse-hyp CONFIRMED by Michael's own retests (orig died 12/0 → now 19 & 21 rounds, grounded answers, fast). Bundled into PR #11 (= "rigor v2 that works on real corpora"). **★ DURABLE FIX BUILT (Michael: "lets build that good carry forward… to really close that up") — `67-rigor-force-final.sql` (`c57a7e9`):** re-authors `chat_post_internal` (later-file-wins over 15b's l32) to extend the force-final-at-cap grace — which pipeline stages already had — to the INTERACTIVE chat loop. When a chat reaches its tool budget (steps-2, derived from the agent's own steps), it drops tools (tools_disabled+tool_choice=none) so the model MUST synthesize → a real grounded answer instead of the silent death; wrap-up nudge at 0.7×; config escape hatch `chat_force_final_enabled` (default true). Proven DETERMINISTICALLY (txn rolled back so no synthetic work hits the bridge): hard→forced / below→tools-stay / soft→notice-only / gate-off→no-force; **virgin-smoke OK 56**; regression-checked (a normal chat still returns in 1 round — the most-used fn didn't break); fresh-image virgin-smoke 00→67 GREEN. **★★ PR #11 = THE COMPLETE RIGOR v2 (4 commits):** da87393 v2 verify-pass + 6b7cf52 OK 55 + b3178ad cap raise 12→40 + c57a7e9 force-final. Fidelity contract + critic rubric + the cap + the silent-death closed for good. **PR #11 NOT MERGED — Michael's Hinge** (touches a grader + the chat cap + the chat loop; all green/proven; he was leaning merge once vetted; vetted now).
- **★ 2026-06-26 PM(10) — NEXT RING IDENTIFIED: "RIGOR MODE" / study-agent-mode for a Stewdio response (Michael's idea, + a coworker critique that is its SPEC).** A coworker ran a CKE (their substrate instance) marketing report (doorbell-camera campaign) and wrote a sharp researcher critique (BEFORE our orientation fixups). The critique's 5 asks map 1:1 onto our battle-tested disciplines = the orientation-arc carry-forwards, now with a concrete target: **(1) PROVENANCE** — every claim must trace to a bucket observation OR be marked inference/model-prior (else can't prove the bucket did the work vs base-model priors) = read-before-quoting + cite-count rule. **(2) VERIFY THE BELIEVABLE CLAIMS FIRST** — the specific/authoritative lines ("$50/$100/mo ceilings", "military-affiliated customers", "quote-form→SMS→callback") are where a confident hallucination hides = oracle-first + "numbers/dates/bio are citations". **(3) CALIBRATION** — flat high-confidence everywhere; signal evidence strength (multi-source/single/inference) = epistemic-humility. **(4) CONFIRMATION BIAS** — prompt said "doorbell cameras" → it confirmed camera-first; clean test = ask a NEUTRAL question ("what drives security purchases?", no camera framing) and see if camera-first emerges on its own = critical-analysis posture-check (discovering vs confirming) + surface-tensions. **(5) OBSERVATION vs RECOMMENDATION** — interwoven; keep "what customers said" separate from "what the AI suggests". **★ Rigor mode = the study agent instantiated in the substrate**, riding the orientation arc just shipped (autoload skills + orient_survey + trajectory-critique-as-grounding-verify). Test bed = the curated **vivint bucket** (no CKE data here; Michael offered vivint). The critique even handed us the ACCEPTANCE TEST (the neutral-question premise-test). CARRY-FORWARDS from the orientation arc (port more disciplines onto the shelf: read-before-quote→digesters, inverse-hyp→coder) ARE this — rigor mode bundles them. SPEC WRITTEN (`.spec/proposals/rigor-mode.md`: output contract [GROUND-OR-FLAG every claim {grounded:slug / inference / model-prior} · verify-specific-claims-first · calibrate by strength · observe-vs-recommend split · premise-neutrality reflex] + rides the orientation arc 62/63/64 + the neutral-question acceptance test). **★ PROTOTYPE PROVEN ON VIVINT (dev):** authored `research-rigor` skill (the contract) + autoloaded onto work-item-chat + dispatched the NEUTRAL question ("what do home-security customers care about / what frustrates them" — no product framing) grounded on `vivint-sentiment-synthesis-taxonomy`. Result = night-and-day vs the critiqued CKE report: GROUNDED (cites source + every number traces — ConsumerAffairs 4.5★/37.5k, Apple 4.6★/796k, Trustpilot 62,823; BBB clusters 3,140/2,144/832/471) · CALIBRATED (frequency×severity tiers; FTC identity-harm = "Critical severity, Very Low frequency, a worst-case boundary condition, not proof of an ongoing systemic flaw") · OBSERVATION-vs-RECOMMENDATION split · premise-neutral (data named the pains, not mirrored). **Honest gaps = the build work:** (a) cited the SYNTHESIS doc not the PRIMARY Trustpilot/BBB (proxy ok since taxonomy is source-weighted, but rigor should mark "via the taxonomy's read of BBB"); (b) local model grounded NARRATIVELY ("the doc notes") not with the literal [grounded:slug] tags → the verify pass (64 critic) enforcing tags is what makes priors mechanically subtractable. Reverted the global autoload (rigor=deliberate TOGGLE per spec); research-rigor skill kept on dev. **★★ RIGOR MODE v1 BUILT IN OSS + LIVE-TESTED + COMMITTED LOCAL (`fd321f8`; chain→65 `65-rigor-mode.sql`):** the full vertical slice. (1) `65-rigor-mode.sql` = the `research-rigor` skill → OSS BASELINE (the GROUND-OR-FLAG contract: [grounded:slug]/[inference]/[model-prior] + verify-specific-first + calibrate + observe-vs-recommend split + premise-check) + **render_skills_block refined (re-authors 62) so a DISPATCHER-loaded `session_skills` body renders even for a skill-DENIED agent** (work-item-chat) — same "lent-not-opted-into" principle as autoload; catalog stays gated. (2) `chat.go` chatSendReq.rigor → INSERT session_skills('research-rigor') before dispatch (per-response; off DELETEs it; no FK so no session pre-create). (3) `ChatPanel.vue`+`api.ts` = a 🔬 **Rigor toggle** in the composer. **LIVE-TESTED through the rebuilt UI** (rigor:true → contract loaded into the session [confirmed session_skills row] → the neutral vivint question returned GROUNDED: "Based on the vivint corpus… ## Frustrations (grounded in the corpus)… P0 severity" — night-and-day vs the critiqued report). virgin-smoke **OK 54** (skill ships + dispatcher-load renders for skill-denied + catalog gated + invariant). **★★★ SHIPPED + CI GREEN (`c13c2ed..fd321f8`, run 28249856414 success; fresh-image virgin-smoke 00→65 green BEFORE push).** Rigor mode is live in OSS — the coworker critique answered in a toggle they can press. **★ Toggle UX clarified + SHIPPED (`bee973a`, CI green run 28250853458):** v1's bare 🔬 (color-change only) was too subtle ("how do I click it? it's just an icon") → now a labeled pill "🔬 Rigor OFF/ON" (fills amber on click, state in the label). Michael confirmed it toggles in-hand. **v2 (deferred, spec'd):** the premise-neutrality reflex (run-question-stripped) + the grounding-verify pass as a pre-delivery gate enforcing the LITERAL tags (the local model grounded NARRATIVELY not with [grounded:slug] — the critic enforcing tags makes priors mechanically subtractable). vivint stays file_private/local-only. Spec `.spec/proposals/rigor-mode.md` (root, uncommitted for Michael).
- **★★ 2026-06-26 PM(9) — ORIENTATION ARC (putting the study into practice; "reaching toward Zion" — Michael). PHASE 1 DONE + dev-tested + committed LOCAL (NOT pushed — holding until the arc is solid). 3-phase plan = the study's 3 moves.** **P1 = the activation keystone (`62-orientation.sql`, chain→62; commits `f689b63`+`7c83093` local):** the skills shelf was opt-in (skill_load) so orientation stayed DORMANT — and skill-DENIED agents (world-build, subagents, digesters) could never get it. Built an **autoload layer**: `skill_autoload` table + re-authored `render_skills_block` (later-file-wins) so a skill listed for an agent-family glob injects as a STANDING `<loaded_skills>` block UNCONDITIONALLY (bypasses the skill-tool deny; honors per-skill deny). **★ RATIFIED core-baseline (AskUserQuestion):** orient-first + bounded-gather join the core baseline (alongside source-verification + reference-linking, which are seeded in schema.rs — my study's "empty shelf" was slightly wrong, core ships 2) + a minimal autoload wiring (corpus-builders carry orientation) → every OPERATOR's substrate is oriented out of the box ("no operator left orientation-poor" = the Zion read). `orient-first` skill = the council moment (Abraham 4:26) ported. Examples deleted (content now core). **Verified on dev:** skill-denied world-build now carries orient-first standing; research carries orient-first+bounded-gather; skill-denied+no-autoload invariant holds substrate-wide (0 violations). virgin-smoke **OK 51** added (mechanism + baseline + invariant); OK 9 comment 2→4. **★ P1 WATCHED — IT OBEYS (Abraham 4:18):** dispatched a real world-build + a chat on dev — the build agent OPENED with "I'll start by orienting" → called world_show (surveyed) → then extracted (80 ent/67 edg); the chat searched→read source→COMMITTED a grounded answer (fr=stop, no spiral). The autoloaded orientation visibly CHANGED behavior, confirmed in the LIVE compose_system_prompt (world-build carries orient-first standing; research carries orient-first+bounded-gather; persona carries neither). Test artifacts cleaned. **★ P2 DONE + dev-tested + committed LOCAL (`224ef14`; chain→63 `63-orient-survey.sql`):** `orient_survey` generalizes intent_work_survey (22, intent→project) to ANY corpus-builder — returns a project's docs(+gists)/worlds(entity·edge counts)/recent-work so a builder EXTENDS not rebuilds; granted world-build/research/subagent-doc-investigate/subagent-docs-audit; virgin-smoke **OK 52**. Proven: orient_survey('cosmere-rpg')→27 docs + the 95-ent world. **★ P3 DONE + dev-tested + committed LOCAL (`c13c2ed`; chain→64 `64-auto-critique.sql`):** the VERIFY half made standing. trajectory-critic(56)+verdict-harvest(59) existed but nothing FIRED the critic (call-on-demand → Glass-Box half dormant, like the shelf was). Added the missing FRONT: a `work_queue` completion trigger → `should_auto_critique(session,family)` predicate (config-gated default OFF [cost, like autonomy_paused] · worker-families allow-list · graders HARD-excluded [no recursion/don't grade graders] · fires once at a committed [finish=stop] tool-using run with no verdict) → `critique_trajectory`; verdict harvested by 59 → self-improvement loop. The loop closes itself; no bgworker change. Predicate proven on dev (OFF→no/ON+worker→yes/grader→no/non-worker→no/dedup→no); virgin-smoke **OK 53**. **★★ ARC COMPLETE: orient(skill 62)→orient(tool 63)→verify(64) = the study's 3 moves, all built+dev-tested+smoke-asserted (OK 50–53), 4 LOCAL commits (f689b63/7c83093/224ef14/c13c2ed) chain 60→64, NOT pushed.** **★★★ ARC SHIPPED + CI GREEN (`b70c96c..c13c2ed`, run 28246625621 success).** Fresh-image virgin-smoke GREEN locally (chain 00→64, OK 50–53 all pass) → pushed → CI confirmed (mirrors local). The oracle (the missing-Dockerfile-COPY class that bit us earlier) was run BEFORE the push this time — no red main. Lessons applied every phase: lib.rs + Dockerfile COPY + virgin-smoke assert together. **The loop is whole: orient→act→verify, the study's 3 moves all live in OSS core.** Follow-ups (not blocking): auto_critique is default-OFF (operator opts in per substrate when they want the Glass-Box half standing); the force-final-at-cap spec (`.spec/proposals/`) remains the post-demo bgworker leg; bounded-gather skill `examples/` was folded into the core baseline (deleted). Journal: `.spec/journal/2026-06-26-orientation-arc.md`. Study: `study/ai/harness/lending-the-substrate-our-orientation.md`.
- **★★ 2026-06-26 PM(8) — STUDY: "Lending the Substrate Our Orientation" (`study/ai/harness/lending-the-substrate-our-orientation.md`) — answers Michael's "I feel like we're missing something."** Fan-out gather (3 readers: workspace-discipline audit + substrate-agent audit + Google-SDLC distill) → single synthesis, full provenance, 3 load-bearing quotes re-verified. **Finding:** workspace & substrate share the SAME architecture (thin core + 3-tier skills shelf) — but the substrate's shelf ships EMPTY by design (`24-skills.sql`) while the workspace's is full of battle-tested orientation (top-5: read-before-quoting · council-moment · watch-what-you-order · stewardship-over-surfacing · verify-the-fix). **The gap = the ORIENT move at the FRONT of the loop.** Three witnesses converge: Boyd (Orient = the irreducible node), the workspace (Council Moment = a HARD universal covenant clause for every agent), Google SDLC (agent loop = perceive→PLAN→act; "plan quality"+"context handling" are first-class eval dims; "success criteria not step-by-step"). **Honest correction (Ben Test):** substrate is NOT orientation-blind — `intent_work_survey` (22) is a real council moment (its own code cites Abraham 4:26) + trajectory-critic (56) already built Day-4 Glass-Box ("fluent answer that skipped verification = MORE dangerous"). But orientation is SURGICAL not universal — one agent orients, the transactional workers (research/chat/doc-build/personas/digesters) ACT immediately; the walk+COMMIT were the same per-failure retrofit. **Reframe/thesis: the workspace IS the substrate's orientation library** — every skill = a past failure crystallized; keep the core thin (maneuver bet) but FILL THE SHELF (loadable skills); bounded-gather = instance #1, ~2 dozen behind it. **3 becoming moves:** (a) generalize intent_work_survey → universal orient-first for any corpus-builder; (b) port battle-tested disciplines as loadable substrate skills (read-before-quote→digesters, inverse-hyp→coder, adjacent-surface→builders), oracle-tested; (c) run trajectory-critic as the standing trajectory-HALF of verification (Google: both halves necessary; data's already in the ledger). Placed in study/ai/harness/ as sibling to the-harness-is-orientation.md (the frame); Michael leaned "overlay for now" — easy move; conclusions = overlay→OSS candidates. Workspace-root file → uncommitted for Michael.
- **★ 2026-06-26 PM(7) — BOUNDED-GATHER audit + skill (Michael asked: where else does the world-build fix apply? + would it be a skill?).** **Audit (data-driven via `stewards.agents` prompt-flags + pipeline `max_tool_rounds_hard`):** the over-search/no-done-signal family is closed in 3 places — force-final cap on pipeline STAGES (35: research-summary/write, code-pr), COMMIT clause in work-item-chat (45), walk+coverage in world-build (61). The real hole = **standalone `dispatch_chat_turn` agents** that gather with neither a stage cap nor a prompt rail: `loremaster`/`subagent-doc-investigate`/`compactor` (→ COMMIT clause) + `subagent-docs-audit` (→ the WALK, it audits a set). `research` LOOKS exposed but is protected as a pipeline stage. **Graders OFF-LIMITS** (judge-brief/world-critic/trajectory-critic/prompt-critic — eval-gaming guard; a "stop early" clause is the wrong thing on an evaluator). **Skill: I first built a `.claude` skill FOR ME — WRONG target (Michael: "I meant a skill inside pg-ai-stewards, you don't suffer from that much"); deleted it** and authored a substrate skill instead: **`examples/bounded-gather-skill.sql`** (SHIPPED `b70c96c` + applied to dev) = a loadable "methodical gathering" instruction module (commit / empty=absent / know-your-frontier / done-is-a-fact) any gathering agent can `skill_load` — the on-demand version of the COMMIT clause. Opt-in example (core seeds no skill content by design → not a chain file, CI-safe). **Spec written (not pre-demo): `.spec/proposals/force-final-at-cap.md`** = the universal floor (force one tools-disabled "answer now" turn at the step cap for ALL standalone agents; reuse the stage-level `max_tool_rounds_hard`; bgworker change → post-demo + council). **★ CARRY-OVERS (Michael deferred, NOT applied):** (1) COMMIT clause → loremaster/subagent-doc-investigate/compactor; (2) the WALK → subagent-docs-audit; (3) ACTIVATE the skill = grant gathering agents the skill levers (skill_load/unload) + nudge them to load bounded-gather; (4) build force-final-at-cap (post-demo). Skill is authored+shipped but DORMANT until (3). Root files (skill-for-me deleted, spec, journal, lane) left uncommitted for Michael (root push is his).
- **★★ 2026-06-26 PM(6) — WORLD-BUILD WORKLIST HARNESS (the scratch-file fix) BUILT + TESTED + SHIPPED + CI-GREEN (`615dc29`, chain 00→61).** Michael confirmed the failure reproduces with GEMINI on MLP ("lots of searching, no done-marker") — a STRONG cloud model failing identically = it's a HARNESS gap not a model gap (harness>intelligence). Built all 3 he asked: **`61-world-build-worklist.sql`** = `world_build_coverage` table (one row per source chunk per world, persists = resumable BoM-walk style) + **`world_build_walk`** tool (seeds the worklist from the world's project chunks, serves the next bounded batch marking them shown, returns `complete:true` when none pending = deterministic done-signal) + world-build agent prompt RE-AUTHORED to WALK-then-extract with a COMMIT clause (count:0 search → move on; mirrors the 45-work-item-chat COMMIT fix); steps 60→120. world.go per-build canon hint aligned to the walk (ui rebuilt). Registered lib.rs + Dockerfile COPY + virgin-smoke **OK 50** (BOTH the CI lessons applied this time). **TESTED 3 ways:** (1) deterministic unit test (seed→drain 27→complete:true, reset is pure, stable post-complete); (2) **cosmere regression** — walk built 73 ent/126 edg with NO hand-steering on the exact corpus that looped to 0 before; (3) **star-trek SCALE** (114 chunks) — old doc_search-first pattern 400'd instantly (99925 tok > 98304 per-slot ctx); **walk-first walked ALL 114/114 → 234 entities, ZERO overflow** (bounded batches + the substrate's existing page_in_cap trim old chunks → cumulative stays under ctx). CI green (OK 50 on a fresh image). **★ FINDING for big corpora:** the walk + existing context caps handle 114 chunks fine; my PM(4) ctx trim (196608/2=98304 per slot) is the exact ceiling the OLD pattern hit — a reason the walk (bounded turns) matters AND a note that a future very-large corpus may still want compact_context wired into the loop (deferred follow-up, not blocking). His cosmere-rpg world (95/143) kept; st-walktest (234 ent) kept as the live scale demo (he can delete). Journal `2026-06-26-world-build-worklist.md`.
- **★ 2026-06-26 PM(5) — COSMERE world BUILT (95 ent/143 edg) on the 2nd kick; surfaced a WORLD-BUILD HARNESS GAP (no scratch/worklist → weak local model loops on search, never extracts).** Re-kicked the Cosmere build after the GPU fix (att:98 already imported = 27 chunks all embedded; build-only via `POST /api/world/build {name,slug,project}` no file → canon="doc_search project"; NO re-import). **Run 1 FAILED 0-entities:** local qwen3.6-35b-a3b did 60 turns / 235 doc_search calls (ALL finish_reason=tool_calls, mostly count:0 hunting narrow Stormlight lore that isn't in a RULES preview) and NEVER called world_entity_upsert — the same "over-gather, never commit" family as the chat reasoning-spiral, but in the world-build agent (no COMMIT discipline in its prompt). **Run 2 SUCCEEDED** with `instructions` steering ("do a FEW broad searches then STOP and EXTRACT; count:0 means move on; extract setting+mechanical entities"): front-loaded ~22 searches, pivoted ("Now I have enough… let me create the world and add entities"), produced 95 ent/143 edg. GPU stable 833 free both runs (no OOM — fix validated under the real workload). **★ Michael's insight (the real thread): our STUDY scratch-file rule applies here** — externalize a worklist so a bounded model methodically drains a large extraction instead of looping. GAP: world-build holds the whole plan in-context; substrate HAS the primitives (CT2 §7 self-notes/working-memory; doc-construction's incremental work-item drafts via doc_current) but world-build isn't wired to them. NEXT (proposed, not yet built): bake COMMIT-after-survey into the world-build agent prompt permanently (durable version of today's instructions, mirrors the 45-work-item-chat COMMIT fix) + optionally a persisted candidate-entity worklist + 27-chunk coverage tracking as a deterministic done-signal. Connects to his open file `study/ai/harness/the-harness-is-orientation.md` (scratch = orientation externalized; harness>intelligence). Model-swap (gemma-4-26b / qwen3.6-27b) = a lever but the harness fix is the durable win.
- **★ 2026-06-26 PM(4) — RIG "GPU FELL OFF" mid Cosmere-import = LM Studio embedder replica spike, NOT context. FIXED (Michael moved nomic→CPU).** Both 4090s were at ~140 MiB free (qwen3.6-35b-a3b 19.7GB on GPU0, gemma-4-26b-a4b 15.6GB on GPU1, dance-moe). I edited `config.json` dance-moe/dance ctx down (qwen 245760→196608, gemma 425984→360448; gitignored, applied live via `/api/unload`+`/api/load` since `ApplyProfile` reads in-memory config — needs restart, not just profile-switch). **★ KEY MEASURED FINDING: context is NOT the VRAM lever for these big models on 24GB cards** — loading qwen at 245760/196608/98304 left idle non-weight footprint FLAT at ~2GB each time (4 nvidia-smi reads GPU0, 2 GPU1); halving context freed ~0. Cards are full from weights + a fixed ~2GB compute buffer + the OS/LM-Studio baseline, none of which the window touches. **Actual cause:** LM Studio auto-spawns nomic embedder **replicas** (`lms ps`=84MB each, but each new llama-server instance grabs ~400-500MB CUDA context) under the import's CONCURRENT embed burst → ~1GB+ transient demand on a card at ~150 free → CUDA OOM → GPU drops off the bus. **FIX (Michael did it): nomic→CPU + removed the 3 replicas** → GPU0 free 107→**1373**, GPU1 →**1143** (>1GB each, his target), both rig models healthy; embeddings now CPU (slower but decoupled from rig GPUs — import burst can't OOM). Left ctx at 196608/360448 (VRAM-neutral, modest = cheap insurance vs runtime-KV growth on the now-~1.3GB cushion; offered to restore 245760/425984 — his call). LESSON: on a near-full shared GPU, suspect co-tenant CUDA-context spikes (Windows WDDM reports per-proc VRAM as N/A — measure by unloading); context trim is a non-lever when KV is small relative to weights+buffers.
- **★ 2026-06-26 PM(3) — ✅ CI GREEN AGAIN (`d4cc045`); was RED for 7 commits — two-part fix.** Confirmed both jobs pass at terminal state (go build+vet ✓, extension build + virgin smoke ✓). **Fix 1 (`806a8d8`):** the Dockerfile COPY list (below). **Fix 2 (`d4cc045`):** once the image built, virgin-smoke finally RAN and tripped on **assert 51** — #282 changed the doc-build gate to sandbox-attribution but the smoke's positive case still stamped its artifact session-only → gate (correctly) flipped it to failed. Updated the test to the new semantics + added the false-pass assertion (artifact from a DIFFERENT sandbox must NOT pass — the exact hole #282 closes). **Verified the gate logic against dev pg BEFORE pushing this time** (failed/failed/completed) rather than trusting the first CI poll. KEY meta-lesson: a broken extension-build masks ALL downstream smoke asserts — they simply never run — so a build break can hide a test break; only a full virgin-smoke catches both.
- **★ 2026-06-26 PM(3) — ⚠️ HONEST CORRECTION: CI extension-build has been RED since `b977546` (7 commits), now FIXED (`806a8d8`).** I'd claimed "CI virgin-smoke GREEN" for the 60-chat-model-pin work and reported #282 CI-green/pending — WRONG. The `go build + vet` job passed each run (what I mistakenly read as green); the `extension build + virgin smoke` job failed every time from b977546 onward. **Root cause:** `extension/src/lib.rs` registers `60-chat-model-pin.sql` via `extension_sql_file!`, but the Dockerfile's explicit COPY list (the compile-time build context the macro reads) still ended at `59-self-improvement.sql` → `cargo pgrx package` → "couldn't read src/../60-chat-model-pin.sql". Local dev never hit it: 60's SQL was applied to the running pg via psql, never rebuilt from the Dockerfile (verified-on-the-wrong-surface). **FIX = one line:** add `60-chat-model-pin.sql` to the COPY list. Single root cause → heals all 7 red runs (b977546→#282) at once. lib.rs already chained 60 correctly; only Dockerfile + lib.rs track the chain operationally (grep-confirmed). Pushed; **watching CI to actual terminal state this time** (the prior miss was trusting the first poll = CI_RED_OR_PENDING). **LESSON (re-noted): for a chain change, the oracle is virgin-smoke on a fresh image — NOT live-apply via psql, NOT the go-build job alone.** Last truly-green run = #7 spawn-bounds (`02cd937`, 01:48).
- **★ 2026-06-26 PM(2) — WORLD GRAPH edges fix + PR #10 + #282 doc-build gate, all committed+pushed.** **Graph (`bdb855d`):** the 3D world graph showed floating nodes + a faint particle animation but no readable lines — 3d-force-graph linkOpacity defaults to 0.2 + dim gray. FIX: colour-code edges by RELATIONSHIP (palette hash, stable per rel) at width 1.4/opacity 0.7 + matching particles/arrows + a bottom-left legend (rel→colour). Verified via playwright screenshot on my-little-pony (115 ent/130 edges, 21 rels): lines+legend render. `WorldGraphPanel.vue`. **PR #10 MERGED** (image-gen cost tracking: pre-gen provider_cap_exceeded check + cost_events insert for generate_image; verified schema; needs the bridge rebuild — done this turn). **#282 doc-build fix (`bd9201c`) — the REAL Star Trek root cause:** build step (local reason model) stuck in a coder_edit loop ("old_string appears 5×"), never exported; AND the existing gate (51 doc_build_verify_artifact) FALSE-PASSED — it checked "any chat_attachment in spawned_from_chat after created_at" but that session held unrelated generate_image PNGs → empty build "completed." FIXED: (1) build template EDIT-LOOP GUARD (don't repeat a failing edit; replace_all/unique-anchor/rewrite; export SOMETHING); (2) gate now attributes via SANDBOX — coder_export_artifact stamps chat_attachments.sandbox (=input.sandbox), gate requires a sandbox-matched artifact (new column+index in 51). Deterministic gate test PASSED (no-artifact→failed; sandbox-match→completed). E/Diagnose artifact check also re-keyed to sandbox. Rebuilt bridge (coder-mcp stamp + PR#9 tools-on-boot) + ui. NOT yet e2e-run: a full doc-build (would confirm a PDF actually comes out now) — recommend Michael run one. All chain edits in-place (50/51, no new file). CI watching. Tasks #276-282 DONE; open: #275 watchdog, #281 dedupe (deferred). Michael: "do both and keep moving."
- **★★ 2026-06-26 PM — STEWDIO CHAT EASE-OF-LIFE A–E ALL SHIPPED+COMMITTED+CI-GREEN; 2 PRs folded; Star Trek PDF bug diagnosed.** Continued from A–D. **E (🩺 Diagnose) BUILT+VERIFIED+pushed (`306cd54`):** `POST /api/chat/diagnose` (cmd/stewards-ui/api/diagnose.go, reuses rig.go's llamaChipURL/rigGet/rigPost) = deterministic GATHER (rig slot-state+util, session/build trouble, down-providers=no-usable-model) → AUTO-FIX safe only (cancel dup in-flight builds keep-most-advanced; cycle a state≠healthy slot, NEVER on saturation) → NARRATE on strongest-reachable (v1 local gemma; LLM OUT of execution path). 🩺 button + result card in ChatPanel. **PROVEN e2e on the real Star Trek work item** (6ce9d3ec): correctly reported "completed but 0 files — deliver couldn't reach the built doc — retry it" + clean narrated diagnosis. Fixed a false-positive (down-provider = ANY-unusable → NO-usable-at-all). Auto-fix paths reuse the proven runaway-halt SQL (code-verified, not e2e this session). Ratified scope: safe-act + paid-recommend. **2 PRs FOLDED (reviewed+merged+CI-green, branches deleted):** #8 fix(cost) research fail-closed reroute (research/* on a paid provider → flexllama; always-on; 36-judge-local-routing.sql; APPLIED live to dev) + #9 fix(bridge) refresh-tools-on-boot (bridge-entrypoint.sh; fresh install registers external MCP tools incl coder_export_artifact — needs a bridge rebuild on dev to take effect). **★ STAR TREK PDF root cause (task #282):** doc-build deliver couldn't reach the BUILT doc — build handed off via a ctx-handle [ctx:bf5e] the deliver session couldn't expand → looped → gave up → BUT marked completed (apology=final stop). FIX = (1) durable build→deliver handoff, (2) a deliver-produced-artifact GATE before completed. E detects it; #282 fixes it. All 5 tasks #276-280 DONE; #281 dedupe deferred; #275 watchdog + #282 doc-build pending. Michael: "the new changes are good, I really like them." NEXT options: #282 doc-build fix · #275 watchdog · #281 dedupe.
- **★ 2026-06-26 — STEWDIO CHAT EASE-OF-LIFE A–D SHIPPED+LIVE on dev :8081 (uncommitted); E ratified; MLP runaway handled.** Michael asked for chat UX love (restart/retry/escalate-to-stronger-model + "what else"). Selected A+B+C+D+E. **BUILT+VERIFIED+DEPLOYED (dev container rebuilt twice):** **A** retry/escalate — ↻ retry (in-place), ⤴ stronger (one-click, private-safe cloud only), escalate-on-error; PROVEN e2e (pinned claude-sonnet/opencode_zen → enqueued row carried that provider+model). **C** always-visible ⚡Fast(local)/🧠Smart(cloud) model switch (⚠ on training providers; private_safe = NOT model_capability.trains_on_data, local always safe). **B** ↻ Retry build / ⤴ Retry stronger on failed/cancelled work-item cards (work_item_dispatch_stage allow_failed=true + model_override; 409 guard verified). **D** model badge (⟐ which model answered, from messages.model), regenerate IN PLACE (rewind last user turn + replies, re-dispatch — verified old assistant deleted + re-enqueued), ⏩ Continue (finish_reason=length), ✎ edit-and-resend. **Substrate:** new chain file `60-chat-model-pin.sql` = `dispatch_chat_pinned` (explicit provider+model, reuses chat_enqueue; registered in lib.rs; applied live). **Go:** chat.go += /api/chat/models (grouped local/cloud+private_safe), /api/chat/work-item/retry, /api/chat/regenerate, provider field on send, model in stream. ChatPanel.vue + api.ts. **Both build clean** (GOWORK=off go build; vue-tsc). **E (Diagnose) RATIFIED + DESIGNED, NOT built** (task #280): safe-act autonomous + paid-recommend-only; arch = deterministic safe-execute + strongest-model narrate (LLM out of execution path). **★ MLP RUNAWAY (2026-06-26, handled):** qwen3.6-35b-a3b (local, $0) mis-routed a "quick rundown" chat Q → a doc-build, then apology-looped start_task ~10× → 10 dup "New Player's Guide" builds. HALTED (cancelled+stage-dispatches killed); the stuck stage-dispatches keyed on `wi--<id>--stage` (not `_work_item_id`) — missed by the first cancel, pegged GPU0 (SATURATION not wedge — Michael's correct call: ~60 queued reqs; cycling the slot just cleared the backlog). SWEPT: 75 stale pending work_items (research-write/planning 06-15→23) cancelled + 271 runaway turns deleted + MLP session removed (regenerates fresh). **GAP (#281, DEFERRED, dedupe-only per Michael — NO per-session cap [kills multi-deliverable] + NO ammon-loop [overkill]):** chat start_task is unbounded (spawn-bounds don't catch chat-spawned ROOTS — depth0/no-parent). Fix = refuse IDENTICAL in-flight build + prompt: answer rundowns INLINE. NEXT: build E; then commit A–D; Michael to re-test chat. Tasks #275-281. Harnesses in scratchpad (kvexp/needle_sweep/agentic_hard).
- **★★★ 2026-06-25 PM — ⚠️ CORRECTED ROOT CAUSE: CHAT STALL = ENVIRONMENTAL (a WEDGED llama-server slot), NOT the model.** Inverse-hyp CLOSED with Michael: a stuck gemma chat "blew past its max context" (impossible = corrupted slot KV/ctx bookkeeping) poisoned the rig; **cycling the GPU recovered it.** On a HEALTHY rig qwen3.6-35b-a3b commits **6/6 on EVERY test** — needle→113k (q8 KV perfect, finish=stop), 20-tool + 4k-noisy agentic loop (3-9 rounds), thinking on/off. **FALSIFIED, in order:** context-length (6/6 to 113k), KV-quant (q8≡fine; f16 only reaches 40k on the 24GB card), **token-cap** (chat is UNCAPPED — verified in `dry_run_chat`/bgworker OpenAI passthrough + 9583/9740 live rows carry NO max_tokens; the 16k I "remembered" = the PERSONA path `17-personas.sql`; 4096 = a DORMANT anthropic-format default, no such models seeded), tool-count (20 tools fine), payload-size (4k-noisy results fine). **FIX = llama-chip slot-health watchdog (TASK #275)** — it auto-restarts on CRASH but a wedged-not-crashed slot slips past; detect health-timeout / hung-gen / ctx>n_ctx and auto-cycle + optional substrate bgworker per-round timeout. Harnesses: scratchpad `kvexp.py`/`needle_sweep.py`/`agentic_hard.py`. **My earlier "the model is root cause / switch to gemma" was WRONG** — it was measured on the wedged rig; qwen is fine. NOW PIVOTED to Stewdio UX ease-of-life (restart/retry/escalate-to-stronger-model — the recovery thread, which is ALSO the user-facing safety net for exactly this wedged-slot failure). **+ PR #7 (my ordering bug) reviewed+merged.** (orig experiment notes:) **(1) PROMPT SWEEP (5 variants × qwen3.6-35b-a3b on the 6-Elements question): ALL identical = NO-commit / 12 rounds / 0 chars / 0-of-6.** Prompt is NOT the lever — budget/broad-first/commit/partial all ignored. **(2) MODEL SWAP (same Q, same prompt): gemma-4-26b-a4b (same rig, FREE) → committed / 5 rounds / 6-of-6 / 438 chars / 33s** (budget variant 7 rounds/6-of-6/1260). **kimi-k2.6 (NVIDIA, ~$5 avail) → commits; budget variant 6-of-6/1572/10 rounds** (base oddly 1-char). **VERDICT: `qwen3.6-35b-a3b` is simply the wrong model for interactive multi-fact synthesis** — not prompt, not the step-cap, not (really) KV. Michael's "it's the model/setup" instinct was right; the fix is "use gemma," NOT KV-tuning (so NO need to drop a parallel). **RECOMMEND: switch interactive chat default → gemma-4-26b-a4b** (offered to wire a dedicated `chat` alias→gemma so the `reason` alias stays qwen for research/planning; nuance: gemma is also ingest/vision → shared slot). This **de-prioritizes force-final-at-cap → backstop not urgent** (gemma commits at 5≪12) and **makes the KV experiment unnecessary**. The live tool-trim (8) likely also moot w/ gemma but still good hygiene + still uncommitted. **PR #7** (`fix/spawn-bounds-grant-ordering`→main, work-box agent): fixed MY bug — leaf `consult_subagent` deny was placed in §1 BEFORE §4 es10 (which sweeps allow to every stage family incl leaves) → es10 clobbered it on FRESH install (my live test passed only because es10 ran long ago so my deny landed last = the works-live/broken-fresh trap). Relocated deny AFTER es10 + chmod +x migrate.sh. CI virgin-smoke GREEN (the oracle for this), merged, applied to dev (0 leaves spawn). LESSON: virgin-smoke (fresh, in-order) is the oracle for chain changes, not live-apply — I should've run it on d0ab780. PR queue empty; everything pushed (work box clear to re-enable per the operations runbook).
- **★ 2026-06-25 — SPAWN-BOUNDS ratified+built+verified (the $50 root-cause fix; OSS `d0ab780`).** Council w/ Michael on recursion limits. KEY: depth-cap ALREADY existed (`trigger_enforce_subagent_depth`, hardcoded ≤2 walking parent_work_item_id) — the gap was NO WIDTH cap → a depth-2 tree = N×N unbounded = the runaway. BUILT (16-subagents.sql + applied live): depth now CONFIG-driven (`subagent_max_depth`=2); WIDTH cap NEW — a node ≤ N children, N = parent-pipeline override else global default (`subagent_max_children`=8; `subagent_max_children.decompose-fanout`=16 since it legit fans to 13). **Inverse-hyp test CAUGHT a bug**: first cut gated width to TG_OP='INSERT' but spawns create-then-UPDATE-parent (chat_start_task_tool) → accepted 12; fixed to run on the parent-setting UPDATE too → **verified live (rolled-back): research-write caps 8, decompose 16, depth 2, 0 leaf families can spawn**. GRANT MODEL = tool-removal at family level: `consult_subagent` DENIED to leaf subagent-* families (a leaf does local research, never re-delegates — Michael's "narrow research, not a 2nd you" = how Claude Code subagents work). Docs: `.spec/proposals/spawn-bounds.md` + **findable/tunable `docs/delegation-limits.md`** (all limits are config rows, raise as models earn rope, no rebuild). Defense-in-depth: depth+width+grants (can't grow) + PR#6 cost-ceiling+brake (can't overspend). Same family as chat (bound work at the chokepoint = compose_tools/compose_messages). **OPEN (Michael's 2nd thread): chat KV-cache hypothesis** — maybe the over-gather is qwen3.6-35b-a3b Q4 + q8 KV-cache degrading long-ctx attention; worth an experiment (reload k=f16 v=q8 → full f16, re-run the failing lookups, compare; tradeoff = drop a parallel for the VRAM). My read: plausible-but-secondary (the spiral was COHERENT over-verification, not garbled = behavioral/small-model not KV-degradation) — but cheap to test; the force-final-at-cap structural fix is still the backstop regardless. His rig = HIS HANDS.
- **2026-06-25 — UPGRADE/OVERLAY model drafted + COST-RUNAWAY PR #6 reviewed+MERGED.** Michael asked: does a Rust rebuild need DB backup/restore? + multi-instance-with-overlays is getting complex, how do better? **ANSWER (grounded in the code): almost never need backup/restore** — CODE is in the IMAGE (Rust .so + the NN chain via `extension_sql_file!` in lib.rs), DATA is in the `pgdata` VOLUME (independent), config seeds are `ON CONFLICT DO UPDATE` (config-as-code). The gap: `CREATE EXTENSION` init only runs first-boot, so no clean in-place migrate → people default to scary nuke+restore. **DRAFTED (OSS `fda7f24`):** `.spec/proposals/upgrade-and-overlays.md` (3-layer model CORE/DATA/OVERLAYS + backup/restore matrix), `scripts/migrate.sh` (idempotent apply of the chain DIFF by sha256 vs `stewards.schema_migrations` ledger, chain order parsed from lib.rs's 61 `../NN-*.sql` refs — VERIFIED 00→59; adopt/status/apply modes; ADOPTs an existing install on first run; +per-instance overlays), `docs/operations.md` (runbook: 3 scenarios + "config is code" drift-killer + verify gate). Draft — not yet wired into pg-start (manual migrate first per the rollout). NOTE: this means my live prompt/grant/tool-trim edits this session REVERT on a migrate unless folded into the SQL files (committed ones survive; the tool-trim is live-only → needs committing if kept). **★ PR #6 (cost-runaway, the recursive research fan-out that spent ~$50) REVIEWED + MERGED** (`fix/cost-runaway-guards`→main): two real gaps — (1) `provider_spend_caps` was ENQUEUE-only → snapshotted/queued chats kept sending; setting cap=$0 didn't stop it. Fix = **send-time ceiling in `compose_messages`** (the universal chokepoint before every send) RAISE insufficient_resources when `provider_cap_exceeded`. VERIFIED SAFE: cap fires only when enforced AND over → uncapped providers + interactive chat NOT blocked (live: only google_gemini capped, not exceeded). (2) `reflect_guard` autonomous brake only counted scheduler+reflect-steward → the runaway `actor='subagent'` (42 rows, confirmed real) ESCAPED. Fix = broaden to scheduler/reflect-steward/**subagent/persona-request** (the autonomous set; excludes operator `agent`/`persona`). CI green; SQL-only (live-appliable). **SAME FAMILY as the chat problem: "bound the work at the chokepoint."** Cost = send-time ceiling; chat = force-final-at-cap. The broadened in-flight cap now also pauses a runaway fan-out by concurrency = partial recursion backstop. FOLLOW-UP: this is the cost BACKSTOP (stop the bleed) — the root cause (recursive fan-out with no depth/child-count limit) still wants a max-depth guard; + confirm bgworker treats insufficient_resources as TERMINAL (no retry-churn). PR queue empty.
- **★★ 2026-06-25 — TOOL-COUNT tested + the CHAT STALL root-caused to STRUCTURAL cap-without-force-final (5 reproductions; live tool trim 20→8).** Michael's hypothesis: "too many tools overloading the model?" **VALIDATED as hygiene** — work-item-chat had **20 allowed tools** (11 overlapping retrieval: doc_search/doc_get/doc_similar/doc_citations/context_search/read_corpus_parents/result_search/result_read/investigate_doc/investigate_session/book_search + heavy non-chat doc_extract/doc_import_corpus/research_codebase/start_brainstorm). Trimmed live to a sensible **8** (doc_search, doc_get, doc_citations, investigate_session, start_task, generate_image, book_search, result_read) — focused the gathering (thrash-among-11 → clean doc_search/doc_get). **BUT tool count is NOT the root cause:** re-ran the failing 6-Elements lookup at just **5 tools → STILL capped at 12 rounds empty** (doc_search×9 + doc_get×3, clean gathering, short reasoning). **CONCLUSIVE:** the model legitimately over-gathers a multi-FACT question (6 elements scattered in corpus) and the `steps=12` cap cuts it off WITHOUT forcing an answer = empty bubble. Proven across **5 reproductions** (orig spiral, PDF-chat, multiturn-turn1, xlsx-chat, trim-test) + survives prompt-nudges AND tool-trimming. **THE FIX (now clearly required, recommend building): force-final-at-cap** — when the chat loop hits its step cap, do one tools-disabled turn forcing "answer now with what you have" (the `max_tool_rounds_hard` mechanism the substrate ALREADY has for pipeline gather stages, just not wired into the interactive chat loop — bgworker/Rust change + extension rebuild). Focused single-fact lookups + file generation work fine; the gap is broad multi-fact lookups. **Tool trim is LIVE (8), pending Michael's call on the permanent set + whether to build force-final.** Also delivered a 3rd artifact (Star Trek factions XLSX, openpyxl, att 54, grounded; minor Ferengi/Breen quadrant slips). Download URLs `/api/chat/attachment/{52,53,54}?download=1`.
- **★ 2026-06-25 — TIRE-KICKED the new build's CHAT + doc-build; delivered real PDF+PPTX; fixed 2 prompt regressions I'd introduced (OSS `24d112d`).** Michael: "take some turns testing the chat — look up info, create a presentation, make a PDF, drop them here." **#1 grounded LOOKUP** (Star Trek factions, project:star-trek via `/api/chat/send`): committed in ~20s, `fr=stop`, 2837b, accurate + corpus-cited — **the COMMIT fix holds on a fresh corpus.** **#2/#3 GENERATION via doc-build** (plan→build→deliver, coder-runtime sandbox python-pptx/reportlab on the LOCAL rig — `[-]` provider = alias→flexllama, sidesteps dead opencode_go): produced a real **1-page MLP PDF** + **5-slide Star Trek PPTX**, both valid, exported via chat_attachments, sent to Michael. **TWO REGRESSIONS I'D INTRODUCED, caught + fixed:** (a) my live COMMIT-prompt rewrite had **clobbered the 51-rich-chat-hardening doc-build delegation** (live prompt had start_task but not `pipeline="doc-build"`) → chat gathered to answer inline instead of making a file. Restored + split FILE→doc-build / TEXT→research-summary; literal `pipeline="doc-build"` makes 51's guarded append a no-op on rebuild (reconciled). (b) chat then **over-gathered 11 rounds before delegating**, and `steps=12` cap cut it off ON the start_task round → tool never executed, no work_item. Added "delegate PROMPTLY, don't pre-gather (doc-build reads the corpus itself)." **Deeper latent bug noted:** a final-round tool call at the step cap should still EXECUTE (or the cap force-flush pending tool calls) — same family as the chat-loop force-final arc. **FIDELITY note:** the small local model in doc-build's build stage drifted one PDF section (named "Grand Galloping Gala" as the opener; actual = Bountiful Bouquet Festival, which the CHAT got right) — generation works, grounding fidelity on a small model isn't perfect (a grounding-check stage / stronger model would tighten). Rig contention: 2 concurrent doc-builds serialized (~9 min for both). All applied live to stewards.agents; test data cleaned.
- **2026-06-25 — REVIEWED + MERGED Michael's 3 stacked work-box PRs (#3/#4/#5 → main; squash+delete-branch; local main synced af6f0a6).** All cpuchip-authored, all green CI (extension build + virgin-smoke + go build/vet). **#3** (`b98947a`) docs-only proposal `native-vertex-embeddings.md` — Vertex OpenAI-compat `/embeddings` 500s for every request (model-id- and dimensions-independent → **re-confirms #2 `dimensions` exoneration**); proposes a native `embedContent`/`:predict` branch in `embed_one` for `AUTH=google_sa` (outputDimensionality + task_type RETRIEVAL_QUERY/DOCUMENT), retires the proxy `/embeddings` route. No code, zero risk. **#4** (`b067900`) 1-line compose parity — `ui` service gains `extra_hosts: host.docker.internal:host-gateway` (pg/bridge/persona-host already had it; verified) so the UI's host-provider health-checks resolve. **#5** (`af6f0a6`) `extension/47-multimodal.sql`: new `gemini_normalize_tool_turns(jsonb)` + applied in `compose_messages` ONLY for google_vertex/google_gemini — repairs functionCall/Response adjacency (Gemini requires each call turn immediately followed by matching responses, counts equal; created_at ordering interleaves async tool results + the soft-cap STEWARD NOTICE → Vertex 400; OpenAI-compat tolerated it silently). Re-emits call→responses (stub if dropped), drops orphans, floats interleaved turns after the block. **Guarded → literal no-op for the local flexllama substrate** (dormant until a Vertex deploy + next extension rebuild; not applied to live pg, no need). Logic sound + virgin-smoke green. PR queue now empty.
- **★★ 2026-06-25 — CHAT REASONING-SPIRAL FIXED (the REAL #3; OSS `1152cbc`; applied live to stewards.agents).** After the 401/loop red-herrings, the true cause of the "errored out" chat: turn `25944` ran on the LOCAL rig (flexllama qwen3.6-35b-a3b, no error) and returned **fr=length, reasoning_content=310,902 chars (91,391 tok), content=EMPTY**. Reading the reasoning: the model had the COMPLETE correct answer in the first ~400 tokens ("I will formulate the response…") then **spiralled ~90K tokens re-verifying** ("let's double-check page 289…") and hit the context wall before writing a word. A **self-verification spiral**, not the model/tokens/provider. Trigger = reasoning-model + open grounded-SYNTHESIS task + a "ground/verify/cite everything, never from memory" prompt + NO commit-signal + wide-open tokens. (World-build doesn't spiral — its output is discrete tool calls, not prose.) Michael's call: don't cap tokens, don't swap model — **fix the instructions** (his hunch, correct). FIX = `extension/45-work-item-chat.sql` prompt: **(1) COMMIT** — "the moment retrieved material answers, STOP verifying and write it; verify at most once; never re-read a source to double-check yourself." **(2) CHAT WINDOW** — a few short paragraphs max. **(3) DELEGATE** — if a faithful answer needs more (report/survey/deep analysis), call `start_task` (e.g. research-summary) to spawn a BOUNDED work_item report that links + shows as a card, then reply briefly with the headline (`start_task` was already granted to work-item-chat in 46; prompt now tells it to use it). **INVERSE-HYP PROVEN on the exact failing question (fresh session):** before 310,902-reasoning/0-content/fr=length → after **~1,100-reasoning / 1,249-content / fr=stop, 1 tool round, accurate grounded answer** (Mane Six + Bountiful Bouquet Festival, correct). Applied to live `stewards.agents` (table is `agents` not `agent_prompts`); authored source committed. Pure prompt-text change in an existing INSERT → no smoke risk. **#3 DONE.** (Deferred backstop idea: when content empty + reasoning has the answer, surface/retry rather than blank bubble. Bigger arc: chat sub-agent farm-out.)
- **2026-06-25 — 3 Stewdio asks: #1 copy session-id (SHIPPED) + #2 richer activity feed (SHIPPED) + #3 stalled-chat DIAGNOSED (OSS `afc1594`; ui rebuilt).** Michael's MLP chat "stalled." **#3 ROOT CAUSE:** session `stewdio-my-little-pony-…` — a **local model `qwen3.6-35b-a3b` tool-looped 12 rounds** (every assistant turn `finish_reason=tool_calls`, clen=0; chasing `doc_get`/`doc_search`/`result_read` pagination "more available, offset=5000") and **hit a soft round-cap, stopping mid-tool-call with an EMPTY final message** = the stall. **The substrate already has the fix pattern** — `max_tool_rounds_hard` force-stops tools + makes the model emit a final answer (35-research-doc-construction.sql:101-105, built precisely because "a weak local model can't loop searching forever") — but it's wired into pipeline **gather** stages, **NOT the interactive chat loop**. **#3 FIX (proposed, not yet built):** wire that force-final-on-hard-cap into the chat dispatch loop (so a capped chat answers with what it has instead of an empty bubble); complementary lever = route grounded-big-doc chat to a stronger model (the local qwen is the weak link). Needs a core dispatch-loop change + extension rebuild → offered to Michael, awaiting his nod on approach. **#1 SHIPPED:** ChatPanel header 🆔 chip when Details (`store.dev`) on → click-to-copy `activeSession` (copySession/shortSession). **#2 SHIPPED:** `/api/activity` recent now selects `cost_events.session_id` + returns a human `label` (`dispatchLabel`: work-item slug, else session decoded by prefix 🌍/💬/👁); ModelsPanel renders it. **Verified live:** feed reads "💬 chat: my-little-pony-…" not "—". cost_events has both work_item_id + session_id (the join just never surfaced session). NOTE the MLP corpus is now 69 small chunks (re-import) so the next chat paginates less.
- **★★ 2026-06-25 — MLP WORLD BUILD FAILED→FIXED→LIVE-PROVEN; doc-extract hardened (OSS `02a3655`; pushed; doc-extract:latest rebuilt).** Michael kicked off My-Little-Pony build himself (att #49 = 34MB Renegade "MLP Core Rulebook", world `my-little-pony`, he referenced project `books` via the new cross-project feature) + asked: does it pull in? does it handle "Michael Stufflebeam (Order #52343719)" properly? **It had FAILED — `doc_import_corpus` imported:0/skipped:1.** Root cause (NOT the "suspicious" scan verdict — that's benign /AA+/JS watermark JS, by-design flags-not-blocks): **two bugs.** (1) **tabula (pure-Go PDF text) silently yielded 0 words** → import skip-on-empty-text; poppler reads it fine (141k words). FIX: `pdfText` falls back to poppler **`pdftotext`** (already in the sandbox image for pdftoppm) when tabula errors/yields <50 words, keeps whichever has more (tabula stays primary for md tables). (2) **DriveThruRPG/Renegade stamp buyer PII `<name> (Order #digits)` on EVERY page (338×)** → would pollute corpus + be mistaken for the lead character. FIX: `stripPurchaseWatermarks` removes whole lines of that shape (precision>recall; generic for any purchased PDF; **CRLF-tolerant** — poppler emits \r\n, the anchor bug my first regex missed → inverse-hyp caught it). Oracle `TestStripPurchaseWatermarks` (CRLF case). **PROVEN on the real bytes:** converter 0→140,998 words, PII gone, lore intact. **LIVE e2e** (rebuilt doc-extract:latest, re-dispatched import on att 49 reusing the file, no re-upload): **69 docs imported, 0 PII in any doc**, world built **70 entities / 50 edges of accurate MLP lore** (Equestria/Celestia/Luna/Nightmare Moon/Twilight/Mane Six/CMC/Discord/Tirek/Chrysalis/Elements of Harmony…), **0 PII entities**. Scratch PII files removed. NOTE: his world still has stale `metadata.reference_projects=[books]` from the failed attempt (the clean rebuild ignored it; books NOT pulled — he can re-add via the form + hide via the new per-project toggle). Deploy = rebuild **doc-extract:latest** (sandbox converter); bridge unaffected. doc-extract `Run`/`ExtractFile` always sandboxes via `docker run`, so the fix lives in `internal/docextract`. Tasks #264 advanced (MLP pilot live).
- **★★★ 2026-06-25 — CROSS-PROJECT WORLDS (reference + link other buckets) SHIPPED+PROVEN (OSS `b8754f6` X1 + `558c3da` X2; pushed; grant/prompt live).** Michael's "crazy ask": a new world that references existing projects for grounding/connections (MLP refs a D&D-5e bucket; a yard-sensor world grounded in `market`+`cke`; ST over star-trek+tng+ds9). **Councilled (AskUserQuestion): BOTH — merge now, cross-world links later; FULL referenced graphs + a per-project show/hide toggle.** Model: **a project = a bucket of source; a world = a graph over a SET of buckets** (primary `worlds.project` + `worlds.metadata.reference_projects` jsonb, no migration). **X1 ENGINE:** `POST /api/world/build` takes `reference_projects` → stored on the world + the world-build agent told the canon spans the SET (doc_search each, ONE graph, merge shared entities, ADD cross-project edges). Build form: a checkbox list to reference other projects. **PROVEN:** a "realm" world referencing a "rules" bucket pulled `Initiative` (rules) in and linked `Aldon's knights --uses--> Initiative` — a node from one bucket edged to a node in the other. **X2 TOGGLE:** `/api/world/graph` enriches each node with `projects[]` (from source_refs→docs.project_association); WorldGraphPanel shows a 📁-chip row (only when a world spans >1 bucket) to hide a referenced bucket's nodes (perf/visual). Verified: nodes carry projects[] (star-trek-adventures→['star-trek'], single-bucket = no toggle). Spec `.spec/proposals/loreworks-cross-project.md`. **FOLLOW-ONS (deferred, ratified-later):** ② cross-WORLD reference edges (link to another world's nodes without re-extract); reuse an existing built world instead of re-extracting a big referenced bucket (CKE perf); node colour-by-project. **NEEDS browser REFRESH to get the new form+toggle.**
- **★★ 2026-06-25 — UPLOAD-IN-THE-BUILD-FORM + EXPAND-A-WORLD SHIPPED+PROVEN (OSS `da875de`; pushed; grant/prompt applied to live pg).** Michael hit the real gap: the 🌍 build form only let you PICK a formal project, but an imported corpus is a doc-TAG (so `star-trek` never showed) + no way to add a PDF or make a project from the form. (Answers his Q: STA canon = project `star-trek` [114 chunks]; the-one-ring = built from inline canon, NO project.) FIX: **(1)** form now takes a FILE (PDF/Office/zip/folder) + a project field that's NEW-or-EXISTING (datalist lists corpus tags via new `GET /api/world/projects` = formal projects UNION doc project_association). **(2)** `POST /api/world/build` accepts multipart → stores the upload as an attachment → world_upsert(private) → dispatches world-build with an IMPORT-FIRST instruction; the agent calls `doc_import_corpus` ONCE (new grant `world-build`→doc_import_corpus + prompt Step 0 in 55, applied to live pg surgically [agents UPSERT + grant ON CONFLICT]) to load+chunk into the project, then builds. **(3)** EXPAND: reuse an existing world name + project + upload more → import adds docs + idempotent world_*_upsert merges. **PROVEN LIVE e2e:** uploaded eldoria.md via the form endpoint (HTTP 200) → att:48 → agent doc_import_corpus'd into NEW project `eldoria-test` (doc source_object=att:48) → built 6 accurate entities (Queen Mira/Sir Caelum/Mirefolk/Eldoria/Shadowfen/Sunspire). `/api/world/projects` lists star-trek 114 etc. virgin-smoke unaffected (world-build allow grants 9→10; assertion >=8). **Michael must REFRESH his browser to get the new form (ui rebuilt).** curl gotcha: `-F file=@<long-abs-path>` → HTTP 000; cd to the dir + relative path works.
- **2026-06-25 — REVIEWED + MERGED Michael's work-box PR #2** (`feat/embed-query-dimensions` → main `ed02eb3`, squash, branch deleted). `embed_one` now SENDS the OpenAI-compat `dimensions` field (not just validates length) so a Matryoshka model (Vertex gemini-embedding, OpenAI text-embedding-3) can serve multiple widths; length-check kept as safety net. **Verified before merge:** diff correct; **live LM Studio nomic IGNORES `dimensions:768` and still returns 768** (the key risk — a provider that rejects vs ignores — cleared, dev embed path unaffected); **extension built + virgin-smoke green on the merged tree (OK 1→49, chain 00→59)**. No dev rebuild needed (nomic ignores it); benefit lands for MRL embed models on the work rig at its next pg build. Verification posted as a PR comment. Per-provider capability gate = a noted follow-up only if a future embed provider *rejects* the field.
- **★★ 2026-06-25 — INTAKE PREP DEPLOYED LIVE + PROVEN (OSS `c0fc8f7`; pushed; bridge rebuilt).** Closed the gaps so MLP (and any) drop→import→🌍build works self-serve. **(1) AUTO-CHUNK** — `doc_import_corpus` splits a large member into ~12KB parts (`slug-001…`, part N/M in fm; env `DOC_IMPORT_CHUNK_CHARS`); a small file stays one doc; each chunk keeps `source_object` (forward-populate). Fixes the giant-doc world-build flail. **(2) CONFIGURABLE CAPS** — archive guardrails env-tunable (`DOC_EXTRACT_MAX_TOTAL_MB`/`_MAX_ENTRY_MB`/`_MAX_ENTRIES`; defaults 200MB/50MB/1000 unchanged) for large trusted corpora. **(3) BRIDGE REBUILT+RECREATED SAFELY** — doc-extract-mcp is baked in the bridge; validated the overlay set with `docker compose config` FIRST (dev bridge = base+coder+doc-extract, NOT gemini-vertex which needs GEMINI_SA_FILE; socket deduped) → rebuilt + `up -d --no-deps bridge` → **all mounts/env preserved** (docker.sock, coder-worktrees, /workspace, DOC_EXTRACT), healthy, doc_import_corpus registered. Landmine avoided. **PROVEN LIVE e2e:** a **zip** (small.md+big.md) dispatched through the rebuilt bridge → big.md (44KB) auto-chunked into **4 parts**, small.md 1 doc, **all 5 stamped source_object=att:47**. **ZIP-as-world already worked** (RC-2/P3e archives→corpus); now big members inside also chunk. **SIZE answer (his 600MB CKE):** two parts — UNPACK caps now env-raisable; but INGEST via browser is the 64MB upload cap, so >64MB needs a non-browser path (server-side/volume load like I did for the STA PDF, or split <64MB, or a future streaming/async upload). Books (35MB) prove the flow. All genericized OSS (env-config, no STA/CKE specifics). Commits `e88b714`+`c0fc8f7`. **MLP is ready: drop ≤64MB PDF → import (auto-chunk+stamp) → 🌍 build → world w/ source links, no operator.**
- **★★ 2026-06-25 — FORWARD-POPULATE + STAR TREK INTAKE (real rulebook → 107-entity world) DONE+LIVE (OSS `e88b714`; pushed).** Michael: "do forward-populate; trigger the star trek intake; I'll do MLP after." **Forward-populate (O3 self-fill):** `doc_import_corpus` now stamps `frontmatter.source_object=att:<id>` on every pooled doc (cmd/doc-extract-mcp/tools.go) → anything built from a corpus opens its EXACT original; + attach cap **25→64MB** (rulebooks; MLP drop too). Code committed; live-deploys on next **bridge** rebuild (doc-extract-mcp is baked into the bridge image — recreate WITH coder+doc-extract overlays or lose docker.sock = the landmine; DEFERRED to avoid risking his live bridge). **★ STAR TREK INTAKE (his STA2e Core Rulebook, 35MB, file_private/local-only/never-pushed):** loaded server-side (docker cp→`pg_read_binary_file`→chat_attachments att:46, bypassing the HTTP cap since I'm not browser-dropping) + host `pdftotext` (1.14MB text, 2s). **★ KEY FINDING: world-build FLAILS on a single giant doc** — first import = ONE 1.1MB doc → agent pulled the whole body + burned its step budget on phrase-searches (pdftotext line-wrap breaks exact matches), 359 msgs / 0 entities. **FIX = CHUNK the corpus** (re-imported as 114 ×10KB docs, project `star-trek`, each source_object=att:46) → re-ran world-build → **107 entities / 123 edges of deep STA lore** (species Andorian/Bajoran/Cardassian/Ferengi/Jem'Hadar/Trill/Vulcan…, events Dominion War/Khitomer Accords/Hobus Supernova, factions Borg Collective/Breen/Dominion, DS9/Bajoran Wormhole/Utopia Planitia/Ambassador-class). **Forward-populate chain PROVEN on real content:** UFP entity → cites chunk `star-trek-core-007` → its `source_object=att:46` → the 34.9MB PDF (🖼 view source). **★ INTAKE FOLLOW-UP (worth building): `doc_import_corpus` should CHUNK a large single doc** (not one giant doc) so world-build + search work — same lesson as book_chunks. Tidied test worlds (kept the-one-ring 69 + star-trek-adventures 107). Tasks #271–274 ✅.
- **★★ 2026-06-25 — SELF-SERVE "🌍 BUILD A WORLD" from the UI SHIPPED+PROVEN (OSS `6630584`; pushed).** Michael: "can I build from the UI by adding the Star Trek PDF, or still through you?" Answer was "through me" (world-build is an agent, not UI-exposed) → built the flow. `POST /api/world/build {name, project|canon, instructions?}` → `world_upsert` (private default) + `dispatch_chat_turn('world-build')` over the canon → `{slug,session_id}`; a **🌍 build button + form** in WorldGraphPanel (name · canon project · paste-canon · extra direction) switches to the new world + opens the build session to watch. **PROVEN LIVE e2e via the endpoint:** tiny canon → 4 ent/4 edg; **Star Trek from PUBLIC canon → 14 ent / 12 edg** (Kirk/Spock/McCoy + Federation/Starfleet/Klingon/Romulan/Vulcans + USS Enterprise + Earth/Vulcan/Qo'noS/Romulus/SF — all grounded, zero invention); 🌍 button + form + star-trek-in-picker browser-verified; test world cleaned up, star-trek kept as the demo asset. **Full PDF→world is one drop away:** user drops the Modiphius PDF → import to a project → 🌍 build picks it (kept the purchased PDF OUT — used public canon; the deep PDF world is heavy+local-private, his drop or my import on request). Task #274 ✅.
- **★★ 2026-06-25 — "BRING THE CONTENT BACK" (object store / O1–O2) SHIPPED+LIVE (OSS `2657956` + `6b76fac`; pushed).** Michael: "add some kind of object store… we digest books/images/videos then it's hard to navigate back to them… paint a youtube video in frame like cpuchip.net… how do we bring content back?" Council finding: the substrate ALREADY has a de-facto store (chat_attachments bytea served by /api/chat/attachment/{id}; docs; book_text overlay) — the gaps were fragmented addressing + a markdown-only viewer + attachments unbrowsable. **Ratified (AskUserQuestion): thin registry over what we have (no new infra) + paint back ALL types + links/work-items/refs that just work.** SHIPPED: **O1 YouTube embed** — ArtifactPanel paints a digested video's player above its notes (id from frontmatter→url→slug `yt-<id>`/`videoyt-<id>`; youtube-nocookie). **O2 links** — useDocLinks now resolves intra-doc `#anchor` (headings carry slug ids), `#123`→work-item, relative `.md`/path→doc slug (was bailing). **O1c media library** — `cmd/stewards-ui/api/objects.go` (`/api/object/list` top-level media + `/api/object/pages?att=` PDF pages; plain reads, no SQL) + a **Media section** in BrowserPanel + ArtifactPanel `'object'` viewer (image inline / PDF page-flip / download fallback). Each: frontend build + go build + docker ui rebuild/recreate + **playwright-verified live** (embed/Zq1IEXTXmsw; heading ids; launchpad.png naturalWidth 1280). Tasks #271/#272 ✅. **★ O3 SHIPPED TOO (`13ed79b`, he chose "add a source-object link, wire it"):** a world source_ref can carry an `object` locator (`att:<id>`+page) and a doc `frontmatter.source_object` — both plain jsonb, NO migration (world.go passes source_refs verbatim). WorldGraphPanel node-detail → "🖼 source · p<n>" opens the exact page/image in the object viewer; ArtifactPanel doc → "🖼 view source" opens the original PDF/image. Verified live via a stamp-then-revert wiring proof (world node + doc both painted att:5, naturalWidth 1280). Task #273 ✅. **FORWARD-POPULATION = the remaining piece (not yet built):** teach the world-build agent's source_refs to record `object` + the doc-extract/import path to stamp `frontmatter.source_object` (so it populates itself going forward; mechanism + render are done). Book-at-chunk reader deferred (book_text is an overlay example, not core). UX nicety deferred: triggering from the World tab updates the Artifact panel behind it (no bring-to-front; same as the existing doc chip). Memory `project_stewdio`.
- **★ 2026-06-25 (post-compact) — WORLD-GRAPH GLOW FIX ✅ DONE+LIVE (OSS `c5da3b8` + docs `42b776b`; pushed).** Michael's carry-forward: "the world tab is incredible! but too much glow i cannot read the nodes. enhance the graphics and turn off the glow." Root cause = `UnrealBloomPass(1.2,0.6,0.2)` smearing the SpriteText labels (sprites → bloomed the TEXT). Fix in `WorldGraphPanel.vue`: removed bloom + import (note on re-adding subtle HIGH-threshold glow that won't touch labels); labels = lightened (-200) kind tint + glyph stroke + near-solid zinc-950 plate + kind frame + larger; spheres smoother (`nodeResolution 18`)+translucency; links dimmed so labels win. `npm run build` green → docker `ui` rebuilt+recreated → **playwright-verified live on the-one-ring: labels read cleanly, glow gone** (before/after sent). Build hygiene: docker stage-1 builds the SPA; local build overwrites the committed `dist/index.html` STUB → `git checkout`-restore before commit. **NEXT (Loreworks remaining): B** (MLP+Star-Trek worlds w/ vision, verb-fix applied) · **G** (persona-host room wiring) · **F** (video assemble). Memory `project_loreworks` updated (carry-forward marked done).
- **★★★ 2026-06-25 (ultracode, 27h→Friday) — LOREWORKS: the FULL EXPERIENCE built + proven live; demo beats 1–7 working.** On top of the engine (A+E, below), this push shipped+proved: **56 trajectory critic** (Google Glass-Box — `assemble_trajectory`+`trajectory-critic` judge; scored the One Ring build FAIL-on-efficiency, catching a real verbatim-batch-repeat bug → prompt-fixed; + `world-critic` pruned 3 real misreads). **57 C** (`world_entity_hybrid` semantic leg + loremaster + lore tools + lore_inject) — embedded the-one-ring's 69 entities; "ruined city by a lake"→Annúminas; loremaster answered the Brandywine grounded (0 hallucination). **58 edge-audit** (deterministic `world_rel_kinds`+`world_edge_audit`; flagged 50 edges on the real world = systematic home_of-misuse → **root-cause fix: taught world-build located_in/dwells_in/home_of directions** before B). **3D knowledge-graph panel LIVE** (`b7c335e`, delegated to a dev subagent: world.go API + WorldGraphPanel.vue, 3d-force-graph, playwright-verified on the-one-ring, 0 console errors — the visual showpiece; screenshots sent). **★ SELF-IMPROVEMENT LOOP `59` shipped+proven+gated** (Michael "can it fix itself?" → ratified full-loop-auto-apply-in-bounds, dominion_in_council): trajectory critic → `agent_failure_patterns` → `agent-improver` proposes a scoped clause → **deterministic GATE** (eval-gaming guard: judges/critics/gates/self escalate; additive-only; ≤600c; re-checked at apply) → in-bounds auto-apply (trailed+reversible), out-of-bounds escalate. **Red-team caught 2 gate holes** (Postgres `\y` not `\b`; "ignore-your-grounding" uncovered) + a `jsonb[]::jsonb` cast bug the live-apply caught that virgin missed → all fixed + smoke-guarded. **E2E LIVE:** improver auto-applied a real summary-fix to world-build; dangerous clauses escalate. virgin-smoke OK 49, chain 00→59. Commit (this) + journal `2026-06-25-self-improvement-loop.md`. Task #270 ✅. Chain 00→59. Commits `13fde61`/`cd35b38`/`7f1950f`/`b7c335e`(+59) + journals `2026-06-25-{trajectory-critic,loreworks-C-lore-chat,self-improvement-loop}`. Design doc + Friday script: `.spec/proposals/loreworks-presentation-plan.md`. **NEXT push (specified):** B (MLP+Star-Trek worlds w/ vision, verb-fix applied) · G (persona-host room wiring; lore_inject built) · F (video: script done, hyperframes MCP + voice-clone = local-GPU setup). Tasks #262/263/265/269/270 ✅; #264/267/268 pending.
- **★★★ 2026-06-25 (innovation week, ultracode) — LOREWORKS flagship: the ENGINE built + PROVEN LIVE (A+E, OSS chain 00→55, 4 commits + 1 journal, all pushed).** Michael ratified the whole arc (A–G) in council, then "work for a few hours while I'm asleep … I want big … git it king." **Loreworks = drop source lore → the substrate BUILDS a World** (meaning-searchable canon + extracted entity/relationship knowledge graph + personas you chat with). **Ratified: engine→PUBLIC OSS core, all TTRPG/world content→local-private.** Pilot: MLP / Star Trek Adventures / The One Ring (TTRPG drop 6.3GB/156 PDFs @ `external_context/ttrpg/**`, gitignored). Spec `.spec/proposals/loreworks.md`. **SHIPPED+PROVEN+LIVE:** **A `stewards.embed_query()`** (`5db54d8`, ext 0.2.0→0.3.0; from branch `feat/sql-embed-query-hybrid`) — synchronous query-time embedding from SQL (the substrate had none) → unblocks hybrid FTS+semantic RRF; `embed_one()` refactor + `#[pg_extern]`; **inverse-hyp proven live vs nomic: synonyms 0.33 vs unrelated 0.64**. **E1** (`32fc9e9`, `54-loreworks.sql`) worlds/world_entities/world_edges (relational graph, no AGE) + upsert/show/graph/entity_search; OK 44. **E2** (`ca29c7a`, `55-loreworks-build.sql`) world_entity_upsert/world_edge_upsert sql_fn tools + `world-build` agent (deny*+allow-list, steps 60); OK 45. **★★ CAPSTONE PROVEN LIVE:** dispatched world-build on a synthetic 4-sentence canon → **10-entity/10-edge grounded graph in ~15s/$0** on the local rig — every entity kinded, EVERY edge from the source, zero hallucination, agent journaled the spine (demo world `lore-smoke` in dev pg). Source lore → explorable world works end-to-end. **NITS:** entity summaries empty (model prioritized structure — prompt-tune); embed_query no-model falls back to provider.default (qwen chat) → callers pass embed model explicitly / config `embed_model` read = small Rust follow-up; full SCHEDULED pipeline deferred (tools+agent build in one dispatch). **F-prep scout DONE** (`loreworks-F-prep.md`: hyperframes-as-MCP + voice-clone plan + draft 6-min script). **NEXT (sequenced, wired):** B TTRPG import (WATCHED — big PDFs, heavy embed) → C loremaster+lore-lens+3D graph (`3d-force-graph`) + embed_query vec-leg on entity search → D #251 binary-sanitize+dead-model prune (lane null-byte ✅) → F assemble video → G world chat rooms in Stewdio. Build gotcha: live volume doesn't re-run CREATE EXTENSION → apply new SQL directly; a new pg_extern needs the .so recreated + a surgical CREATE FUNCTION (`embed_query_wrapper`). Live embed = lm_studio@1234 `text-embedding-nomic-embed-text-v1.5`@768 (ollama:11434 down). **HOUSEKEEPING:** MEMORY.md over the 24.4KB read limit (36KB) — needs a compaction pass (deferred from this night). Tasks #262/#263 ✅; #264-268 pending. Journal `2026-06-25-loreworks-night-A-E.md`; memory `project_loreworks`.
- **★★★ 2026-06-25 — RC-2 + RC-3 COMPLETE + ALL THREE (RC-1/2/3) DEPLOYED LIVE ON DEV (OSS `6901c3c` RC-2, `f61e780` RC-3; pushed).** Michael (away, kids to bed): "finish rc-2 and rc-3 … I know you are capable of it." Drove the whole arc autonomously on the proven loop (build→oracle→adversarial QA→fix→commit), the loop as the ceiling under depth. **RC-2 (explore a DROPPED code archive read-only):** `docextract.Unpack` export + `sandbox.UnpackArchiveToWorktree`/`writeMembers`/`withinDir`; `coder_sandbox_start`+`research_codebase` gain `attachment_id`; doc_extract code-hint → `research_codebase(attachment_id=N)`. **★ the adversarial QA (`wf_9059f0b6`) caught a BLOCKER I missed — severe:** I hardened the archive MEMBER names (zip-slip) but NOT the sandbox id — `sanitize(".")=="."`/`sanitize("..")==".."` → a prompt-injected `sandbox=".."` → `root=/worktrees/..` → **`rm -rf /` + `chown -R /`** on the bridge (pre-existing in CloneRepo too). Fixed in layers (all oracle-covered): sanitize strips leading dots; `worktreeChildOK` strict-parent guard (UnpackArchiveToWorktree AND CloneRepo); tool-boundary rejects path-separator/whitespace/".." ids; + HIGH reuse-skip-for-attachment, HIGH force-NetOff-on-untrusted-unpack, MED per-member structural scan + 128MB caps. **RC-3 (lift the ~120s import cliff):** the binding cliff was the bridge daemon's UNIFORM `--call-timeout 120` (`bridge_run.go`); added `--slow-call-timeout 600` for a slow-tool set (`doc_extract`/`doc_import_corpus`, env-overridable) via `callTimeoutFor` (per-tool, oracle-tested) + `ExtractArgs.TimeoutSecs`→converter `-timeout`+container ctx (aligned bridge 600 ≥ container 570 ≥ converter 540). **Design call (honest):** RAISED the cliff (the goal) rather than rearchitect async — RC-2 already routes the common painful case (a code repo) to extract-free explore; full async = over-engineering for the rare big-DOC-corpus case (deferred, the plumbing + activity feed are its foundation). **DEPLOYED + VERIFIED LIVE ON DEV:** applied 53 grant to live pg; rebuilt+recreated the bridge (all RC Go); `refresh-tools` → `research_codebase`+`coder_sandbox_start` carry `attachment_id` live, research_codebase required=["question"]; bridge log shows `slow-call-timeout=600s (slow tools: [doc_extract doc_import_corpus])`; coder-runtime+doc-extract images present. **HIS HANDS (the one un-driven check): a model-driven e2e** — drop a repo zip → it explores; or `/explore <url>` → public clone+explore. Logic is oracle+QA-covered; plumbing verified live. Journals `2026-06-25-rc2-explore-dropped-archive.md` + `2026-06-25-rc3-raise-the-import-cliff.md`. Tasks #259/#260/#261 ✅. LESSON: a zip-slip test on the input I was thinking about isn't "found every untrusted input" — the QA found the id flowing into rm -rf; `worktreeChildOK` is that lesson generalized.
- **★ 2026-06-24 — ACTIVITY PANE shows NON-LLM activity + LIVE DIAGNOSIS (OSS `d562b5d`; pushed). UI rebuilt+recreated on dev (feature is LIVE there).** Michael (live cockpit): "model errored out, look into it" + "activity feed show docker/non-LLM activity like virus scans." **DIAGNOSIS:** "AI(50)" = chat `stewdio-project-ai` (ai project, 50 docs); he attached the **pg-ai-stewards repo zip** → `doc_import_corpus`+`doc_extract` both **timed out at ~120s** (converter deadline) → agent said "session error." **NOT the model — `doc_extract` timeout on a big repo = the RC-3 gap** (his "premature without RC-2/3" was right). Separately a real model wave (opencode free promos expiring: `qwen3.6-plus-free` 401 "promotion ended", ~half `opencode_go` 429) but **auto-probe marks unusable + `reason` fails over to local flexllama gemma** (priority 0/1 usable; cost_events prove serving) → self-heals; dead members = probe-noise (prune-from-overlay follow-up). **SHIPPED:** `/api/activity` `tools[]` (5th query = recent `mcp_proxy` work_queue rows = container/tool calls: doc-extract/coder/scan w/ tool/server/status/error/duration) + ModelsPanel **"Tools & sandboxes"** section (in-flight pulse, errored shows failure). Live-proven shows `doc_extract→error→120006ms` (the stall, made visible). DB-based (no docker socket); oracle 21/21 (+sandboxes assertion). Journal `2026-06-24-activity-non-llm-and-diagnosis.md`. **★ RC-3 is now the clearly-urgent next build (real use hit the ~120s cliff).** NOTE: dev BRIDGE+PG still old code → `/explore` (RC-1) + RC-2 classifier NOT live on dev until a bridge+pg rebuild (UI-only rebuilt this round); work rig gets all on next rebuild.
- **★ 2026-06-24 — RC-2 DETECTION HALF SHIPPED (OSS `12c7512`; pushed).** When a CODE archive is dropped it should be EXPLORED in a sandbox, not embedded file-by-file. Shipped the safe deterministic routing brain: `internal/docextract/classify.go` `Classify(paths)→code|docs|mixed`+reason (list-only=pure oracle; build-manifest [go.mod/package.json/Cargo.toml/Dockerfile/…] or `.git` decisive for code; oracle 14/14) + wired into `doc_extract` (a dropped archive carries `repo_kind`/`repo_reason`; for code the summary tells the agent to explore via research_codebase/the RC-1 `/explore` URL path instead of embed). Advisory+list-based = no security surface, oracle suffices (no adversarial QA warranted — match rigor to risk). **DEFERRED (the hot half, NOT rushed at depth): the untrusted-unpack-into-worktree seam** — `sandbox.UnpackArchive` (zip-slip/bomb caps, reuse docextract caps, read-only explore) + `coder_sandbox_start` attachment mode + `research_codebase` taught to explore a LOCAL tree (today URL-only; a dropped zip has no URL — the genuine seam decision). = a fresh untrusted-input security surface (RC-1's class) → fresh adversarial pass. Journal `2026-06-24-rc2-code-vs-docs-detection.md`. #260 detection done, seam pending.
- **★★ 2026-06-24 — RC-1 SHIPPED: explore + build-off PUBLIC repos from chat, anonymous clone, NO db embed (OSS `9924d9b`; pushed). Michael: "widen it a bit… research and build off public repos… no db embeddings… lets go."** The explore machinery already existed (`research_codebase`→`coder_sandbox_start`→read-only grep/read, file:line cites); the gaps were the deny-by-default repo allow-list + the missing chat grant. **Built:** (1) `cmd/coder-mcp/sandbox/sandbox.go` **public-repo clone lane** — `cloneMode`→token(allow-listed,credentialed)/anon(public host, anonymous)/refused; anon is **self-enforcing public-only** (a private repo fails to clone anonymously); ON by default, `CODER_PUBLIC_REPOS=false` disables, `CODER_PUBLIC_HOSTS` overrides. (2) `53-explore-repos.sql` grants `research_codebase`→`work-item-chat` (+lib.rs+Dockerfile COPY; virgin-smoke OK 43). (3) `/explore <url>` chat slash command. **★ The adversarial SECURITY QA (`wf_76537cf2`) caught TWO REAL vulns — both fixed + oracle-covered (`sandbox_test.go`):** (a) **credential leak** — `-c credential.helper=` does NOT clear a SYSTEM/GLOBAL helper (config merges system→global→`-c`), so the "anon" lane could still offer the bridge's token → fixed with a **hermetic `anonGitEnv()`** (strip GITHUB_TOKEN/GH_TOKEN, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`, no-`~/.netrc` HOME; closes url.insteadOf/extraHeader/netrc too); (b) **token EXFILTRATION** — the allow-list matched `strings.Contains` over the WHOLE URL, so `https://evil.com/github.com/owner/x` took the CREDENTIALED path → token to evil.com → fixed with **`hostRootedPath` anchored `HasPrefix`** (pre-existing bug RC-1 newly made chat-reachable). + bare-name `/explore react`→404 now refuses clearly; SECURITY.md/.env.example no longer claim "deny-all by default" (document the lane + off-switch + widened build-script residual risk; explore is exec-DENIED, build/exec flows operator-gated). **VERIFIED:** go vet/build, security oracle (exfil refused + hermetic env), **live anon clone** (octocat/Hello-World clones, private fails fast), virgin-smoke 00→53. **DEFERRED:** full chat→explore e2e (needs the coder overlay+runtime+model+net — the WORK RIG has it; dev stack bridge/pg NOT yet rebuilt w/ RC-1 so /explore isn't live on dev until a rebuild) · 2 LOW nits (`:443` port falls off anon lane; `http://` only takes token path). Journal `2026-06-24-rc1-explore-public-repos.md`. Task #259 ✅. **NEXT: RC-2 (#260 route a dropped CODE archive to explore, not embed) · RC-3 (#261 async corpus import).** LESSON: on a security widening the adversarial pass is not optional — a green functional oracle said "works" while quietly carrying a token-exfil primitive + a credential-leak hole.
- **★★ 2026-06-24 — STEWDIO "DETAILS MODE" SHIPPED (OSS `0c86d75`; pushed) + import/clone arc RATIFIED.** Michael: "we need a debug/details mode … see all the models and a stream of tokens, tool calls … sometimes it looks like nothing is happening, no thinking badge." Same loop (build→oracle→workflow QA→fix→commit). **Built:** (1) **working pulse (unconditional bug fix)** — chat badge now driven by an authoritative `working` polled from `chatSessionStatus` (in-flight work_queue), folded into the unified `pollWorkItems`/`scheduleWorkItems`; `busy = pending||working||card-in-flight`; **names the current activity** ("working · searching docs") from the last turn's tool calls. (2) **Activity stream (Details-gated)** — Models pane → **Activity** + a "Live dispatches" feed (model/↑in↓out tokens/pipeline) off `/api/activity.recent` (was never surfaced), 2.5s busy-poll. (3) **relabel ⚙ Dev → ⚙ Details** (same `store.dev` flag). Oracle `dev-toggle.oracle.sh` **20/20** (+Live-dispatches +relabel). **★ QA workflow `wf_459f9779` caught 3 HIGHs the oracle couldn't — incl. my first pulse fix only NARROWED the bug (forever→≤15s dead air; idle-cadence snapshot) → 45s cool-down + `pollSoon()` on send/terminal; AND making `working` authoritative re-raised the badge ~3s after ■stop → `cancelling` guard (live-verified stays off 12s); + stale-session write race → capture-sid guard; + dockview stale title → `applyCatalogTitles()`; + orphan-timer revive → `alive` guard.** LESSON: making a signal trustworthy isn't enough if you only sample it every 15s; authoritative-working defeated stop()'s optimistic clear that silently worked before — both invisible to a green oracle, the adversarial trace found them. Journal `2026-06-24-stewdio-details-mode.md`. Task #258 ✅.
  - **★ ZIP-TIMEOUT DIAGNOSED + IMPORT/CLONE ARC RATIFIED (Michael, this session).** A `pg-ai-stewards` repo **zip timed out**: `doc_import_corpus` runs ONE **180s synchronous** extraction container over the WHOLE archive + embeds every file inside the chat turn (`cmd/doc-extract-mcp/tools.go`, `runner.Timeout=180s`). Michael ratified all three fixes: **F1** async large-file corpus import = work item + progress card (#261/RC-3); **F2** LLM content-routing — detect a CODE repo → explore-in-sandbox not embed (#260/RC-2); **F3** clone a public repo from chat (#259/RC-1). **★ KEY FINDING: the machinery already exists** — `research_codebase` (read-only explore: cheap sub-agent → `coder_sandbox_start` → grep/read, no write/exec/git, file:line cites) + `git_clone`; the gap is the **repo allow-list** (`CODER_REPO_ALLOWLIST`, default just ai-chattermax) refusing arbitrary public repos. RC-1 = add a PUBLIC-repo lane (https/known-git-hosts/no-creds/read-only sandbox, **runs LOCAL not NOCIX**) + wire `/explore <url>` into chat. **SURFACED to Michael for his nod on the security defaults before relaxing the deny-by-default allow-list** (dominion_in_council on the clone-arbitrary-public-repos surface). Tasks #259/#260/#261. **NEXT: his go on RC-1 defaults → build RC-1 the proven way.**
- **★★ 2026-06-24/25 — AUTONOMOUS FOCUSING-RELEASE RUN: S5 intent launcher SHIPPED (OSS `eefa562`; pushed) — second full tested increment of the night.** Michael: "keep going as far as you can push it without my input!" Same proven loop (build→deterministic oracle→workflow QA→fix→commit). **S5:** the raw `<select>` of 53 pipeline slugs → a VERB GRID (the things the engine is for), rendering each pipeline's description not its slug; `launch()` submits the family unchanged. **The QA workflow (`wf_af3ab766-81c`) + a confirming grep reshaped the design:** dropped **Build** (code-pr needs repo+acceptance_criteria — fails from a topic string, QA-proven vs 20-coder.sql) + **Digest** (book-digest is shelf-queue-driven via book_next = the scheduled digester, not topic-driven — grep caught what the QA missed); **gated "more pipelines…" (raw slugs) behind store.dev** (it re-opened the exact jargon the S1-S4 Developer toggle had just closed). Everyday verbs = **Research/Generate/Reflect** (binding-question-driven, actually work). + desync-fix, empty-state, reset-on-open/launch. Oracle **19/19** (verbs render, Build dropped, "more" Dev-only). **LESSON: a single topic textarea only fits binding-question pipelines — verb concept must match the pipeline's required input or the everyday user gets a button that silently fails; verify the QA's findings, don't just trust the list.** Build+Digest need adaptive input forms (repo / source-shelf) = **task #257** before re-adding. Journal `2026-06-24-stewdio-intent-launcher.md`. **RUN BANKED at S5** (2 clean tested increments; not degrading at depth). **REMAINING (no new verbs): S6** session-merge + chat-header declutter (drop ⬇/label 💬; autonomous) · **S7** brandTitle hook (trivial — batch w/ S8, both touch App.vue) · **S8** collapse ~18-route nav into Developer menu — **NEEDS Michael's call on the everyday-nav minimal set** (hiding /covenants,/sabbath,/trust changes reachability) · **S9** Tufte SparkMetrics (largest; define data-binding oracle first). Tasks #253(streamline,in-progress)/#257.
- **★★ 2026-06-24 — FOCUSING RELEASE S1–S4 SHIPPED: the Stewdio "Developer toggle" (one surface, two depths), workflow-tested (OSS `5d96bc9`; pushed).** Michael: "keep pushing... test the snot out of it, automagically with workflows." Demonstrated the full loop = **build → deterministic oracle → workflow QA → fix → commit.** Built `store.dev` persisted flag (OFF=clean everyday cockpit; ON=power surfaces back, zero capability loss): gates the model-role select (+added missing `vision` option), the 🔧-retrieving/provenance row, ArtifactPanel s.model+raw-input-JSON+pipeline/maturity jargon, the Models pane. Labels: Work items→**Library**, ＋New→＋New task/＋New chat. **Oracle-first** (`cmd/stewards-ui/frontend/test/dev-toggle.oracle.sh`, playwright-cli, **13/13** — caught 4 of my own bad test assumptions incl. a CSS-uppercase/innerText case trap). **4-lens QA workflow** (`wf_4c77ff4a-461`: leak/regression/ux/completeness) **found the BLOCKER the oracle missed** — Models pane leaks on Dev OFF via saved-layout-restore + runtime-toggle (visiblePanels only governs the launcher, not open panes). **Fixed:** `closeDevPanes()` watch(store.dev)+onReady close-loop (reuses PANELS dev:true); LAYOUT_KEY v2→v3; un-persisted chatModel (footgun: Dev-only select strands everyday user on a hidden role); typeof-guarded persisted(). Oracle EXTENDED to drive the leak path (open Models→toggle off→assert closed). **LESSON:** oracle=floor (perfect recall on what it drives); adversarial workflow catches what the oracle structurally can't (oracle drove launcher path; leak lived in restore/runtime paths). When a bug escapes the oracle, extend the oracle. **NEXT (no new verbs): S5 intent-named launcher** (READY, no backend dep — render `pipelines.value.description`, map to Research/Generate/Digest/Build/Reflect; highest-leverage discoverability win), then S6 session-merge, S7 brandTitle, S8 nav-collapse (needs Michael's everyday-nav call), S9 Tufte SparkMetrics. UX carry: drop ⬇ / label 💬 in chat header; persist lens. Journal `2026-06-24-stewdio-developer-toggle.md`. Tasks #253(streamline, in-progress)/#256(key-model on-ramp).
- **★★ 2026-06-24 — "ULTRACODE" KNOWLEDGE-ENGINE DIRECTION EXPLORATION + brand reconciliation + focusing-release plan (OSS uncommitted: new README draft + anchor doc).** Michael's big strategic ask: test pg-ai-stewards, streamline UI, compare to providers, branding refresh ("Knowledge Engine"?), Tufte metrics, AND "is the direction right / did we lose focus / gaps?". Ran a 7-agent workflow (`wf_3d44035b-af3`: UI audit · capability map · strategy review · competitive scan → branding · streamline · red-team; 617k tok) + first-hand live drive (nav = ~20 items incl. plumbing; `doc_search` FTS-only → 0 on NL query). **VERDICT:** engine is genuinely ONE thing at the architecture layer (focus NOT lost there); focus WAS lost on the vision axis (install/everyday-UI/robust-self-learning got ~0 investment while breadth accreted — "four Rounds in a day"; *the builder is building for the builder*). Red-team hard truth (kept): the rebrand is the FUN task, not the first one; brand is the reward AFTER the on-ramp. **"Knowledge Engine" = great descriptor, weak brand** (Wolfram owns it since 2009, USPTO-descriptive/unregistrable, SEO-diluted) → keep the ownable mark. **★ MICHAEL'S BRAND RECONCILIATION (better than any pure option): Stewdio (app, stew-dio/stewards-studio) + Worlds (organizing noun — a world = a corpus you study OR invent: business KB / topic / book / D&D campaign, same machinery gather→digest→fact-find→relate→present→engage) + "knowledge engine" descriptor.** Worlds makes the serious business generally applicable (dossier-builder = dungeon master, one pipeline). Chose **"A focusing release (no new verbs)."** **IN:** README/positioning reframe (DRAFTED, uncommitted — pending his read), Developer-toggle + intent-named-launcher streamline (S1–S9), one bulletproof self-learning demo, Tufte SparkMetrics. **NEXT ARC (Worlds, deferred, named so it doesn't leak):** world/canon model + good 3D knowledge graph (current is bad) + personas live in Stewdio chat for role-play (ai-chattermax-style, "make it come alive"). Anchor: `.spec/proposals/positioning-and-focusing-release.md`. Tasks #252–255. **NEXT: Michael blesses/tunes the README → commit+push; then build the Developer toggle + intent launcher.**
- **★ 2026-06-24 — DOCS REFRESHED FOR THE WORK-BOX PORT (OSS `30fa06f`; pushed).** Michael: "make sure docs are updated so I can push this to my other computer and soak." Rewrote `docs/rich-chat-and-artifacts.md` (the canonical fresh-rig bring-up runbook) current to this session: chain 00→52 / virgin-smoke OK 42; **native Vertex Gemini (no-train, google_sa) as the recommended work-confidential provider** (gemini-vertex overlay + role-alias seeds w/ google/ prefix; AI-Studio key reframed public-only); **the overlay `-f` flags are load-bearing on RECREATE too** (a base-only force-recreate drops the bridge's docker.sock → doc-build/doc-extract break — the bug this session hit; gave a `stew` alias); **book corpus backfill step** (`examples/backfill-book-corpus.py`); the cockpit additions (live cards, sessions hierarchy, collapse rails, grounding picker, generate_image, artifact cards — fixed the stale "deferred"); dispatcher-owned session_id; retry/backoff; known rough edges (Vertex preview 429s, the #251 coder_read-binary 22P05 wedge). + `docs/wiring-up-models.md` gained a Vertex no-train variant. README's high-level "core boots on plain compose up" left as-is (accurate). **Substrate is port-ready.**
- **★ 2026-06-24 — SOAK cont.: doc-build routing fix + Stewdio Sessions hierarchy/collapse (OSS `cbca008`; pushed).** Michael's doc-build (5-books pptx) hit the artifact gate 3× — build worked every time; 3 distinct routing/infra issues: **(1)** I'd force-recreated the bridge with BASE compose only → dropped the coder overlay's `/var/run/docker.sock` → no sandbox. **LESSON: recreate bridge WITH overlays** (`-f base -f coder -f doc-extract`). **(2) my #245 inject_session mis-routed the export** — coder_export_artifact does DELIBERATE cross-session routing (build stage → spawned_from_chat via template) but inject overrode it to the transient build session → gate failed. **FIX (52): dropped inject_session from coder_export_artifact** (only generate_image keeps it; .so unchanged; virgin-smoke 00→52 green). **(3) coder_read of a binary pptx wedged the loop** — its `` → jsonb 22P05 → mcp_proxy child stuck in_progress → tool_dispatch hung ~15min (= **task #251**, bridge result sanitize). Delivered the built deck (re-pointed attachment 39→books chat, completed its item, cancelled the wedged run). **STEWDIO (Michael's asks):** SessionsPanel groups chats hierarchically under their target (🗂project/◆wi/📄doc) w/ collapsible headers (multiple books chats nest under "books"); collapse rails → **geometric** (left=leftmost col, right=rightmost, tracked by group id) after the panel-anchored attempt misfired (left rail collapsed the right col). Both verified live (playwright). Journal `2026-06-24-soak-docbuild-and-sessions-hierarchy.md`. **CARRY: #251 (jsonb sanitize, high value) + inject_session FALLBACK refinement (inject only when arg absent/placeholder).**
- **★ 2026-06-24 — SOAK FOUND + FIXED 2 BUGS (Michael's Stewdio chat "stalled"; OSS `77bcd9e`; pushed).** Diagnosed (looked, didn't guess): nothing hung — session `stewdio-project-books` ran 12 iters, ended `steps_exhausted`. Root: **`work_item_list` errored `can't scan into dest[7] (token_budget)`** — `cmd/stewards-mcp/inspection.go` scanned nullable work_items cols (token_budget no default → NULL on a launched item) into non-pointer Go fields; same class as the old NULL-slug bug. Agent floundered → burned step budget. **Fix:** COALESCE every nullable-into-non-pointer col in work_item_list + the 2 watchman_passes queries. Proven via mcp_proxy: now returns `{count,items}` (was error). **Bug 2 (UX):** a loop stopping on steps_exhausted/truncation leaves the last assistant msg at `finish_reason=tool_calls` → SSE never sends a terminal → ChatPanel spinner sticks. **Fix:** `GET /api/chat/session-status?session=`→`{pending}` (any work_queue row pending/in_progress/waiting for the session); ChatPanel polls 5s while thinking, clears when truly idle (substrate's real state, not a timer). Proven `{"pending":false}` on the stalled session. bridge+ui rebuilt/recreated. Journal `2026-06-24-soak-bugs-work-item-list-and-spinner.md`. **The soak is working — real use surfaced real bugs within the hour.**
- **★ 2026-06-24 — BOOK CORPUS BACKFILLED LIBRARY-WIDE (Michael: "back port all of those books!"; OSS `0c00ac7` = `examples/backfill-book-corpus.py` + journal `2026-06-24-book-corpus-backfill.md`; pushed).** Stewdio P3's book corpus only persisted On Liberty; ~59 digested books had a study doc but no `book_text`/`book_chunks` → `book_search` couldn't quote them. Backfilled via `book_persist_corpus(slug,title,author,url,full_text)` (takes text directly — no model/re-digest); fetched each source (gutenberg.org catalog URL → canonical `/cache/epub/<id>/pg<id>.txt`; pristine text for verbatim quoting), boilerplate-trimmed, title-verified, loaded server-side via `pg_read_file` (text never on a shell arg). **RESULT: 52 books / 23,628 chunks / 34 MB now quotable** (Don Quixote, Brothers Karamazov, Wealth of Nations, Republic, Leviathan, Critique of Pure Reason, Iliad, …). PROVEN: Republic `found_verbatim=true` "justice is the interest of the stronger" +14 passages; the-prince exact line true; quote-guard returns false for paraphrases (anti-hallucination working). **12 intentionally NOT ingested (reported):** in-copyright (Camus/Becker/ESR×2/Seneca-translation/Woolf), dup (tao-te-ching=the-tao-teh-king), not-a-book (critique-revised-digest), no-clean-source (self-reliance, five-rings HTML-only, non-euclidean+science-and-hypothesis = Gutenberg PDF/HTML-only no text path). **★ corpus = gitignored DB data → the WORK RIG must run the script once to populate its own book_text** (that's why committed; `STEWARDS_PG_CONTAINER` overrides). Idempotent (skips persisted). gotchas: Gutenberg throttles ~50 rapid fetches (retry w/ backoff); `pg_read_file` (superuser) is the clean large-text load path; ad-hoc `docker exec` heredoc needs `-i`. Task #250 ✅.
- **★ 2026-06-24 — #243 RETRY/BACKOFF HALF SHIPPED + PROVEN (oracle-first; OSS `91823d3`, journal `2026-06-24-retry-backoff-243.md`; pushed).** Michael: "should we tackle 243 now?" → oracle-first. Traced the propagation (chat()/embed() = ONE `req.send()`, NO retry → 429→Err→chat row error→work_item_advance trigger→work_item failed→32 only re-runs the whole STAGE). **Fix: `send_with_retry(build,label)` in bgworker.rs** — retry POST on transient (HTTP 408/429/5xx incl Cloudflare 52x, or network err) w/ exp backoff (base*2^(n-1) cap 10s); non-transient 4xx fail fast; bearer minted once before loop (SA cached) + rebuild RequestBuilder per attempt; chat()+embed(); env-tunable `STEWARDS_HTTP_RETRY_MAX`(3)/`_BASE_MS`(800). Blip absorbed in place; persistent transient falls through (bounded) to 32 backstop. **ORACLE KEPT `tests/retry-oracle/`** (controllable 429 stub as a provider on scratch pg). **INVERSE-HYP PROVEN:** baseline(no-retry .so) 429×1→error/1req; absorb 429×2→200→done/3req; exhaustion 429-always→error after exactly 3(bounded)+backoff logs+no loop. virgin-smoke 00→52 green; dev pg recreated w/ retry. **GOTCHAS:** messages.session_id FKs sessions (ad-hoc enqueue must INSERT session first or dispatch tx rolls back+loops); Win `pkill -f` won't kill python→PowerShell Get-CimInstance|Stop-Process; `docker run` scratch needs `--add-host=host.docker.internal:host-gateway`. **#243 retry half DONE (task ✅); deeper half = #249** mid-loop MEMBER re-resolution (switch alias members w/o restarting — needs the alias threaded into the continuation payload; 32 backstops). Pre-existing lib.rs unused-import warning (from Vertex merge) = separate cleanup.
- **★ 2026-06-24 — MERGED PR #1: native Google Vertex SA auth (`google_sa`, no-train, no proxy) — `60c1300` (journal `ca92c06`); PR MERGED.** Michael's work-side PR; reviewed + merged. Adds `gcp_sa.rs` (RS256 JWT→jwt-bearer→scoped Vertex OAuth token, cached/refreshed, **key/JWT/token never logged**) + `AuthMode{ApiKey|GoogleSa}` + `Provider::bearer_token()`/`auth_label()` (providers.rs) + bgworker chat/embed dispatch via bearer_token (Anthropic still x-api-key) + `jsonwebtoken` dep + opt-in `docker-compose.gemini-vertex.yaml` (SA key mounted RO). **Backward compatible** (no AUTH → api_key). The work-confidential Gemini path (Vertex=no-train vs AI-Studio key=trains); native OpenAI-compat so the existing Gemini tool-loop fixes apply (no shim). **VERIFIED here (no Vertex creds):** compile oracle (pg image builds w/ jsonwebtoken+gcp_sa), virgin-smoke 00→52 green, live boot shows new `auth=api_key` label on all existing providers (backward compat) + 52 trigger persisted. Michael proved the live Vertex path on the work box (doc-build, ~17 tool turns, real PDF, thought_signature round-trips). Merge auto-resolved clean vs my Round-4 (lib.rs: mod gcp_sa + 52 entry). Dev pg recreated = matches main. **★ PR carry = #243 SHARPENED:** the stage model is resolved ONCE → a single 429/503 on tool turn N fails the whole stage → fix = backoff/retry + **mid-loop alias re-resolution INSIDE the bgworker tool loop** (hot path, own pass). Surfaced by a Vertex preview-model (gemini-3.1-pro) 429; 3.5-flash absorbs transient 429s. (Pre-existing lib.rs unused-import warning noted, separate cleanup.)
- **★ 2026-06-24 ROUND 4 — DISPATCHER-OWNED session_id + LIVE WORK-ITEM CARDS + GROUNDING PICKER + COLLAPSE RAILS (Michael's "a few thoughts"; OSS chain 00→52, `385058f` + journal `f0a9807`; pushed).** (a) session_id is the DISPATCHER's not the model's — `inject_session` marker on tool_defs + `tools.rs::exec_one_tool` overrides the model-supplied id with the real dispatch session (answers Michael's "can the harness populate it?"=YES, build-the-oracle). Marker lives in a **BEFORE INSERT/UPDATE trigger on tool_defs (52)** so it survives refresh-tools (which upserts execute_target) + is correct on virgin boot (runtime-populated rows). (b1) chat grounding picker always present (default follows the selected doc, pick a project/Everything to override). (b2) **live work-item cards** — `GET /api/chat/work-items?session=` → spawned tasks w/ stage path + current stage + artifact; ChatPanel polls (4s in-flight/20s idle), card walks pending→running→completed (surfaces silent exports). (b3) **VS-Code collapse rails** on Work-items + Chat columns (dockview group setSize 0↔restore). **PROOF:** virgin-smoke 00→52 (OK 42); **inverse-hyp e2e** — generate_image tool_call w/ `session_id="WRONG-PLACEHOLDER"` overridden to the real session, image landed there w/ 0 in placeholder (pre-fix .so failed it); durability proven (wiped execute_target → trigger restored flag); b1/b2/b3 live in browser (card shows ai-workflow-patterns.pptx ✓plan/✓build/✓deliver; rails 426↔0↔restore). **★ BUILD-CACHE TRAP:** first `build pg` exit-0-in-seconds was a CACHE HIT (running .so kept old mtime, override silently didn't fire) → **verify the .so mtime, don't trust "build exit 0"**; root cause = `extension/Dockerfile`'s explicit NN-*.sql COPY list omitted 52 (latent clean-build break; extension_sql_file! reads at compile time) → **rule: new chain file → add to BOTH lib.rs AND Dockerfile COPY**. (docker exec needs `-i` for heredoc; `docker compose -f <abs>` from a stray CWD.) Tasks #245/#246/#247/#248 ✅. **STILL CARRIED: #243 alias-failover auto-shed** (bgworker hot path) · **#244** @-mentions + clickable source pills (SSE tool-result surfacing) + Arc D subset-select. Journal `2026-06-24-rich-chat-and-artifacts.md` Round 4; memory `project_stewdio`.
- **★ 2026-06-24 ROUND 3 — STEWDIO COCKPIT BATCH: 4 of 6 slices SHIPPED+LIVE+pushed (`ebd9835`→`b4e77bd` + journal `282a189`).** Michael's batch (sessions view, windowing, models panel, all 4 fast-follows, hardening quick wins, image gen); sequenced gemini-image-first. DONE: (1) **Sessions panel + ▦panels windowing** (`/api/chat/sessions/all`; click reopens exact session; closes "can't get back to a chat" gap) (2) **Models & usage panel** (`/api/models/aliases`+activity: running-now/aliases/24h cost) (3) **rich artifact cards** (links→icon/name/size/⬇; serve `?meta=1`+`?download=1`) (4) **generate_image** (Gemini Nano Banana→chat_attachment; core tool off-remote-profile; PROVEN 853KB PNG; grounding now carries agent's session_id). All vue-tsc/go-build green, playwright-verified, 0 console errors. **CARRIED (fresh-pass, 2 of 6): alias-failover auto-shed** (503'd model→shed so `reason` alias fails over; bgworker.rs HOT PATH — needs pg rebuild + careful e2e; deferred deliberately to avoid a tired hot-path change) · **chat polish trio** (@-mentions / Arc D subset=new grounding mode / source-pills=SSE tool-result surfacing). Tasks #243/#244. Journal `2026-06-24-rich-chat-and-artifacts.md` Round 3; memory `project_stewdio`.
- **★★ 2026-06-24 ROUND 2 — fix-and-retest + GEMINI TOOL-LOOP FIXED (chain 00→51; `0591185`→`d11ed6f`).** doc-build e2e was the oracle. **51-rich-chat-hardening:** artifact-exists gate (empty doc-build→failed; `pipeline_family` not `pipeline` gotcha), chat→brainstorm (1 msg→6 techniques), chat→/generate delegation (now spawns doc-build, full chain closed on qwen), deliver-handoff + recipes. **★★ GEMINI FIXED (bgworker.rs `d90d56d`) — load-bearing, work rig can't run qwen:** 3 layered OpenAI-compat divergences — finish_reason="stop"-with-tools (key off presence) + missing streaming `index` (fall back to id) + Gemini-3.x `thought_signature` round-trip (capture+replay extra_content). **gemini-3.1-pro drives doc-build e2e (real PDF); qwen unchanged.** 3-flash-preview 503s. Bring-up runbook `docs/rich-chat-and-artifacts.md` for the work port. virgin-smoke 00→51 GREEN. **Michael pulls the gemini fixes onto work.** freshclam AV sidecar up.

## Working on
- **★★★ 2026-06-24 — RICH CHAT + ARTIFACTS: Arcs A/B/D/C ALL BUILT + PROVEN LIVE + SHIPPED (autonomous /goal, Opus 4.8; OSS chain 00→50; 7 commits `0e39e74`→`ea7cac5`).** Michael's morning "what can we make now?" sweep → plan/council/ratify (`.spec/proposals/rich-chat-and-artifacts.md`; B+C = dominion_in_council) → built all four. **A (chat polish):** drag-drop, internal/external links (useDocLinks), session export + doc download, ■stop, hover message-actions (copy/retry/⊕task), keyboard slash palette. **B (doc-build SHOWPIECE — Michael's reframe = coder-sandbox model-in-the-loop):** coder-runtime + doc toolchain (python-docx/pptx/openpyxl/reportlab/Pillow/markdown/pandoc/wkhtmltopdf); `coder_export_artifact` (read file out → chat_attachments → download URL); `50-doc-build.sql` pipeline (plan→build→deliver, spawn via /generate); sandbox id in chat_task_input. **C (remote MCP, local-bound first):** `stewards-mcp -http-addr` = READ-ONLY (doc_*+inspection) over HTTP /mcp + bearer auth. **D:** "✸ Everything (whole pool)" lens. **PROVEN LIVE:** sandbox made real xlsx+pdf(%PDF-)+docx; generate→store→serve→download = valid xlsx; Arc C 401/handshake/read-only-profile (zero write tools); ui 0 console errors (Everything lens + 📎 docs/zip/7z); **virgin-smoke 00→50 (OK 39/40)**; refresh-tools = 17 coder tools incl export + doc-build pipeline. dave-rule deferrals: @-mentions/source-pills/artifact-cards, full /generate→model→download e2e (demo step), Gemini generate_image (key works, unwired), C multi-token+mesh, D subset-select. Live stack rebuilt + up (pg/bridge/ui/coder-runtime). Journal `2026-06-24-rich-chat-and-artifacts.md`; memory `project_stewdio`.
- **★★★ 2026-06-24 — RICH-DOCS P3 (a–f) + P4 BUILT + PROVEN + SHIPPED (autonomous /goal "finish p3+p4, update docs to port to my work rig"; Opus 4.8; OSS 7 commits `538438a`→`36abf9c`, chain 00→49, all pushed).** The ratified **doc-extract** capability is live: attach a PDF/Office/HTML/zip → parse in a HARDENED no-network sandbox → only safe text + page pixels cross back (4-layer defense). **Shipped:** `internal/docextract` (pure-Go converter core + the safety ORACLE: zip-slip/zip-bomb/structural via `go test`, no Docker) · `cmd/doc-extract` (in-sandbox converter) · `cmd/doc-extract-mcp` (`doc_extract` + `doc_import_corpus`; `runner/run.go` = the `--network=none`/`--read-only`/tmpfs/nofile/cap-drop hardening delta) · `extension/doc-extract.Dockerfile` (Go + poppler + ClamAV, Go-pure structural check, no Python) · `docker-compose.doc-extract.yaml` (freshclam + clamav-db, opt-in) · `49-doc-extract.sql` (chat_attachments +parent_id/scan cols, chat_attachment_parts re-author = text + **page-image overlay** + doc_extract nudge, server+grants, `chat_task_input` P4) · **ChatPanel** doc-attach (📄) + empty-chat **project/corpus lens picker** (`/api/chat/projects`, `project:<name>` grounding). Router: text-always + auto-render-short-docs (pdfinfo probe). P4: spawned work inherits the chat's extracted docs (binding_question + attachment_ids). **PROVEN every layer:** go-test oracle · trust-floor smoke through the REAL container (**EICAR quarantined by ClamAV**, macro PDF→suspicious structural, benign clean, `--network=none` NO-EGRESS) · virgin-smoke 00→49 (OK 36/37/38/39) · LIVE tool path (2pg PDF→text+auto 2 PNGs+overlay; bundle.zip→2 members pooled+FTS-found, zip-slip refused) · LIVE deploy (bridge catalog has both tools post-refresh-tools; UI lens picker renders 0 console errs). **★ LATENT-BUG FIX (stewardship, reported):** 47-multimodal re-authors page_in_cap/compose_messages/dispatch_chat_turn but only required create_chat_tasks → cargo-pgrx topo-sort under-constrained → adding 49 silently reverted page_in_cap's array guard (OK 36 had passed by sort LUCK) → fixed 47 `requires` += create_page_in + create_context_surface (a SHIPPED 00→48 chain bug, not just P3). + bridge.Dockerfile was missing `COPY internal/` (refresh-tools FAIL caught it). **PORT GUIDE for the work rig: `docs/rich-documents.md`** (architecture + build/enable/refresh-tools + config). Digester-reads-repos inbox item RESOLVED (rides doc_import_corpus — a repo is a folder). Autonomy stays PAUSED. CARRY (non-blocking): full browser playwright walk; gVisor/libreoffice/oletools/git-clone-into-sandbox deferred tiers. Journal `2026-06-24-rich-docs-p3-doc-extract.md`; memory `project_stewdio`.
- **★ 2026-06-24 — RICH-DOCS P3 SECURITY DESIGN written + council-ratified-in-part + PUSHED (proposal only, NOT built; OSS `2b3da4b` `.spec/proposals/doc-extract-sandbox.md`).** Michael paused after P2 and asked: prior art w/ coder-pr? security-minded containers to "scan the doc out"? markdown pass? a tool-free / one-tool `is_safe` model gate? search what industry does. **RESEARCH (Explore agent on our coder sandbox + 3 exa searches):** (1) **coder-pr sandbox = ~70% reusable** — `sandbox.Provision/Exec/WriteFile/ReadFile/Teardown` is doc-agnostic; **`--network=none` + global `CODER_SANDBOX_NETWORK=off` kill-switch already built**; cap-drop=ALL/no-new-privileges/non-root/resource-caps there; git-token path bypassed in ephemeral mode; GAPS = no read-only rootfs/tmpfs, no custom seccomp, converters not installed. (2) **`fetch-md-mcp` carries reusable extractors** — go-readability + JohannesKaufmann/html-to-markdown (HTML→md, pure Go) + chromedp (headless Chrome renderer) — Michael's hunch was right. (3) **Industry:** Dangerzone (FPF) = render untrusted doc→PIXELS in a **gVisor (runsc)** no-net sandbox (only a bitmap crosses out); markitdown (MS, MIT, wraps pdfminer/python-pptx/mammoth, no models/GPU/net, ~10s — ideal sealed converter); docling (IBM, better tables but 600MB HF weights — defer); CDR/Glasswall (rebuild-to-spec, enterprise — we don't need it). **★ THE REFRAME = pixels are our edge:** Dangerzone renders to pixels then rebuilds a PDF for a human; **we have a vision model so we skip the rebuild — pixels feed straight to gemma (P1/P2 already serve this).** Industry RAG runs parsers UNSANDBOXED → one weaponized PDF owns the host = the gap (Michael's "1000× better"). **DESIGN = hybrid router, BOTH outputs (his call):** Path A render→pixels (visual/short, only bitmaps escape; poppler pdftoppm) + Path B sandboxed text-extract (long docs). **★ ENGINE FINDING (his "I thought I had full office already" — RIGHT): `github.com/tsawler/tabula` (pure-Go) already extracts PDF/DOCX/XLSX/PPTX/ODT/EPUB → markdown in fetch-md (ES.5.s2 path); Go-only + full-office in ONE in-tree lib → no markitdown, no Python, no libreoffice in v1 (libreoffice only for optional faithful office→pixels). memory-safe Go parser = safer for untrusted input than C++ poppler/libreoffice.** + in-tree readability[HTML] → markdown → chunk+index like the book corpus; `is_safe` = tools-off triage on EXTRACTED TEXT not bytes (+ existing compose_messages injection regex); pixels ungated. **RATIFIED (AskUserQuestion):** (1) isolation tier v1 = **container + no-net** (reuse coder spine + read-only/tmpfs/nofile delta), **gVisor runsc = LATER toggle**; (2) **build as ONE router, both pixels AND text out**; (3) **write proposal, PAUSE for council** (done — not built). **Same lane serves the deferred digester-reads-repos inbox item** (§7 P3e — reads a read-only repo checkout; inbox note updated, kept open till ratified). Phasing P3a sandbox+image+hardening → P3b pixels → P3c text+is_safe → P3d router+empty-chat lens picker → P3e digester-repos. **9 open Qs for council** (image weight libreoffice~600MB vs poppler vs Chrome; per-doc cost ceiling/page threshold; markitdown=Python-in-sandbox vs all-Go poppler; gVisor on NOCIX installable?; extracted-artifact retention). **★ 2 MORE REFINEMENTS folded (his "thoughts before P3a", OSS `e5e5aa2`):** (1) **SCAN LAYER added** (his "run Windows Defender on the file") → 4-layer defense: **scan** (ClamAV official docker, air-gapped via shared DB volume + networked freshclam sidecar; + structural maldoc check oletools/pdfid for techniques AV misses; verified via exa) → **contain** (sandbox) → **disarm-by-non-execution** (tabula/poppler DON'T run VBA/JS — a macro doc is safe to text-extract) → **content-gate** (is_safe + injection regex). Scanner runs SEALED too (parses untrusted bytes). Honest: AV=early-reject+transparency, sandbox=the guarantee. **Go-purity tension named** (ClamAV=C, oletools/pdfid=Python; option d = a Go-only minimal magic-bytes+keyword check is the fallback) — recommend BOTH for the sealed sandbox = the "1000× better than industry" layer. (2) **TEXT ALWAYS + pixels as additive overlay (NOT XOR)** — every readable doc yields text (cheap, models excel at text); pixels added for visual/short by page-count threshold, user can force-render ("even short PDFs I want text too"). Phasing reordered: **P3a = scan layer + sandbox + malicious-doc smoke (the trust floor)** → P3b text-always → P3c pixels overlay → P3d router+UI. **★ SCANNER RATIFIED + ARCHIVE SUPPORT ADDED (OSS `7428a24`):** scan combo = **(c) BOTH** ClamAV + structural maldoc check (oletools/pdfid), all sealed in-sandbox so the Python never touches our Go/host. **+ ARCHIVE/FOLDER upload (§3.5, his ask):** drop a zip/tar of 1..many files/folders → unpack IN-sandbox under strict caps → EACH member runs per-file pipeline (per-file scan+text+pixels) → folder-tree result; two modes **"work with"** (subject, ref by path) + **"import"** (folder → corpus/project, book-chunks reuse = drop a folder of PM/UX/marketing docs → searchable project). Zip = highest-risk input → guards: decompression-bomb caps (size+ratio+tmpfs backstop), zip-slip safe-join, refuse symlinks, no-auto-recurse nested, entry-count cap (Go archive/zip = pure-Go). digester-reads-repos rides this (repo=folder). Phasing now P3a scan+sandbox+malicious-doc smoke → P3b text-always → P3c pixels → P3d router+UI → **P3e archives (+zip-bomb/zip-slip smoke)** → P3f digester-repos. **★ RATIFIED IN COUNCIL 2026-06-24 (OSS `9b37601`) — dominion_in_council SATISFIED, approved to build.** + **7z/archive family** (`mholt/archives` pure-Go: zip/tar/7z/rar/gz/bz2/xz) + **council-moment blind spots (§8.5):** ClamAV-DB-freshness=silent-degrade (freshclam sidecar must really run; structural check doesn't rot); image ~700MB-1GB; bridge=trust boundary; doc-extract smoke in a SEPARATE CI lane; handler = a **`doc-extract-mcp` mirroring coder-mcp** (reuses the sandbox spine). **DESIGN EXECUTION-READY.** **NEXT = BUILD P3a** (the trust floor: lean doc-extract image [Go tabula+poppler + ClamAV + oletools/pdfid] + sandbox hardening delta in `sandbox.go` [read-only/tmpfs/nofile] + deterministic extract handler + scan layer, proven on a benign AND a malicious doc [EICAR + macro-docx + zip-slip + bounded zip-bomb]). Michael is GO ("ready to build when you are"); I recommended P3a as a FRESH focused arc (keep_the_watch_whole — a malware-handling sandbox wants fresh context, not the tail of a long session) — compact+relaunch like the P1 kickoff. Decomposition in proposal §7 (P3a→P3f). Defaults to set at build: archive caps + import-target. Proposal `projects/pg-ai-stewards-oss/.spec/proposals/doc-extract-sandbox.md`. **★ TOPOLOGY CORRECTION (Michael 2026-06-24, OSS `48b2bea`; → memory [[reference_ibeco_deploy_topology]]):** the substrate (`stewards-oss-*`) + doc-extract run **LOCALLY (dev box / work laptop / work VM), NOT NOCIX** — NOCIX = bridge/control-plane only (llama hub + public web apps). So ClamAV freshness = a **`clamav-freshclam` service IN the local pg-ai-stewards compose** (refreshes a shared read-only `clamav-db` volume at startup + daily; diffs only) and the **scan stays air-gapped — NEVER pull-at-scan-time** (don't put the scanner online while it parses a hostile file). gVisor host (when revisited) = the local laptop/work-VM, not NOCIX. Journal: P2 journal + this lane.
- **★★★ 2026-06-24 — RICH-DOCS P2 (attachments, the MVP) SHIPPED + PROVEN EVERY LAYER + PUSHED (OSS `ce594c0`, chain → 48).** Attach an image to a Stewdio chat → vision model reasons over it → renders inline. **Substrate `extension/48-chat-attachments.sql`:** `chat_attachments` (durable session-scoped bytea + mime + kind[image/document] + extracted_text[P3] + consumed_at; no session FK — upload can precede the turn) + **`chat_attachment_parts(ids, session)`** = assembles the 47 content_parts from stored bytes (image → image_url part w/ a **server-built data URL**, base64 never round-trips the app; session-scoped = no cross-session inject; NULL→text-only fallback). **Go `api/chat.go`:** `POST /api/chat/attach` (multipart, 25MB, mime-sniff) + `GET /api/chat/attachment/{id}` (serve bytes for inline render) + `/api/chat/send` gains `attachment_ids` → dispatch_chat_turn 6th arg = chat_attachment_parts → vision alias fires (no attach → NULL → text path unchanged) + SSE stream emits `images[]` (image_url URLs from content_parts) so history re-renders inline. **UI `ChatPanel.vue`+`api.ts`:** 📎 attach button + staged thumbnails + remove + upload-on-send (media-only turn allowed) + inline image render (live=object URLs, reload=stream images[]). **PROVEN:** virgin-smoke **00→48 GREEN** (new OK 37) · go build · frontend build · **API e2e** (curl upload First Orbit screenshot → gemma-4-26b-a4b quoted ALT 0m/Periapsis -600.0km/TWR 0.00 verbatim) · **full UI e2e** (playwright: select On Liberty doc → 📎 → upload → inline thumbnail staged+rendered → send → grounded answer; **screenshot sent to Michael** `p2-image-chat.png`). **★ BUG the e2e caught (verify-under-real-conditions):** PostgreSQL `encode(bytea,'base64')` MIME-wraps at 76 chars w/ newlines → the data URL `Failed to load image` (HTTP 400); P1's Python-base64 proof never hit it (test the REAL production path, not a hand-built proxy); fixed `translate(encode(...),E'\n\r','')`. **SCOPE call:** P2 = work-item chats (attach while chatting about a work item — the "work item AND this image" shape); the spec's empty-chat entry rides **P3**'s corpus/project lens picker (surfaced, not dropped). FOLLOW-UPS (non-blocking): attachment retention/cleanup (DB bloat, spec §6); history re-sends the image's content_parts every turn (cost; cap-old / serve-by-id later); anthropic dispatch path still flattens arrays (Claude vision deferred). **NEXT = P3** (PDF/office extraction [sandboxed — CVE surface] into extracted_text/chunks + the empty-chat corpus/project lens picker) → **P4** (start_task carries the attachment + work_item into spawned work). ui container rebuilt (P2 live on :8081). Autonomy PAUSED. Journal `2026-06-24-rich-docs-p2-attachments.md`; memory `project_stewdio`.
- **★★★ 2026-06-24 (post-compact) — RICH-DOCS P1 SHIPPED + PROVEN BOTH WAYS + PUSHED (Michael: "lets roll! pick up P1+").** The load-bearing substrate slice: **the substrate carries an image.** OSS `2d1b379` (new `extension/47-multimodal.sql`, chain → 47) + ws `87bcaf6` (overlays). **What 47 does (later-file-wins over 15b/33/45):** (1) **`content_parts jsonb` on messages** = OpenAI-style content array; `content` text stays as a fallback for pressure/history. (2) **`model_capability.supports_vision`** bit + `model_supports_vision()`. (3) **`page_in_cap` re-author** — never truncate ARRAY content (it would `jsonb_set` a truncated string over the array → corrupt). (4) **`compose_messages` re-author** — a content_parts row renders as the message `content` VERBATIM (NO `[ctx:]` prefix, NO engram/state rewrite, NO page-in cap); carries the 15b FINAL body unchanged otherwise → **THIS FILE NOW OWNS compose_messages.** (5) **`dispatch_chat_turn` re-author** — optional 6th `p_content_parts jsonb`: media attached → auto-select the **`vision` alias** (→ `flexllama/gemma-4-26b-a4b`, falls back to the requested alias), insert the user turn as `[text part]+media`; text-only turns byte-identical to 45. **The 5-arg overload is DROPPED** (a 5-arg call would be ambiguous with the defaulted 6-arg → Go `/api/chat/send`'s 5-positional call resolves to the 6-arg w/ NULL — verified intact). **★ NO bgworker change** — the OpenAI dispatch path already `body_orig.clone()`s the messages array verbatim (the de-risk bright spot held), so array content reaches a vision model untouched; :8090 router passes it through. (anthropic_body_from_openai flattens → Claude vision is a later phase; P1 routes to the local openai-kind rig.) **PROVEN BOTH WAYS:** (a) **live e2e** — a First Orbit screenshot injected via `dispatch_chat_turn(p_content_parts)` → bgworker → `gemma-4-26b-a4b` (vision alias, $0/local) → **quoted the on-screen telemetry VERBATIM** (ALT 0m, Periapsis -600.0 km, Stage 1/2 4.5k fuel, TWR 0.00) — unforgeable from the text fallback (held only the question) = the image reached the model THROUGH the substrate (inverse-confirmed). (b) **virgin-smoke 00→47 GREEN** (fresh build + CREATE EXTENSION; new **OK 36** = column + compose_messages array passthrough + page_in_cap array guard; OK 34 hardened to `pronargs` — pg18 `pg_get_function_identity_arguments` includes param NAMES, so the old exact-type-string match was brittle). **★ overlay (ws `87bcaf6`):** `vision` alias → gemma-4-26b-a4b (local, free, no-train → file_private images stay local) in `role-aliases.sql`; `supports_vision=true` on gemma + qwen3.6-35b-a3b in `flexllama-models.sql`. **Live already has the vision alias** (seeded during the proof) + gemma flagged → vision works in Stewdio chat now (substrate-side). **gotcha:** gemma-4-26b-a4b is a REASONING model — a vision probe needs max_tokens ≥~2500 or empty content+finish_reason=length (chat dispatch uses llama-server default n_predict, fine; manual probes must budget). **NEXT = P2** (the MVP, first real win): `chat_attachments` table (session-scoped bytea + extracted text) + upload API + chat injects attached image as subject + **UI upload control AND inline media rendering** (his explicit add) — works in empty AND work-item chats. Then P3 (docs+corpus-lens) → P4 (spawn-from-combination). **P1 is substrate-only — no UI surface yet** (that's P2). Rig serving qwen3.6-35b-a3b + gemma-4-26b-a4b + gemma-4-12b on :8090 (mine; vision-capable). Autonomy PAUSED. Journal `2026-06-23-rich-docs-p1-substrate-multimodal.md` (to write); memory `project_stewdio`.
- **★★★ 2026-06-23 (cont.2) — SUB_WORK_ITEM (Delegate) SHIPPED + RICH-DOCS RATIFIED + MULTIMODAL P0 PROVEN.** **(1) sub_work_item / Delegate (OSS `47046ad`, chain → 46):** `extension/46-chat-tasks.sql` = `start_task` tool (`chat_start_task_tool`) → `work_item_create`+dispatch; **RELIABLE parent link from the session id** (work-item chat = `stewdio-<uuid>` → pull parent server-side, set `parent_work_item_id`; doc/empty → top-level; `check_and_dispatch_fanout_aggregator` no-ops for non-fanout parents = safe). Chat still can't call `work_item_create` directly (start_task = the one controlled write path); 45 prompt softened read-only→Ask/Delegate. virgin-smoke OK 35. **PROVEN twice live:** tool created+linked+dispatched a child; AND the agent itself chose start_task when asked → child nested under parent → "started it, watch in cockpit." (= closed NL-launcher #224.) **(2) RICH-DOCS council→ratified→spec (OSS `911d9d8`):** attach media (img/pdf/office/text) to ANY chat as **injectable subject material**; chat holds work_item+media+corpus-lens, reasons over the combination, renders media inline (his add), spawns work via start_task. De-risk: :8090 router forwards image parts UNTOUCHED (no router work); substrate `content` is text → parallel **`content_parts jsonb`** + `compose_messages` passthrough is the load-bearing change (OpenAI dispatch path forwards arrays verbatim); **Gemini paid/Vertex = private-safe** (privacy fork → preference, not wall); rig models vision-capable, mmproj on disk. 6 decisions, phasing P0→P4. Spec `projects/pg-ai-stewards-oss/.spec/proposals/rich-docs-in-chat.md`. **(3) P0 local vision serves (llama-chip `ab4c9de`) — PROVEN:** `--mmproj` launch support (`mmproj`/`no_mmproj` Slot field + `models.Discover` pairs co-located `mmproj-*.gguf` + `FindMMProj` + `supports_vision`; `rig.args()` appends `--mmproj`); unit-tested, build/vet/test green. **LIVE (Michael: "you own the rig for now"):** stopped rig→rebuilt→relaunched new binary→`vision` profile loaded gemma-4-26b-a4b w/ auto-mmproj→sent a First Orbit screenshot→**it read every telemetry value correctly** ("space flight sim… TERRA First Flight… ALT 0m… Periapsis -600km… Stage 1/2 4.5k fuel… TWR 0.00… LANDED on Terra"); local/$0/private. Then `dance-moe` (both substrate models) up on the new binary — **fits WITH vision towers (24106/24104 MiB, ~99.7%, tight) + both health-ok** → substrate whole again + vision-capable. **RIG STATE: I own it (my bg process, new binary, dance-moe + vision); Michael takes it under his terminal later.** Config `vision`+`dance-moe` profiles in `projects/llama-chip/config.json` (gitignored). **NEXT = P1 (the load-bearing substrate slice: `content_parts jsonb` + compose_messages passthrough + `vision` alias + inject-image-into-a-chat-turn e2e) → P2 attachments+UI inline → P3 docs+corpus-lens → P4 spawn-from-combination.** FOLLOW-UPS: VRAM-trim a `dance-vision` profile (small); autonomy stays PAUSED til more core work lands (his call); digester-reads-repos deferred til sandboxed. Journal `2026-06-23-sub-work-item-and-rich-docs-p0.md`; memory `project_stewdio`+`project_llama_chip`.
- **★★★ 2026-06-23 (cont., post-compact) — STEWDIO P3 + P4 SHIPPED + PROVEN + PUSHED → FEATURE-COMPLETE (Michael: "Lets roll, can you get p3 and p4 out the door?").** **P3 (OSS `127e31c`) — book SOURCE corpus so a book chat quotes the BOOK.** Placement call: the YT corpus is an *example* (`examples/yt-transcripts.sql`) + the book digester is an example → book corpus → sibling **`examples/book-corpus.sql`**, NOT core (chain stays 00→45); the `book_search` grant to `work-item-chat` lives with the tool. `book_text` (book_slug PK, full_text+tsv) + `book_chunks` (FTS body_tsv + **opt-in** `embedding` col gated by config `book_corpus_embed_chunks` DEFAULT OFF — **the substrate has NO synchronous query-embed: `doc_search`=FTS, `doc_similar`=PRECOMPUTED edges → live path is FTS, embeds forward-looking**) + `book_persist_corpus(+tool)` (reads the fetched book SERVER-SIDE by the **page-in HANDLE** — same `context_handle` resolver as `result_read`; model never re-emits the text) + **`book_search`** (verbatim substring + FTS-ranked passages w/ location chunk_idx; **tolerates the doc slug `book-on-liberty` OR `on-liberty`** — agent passed the doc slug first). Book digester BUILD stage calls `book_persist_corpus` after fetch_url (step 2b); `book_publish` frontmatter gains `source_url`+`has_corpus` (to_regclass-guarded so the digester stays standalone). **PROVEN (live + local rig, $0):** persisted On Liberty (220 chunks); Stewdio chat "where does Mill state the harm principle?" → **called book_search → quoted the book VERBATIM @ chunk 32** (British "civilised"). Inverse-hyp held twice: American "civilized" → found_verbatim=false (quote-guard working); "tyranny of the majority" → verbatim @ chunk 23. **P4 (OSS `b2b4b69`) — polish:** dockview **layout serialization** (toJSON/fromJSON↔localStorage, "⟲ layout" reset; **reload-proven** 3 groups restore, 0 console errs) + **multi-session history** (`GET /api/chat/sessions` by-prefix w/ preview/last/count + 💬 sidebar + ＋New + per-target last-session in localStorage) + **provenance chips** (SSE now carries each turn's tool NAMES → facet chips doc/book/corpus/sessions/citations/work-item on retrieving rows; verified "doc","book" render) + optimistic user echo. **★ Bug P3 surfaced + fixed: `/api/chat/send` 10s→45s** — `dispatch_chat_turn`→`chat_enqueue`→`compose_system_prompt` composes prompt+tool-schemas INLINE (~7-11s for a tools-on agent, NOT history length — session had 2 msgs); this was the P1 "transient 500." Reply still streams over SSE; enqueue is the only sync part. Deeper fix (defer compose to bgworker) = follow-up. Two e2e commits + 2 screenshots sent. ui container rebuilt once (P3+P4). README example row added. **FOLLOW-UPS (none blocking):** book backfill needs re-digest (only On Liberty persisted; existing docs has_corpus=false); compose latency papered by 45s; `book_chunks` embeddings dormant until a `book_similar` precomputed-edge builder; the local chat model over-explores (book_search ~12× before answering — correct, chatty); NL-chat-driven launcher still future. Autonomy stayed PAUSED throughout (his innovation-week call). Inbox council item (digesters read our repos) still OPEN — dominion_in_council, left for Michael. Journal `.spec/journal/2026-06-23-stewdio-p3-p4.md`; memory `project_stewdio`.
- **★★ 2026-06-23 — STEWDIO (the work-item cockpit) FULLY SPEC'd + COUNCIL-RATIFIED, execution-ready (OSS `f2b0607`; Michael: "lets fully spec this, then council and ratify so we're fully prepped to build").** Reframe (his): the doc + source corpus + building sessions are FACETS of a work_item → the unit is **"chat with a work item"** (+ a UI to kick off pipelines). A VS Code-like cockpit in `stewards-ui`: left = project-filtered work-item/doc browser, center = tabbed artifact OR live plan/progress, right = multi-session model-switchable chat that talks to a work item AND launches book/video/task pipelines with live progress. **Research done (web + a 4-agent code sweep mapping the substrate):** borrow **`dockview-vue`** (MIT, VS-Code docking, Vue3) for the shell; DON'T adopt a chat app wholesale (they don't know our work_item model); the agent-cockpit field converged on editor/manager-surface separation + "plan=progress" (= our pipeline stages exactly). **★ KEY FINDING: book digests DON'T persist their source corpus** (fetched at build, lives only in that run's messages, discarded; doc keeps only book_slug/title/author) — **YouTube DOES** (`yt_transcripts`/`segments` keyed by video_id + frontmatter backlink). So "chat with the book's passages" needs a persisted book corpus (phase **P3**); P1/P2 ship doc+sessions+citations (work for all) + video's corpus now. Grounded resolvers confirmed: doc→work_item via frontmatter `session=wi--<uuid8>`/`work_item_id`; sessions = `work_items.session_ids`; private wall only in `context_search(descendants)`; local routing via `reason` role alias (overlay→rig); dispatch = `work_item_create`+`work_item_dispatch_stage`; plan=progress from `pipelines.stages` vs `stage_results`. **8 ratified decisions:** Stewdio/`/stewdio`/OSS-core · local model · **SSE from P1** (DB-poll relay) · **Pinia** · book-corpus=P3 · order P0 shell→**P1 chat-with-a-work-item**→P2 kickoff+progress→P3 book corpus→P4 polish · **persistent chat sessions** (+ lean `dispatch_chat_turn` helper, NOT work-item-per-turn). Spec: `projects/pg-ai-stewards-oss/.spec/proposals/stewards-studio.md`. **★ P0 SHIPPED + PUSHED (OSS `fc32c14`; Michael: "ready go!"):** dockview-vue cockpit shell — lazy `/stewdio` route, 3-zone dock (Work items | Artifact | Chat) draggable/resizable, abyss dark theme; deps dockview-vue 7.0.2 + pinia 3.0.4 (lockfile committed); `useStewdioStore` (Pinia, first store in the app); App.vue nav link + full-bleed special-case (route.name==='stewdio' → drop main padding+footer, h-screen); dockview + theme CSS imported LOCALLY in Stewdio.vue → its own ~76KB-gzip lazy chunk, off every other route; panels = placeholders (inheritAttrs:false). Oracle: `npm run build` green (vue-tsc+vite) + headless playwright render (3 dockview groups, theme, placeholders, only favicon-404 console err); other routes unaffected (additive). vue-tsc friction fixed: SFC→dockview `VueComponent` needs `as unknown as VueComponent` cast. gotcha handled: `npm run build` overwrites the go:embed stub `dist/index.html` → restored before commit; staged ONLY P0 files (left pre-existing OSS journal changes alone). **★ P0 LIVE-VERIFIED** — rebuilt the `ui` container (his ask; `docker compose build ui`+`up -d ui`, recreated stewards-oss-ui, healthy) → screenshotted the prod build of `/stewdio` (3 dockview groups, 0 console errors); sent to Michael (he's away). **★★ P1a SHIPPED + e2e-PROVEN + PUSHED (OSS `630a41e`):** the chat-with-a-work-item BACKEND. `extension/45-work-item-chat.sql` = the `work-item-chat` agent (retrieval-only, read-only **allow-list** = deny `*` + allow doc_*/investigate_session/doc/context_search/read_corpus_parents/result_*/expand_message/work_item_show/list) + **`dispatch_chat_turn(session_id,user_input,agent='work-item-chat',alias='reason',grounding)`** — thin wrapper over the existing bare-session `chat_enqueue` (Explore-mapped: marker-free `kind='chat'` → bgworker tool loop → reply in messages by session_id; NO `_work_item_id` so the work_item/watchman triggers never fire). Chain wired 00→**45** (lib.rs `extension_sql_file!` requires create_graph_organize + Dockerfile COPY + virgin-smoke **OK 34** assert). **PROOF (live substrate + local rig, then cleaned up):** a chat asking On Liberty's central argument → agent made a `doc_get` call → answered GROUNDED in the digest **with a verbatim quote** ("...the only purpose for which power can be rightfully exercised...is to prevent harm to others"), local qwen3.6-35b-a3b (`reason`), $0, ~3s; marker-free (0 work_item rows). **★★ P1b+P1c SHIPPED + e2e-PROVEN through the LIVE UI + PUSHED (OSS `3570055`; Michael: "keep working on this with p1b and p1c. post screen shots when finished" — he's away):** the chat-with-a-work-item is LIVE end to end. **P1b (Go `cmd/stewards-ui/api/chat.go`):** `POST /api/chat/send` (deterministic session-per-target + first-turn grounding → `dispatch_chat_turn`, local model; logs+500s on error) + `GET /api/chat/stream` **SSE relay** (replay-from-`after` then tail `messages` via `http.Flusher`, `X-Accel-Buffering:no`, 10s heartbeat — a DB-poll relay, NOT provider token-stream, so cost/trust accounting intact) + `registerChat`. **P1c (Vue):** `api.chatSend`; Pinia store (selectedRef/Kind/Title + chatModel + select); **BrowserPanel** (project-filtered work-items + docs, click→select); **ArtifactPanel** (doc markdown / work-item summary+stages); **ChatPanel** (mirrors the server session id, `EventSource('/api/chat/stream')` on select = replay+tail, user/assistant bubbles + 🔧 tool turns, send, reason/ingest/critic switcher). **PROOF (live `:8081` container + rig, cleaned up after):** clicked On Liberty study → rendered center → asked "Mill's central argument? quote the doc" → agent did `doc_get`+`result_read` → **streamed a grounded answer with a verbatim quote into the chat panel**, local qwen, ~9s; build green (vue-tsc+vite, go build); rebuilt+recreated the `ui` container twice (P0 view, then P1). **Screenshot sent to Michael.** GOTCHAS: playwright `fill`+`press` across separate CLI calls didn't fire Vue's v-model/keydown → drove send via one eval (set value+input+keydown); `dist/index.html` go:embed stub overwritten by `npm run build` → restore before commit (twice now); **one transient 500 on /chat/send right after container restart (rig warmup), not reproduced in 3 subsequent sends** (added a server log on the dispatch-error path). **★ STEWDIO P1 = DONE (P0 shell + P1a backend + P1b SSE + P1c panels).** **★★ P2 SHIPPED + e2e-PROVEN + PUSHED (OSS `3783333`; Michael: "lets keep going, p2!" — away, posting screenshots):** kick off pipelines from the cockpit + **live plan=progress** (Devin's "plan = progress"). `GET /api/pipelines/get?family=` (ordered stages = the plan) + api.pipelineGet; **BrowserPanel "＋ New" launcher** (pick pipeline + optional binding question → `workItemCreate(dispatch:true)` w/ readable slug → auto-selects); **ArtifactPanel plan=progress** (stages render ✓done/▸active/○pending from `pipeline.stages` vs `stage_results`+`current_stage`, polls `work_item_get` every 3s while running = "· live", stops at terminal/awaiting_review). **★ FIX (latent bug the e2e caught): `workItemsGetHandler` scanned `wi.slug` into `*string` w/o coalesce → 404 "cannot scan NULL into *string" for any NULL-slug work item (which the substrate allows + a launched one has) → `coalesce(wi.slug,'')`.** PROOF (live container+rig, then `work_item_cancel`+cleaned): ＋New→book-digest→Launch → center rendered **▸ read · ○ build · ○ critique**, in_progress, "· live". Screenshot sent. ui container rebuilt (3rd time this arc). **NEXT = P3 (persisted book source corpus so book chats can quote the BOOK's passages — video already has `yt_transcripts`; books discard the source) → P4 (polish: dockview layout serialization, multi-session chat history, per-message provenance chips).** Pulled into his work instance via the overlay (`reason`→rig); autonomy stays paused. Constraints flagged: no SSE/Pinia precedent (net-new but bounded), `App.vue` single hardcoded layout (full-bleed special-case), `go:embed` stub (npm build before go build), commit package-lock.
- **★ 2026-06-23 — llama-chip REMOTE MANAGEMENT (the federation write half) SHIPPED + LIVE-PROVEN (`cpuchip/llama-chip` `7529828`; Michael: "lets build the remote management! also make sure we can test chats against remotes").** Any node's loader UI is now a control surface for EVERY node, not just a viewer. **`fed.NodeAddr`/`IsSelf`** (nil-safe; roster or static peers) + **`/api/remote?node=&op=`** one-proxy (op allowlist: status/gpu/models/backends/profiles/live/guess-context GET + load/unload/unload-all/profile/ensure POST; forwards the fed bearer, streams back; unknown node→404/op→400) + **`proxyByModel` `?node=` override** (pin a chat to a named peer even on a model-name clash; strips the hint before forwarding → no double-hop). **UI:** Pool cards gained per-node ＋Load (form lazily populated from THAT node's GGUFs/profiles/GPUs over the proxy, in a container decoupled from the 2s refresh so it can't wipe mid-edit) + Unload all + per-model ✕; Chat picker groups by node (local first, remote optgroups), remote pick routes `?node=`. Self↔peer share one `remoteCall` path. **Auth model:** peer mgmt endpoints gated by ITS fed token over the mesh = same token we forward (shared-secret cluster authenticates; open mesh-only [live: no token, firewall 100.64/10] needs none); a new-build controller manages an OLD-build peer (proxies to pre-existing `/api/load` etc.). **PROOF:** oracle green (`GOWORK=off go test ./...` — NodeAddr/IsSelf hub+static+nil; remote GET/POST/bearer; unknown node/op; node-override picks named peer + strips param) + **live, non-disruptive scratch node (zero models, :18090, static-peer of the running rig):** /api/pool saw both; /api/remote read fermion status+52 GGUFs; **`?node=fermion` chat → qwen answered "pong"**; headless UI clean (no console errs, Pool cards+controls, remote Load form opened w/ fermion's GGUFs, `fermion (remote)` optgroup); negatives 404/400; rig confirmed healthy + untouched before/after; scratch killed + artifacts removed. Screenshot sent. **★ CROSS-MACHINE PROVEN over the real NetBird mesh (same day, Michael updated workchip):** a new-build controller on fermion peering both nodes → /api/pool saw scratch+fermion+workchip; remote-READ workchip status+GPU; `?node=workchip` chat → **gemma-4-12b answered correctly (MoE-vs-dense, 47.5 tok/s)**; WRITE path — profile op proxied (clean "no profile"), unload gemma → load+chat+unload Qwen3-4B → **restore gemma-4-12b EXACT** (ctx 376832/par2, GPU 11457/12282 identical, "Hi!"). gemma-4-12b-it-QAT = a REASONING model (reasoning_content; needs ≥~600 max_tokens). workchip left as found; rig untouched. Autonomy stays PAUSED (his call, GPUs free for innovation week). Michael manages-grant: "I'll let you manage llama-chip until it's stabilized." Journal `2026-06-23-llama-chip-remote-management.md`.
- **★★ 2026-06-22 — VIVINT FOUNDATION SHIPPED TO PUBLIC CORE + PROVEN (council OPENED + all 4 forks ratified; Michael: "lets open a council and build vivint… promote B into OSS too… review the overlay… 1 yes build+cutover, 2 prompt good, 3 recency now, 4 just B").** THREE primitives now in OSS core, chain 00→44, each gated green + applied to live, clobber-clean (0 clobbers): **A `route_on`** (42, `59be99f`, OK 31) · **B `request_research`+`gather-feedback`** (43, `3bf7938`, OK 32 — promoted from overlay; ws overlay trimmed to the Vera grant `8653333`) · **organize keystone `graph_node`/`graph_supersede`/freshness** (44, `ebc4117`, OK 33 — the missing node-maker + `observed_at`/`status` recency + `SUPERSEDES` aging + opt-in `fresh_only` recall + `graph-organize`/`graph-read` tool_groups). **★ OVERLAY AUDIT verdict: cut classification HELD — B was the only generic-but-overlay capability worth pulling public; everything else correctly private or already-core-mechanism.** **★ ORGANIZE PROVEN e2e on a real book** (`examples/corpus-organize.sql` generic pipeline; ran on `book-the-tao-teh-king` via qwen/reason in ~48s → **14 freshness-stamped nodes** [concepts/principles/persons/**claims** incl the ineffability paradox + a wu-wei-passivity tension] + **25 typed edges** [CONTRADICTS/EXEMPLIFIES/MENTIONS]; kept — real knowledge). **GOTCHA learned: `work_item_create` does NOT auto-dispatch — caller must call `work_item_dispatch_stage(id)`** (the scheduler does it; a manual work_item sits pending; inverse-hypothesis cleared the 10-min "stuck" as the missing call, not the keystone). **★★★ VIVINT PIPELINE BUILT + PROVEN e2e + CUT OVER (Michael: "keep going! I love this momentum!").** `overlays/vivint-pipeline.sql` (ws `5679e2b`) = the §6/§7 assembly line as ONE study-workflow-shaped pipeline, file_private, all-local (dance-moe), $0: **gather → organize → analyze[A `route_on` critical⟲ + B `gather-feedback`] → draft → refine.** Wires all three new core primitives. **PROVEN e2e on live (vivint-proof-1, ~10 min, all 5 stages, $0):** organize built a **22-node fault graph** (category/complaint/product/root_cause); analyze `VERDICT: COMPLETE` (analyze_loops=0); refine finalized a grounded **7-proposal improvement doc** (`vivint-improvement-proposals`, frontmatter `session=wi--38a333bc--refine`). **★ CUTOVER DONE + ACCOUNTED:** `vivint-cron→vivint` ENABLED (next 20:00, cron `0 */3`), `vivint-reflect→planning` DISABLED — the new decoupled assembly line is the live autonomous vivint loop; the coupled planning loop (the research-write backlog source) is retired. **BUGS caught this arc:** (1) gather template referenced `{{input.gather_gaps}}` before the first route_on loop-back → seed it empty (render errors loudly on missing paths); (2) `work_item_create` does NOT auto-dispatch — caller must `work_item_dispatch_stage(id)`; (3) psql multi-statement `-c` = ONE txn → an error in the batch rolled back my UPDATEs ("UPDATE 1" printed but reverted) → run cutover UPDATEs as separate statements. Build-order ALL DONE: A✅ B✅ keystone✅ book-proof✅ vivint-e2e✅ cutover✅. Tasks #206/#207/#208✅ (#198 Phase K subsumed). **CARRY:** watch the first scheduled vivint-cron fire (20:00) — confirms the scheduler path (proof used manual dispatch); the existing ~66 old research-write backlog is separate (Michael's triage or let the disciplined new loop supersede); the **dashboard introspection** (which GPU/model↔work_item/pipeline, "if possible") is the open separate UI arc Michael asked for. autonomy_paused=false, rig=dance-moe up.
- **★ 2026-06-21 llama-chip BUILT (Phase 1+2, PROVEN) — rolled-our-own FlexLLama (Michael's call after the runner-death).** New repo `cpuchip/llama-chip` (PRIVATE) at `projects/llama-chip`, in root go.work. Diagnostic: 4090s + LM Studio HEALTHY, FlexLLama-in-Docker was the lone fault → **FlexLLama torn down** (`flexllama-stewards` stopped; substrate autonomy still PAUSED). Native Go binary drives LM Studio's bundled CUDA llama-server + GGUFs: Phase 1 discovery CLI (backends/models/gpu), Phase 2 the rig (per-GPU pin via CUDA_VISIBLE_DEVICES + split-mode + OpenAI router :8090 drop-in + AUTO-RESTART). **PROVEN e2e** (model on GPU0/GPU1 empty). **★ NOW FEATURE-COMPLETE — Phase 1-4 all PROVEN:** P3 = runtime load/unload API + embedded loader UI (`--flash-attn` needs a VALUE gotcha); P4 = **dynamic model management** (config Profiles + `rig.ApplyProfile`/`Ensure`; router `/api/profile`,`/api/ensure`; UI profile buttons) — **proven: `diverse` loaded 2 models one-per-GPU [GPU0 5924+GPU1 6012 MiB], switch to `single` freed GPU1, ensure load-by-need** = Michael's exact vision (substrate POSTs /api/profile per work phase). 3 gotchas in memory `project_llama_chip`: vendor-DLL-path, crash-detect (Wait/exitCh), --flash-attn-value. Build `GOWORK=off`. **NEXT: backend-download "use latest" + wire substrate oss/.env provider @ :8090 (resume autonomy) → THEN vivint (the other priority).**
- **🌙 2026-06-21 OVERNIGHT HINGE WATCH (Michael asleep; "keep an eye on it over the next few loops").** RUNNING: the **Hinge daemon** (`hinge-daemon.ps1`, background id b97tklhca, polls `hinge_gate_status` every 5min, reviews pending via `claude -p`, OBEYS `autonomy_paused`) + **memory-tend schedule** (`*/30`, generates graph-link proposals; Hinge auto-approves good ones per the grant → edges; SELF-LIMITS as co-citation candidates deplete). Auto-approve grant = digest-skill-rule/graph-link/pipeline-adjust; structural escalates. **KILL SWITCH: `config_set('autonomy_paused','true')`** halts source+digesters+daemon. I check in ~every 15-30min, summarize each loop, pause if anything's off. Morning summary → journal. digest-tuning NOT on cron (could loop; memory-tend depletes safely). The applied verbatim rule (from the watched 2-turn cycle) is live in the digesters. **★ WATCH CONCLUDED ~01:37: loop ran GREAT (Hinge reviewed 5 proposals — caught an inverted-direction DERIVED_FROM [chronologically impossible], approved an ANALOGOUS_TO verified verbatim in both sources; 3 applied/2 revised, 4 edges created, $0 local-proposing), THEN the watchman correctly auto-paused on a STANDING backlog of 72 un-triaged pending work_items ≥70 cap (66 research-write June-15→today + 6 planning; 63 predate tonight; $0 spend). The "obey emergency stops" design PROVED LIVE — daemon held. Left autonomy_paused=true (did NOT override the safety). Morning summary + Michael's decision → `.spec/journal/2026-06-21-overnight-hinge-watch.md`. The real signal = the week-old research-write backlog (triage it, then resume). Daemon still running (ready for resume). Stopped the wakeup loop (stable paused state = Michael's to resolve). **★ RESUMED 2026-06-21 (Michael: "raise the limit to 120 and continue our experiments tonight; restart pg-ai-stewards + the hinge; want good data to review"):** cap `reflect_guard_max_proposals_pending` 70→120 (his call), autonomy resumed (would_trip=false, 72/120), **digest-tuning enabled HOURLY + memory-tend */30** (both seeded now; 9 graph-link candidates left), fresh daemon **bhp4nb1ri** (old b97tklhca stopped). Gathering data overnight, ~30min check-ins. **Michael's NEXT WORK = the vivint loop** (Phase K / two-loop redesign) to SOLVE the backlog problem (research-write/planning piling up un-triaged). $0 spend, local proposing. **★ Run-2 ~03:10:** Hinge judged 14 graph-links (4 approved→edges incl corrected DERIVED_FROM + a cross-domain ANALOGOUS_TO; revises caught an overreaching SUPPORTS, a DUPLICATE edge, soft-verb reason issues) — rich data. **★ FOUND+FIXED a re-proposal cost loop** (revised pairs stayed candidates → re-proposed every cycle → $ on Max sub invisible to substrate $0; one pair judged 4×): `graph_link_candidates` now excludes already-reviewed pairs (OSS committed+pushed, virgin-smoke OK 29). Fix HOLDING (no recurrence). Real claude-p cost $7.65/12 reviews, now bounded. Scheduler firing (mt 4× / dt 2×) but loops correctly QUIET (precision-over-volume; rules covered) = healthy steady-state. Watch → hourly cadence; morning Run-2 tally in `.spec/journal/2026-06-21-overnight-hinge-watch.md`. **★ WATCH CONCLUDED ~04:20:** Hinge judged 17 graph-links (5 approved→edges, 9 revised — sharp catches: overreaching SUPPORTS, DUPLICATE edge, inverted DERIVED_FROM, soft-verb reasons) + the digest-skill-rule cycle. Final claude-p cost **$7.65/12 reviews** ($0 substrate). **★ WATCHMAN SAVED IT TWICE:** (1) re-proposal cost loop → I fixed in code (graph_link_candidates excludes reviewed pairs, committed); (2) ~03:30 FlexLLama's RUNNER died (proxy/`/v1/models` up = HTTP 200, but `/v1/chat/completions` fails for ALL substrate dispatch; 6 fails/5 families) → "5 consecutive failures" guard auto-paused. Probed (resume+real dispatch) → still fails → **HELD paused** (did NOT blind-restart the finicky tuned-VRAM dance rig at 4am — surface-don't-force). **RECOVERY for Michael: `docker restart flexllama-stewards` (wait ~60-90s model load) → verify a chat completion → `config_set('autonomy_paused','false')`.** Daemon bhp4nb1ri still running (resumes on unpause). STOPPED the wakeup loop (paused awaiting Michael's rig restart). Both saves = "obey emergency stops" validated under real conditions. NEXT = vivint loop (backlog + dependency-fragility both argue for the two-loop redesign).
- **★★★ 2026-06-20 (cont.4) — THE SELF-TENDING MEMORY CORE BUILT + SHIPPED (Michael: "lets build this!" + full Ammon authority; /goal).** One autonomous run, phase-gated, each a tested commit+push+journal (OSS chain 00→41, virgin-smoke OK 26→29). **M1** `38-edge-vocabulary` (19-verb grammar + graph_link/graph_vocabulary; `is_symmetric` ≠ reserved `symmetric`). **H** `39-hinge` + `scripts/hinge-review/` (a curated `claude -p` reviews the hinge_reviews queue; bounds ENFORCED in-substrate [auto_approve/escalate_always]; e2e proven — approved a sound rule, ESCALATED the cutover with covenant reasoning; ~$0.08/call→lean folder; config col=`description`). **G** `40-rte` (the oracle as a GRADIENT: quote_flags→rte_propose→Hinge→trigger auto-activates→digester reads via quote_rules; LOOP PROVEN; digest-tuning pipeline; schedule→overlay self-tuning.sql). **M2/M3** `41-memory-tend` (WALK graph_recall = HippoRAG PPR, recalled a co-cited doc cosine misses; LINK graph_link_candidates+memory_link_propose→Hinge→edge; memory-tend pipeline; overlay memory-tend.sql). **Autonomy RESUMED at checkpoint** (new infra dormant; schedules disabled). Journal `pg-ai-stewards-oss/.spec/journal/2026-06-20-self-tending-memory.md`. **REMAINING: M5 PRUNE (follow-up) + Phase K (vivint two-loop = M specialized; K4 cutover = Michael's Hinge)** — focused next arc. Plan `private/vivint/plans/digest-trust-and-knowledge-loops.md`; memory `project_doc_construction_rollout`. Gotcha: `scheduled_pipelines.input_template` is JSONB; core ships no schedules (→overlays).
- **★ 2026-06-20 (cont.3) — DIGEST-TRUST PILLARS 3a/2/1/3b BUILT+SHIPPED (Michael: "build all of those planned items").** 3a verbatim-quote instruction (both digesters). **Pillar 2 gate:** oracle `--mark`→`docs.frontmatter.quote_check`; Go materializer holds flagged digests (no Python-from-Go); corpus marked 28 flagged/5 passed/47 unverifiable. **Pillar 1 transcripts:** carried yt tables+import_yt_transcript to OSS `examples/yt-transcripts.sql` (config /yt path), mounted yt vol :ro into pg, **pg recreated (paused/resumed, data intact)**, `yt_persist_transcript` wired into playlist read stage; 4 backfilled, 13 evicted deferred. **Pillar 3b self-check:** build-stage verifies quotes pre-publish (books=result_search, videos=new `transcript_search`). All examples/compose (no rebuild) + 1 Go (materializer) + oracle; 4 commits pushed. **Pillar 4 vivint two-loop = SCOPED NOT BUILT** — the live-proposal-engine redesign; safe additive path (disabled new schedules, reversible cutover); discriminator design fork (knowledge vs evaluation kind/tag); deferred to a focused pass (slow-local e2e). Memory `project_doc_construction_rollout` has all detail.
- **★ 2026-06-20 (cont.2) — DIGEST-TRUST follow-ups: safe transcript backfill DONE + hard-parts PLAN written.** Michael set 1/2/3 (transcript persist+backfill / quote gate / source-side both) + redesigned vivint to a TWO-LOOP corpus model. **Safe backfill DONE (no live-infra risk):** the 4 videos whose transcript files survived in the bridge `yt-transcripts` volume were docker-cp'd into pg + `import_yt_transcript`'d → `yt_transcripts`/`segments` populated; **oracle now verifies videos** (and shows video quotes are LOOSER than books — heavy paraphrase-in-quotes, e.g. B95cu7seTm8 10/17 FLAG). Other 13 videos evicted → need re-download (planned). **ARCH GAP found:** OSS yt files live in the BRIDGE volume; pg can't see them + `import_yt_transcript` hardcodes `/opt/yt/yt` (pre-cut path) + the tables/function are live-only DRIFT not in OSS source. **PLAN written (private, gitignored — has vivint name):** `private/vivint/plans/digest-trust-and-knowledge-loops.md` — 4 pillars: (1) ongoing transcript persist = mount yt vol into pg [needs deliberate pg recreate, 4-file compose] + config-ize path + carry tables to OSS + wire read stage + re-download 13; (2) quarantine gate = `docs.quote_check` flag the oracle writes + materializer holds flagged (NOT inline Python-from-Go); (3a) tighten quote instruction + (3b) critique self-check via `transcript_search`/source-handle (videos easy post-P1); (4) **vivint TWO-LOOP** = knowledge loop (research-write-shaped, doc-construction, survey-gated, builds corpus, `kind=knowledge`) + evaluation loop (trimmed planning reading the corpus, data-marking trigger, evaluates NEW-with-whole-corpus-available incl prior evaluations → proposals) — **this dissolves the B coupling** (synthesize.output triple-purpose). Decisions: data-marking trigger; evaluate-new-with-corpus-available. **★ SECURITY catch:** wrote the plan into OSS `.spec/proposals/` first, then MOVED it to private (vivint name must not leak to public OSS). Build order: 3a→2→1→3b→4. Tasks #191(safe done)/#192/#193 + #189(superseded by Pillar 4).
- **★ 2026-06-20 (cont.) — AUTONOMOUS-DOC REVIEW + 3 fixes (Michael: "dig into all of those").** Read live digester output: book/video digests GOOD, vivint reflect docs MESSY, tagging rough. **A — quote oracle SHIPPED** (OSS `scripts/verify-digest-quotes/`, pushed): per-quote check vs the source (Gutenberg/transcript); validated on Karamazov (8/12 verbatim, 3 FLAG incl fabricated chapter "Epic of the Deified Earth"); exit1=pre-materialize gate; full-corpus audit still running. **C — book dedup+tagging SHIPPED** (OSS `main`, virgin-smoke OK 25, pushed): 4 narrow single-finalize tool_groups + scope book critique to [doc-edit,book-finalize] (kills the doc_finalize→digest-* dup; research 116→8 tools there) + book/playlist publish work-item project fallback; live cleanup → 0 untagged/0 dups, 121 docs. **B — reflect/planning recast DEFERRED+scoped:** planning synthesize.output is TRIPLE-purpose (materialized plan via file_content_jsonpath + pooled doc + propose_work input) → coupled core recast of the LIVE vivint engine, own e2e pass. Interim option = harden synthesize prompt (no fence/preamble/[ctx:]). Found+surfaced: dry_run_chat nests response_format in the top_p IF (drops when top_p null) — not fixed. Tasks #188(A done)/#189(B deferred)/#190(C done). Autonomy paused/resumed for C cutover (accounted).
- **★ 2026-06-20 — gemma-26b MAXED + ENGRAM EMBED BUG FIXED + overnight report.** (1) **Rig:** dropped nemotron-4b; gemma-12b alias (=gemma-4-26B-A4B MoE) now `--parallel 2 @ 425984` (2×212992 slots) on GPU1 ALONE = 23524MiB / 1040MiB free. 2-pt VRAM fit: fixed ~17260MiB + q8 KV **~0.0147 MiB/tok of total n_ctx** (~0.94GB/64k — KV is CHEAP). Largest keeping ≥800MiB free ≈442k total (~221k/slot); chose 213k/slot for a ~1GB cushion. PREFILL 3-10k tok/s so a big input window is ~free — only OUTPUT (~45 tok/s) is slow → runaway OUTPUT (not input) risks the reaper. gemma-26b is a REASONING model: needs max_tokens ≥2500 or empty content+finish_reason=length (substrate dispatches 16000, fine). ws `flexllama-models.sql` updated+pushed; rig `stewards-dance.json` (gitignored) backups `.bak-pre-{gemma26b,parallel2,maxctx}`. nemotron usable=false. (2) **Overnight report:** flexllama 1789/1800 done (99.4%), $0 (32.3M in / 1.4M out tok all local); judges-local validated (opencode_go just 6 errs vs 217/24h before); 27 work_items completed. (3) **★ ENGRAM EMBED MISROUTE FIXED (OSS `main` pushed, virgin-smoke OK 24).** Engram embeds were ~72% failing (LM Studio 400 'invalid model qwen/qwen3.6-27b') — the engram populate trigger enqueued embed jobs with NO payload.model → bridge fell to the lm_studio DEFAULT (a CHAT model). docs/brain worked (payload carries model=nomic-embed-text-v1.5 + dimensions). FIX: extended the es2 trigger `trigger_embed_provider_route` (already forces provider=lm_studio "in one place") to ALSO fill payload.model (config `embed_model` default nomic-embed-text-v1.5) + payload.dimensions (config `embed_dimensions` default 768, JSON number) when absent — COALESCE leaves explicit ones. Applied live via CREATE OR REPLACE; **backfilled 1183 vectorless engram rows → 0 errors, coverage 1585/1585**. KEY: `engram_embeddings.embedded_model='qwen...'` was a MISLABEL (vector(768) col proves the vectors were nomic-768 all along → existing compatible, only backfill missing). Rebuild + virgin-smoke 00→37 green gated the public push. Task #187 DONE. CARRY: the digester-repo-read council item still in inbox (unacted, dominion_in_council).
- **★ 2026-06-18 — LOCAL DUAL-4090 INFERENCE via FlexLLama: PROVEN + WIRED + 3-WAY VERDICT.** Record = `projects/pg-ai-stewards-workspace/research/local-inference-flexllama.md` (nothing committed to OSS; `oss/.env` flexllama block is a local gitignored config change). Michael wanted a multi-GPU server better than LM Studio (LM Studio can't pin a model→GPU / budget VRAM). Cloned `yazon/flexllama`→`external_context/flexllama` (gitignored); built CUDA image trimmed to **sm_89** (`flexllama-gpu:latest`); **per-runner `CUDA_VISIBLE_DEVICES` pins one model per card**; shares `C:\Users\cpuch\.lmstudio\models` GGUFs read-only (no copy). **★ CONTEXT CEILINGS (1×4090, Q4, flash-attn, --parallel 1):** qwen3.6-27b **~128k** (fills card; native 256k); **gemma-4-12B-QAT FULL 256k @ 12.3GB** (native 256k; KV ~3GB/128k — the long-ctx champ); nemotron-3-nano-4B native **1M**, 204 tok/s. **PAIRS on ONE card:** gemma256k+nemo512k f16 (23.9G); gemma256k+nemo **FULL 1M** with `q8_0` KV (23.9G). Both-GPU concurrent 27B+gemma ≈ **136 tok/s, 0 errors** (E1 500s gone — dedicated cards + llama.cpp continuous-batching). **★ WIRED into LIVE substrate:** `flexllama` provider in `oss/.env` (`host.docker.internal:8090/v1`, openai, no key — mirrors OLLAMA), recreated **stewards-oss-pg --no-deps --force-recreate** (data-safe, pgdata persists; the bgworker reads provider env at pg startup), `providers_loaded()` shows it, `model_capability`+`$0 pricing` for qwen3.6-27b/gemma-12b/nemotron-4b (usable, trains_on_data=false). **★ E2 PASS (tool loop):** E2a raw-OpenAI tool_call — both gemma+nemotron emit+synthesize; **E2b REAL substrate gather — gemma-12b drove the multi-round web loop (web_search_exa/fetch_url) → correct items brief → completed, $0, tokens_in=258k cumulative.** **★ 3-WAY VERDICT (same research-summary gather, each forced local via work_items.model_override+provider_override):** gemma-12b ✅ rich 3.8k-char brief = **GATHER/doer winner**; qwen-27b ❌ HTTP-400 ctx (69k>65k at n_ctx 64k; needs 128k = its whole card) → **CRITIC/COORDINATE/SYNTHESIZE** not raw gather; nemotron-4b ⚠ drove loop but `output=[]`/never converged (1 tool call, 76 out-tok) → **FAST PERSONAS only** (Michael's instinct confirmed). **★ CONFIG LESSON:** this llama.cpp build = `--ctx-size` is **PER-SLOT × default --parallel 4** → set `--parallel 1` + size `n_ctx` to the CUMULATIVE loop (gather is token-heavy; 32k gemma + 64k qwen both 400'd mid-loop, 128k gemma sailed). **NEXT (Michael's plan, not started):** the **1-2 punch = gemma doer → qwen critic → gemma revise** via per-STAGE models (book-digest critique / research-write review are the hosts; `work_item.model_override` is all-stages so use stage `model`/`stage_models`); per-model "special instructions" tuning flagged. **★ CLEANUP TODO (live DB test cruft):** `lm-eval` intent+pipeline (earlier experiments) + work_items `e2b-gemma-ainews`/`e2b-gemma-2`/`e2b-qwen`/`e2b-nemo` — purge when done; flexllama model rows = KEEP if adopting. **Container `flexllama-stewards` UP holding BOTH 4090s**; `docker rm -f flexllama-stewards` frees them for LM Studio. **★ MAX-VRAM proven** (`stewards-max.json`: qwen 65k GPU0=20.5G + gemma 256k & nemotron **full 1M** q8_0 KV GPU1=23.9G). **★ STRICT-TEMPLATE BUG FIXED + SHIPPED (OSS `main` `258eaea`, applied live):** `compose_messages` relabels any mid-history system row (the soft-cap `[STEWARD NOTICE]` from `build_soft_cap_notice`) → `user` in place, so the array has exactly one system msg at front (Qwen3.6's template requires system-first → 400'd; gemma/nemotron tolerated but it's semantically weak for them too — Michael's instinct). Inverse-proven: qwen@65k gather that 400'd now runs clean. Follow-up: virgin-smoke assertion. **★ "punch above context weight" VERDICT = NO for gather:** qwen@65k survived 317k cumulative (reactive engine folds) but EMPTY brief + 0 agent context-tool calls (#136 reaffirmed: reactive floor carries it, models don't self-manage even when told); gemma@32k 400'd FIRST round (37.6k>32k — irreducible web-gather working set ~35-65k); gemma@128k = rich brief. Lever = native context matched to job (gather ≥64k; critique fine small). Substrate gap: context engine defaults flexllama window ~200k (doesn't know real window → can't fold to fit small). **CLEANUP TODO grows:** live work_items e2b-gemma-ainews/e2b-gemma-2/e2b-qwen/e2b-nemo/e2c-qwen-ctx/e2d-qwen-fix/e1-gemma32k + lm-eval intent. **NEXT unbuilt payoff: the 1-2 punch** (gemma doer → qwen TOOLS-OFF critic → gemma revise; per-STAGE models on book-digest/research-write; qwen-as-critic is tools-off so the template bug never bites it). **★ "ROOM TO BREATHE" BRAINSTORM → 1+2 SHIPPED, 3 PROPOSED (OSS `d48bc5b`, applied live):** Michael's framing — context window = input + reasoning + output sharing ONE budget; a near-full window starves generation. **#1 soft-cap notice now gateable** (chat_post_internal, config `soft_cap_notice_enabled` default true; **set FALSE live** per Michael — compose_messages already relabels it harmless, this kills it). **#2 WINDOW-AWARE BUDGET** (`effective_budget` 15a Layer 2.5 + new `model_capability.context_window` col in 31): flexllama had NO provider_rules row → fell to the **64000 fallback** (gemma over-folded to 64k despite 256k; qwen got ~full-window budget → no room → empty output). New layer: model window×**0.70** (Michael's 30% reserve) when context_window set; paid providers unchanged. Live-proven gemma→183500, qwen→45875. **★ HONEST: #2 is correct + improves gemma but NOT sufficient for small-window gather** — qwen@65k STILL 400'd (at 65,750) because compose_messages preserves the fresh ~8-msg TAIL raw, so a single big fresh fetch (~40k) can't be folded → blows a 65k window in one round. **= empirical proof #3 is the real fix.** **#3 PROPOSAL `page-in-large-results.md`** (dominion_in_council): stop inlining big fetch/doc results — store + summary + handle + read-slice tools (result_read/result_search), model-chosen retrieval; reuses judge-brief intercept + context_search + expand_message + fs_read offset/limit (~70% built); helps small local models AND cuts PAID token cost. **VERDICT: gather wants native ctx that holds a round's ~35-65k working set (gemma 128k); small windows need #3.** Template fix `258eaea` + this `d48bc5b`. Follow-up: virgin-smoke asserts for the relabel + the budget layer. **CLEANUP TODO += e2c/e2d/e2e/e2f/e2g-qwen + e1-gemma32k work_items.** **★ #3 PAGE-IN BUILT + SHIPPED (OSS `e1bc8e2`, new `33-page-in.sql` + compose_messages wrap + lib.rs/Dockerfile; Michael "lets build number 3" = the council go):** `page_in_cap` truncates any single rendered message over `effective_budget * page_in_single_msg_ratio` (0.5, window-aware) to head + a [page-in] banner with the handle; new sql_fn tools `result_read(handle,offset,limit)` + `result_search(handle,query)` (own+watch, reuse 27 resolver) granted research/dev. **MECHANISM PROVEN (10x): a qwen@65k gather cut ~15 raw fetches from ~600k→70k tokens.** **★ BUT HONEST: per-message capping does NOT enable small-window gather** — qwen@65k STILL 400'd (70,927) because ~15 capped messages sum past 65k; a per-message cap can't bound the TOTAL of a many-message loop, and the async torso-folding doesn't keep pace in a fast loop. The COMPLETE small-window fix = a **total-render cap** (drop/summarize oldest until rendered total ≤ budget) = **P1, not built** (chases qwen@65k gather which is a NON-GOAL — gemma 128k does gather). So #3 P0's real value = cap a pathologically-large SINGLE result + paging tools + cost-trim; it does NOT make small models gather. **VERDICT (4x-confirmed): gather needs native ctx that holds the ~15-msg loop (gemma 128k); qwen = tools-off critic.** Proposal `page-in-large-results.md` (P1 total-cap noted). Follow-ups: P1 total-render cap (only if small-window gather ever becomes a goal) + virgin-smoke asserts. **★ SESSION = 3 OSS commits: `258eaea` strict-template fix, `d48bc5b` window-aware budget+notice-gate+proposal, `e1bc8e2` page-in.** **★★ CORRECTION (Michael pushed back, RIGHT): qwen@128k IS a strong LOCAL agentic coordinator — I overclaimed "qwen tool-loop unreliable."** Every qwen failure was window/template/budget, NEVER tool-calling. Loaded qwen at its real **128k** (fits a card, 24GB) + template fix + window-aware budget → drove the full multi-round web gather: **err=NONE, 8342-char brief (RICHER than gemma's 3802), 270k cumulative, reasoned WHILE gathering** (excluded stale items by checking dates). So **qwen-27b@128k = local coordinate/reason/critic (free, no-train) → kimi paid fallback** (only for >128k or heaviest agentic). **The 3-MODEL DANCE casting:** ingest (hold raw)→deepseek/nemotron/gemma; coordinate+reason+critic→qwen@128k LOCAL FREE→kimi. **★ The architecture is already there:** stages pass COMPACT `{{stage_results.X.output}}` briefs forward (raw stays in the gather session), so reasoning stages get small inputs that fit any window; research-summary already casts review→qwen tools-off. **NEXT (offered): role-aliases** (`ingest`/`reason`/`critic`/`coordinate` via the 31 alias system, members = the fleet) so pipelines reference ROLES + the router picks best-available + fails over; recast digester/research pipelines. = the real payoff; makes Vivint analysis fully local+free+private. Findings doc has the full casting + correction. **★★ DANCE BUILT + PROVEN E2E + SHIPPED (ws `276ea52`): role-aliases `ingest`(gemma→nemotron→kimi)/`reason`(qwen→kimi)/`critic`(qwen→kimi) + flexllama-models overlay (3 models, no-train, $0, ctx 131072) + research-summary recast to roles.** Dispatched research-summary NO-override → **gather=gemma(7 tool rounds), synthesize=qwen, review=qwen — ALL LOCAL, $0** (each role auto-resolved per stage; handoff = compact `{{stage_results.X.output}}`). Vivint analysis can now run fully local+free+private. ★ context_window in flexllama-models.sql MUST match the loaded FlexLLama config (stewards-qwen128.json = all 128k); update if runtime changes. **★ CLEANUP DONE:** purged 23 test work_items + 31 sessions + 250 msgs + 151 wq rows + the lm-eval intent/pipeline/agent (transactional). **SESSION TOTAL: OSS `258eaea`+`d48bc5b`+`e1bc8e2`, ws `276ea52`; FlexLLama on **stewards-dance.json** (canonical: **qwen q8 KV @ 192k GPU0 [garrison needs max ctx; KEY LESSON: the KV is touched every token so it MUST sit in DEDICATED VRAM — f16@128k spilled KV to system RAM on WDDM → 13 tok/s; q8 halves KV bytes → 192k fits dedicated → 47 tok/s; loaded ~20.5GB, ~720MB inference peak; nudge down if a near-full 192k prompt slows]** + gemma **256k f16** + nemotron **512k q8 KV** [f16 512k = 24.0GB/565MB-free GPU1 → same WDDM spill: slow AND wouldn't load (GPU1 sat at gemma-only 12.7GB); q8 → ~22.6GB, ~1.9GB free → loads + 223 tok/s; window/budget unchanged, q8=bytes-not-tokens] GPU1; windows aligned live+overlay). **★ REPRO SCRIPT: `projects/pg-ai-stewards-workspace/scripts/setup-flexllama.ps1`** (ws `1f75cc4`) — one-shot: clone+build FlexLLama (arch-trimmed), write the dance config, mount LM Studio GGUFs RO, launch+wait; idempotent (-Rebuild); header documents the substrate prereqs (oss/.env provider + flexllama-models/role-aliases overlays). canonical config = `external_context/flexllama/stewards-dance.json`.** Follow-ups: P1 total-render cap (only if small-window gather becomes a goal); virgin-smoke asserts (relabel/budget/page-in); recast book-digest/playlist/planning to roles; deepseek as an `ingest` member.
- **★ 2026-06-17 — NVIDIA FREE PROVIDER WIRED + PROVEN e2e (OSS `da03dfe`, ws `f01afc1`; task #185 in-flight, guard-rail+routing remain).** Michael found build.nvidia.com free preview endpoints (OpenAI-compat; **trains on data → PUBLIC lines ONLY**). Tested raw: `moonshotai/kimi-k2.6` HTTP200/~0.9s + **tool-calling YES** (digesters need it); `qwen/qwen3.5-397b-a17b` returned empty/0-tokens (quirk — kimi covers the doer). Wired: `oss/.env` `STEWARDS_PROVIDER_NVIDIA_{BASE_URL,API_KEY,KIND}` (gitignored; env_file feeds pg+bridge). **★ GOTCHA: chat dispatch is the IN-PG bgworker (`providers.rs` from_env, reads env at pg startup, never reloads) — NOT the bridge.** First recreated the bridge → still "unknown provider: nvidia"; recreating **stewards-oss-pg** (env_file reload; pgdata volume persists, data intact) fixed it. model_capability+pricing **$0** for kimi-k2.6/minimax-m3/deepseek-v4-flash (`overlays/nvidia-provider.sql`). PROVEN: a work_item forced to nvidia/kimi-k2.6 completed e2e, output correct, $0. **★ KEY DESIGN REALIZATION: provider choice must be INTENT-aware, not per-pipeline** — research-write/planning are SHARED by public + vivint intents, so routing those pipelines to nvidia would leak vivint to a train-on-data endpoint. So the **file_private guard rail (refuse nvidia for private intents) is the KEYSTONE**, not just defense-in-depth. **REMAINING:** (1) guard rail [core dispatch — refuse/avoid nvidia when intent.file_private]; (2) route public digesters → nvidia free (book/playlist single-intent pipelines safe immediately; research/planning gated by the guard rail) = off opencode-go spend. NVIDIA model ids carry org prefix (moonshotai/, deepseek-ai/, minimaxai/). (Board megaline not extended this turn — saturated + sibling-active; this lane + the live state are the record.)
- **★ 2026-06-17 — DIGESTER EMPTY-SOURCE HALT FIXED + LIVE (OSS `244ae38`/`6407e42`, pg18; task #184 DONE).** From the morning "how many runs overnight" dig: book-digest ran ~18× overnight (3 REAL digests — no junk, content-gate holds; 3 SHELF-EMPTY no-ops that ran all 4 stages; 2 13h-zombie Moonshot hangs → I cancelled them). **Root cause (reproduced via work_item_advance):** the per-pipeline BEFORE-UPDATE empty-guard set status=cancelled, but `work_item_advance` STILL RETURNED the next stage name + the bgworker dispatches off the RETURN → cancel and return disagreed → 4 stages ran. A BEFORE-trigger can't win that race. **Fix:** halt moved INTO `work_item_advance` — `metadata.halt_on={stage,outputs[]}`; on sentinel → cancel + RETURN NULL (no dispatch). book-digest/playlist-digest declare halt_on; 2 triggers retired. virgin-smoke OK17 + clobber 3/0 + **live inverse-hypothesis proven** (SHELF EMPTY → NULL/cancelled/stays-read). Lesson: a status-cancel that doesn't also halt the RETURN value loses to the dispatcher; halt at the choke point, not in a racing trigger. = digester-empty-source-halt SHIPPED.
- **★ 2026-06-17 — A (intent-private routing) + B (tool primers) + pre-commit hook REMOVED (OSS `30a0991`/`5d7133a`, ws `4a9878e`; pg18; tasks #182/#183 DONE).** (1) Removed `.git/hooks/pre-commit` (targeted retired `pg-ai-stewards-dev`; RW /workspace mount auto-materializes now). (2) **A — `29-intent-private-routing.sql`:** `intents.file_private` + a BEFORE INSERT/UPDATE OF file_destination trigger that prefixes `private/<intent_slug>/` (idempotent; catches every stamping site since enqueue_work_item_file re-reads the col). Overlay `intent-private-vivint.sql` marks vivint private. **Vivint drops moved → `private/vivint/{plans,research}/` + root .gitignore safety-net** (`plans/vivint-*`, `research/vivint-*`, root commit `30f384a3`, NOT pushed). Live-verified: `plans/x → private/vivint/plans/x`. (3) **B — `30-tool-primers.sql`:** `tool_primers` table + `render_tool_primers(agent_family)` gated per group (context=context_tools_on, skills=skill perm) + core primers; compose_system_prompt (09, lines 257-468) injects it late-bound. Telemetry drove it: context_search/todo/goal/skill = **0 uses** (models untrained on them; reactive engine fine w/ 8 folds). virgin-smoke OK15+OK16 + clobber 3/0; live-verified (research gets the context primer). **Deploy gotcha:** scratch-container extraction flaked → extracted compose_system_prompt from the repo file block (257-468) instead. **One-shot Q answered:** 57 tools-ON stages (multi-round, coder-pr-style) vs 24 tools-OFF (true one-shot judges); primer aimed at the long-runners (coder/steward/personas).
- **★ 2026-06-17 — GUARD NARROW AUTO-RESUME BUILT + LIVE + pushed (OSS `c1dc09f`/`5d7133a`, pg18; task #181 DONE).** The guard auto-paused tonight on the 24h spend cap (worked as designed); Michael bumped cap $10→$12 + resumed, then ratified "narrow resume" — the guard should release its own brake when a breach self-clears. `28-guard-autoresume.sql`: `reflect_guard_autoresume_tick()` lifts a pause ONLY if guard-set (`reflect_pause_source='guard:<breach>'` marker; human pause='manual' stays) + breach self-clearing (spend|in_flight; failures/proposals stay for a human) + no active breach + metric <75% of cap (deadband). Logs `auto_resumed`. Re-authors reflect_pause/reflect_watchman_tick/watchman_scheduler_fire/reflect_status (carried 22/23 bodies + marker; confirmed 22/23 were latest pre-28 defs). virgin-smoke OK14 + clobber 3/0 + live-verified. **Live spend cap = $12** (Michael's bump). Pause-source-marker pattern is the reusable bit. Journal `2026-06-17-context-search-and-guard-autoresume.md`.
- **★ 2026-06-17 — `context_search` P0 BUILT + LIVE + pushed (OSS `de52a24`/`14fce53`, pg18 rebaked; task #180 DONE).** `27-context-search.sql`: `context_search`(scope session|descendants; curated default + `include_folded` recovery; snippet+`[ctx:handle]`) + `context_session_private` wall (sessions.private, beats the watch, absolute) + `context_descendant_sessions` (work_items lineage, excludes private). `context_*` auto-surfaced (no gating change), sql_fn (no refresh-tools), tool descriptions written to teach. virgin-smoke OK13 + clobber 3/0 + live-verified (snippet+handle, surfaced to research). Schema-investigation notes for future builds: messages has `context_state`(verbatim/muted/compressed/pinned)+`context_tags`; sessions had NO parent link → lineage via `work_items.session_ids`+`parent_work_item_id`; `context_handle(id)`→4hex; `pipelines.stages` CHECK requires ≥1 elem (smoke caught `[]`). **`self` deferred to P1** (no session→agent map). Michael's adoption idea captured in proposal: per-tool-group usage primer + usage telemetry.
- **★ 2026-06-17 — `context_search` RATIFIED (council w/ Michael; OSS `ce79e60`), cleared to build P0.** Decisions: P0 = own (session+self) + descendants (the watch) + a MANUAL session `private` flag that beats the watch (private child invisible even to parent — security primitive for local-non-cloud sensitive work); upward (ancestors) private-by-default + per-msg private + a `sensitive` intent/agent flag (forces local+private) = P1; recall-surface + global = P2; wall is absolute (D&C 121). P0 build = a fn over `messages` + tool_def + private/descendants resolution, in the `context_*` grant family; smoke = folded-only-with-flag + handle→expand_message + parent-sees-normal-child-not-private. NOT built yet (awaiting Michael's build go).
- **★ 2026-06-17 — PROPOSAL `context_search` WRITTEN (awaiting council; OSS `dc5d106`).** Michael's idea: durable messages → give every agent grep over its OWN context + inject into docs (the Ctrl-F a model can't do). Captured his refinements: name `context_search`; `include_folded` flag (recover muted/compressed for re-open — loops w/ productivity auto-fold); scope ladder = D&C 121 walls (own→self→descendants[watch]→ancestors[private-aware]→global[gated]); per-msg `private` flag (parent walls privileged from children; opt-out-vs-opt-in = the council fork); provenance≠truth; snippet+handle results ride the context engine. P0=own-context. dominion_in_council. `.spec/proposals/context-search.md`.
- **★ 2026-06-17 (cont.) — two tunings (Michael):** (a) **qwen3.7-plus PRICE CONFIRMED $0.40/$1.60 per Mtok** (opencode zen, his link — cheaper than qwen3.6-plus's 0.50/3.00; my placeholder was too high) → corrected live + repo (OSS `62f9a27`, ws `82ce3aa`); spend guard now accurate. (b) **book-curate cadence 6h→2h** (`0 */2 * * *`) — book-digest eats ~1 book/hour so 6h drained the shelf; 2h keeps pace (a STOCKED run is one cheap no-add call). Live + repo.
- **★ 2026-06-17 — QWEN MODEL ROTATION (Michael: "rotate those models up… max is most expensive").** `qwen3.6-plus`→`qwen3.7-plus` (newer, same price) across ALL dispatch paths: 25 pipeline stages, 15 stage_models, 6 brainstorm metadata, 3 gate-fn hardcodes (evaluate_gate/verify_work_item/sabbath_dispatch), 5 escalation rules; + `prompt-critic` review `qwen3.7-max`→`qwen3.7-plus` (max = priciest + usable=false). **★ CAUGHT qwen3.7-plus had NO price row** → would blind the watchman spend cap; priced = qwen3.6-plus rate ("similar price" — ⚠ CONFIRM real opencode rate, 1 row live+repo). Catalog keeps old models; factual comments/caveats/verify-* fixtures preserved. Live: direct UPDATEs + 3 gate-fn CREATE OR REPLACE (extracted final bodies). virgin-smoke OK 1-12 + clobber 3/0; pg18 rebaked. OSS `73f7c81` + ws `51534d9` pushed. (Cadence answered: reflect-steward 3h, book-curate 6h.)
- **★ 2026-06-16 (cont.) — DIGESTER-STEWARD P0 (book-study CURATOR) SHIPPED + e2e-PROVEN + pushed (OSS `38f6c6d`).** Council-ratified ("I want this change… enable by default"; "always push back on spend" confirmed-wanted). The missing back-office leg = the reflect-steward generalized to a queue. Built in `examples/book-digester.sql`: `book_shelf_status()` fn+tool, the `book-curate` pipeline (1 tools-on `curate` stage, research agent, kimi-k2.6: runway→survey→pick+**verify-findability**→book_add; dry→start_brainstorm), `book-curate-cron` (6h, enabled), dials (`runway_threshold=3`/`max_adds=5`), research grants. **Proven BOTH branches on live:** feed→3 non-dup intent-aligned books (Nicomachean Ethics/Hume Enquiry/Bacon Novum Organum, Gutenberg URLs I confirmed HTTP 200 — inverse-hypothesis on the verify-gate); restraint→"SHELF STOCKED", added nothing. Snags: tool_defs INSERT was 5-col/4-val (added `active=true`); **Moonshot AI [kimi] DOWN (521/522)** → proved logic by overriding the e2e work-item only to qwen3.6-plus (opencode-go flat-rate ~$0; production stays kimi, retries next tick). Journal: OSS `.spec/journal/2026-06-16-digester-steward-curator.md`. Task #179 DONE. **P1 (ratified, not started):** generalize curator→video-study/ai-news; generic `digester-empty-source-halt` (retires the 2 per-pipeline triggers); slow book-digest hourly→6h to match the curator.
- **★★★ 2026-06-14 PERSONA-LEG + SCRIPTURE-MCP ARC (Michael's direction; IN FLIGHT, big outward-facing infra):**
  - **✅ M6 (tail-bounded) DONE+PUSHED (OSS `15a1084`):** stamp_code_write_sandbox defaults `acceptance_criteria=''` (the M2 stall gotcha) + virgin-smoke OK 6 (compact_context ships complete; chain 00→21). M6-tail REMAINING (bigger, deferred): bgworker surface-not-swallow render errors (Rust); capability-aware model substitution (probe tool-USE not just chat).
  - **✅ PARENT-REPO GITIGNORE HYGIENE FIXED (root commit, NOT pushed — Michael pushes root):** the parent was tracking **9 nested external repos as bare gitlinks** (ai-jumpstart, dnd-tools, md-mcp, pg-ai-stewards-oss, pg-ai-stewards-workspace, scripture-book, spin, spoken-study, strongs-concordance-mcp) → `git rm --cached` (working trees intact) + .gitignore'd all + future (webster-mcp/byu-citations/gospel-engine). Parent now tracks 0 projects/ gitlinks. (projects/pg-ai-stewards [private substrate] is NOT a separate repo — stays tracked.)
  - **★ GOSPEL-ENGINE → REMOTE HTTP MCP: opus dev subagent RUNNING (bg, agentId ae1c2240a26eea361)** — Michael's explicit "do it in a subagent, it'll auto-deploy." Spec: ship gospel-engine as remote HTTP MCP like dnd (dnd.ibeco.me/mcp) so the lean OSS bridge + Claude cowork reach it over http. ⚠ NO SendMessage in this harness — can't live-redirect it; it's extending engine.ibeco.me (NOT creating a public repo, so low corpus-publish risk). **Michael's NEW direction to fold in when it reports:** (a) fix broken `engine.ibeco.me/api/admin/tokens` (token-settings link dead-ends, no way to get an API token); (b) move to a PUBLIC repo in `projects/gospel-engine/` — but **CORPUS STAYS PRIVATE** (gospel-library = church content, gitignored; code public, corpus mounted). Diagnostic seen: gospel-engine-v2/internal/mcpserver uses mark3labs/mcp-go (broken import — the subagent's WIP).
  - **★ SCRIPTURE-MCP LAYER → PUBLIC REPOS + SUBDOMAINS (Michael BLESSED, NOT yet started):** webster-mcp + strongs-concordance-mcp + byu-citations → each (1) move scripts/<x> → `projects/<x>/` as its OWN public GitHub repo (gitignored from parent — done preemptively), (2) add remote HTTP transport like gospel/dnd, (3) deploy to a subdomain (webster.ibeco.me/mcp etc.). These are librarian's toolchain. **Plan = serial-probe-then-parallel-scale: let gospel prove the full pattern (projects/ repo + public + HTTP + subdomain + deploy + verify), THEN fan out webster/strongs/byu copying it.** webster/strongs = public-domain data (clean public); byu = check usage terms.
  - **PERSONA MIGRATION — scoped REALITY (Michael: "pull in all 3 + Callie"):** 3 substrate personas: **codewright + gamemaster EASY** (core tools + dnd is already remote-http); **librarian GATED on the scripture MCP layer above** (gospel/webster/strongs/byu). Persona-host hosts **5 chattermax personas** (chip-assistant, chattercode, dm-assistant, npc-ally, **callie**) — keys in OG persona-host env (`CHATTERMAX_PERSONAS`, cmk_* tokens); their personalities + the chattermax→substrate-agent mapping live in the **remote chat.ibeco.me DB** (no local ai-chattermax container). Cutover = move 5 keys to OSS persona-host .env + apply 3 substrate persona overlay seeds (r9/r13/r14/r15/r17) + **stop OG persona-host → start OSS** (⚠ outward-facing live chat.ibeco.me rooms; double-fire safe ONLY if stop-OG-first; general-workspace lane owns the OG persona-host container — signal it). HELD until scripture MCP layer is remote (so librarian works).
  - **dnd + chattermax OSS EXAMPLE DOCS** (Michael: dnd is public, document so others use chattermax+dnd as examples) — TODO.
  - **ANSWERS captured:** Callie = chattermax persona (not substrate agent). Thummim = NOT an MCP — the "Thummim 2026 Restoration Dictionary" generation pipeline (overlay thm1, schema+3-stage, never dispatched). Embeddings = pgvector storage + local Ollama/LM-Studio nomic-embed at index time (docs/engrams/corpus), not "everything." ibeco.me UP (200); the broken link = engine.ibeco.me/api/admin/tokens.
- **★★★ 2026-06-14 SESSION (Michael fixed the PAT repo-scope; "finish the test… get OSS up and running fully before Sabbath"):**
  - **✅ M2 FULLY CLOSED — DRAFT PR #1 LANDED e2e** (`github.com/cpuchip/pg-ai-stewards-coder-proof/pull/1`, draft, OPEN). Fresh code-pr work item `6aeb7265` ran clone→plan→plan_review→implement→verify→review→pr→push→gh-pr-create on the OSS stack. Token fix verified by a bridge-side dry-run push (exit 0) BEFORE the run. **NEW BUG FOUND (M6): review-verdict parser is start-anchored** — `work_item_advance` cv6 checks `v_verdict_text !~* '^\s*REVIEW:\s*passes'`, but glm-5.1 preambled ("All files verified…\n\nREVIEW: passes") so a genuine PASS misreads as REVISE and parks at awaiting_review. Worked around by applying the verdict in the expected format (`work_item_advance` with output prefixed `REVIEW: passes`); the real fix = make the cv6 AND cv11 (`^PLAN:\s*approved`) matches line-anchored/multiline (`(^|\n)\s*REVIEW:\s*passes`). Also confirmed: review→pr auto-advance after a TOOL-USING review stage doesn't fire (bgworker didn't enqueue the advance) — manual `work_item_advance` is the bridge; both are M6.
  - **✅ M3 DONE + PUSHED (OSS `08850f6`) — the web UI is RUNNING.** Ported `scripts/stewards-ui` (59 files, ~11K LOC Vue+Go, 23 views / **61** API routes — not 23) → `cmd/stewards-ui/` in the single OSS module (no go.work/stubs). **doc_* rename done SCHEMA-VERIFIED (not blind):** studies→docs, `study_search_text`→`doc_search` (non-1:1, drops _text), study_citations/similar→doc_*, verdicts.study_id→doc_id, intents.scripture_anchor→**values_anchor** (the docs table has NO values_anchor — it's an intents col; a blind rename would've broken). Go idents + `/api/studies/*` routes + JSON labels KEPT (frontend contract); cosmetic "Studies"→"Docs" relabel = polish follow-up. New `extension/ui.Dockerfile` (node→go-embed→alpine, clean-room) + default `ui` compose service (local-only 127.0.0.1:**8081**; committed default) + committed `frontend/dist/index.html` stub for local go:embed + frontend `.gitignore`. **VERIFIED RUNNING:** image builds, container up (verified this run on **8082** — 8081 was transiently held by a sibling vite dev-server PID 30556, NOT killed per presiding covenant), healthz 200, SPA serves, **all 36 GET routes 0×HTTP-500**, renamed surfaces (studies/get, studies/search→book-self-reliance, intents/get, watchman/pass) exercised with REAL data → all 200. Route-verify done as a curl-sweep ORACLE (beats fan-out for a mechanical "is it 500?" check — told Michael).
  - **✅ M6 CODER-HARDENING DONE + PUSHED (`6078771`) — code-pr works out-of-the-box now.** Found 2 real public-repo defects: review model qwen3.7-max (401s oa-compat / 400s Alibaba-tools) → glm-5.1 (pipeline JSON + stage_models); verdict parser START-anchored (`^REVIEW: passes`) misread preambled passes as revise → LINE-anchored `(^|\n)\s*REVIEW:\s*passes` (cv6+cv11); pr stage no round-cap → 40. Live-applied (MSYS_NO_PATHCONV=1 docker cp) + **PROVEN by a fully AUTONOMOUS no-touch run → DRAFT PR #2** (clone→…→review→pr, zero manual nudge). Live↔source coder drift reconciled. Journal `2026-06-14-m2pr-m3-ui-m6-coder.md` (`26ca4f0`).
  - **✅ M4 SCHEDULER LEG CONFIRMED RUNNING** — book-digest fires hourly (22:00→01:00) + playlist-digest at 00:00 autonomously on OSS, producing real docs (incl `yt-RB8vjn1QPeM` self-improvement video off the playlist). Only M4-PERSONAS remains ⚠key-safety (the persona *pipeline* can be verified safely via SQL dispatch w/o the gateway = no live-room double-fire; connecting to a REAL room needs Michael).
  - **✅ M5 COUNCIL DONE + RATIFIED 2026-06-14 (Michael chose "convene the council now"). dominion_in_council SATISFIED. 4 decisions:** (1) timing = **mid-turn** (waiting_for_tools); (2) compactor = **fixed cheap model, fast + ~1M ctx, but TUNABLE via config** (Michael: "run experiments to find a good compactor counselor"); (3) sees = judge-brief condensed surface; (4) trigger = **agent-initiated + ≥threshold pressure-line nudge** (persuasion not compulsion). **Mechanics VERIFIED (all primitives present):** mute is by message-id (`context_mute(msgid)`) + `context_resolve_handle(parent_session, handle)→msgid` so the compactor acts on the PARENT's msgs; **`context_expand` = the reversible unmute (blind-spot resolved, safe-by-construction holds)**; mid-turn rails already exist (`tool_dispatch_complete_waiting` resumes a `waiting_for_tools` parent — spawn_subagent rides them, compact_context reuses); agents have a `context_tools_enabled` flag. **★ CORRECTION found in grounding (would've broken a rushed build):** `render_judge_brief_surface` is **per-MESSAGE**, not whole-session — session-level "what it sees" = the `context_pressure(session)` foldable list (handle+est_tokens) + per-msg reads. **★ M5 BUILT + PROVEN E2E SAME SESSION (OSS `a8d5cc5`)** — Michael corrected the timezone ("only 8:41pm CDT, I still got time!"; the DB UTC stamps misled me to "past midnight"), so we built it. `extension/21-compact-context.sql` (compact_context_surface = what the compactor sees; compact_context_apply = the substrate acts, mute/compress/pin by msgid only-if-belongs + honest curated-footprint metric + [COMPACTED] marker; pressure-line nudge; **compactor agent = TOOLS-OFF JUDGE** returning a {mute,compress,pin} JSON verdict [judges-not-executors — sidesteps the _session_id-is-the-compactor's-own-session trap]; compact-context 1-stage pipeline [deepseek-v4-flash, tunable]; deny-all-heavy grants; compact_context tool_def) + `cmd/stewards-mcp/compact_context.go` (mcp_proxy handler: reads injected `_session_id`, renders surface, **inherits caller's work_item intent** [spawn_subagent_create p_parent_work_item_id — core ships no default intent], spawns+polls compactor like spawn_subagent, applies verdict; extractJSONObject tolerates wrapping) + lib.rs chain (create_compact_context requires create_coder) + Dockerfile COPY + main.go register. **PROVEN: 14-msg migration session clogged w/ spent grep+schema dumps → compactor verdict `compress [1106,1107]` keep the plan → applied (compressed=2, [COMPACTED]) in 25s @ $0.** Bugs found+fixed in e2e (all MY plumbing, not design): (1) agent ON CONFLICT was (family) not (family,model_match); (2) spawn needs intent → inherit parent work_item; (3) live pipeline had stale model "openai" (re-apply); (4) poll waited for maturity=verified but a 1-stage tools-off pipeline ends at status=completed → broke into the bridge's 120s call-timeout → fixed to treat completed as terminal + cap wait at 110s. **Relief is governed by existing pressure-rendering** (muted/compressed fold to tombstones UNDER pressure; not an immediate delta — honest metric reports curated footprint). FOLLOW-UPS: tests/virgin-smoke.sql assertion; **pg/extension image rebuild validating pgrx compiles 21** (running; psql-applied clean + go build/vet green; CI also gates). compactor-model tuning = Michael's experiments.
  - **REMAINING:** M5 BUILD (spec'd, execution-ready) · M4-personas ⚠key-safety · M6-tail (stamp default + bgworker-surface + capability-substitution polish) · M7 soak · CUT ⚠his Hinge. **Net tonight: M2(PR#1)+M3(UI running)+M6-coder(PR#2)+M4-schedules DONE/verified, M5 councilled+spec'd.** OSS UI running on 8082; digesters hourly on Michael's lent keys.
- **★★ PARITY ROADMAP RATIFIED 2026-06-13 (tasks #161-168). Michael: "keep going until we hit parity… only braking for critical matters." Revised parity = #4 + coder enablement + UI/CLI up, before the cut. Ratified up front (3 Qs):**
  - **yt-mcp placement = OSS cmd/ + opt-in `docker-compose.yt.yaml`** (generic, public, but Python/yt-dlp behind an opt-in layer; default bridge stays lean; workspace overlay does NOT grow).
  - **Cockpit scope = ADD WRITE VERBS now** (dispatch/council/ratify/review) — Michael chose the fuller path over read-only-is-parity.
  - **compact_context = BUILD AS PARITY** (net-new in OSS core; Michael chose it in-scope over defer-to-post-cutover). Sketch defaults: between-turn, judge-pattern, cheap compactor; parked Qs in `projects/pg-ai-stewards/.spec/proposals/substrate-compact-context-sidequest.md`.
  - **VERIFIED THIS TURN (real-path, not memory):** OSS `cmd/stewards` cockpit ALREADY extracted (read-only P1: project/board/watch/cost; pgxpool); NO web frontend ever existed in either repo (UI = the terminal cockpit). `compact_context` = ZERO occurrences in OSS **and** private (never built; "pulled in at council" never happened — authoring leg only consolidated existing). Reactive context engine IS in OSS (compose_messages/compose_system_prompt/intercept_*/extract_engrams/render_judge_brief_surface). yt-mcp = trivial fold (package main, no cross-pkg imports, hand-rolled MCP, empty go.mod require; only runtime dep = yt-dlp/python, NO ffmpeg).
  - **MILESTONES (run straight through; brake only at ⚠):** ✅M1 yt-mcp+playlist · M2 coder e2e ⚠repo · M3 UI-extract(as-is) · M4 personas+schedules ⚠key-safety · M5 compact_context ⚠parked-Qs · M6 cleanups(promote_trigger sabbath-wrap, cv4→overlay, BYO-MCP docs) · M7 soak→parity-proven · CUT ⚠Michael's session (Hinge ①+③).
  - **★ M3 CORRECTED + RE-RATIFIED:** the "UI" is NOT the small cockpit — it's `scripts/stewards-ui/` = a ~11K-LOC **Vue+Go web app, 23 routes** (the live `pg-ai-stewards-ui` container, `stewards-ui --addr :8080`, served from `extension/ui.Dockerfile`), NOT yet in OSS. Michael re-ratified (correcting his earlier "add write verbs now" which was against my wrong description): **EXTRACT AS-IS** (existing write islands NewWork/ratify/Trust/Councils) + doc_* rename across it + go.work→single-module + node/go Dockerfile + compose + route-verify; evolution-proposal additions (substrate chat/intent authoring, `.spec/proposals/stewards-ui-evolution.md`) = POST-cutover. The read-only `cmd/stewards` CLI cockpit IS already in OSS (separate, smaller).
  - **✅ M1 DONE + PUSHED (OSS `3e5ef66`):** folded scripts/yt-mcp→cmd/yt-mcp (+ NEW `yt_playlist` flat-playlist enumeration tool — the discovery step the download-first tools lacked) behind opt-in `docker-compose.yt.yaml` (WITH_YT=1 → yt-mcp + python3/yt-dlp; default bridge stays lean). `examples/playlist-digester.sql` = playlist-digest pipeline (read→digest→critique[qwen3.7-plus]→recommend, kimi-k2.6 doer) + playlist_watch/playlist_seen + playlist_next/publish/add + video-study intent + 6-hourly schedule + ai-research playlist seed. Genericized 3 personal strings. **PROVEN E2E on the OSS stack** (bridge rebuilt WITH_YT=1; refresh-tools **7/7**, yt 5 tools; ran a real digest of WGwRCw9TRyo "This 1 Book…Geniuses" [the Euclid vid] off the playlist → 7804-char digest → study/yt/WGwRCw9TRyo.md doc + pending_file_write + brain entry + playlist_seen+1). Gotchas: yt-mcp needs args=['serve']; work_item_create does NOT auto-dispatch (call work_item_dispatch_stage; the scheduler does both). yt_playlist NOT backported to scripts/yt-mcp (deliberate fork; live tool untouched).
  - **M2 IN FLIGHT — coder PROVEN to write+test on OSS; PR hop being nailed:** created throwaway repo `cpuchip/pg-ai-stewards-coder-proof` (host gh — the bridge's scoped PAT can't create repos, 403; correct hardening). CODER_REPO_ALLOWLIST=`github.com/cpuchip/` already covers it. **Run 1 (53a8e452, CANCELED):** clone stage hit a **GitHub propagation RACE** (repo created seconds earlier → git backend not ready → "repository does not exist") so the coder initialized a fresh LOCAL module, wrote greet pkg + table tests + main.go, **`go test ./...` PASSED in the hardened sandbox** — coder capability proven on OSS (sandbox spawn via host socket, token bridge-side, code-write, test-green). Stalled at **plan_review/awaiting_review**. **ROOT CAUSE FOUND (not a bgworker gap — my incomplete input):** the code-pr templates reference `input.acceptance_criteria` (+ binding_question/repo/base_branch; sandbox/plan_feedback/review_feedback auto-stamped by `stamp_code_write_sandbox` trigger). I omitted acceptance_criteria → plan_review auto-dispatch's `render_stage_input` RAISED `resolve_template_path … NULL` → bgworker SWALLOWED the error → parked at awaiting_review with NO plan_review in stage_results + NO work_queue error. The bgworker auto-advance is FINE (digesters prove it). **Run 3 (e9364f01) dispatched WITH acceptance_criteria** — should clear plan_review → implement → verify → review → pr → DRAFT PR. Separate concern to watch: runs 1+2 clone stage narrated "repo did not exist" + fell back to local-init even though raw bridge `git clone` works (exit 0) — propagation race on run 1, run 2 ambiguous; watch run 3. **M6 polish candidates:** stamp trigger should default acceptance_criteria='' (forgiving input) like plan_feedback; bgworker should surface (not swallow) template-render errors. Gotchas: work_item_create needs an intent (no 'default' seeded → reuse video-study); work_queue has NO work_item_id col (links via payload); stage outputs live in work_items.stage_results jsonb; CODER_REPO_ALLOWLIST=`github.com/cpuchip/`; bridge PAT can't create repos (403, correct).
  - **M2 VERDICT (4 runs): CODER PROVEN ON OSS; full code-pr→DRAFT-PR blocked by provider/model-format issues (M6/debug, NOT coder defects).** Run 4 (5633b0e3) ran ALL of clone(REAL repo — race gone)→plan→plan_review→implement→verify→review; implement reports "Build, tests, run pass exit 0" on go.mod module cpuchip/pg-ai-stewards-coder-proof. **review stage FAILED: HTTP 400 "Error from provider (Alibaba): When using tool_choice, tools must be [non-empty]"** (qwen3.7-plus via Alibaba — a dispatch sends tool_choice with empty tools on a tools_disabled review stage → provider rejects; substrate provider-adapter bug, Go/Rust). My poll auto-nudge (dispatch on awaiting_review) made review OSCILLATE+fail — MY artifact (review's awaiting_review is normal verdict-pending, not stuck; do NOT nudge it). **MODEL CHECKER (Michael's recall — CONFIRMED WORKS):** probe flips qwen3.7-max→usable=f (401 oa-compat) / qwen3.7-plus→usable=t; substitution (model_substitutions + dispatch M.2) exists. GAP: auto-probe NEVER RAN on OSS (was 0/13; NOW 13/13 after manual enqueue_due_model_probes — 1 unusable/12 usable). **auto-probe IS wired** (CORRECTED): trigger `watchman_passes_schedule_model_probes AFTER INSERT ON watchman_passes` → enqueue_due_model_probes (19-models.sql §5/m5). Watchman cron = **weekly@sun-03:00** → probes refresh weekly; fresh stack hadn't probed yet (0/13). Manually probed all 13 now → current. NOT a bug; shorten watchman cron for faster refresh if wanted. **REAL review-stage root cause:** review (tools_disabled=FALSE — legit, template calls coder_shell/read to inspect the diff; all code-pr stages = `dev` agent w/ coder tools) used **qwen3.7-plus via Alibaba**, which rejects tools+tool_choice (`HTTP 400: tools must be non-empty`) — a qwen/Alibaba tool-format quirk; kimi-k2.6 (implementer) handles tools fine. tool_choice set in 15b chat_post_internal/dry_run_chat. **FIX (run 5, ad44ed67):** review gates plan_review+review → **glm-5.1** (opencode_go, tool-capable, non-qwen, probed usable, ≠ kimi implementer) in BOTH pipeline def + stage_models; re-running observe-only (NO nudge). **M6 ITEMS:** (1) review-model provider-portability — OSS code-pr ships qwen3.7-max review which fails on non-qwen-tool providers; make it capability/provider-aware OR per-stack stage_models override (proper fix vs my live pipeline-def edit); (2) probe TOOL-use not just chat (qwen3.7-plus passes chat-probe but fails tool-use) — capability-aware substitution; (3) stamp trigger default acceptance_criteria=''; (4) bgworker should SURFACE not swallow render errors; (5) reconcile my live code-pr pipeline-def edits (qwen→glm) back to canonical. Throwaway repo cpuchip/pg-ai-stewards-coder-proof exists (host gh; bridge PAT can't create). **★ M2 FINAL VERDICT (5 runs): CODER FULLY PROVEN ON OSS; DRAFT PR blocked ONLY by GitHub token repo-scope (Michael's grant, NOT a code defect).** Run 5 (ad44ed67) with glm-5.1 reviewers cleared plan→implement→verify→review cleanly; pr stage (after raising max_tool_rounds_hard→40 on the live sandbox: write 4 files, `go test` GREEN, local commit) **blocked on push: `remote: Permission to cpuchip/pg-ai-stewards-coder-proof.git denied to cpuchip. 403`** — confirmed by a direct bridge push test. The fine-grained PAT is scoped to specific repos (clone=public-read OK, push=write DENIED on the new repo); same root as the create-403. **TO LAND THE PR:** Michael adds the repo to the PAT's repo access (or retarget the coder at a repo the token already writes, e.g. ai-chattermax, or use a broader token) → then re-dispatch the pr stage (code is proven; ~30s + 1 stage). **GENUINE FIXES FOUND (promote to OSS-canonical in M6, NOT band-aids):** review gates glm-5.1 (portable vs qwen3.7-max which 401s + qwen3.7-plus which 400s-on-Alibaba-tools); pr stage max_tool_rounds_hard=40 (default too low for clone+diff+commit+push+gh-pr). Live pipeline-def edits on Michael's dev stack to reconcile. Proof sandboxes reaped. Runs 1-4 canceled/failed.
  - **Context-engine answer (Michael's Q):** OSS has the REACTIVE engine (compose_messages/compose_system_prompt/intercept_*/extract_engrams/render_judge_brief_surface); `compact_context` (proactive between-turn) = ZERO in OSS AND private — never built (M5 builds it).
  - **Checkpoint discipline:** commit+journal at each milestone close so a compaction can't lose progress (bom-walk resume mechanism).
- **★ ROADMAP 2026-06-13 (Michael's 7-item plan). #1 + #2 SHIPPED this turn, then journaled:**
  - **#1 DONE — exa web search = the OSS default** (`fd08fea`): seeded exa-search keyless
    free-tier (works OOTB), reversed M2's BYO call; smoke + prompts updated; refresh-tools
    6/6 incl exa-search [OK]. (Live instance already had it + grants — verified real-path.)
  - **#2 DONE — model-wiring examples** (`fd08fea`): `docs/wiring-up-models.md` (opencode
    zen-free/go/gemini/lm-studio env pattern) + `examples/models.sql` (real-price snapshot
    catalog, free models flagged) + `.env.example` provider block + `examples/README.md`.
  - **JOURNALED** (`096c7df`): `.spec/journal/2026-06-13-mcp-packaging-coder-and-usable.md`
    covers the whole session (plan→M0→resolver→harness→study-spec→M1 coder→M2→#1+#2).
  - **#3 DONE + RAN — book-digester** (OSS `eacb6c7`, `examples/book-digester.sql`):
    book_shelf + book_next/book_publish/book_add tools + 4-stage `book-digest` pipeline
    (read→digest→critique→recommend) + `book-study` intent + hourly schedule + shelf
    (Self-Reliance→Meditations→Tao Te Ching→Art of War). **PROVEN END-TO-END on the OSS
    stack with Michael's keys** (he copied his live .env in — NOT the cutover, just lending
    the dev stack his model keys side-by-side): first run digested Self-Reliance (8KB digest;
    **the qwen3.7-plus critique stage caught + corrected a real placement error** — null-case
    working), published study/books/self-reliance.md + a brain entry, advanced the shelf to
    Meditations. Single-pass v1 (long-book map-reduce = v2). Uses stewards-explore (kimi-k2.6
    doer / qwen3.7-plus critic). Verify-loop catches: grant source enum=manual; core ships
    NO intents (seed own) + NO 'research' agent (use stewards-explore). chat-post deferred
    (double-fire: don't run OSS --profile personas with live persona keys). **OSS stack left
    RUNNING — hourly tick will digest Meditations next.**
    - **★ AUTONOMOUS TICK CONFIRMED (`7ceb658`):** the hourly schedule fired at 22:00 ON ITS
    OWN + digested Meditations (done 22:08, 11KB) — the #6 self-improvement-loop heartbeat,
    live, on a human shelf. Self-limits: when the shelf empties, book_next→null→read stage
    outputs "SHELF EMPTY" + no-ops (no runaway). tao-te-ching queued for 23:00.
    - **MATERIALIZE WIRED:** book_publish now also enqueues a pending_file_writes row →
    digests are materialize-capable (DB always; disk when /workspace RW or at cutover →
    real study/books/). Proven via `stewards-cli materialize-writes` (wrote self-reliance.md
    8KB + meditations.md 11KB to disk from the queue). OSS /workspace stays RO (safe default).
  - **NEXT per Michael: #4 playlist digester** → #5 cutover → #6 self-improvement loop → #7 fun.
    Details below.
- **★ ROADMAP DETAIL (captured, #3+ NOT yet built):**
  - **Search VERIFIED WORKING in LIVE** (`pg-ai-stewards-dev`): `exa-search` enabled +
    granted to research/study/research-gospel + **real-path test PASSED** (web_search_exa
    "Euclidean algorithm" → Wikipedia article, 4779 chars, no error). Keyless = **Exa
    free/anonymous tier** (no EXA_API_KEY anywhere; that's fine, it works). Old DuckDuckGo
    `search` also still enabled in live (harmless; archived in OSS). His project is unblocked.
  - **Both schedules INTACT:** `ai-news-7am` (research-summary, weekdays 13:00 UTC) +
    `science-news-weekly` (research-write, Mondays 13:00 UTC), enabled, next 2026-06-15 —
    live AND ported to overlay (pe7/pe8). "They are fun, keep them." → they stay.
  - **WANT #1 — YT-playlist digester (most actionable):** poll his "AI research" playlist
    (youtube.com/playlist?list=PLcHf1NPbY2qXi5MkL-BzJb7t4r-m8SIEq) a few times/day → new
    video → transcript → digest → **actionable "what to learn/do" recs**. = the study-pipeline
    spec + scheduler(18) + yt_transcripts(live). **Models: kimi-k2.6 doer, qwen3.7-PLUS
    critic (NOT qwen3.7-max — ~2x cost of k2.6/3.7-plus).** New: kimi-k2.7-coder just dropped.
  - **WANT #2 — model-API examples (→ M3):** opencode zen (FREE models OOTB) + opencode go +
    google gemini key + LM Studio local, as easy import/example pipelines; price-tier import
    for zen/go "as we had them" OR cite source so an AI keeps them current.
  - **WANT #3 (VISION) — hourly self-improvement loop:** agent picks a subject within a sphere
    (AI/physics/startups/book-writing), every hour pulls something of its OWN choice, learns,
    does something interesting, idles. Ref video https://youtu.be/RB8vjn1QPeM . "automate
    something for itself, may or may not be useful for us." = scheduler + study-pipeline +
    agent-chosen subject. NORTH-STAR — spec carefully (watch the video first), don't rush.
- **★ M2 — fetch-md + git UTILITIES SHIPPED 2026-06-13 (OSS `4a31b03`, pushed;
  Michael "Lets do M2").** Ported `cmd/fetch-md-mcp` (fetch_url/fetch_urls/
  extract_links/fetch_url_raw; chromedp js path kept but NO chromium in bridge —
  static fetch works, js:true degrades, documented) + `cmd/git-mcp` (git_* +
  gh_pr/issue; agent/* branch namespace, main/master/release/* refused) into the
  OSS root module (folded, flat package main, `go mod tidy` pulled fetch-md's
  chromedp/html-to-markdown/readability/tabula tree). Seeded `fetch-md` + `git`
  mcp_servers in 05-mcp-bridge (deny-by-default grants stay operator; git reads
  GITHUB_TOKEN at exec). bridge.Dockerfile builds both. Genericized fetch-md UA +
  git co-author default. Dropped archived `web_search (DuckDuckGo)` from the 13
  research prompts. **★ KEY DECISION: web search is NOT core** — the virgin-smoke
  denylists `search` AND `exa-search` as personal (needs an operator API key), so
  web search is BYO → M3 docs. virgin-smoke now asserts the **5-server generic
  core** (fs-read/pg-ai-stewards/fetch-md/git + coder). **VERIFIED:** virgin-smoke
  PASS; `refresh-tools` 5/5 OK (fetch-md 4 tools, git 8 tools live). Gotcha: the
  scratch `pgdata` volume persisted old seeds → needed `down -v` (scratch oss
  project only) for a fresh install to see the new servers. **Task #160 DONE.
  Only M3 (BYO-MCP docs + example web_search_exa overlay) remains in the plan.**
- **★ M1 — CODER-MCP SHIPPED 2026-06-13 (OSS `321176c`+`7897093`, PUSHED to
  public main after Michael's Hinge ② ship nod).** The inert 20-coder
  surface is now alive. coder-mcp folded into the root module (was own module on
  go-sdk v1.6.0→v1.6.1, builds clean). Files: cmd/coder-mcp/{main,tools}.go +
  sandbox/sandbox.go (clean-room; **CODER_REPO_ALLOWLIST now DENY-ALL default**,
  commit author env-configurable); extension/coder-runtime.Dockerfile (hardened
  sandbox, non-root coder uid 1000); bridge.Dockerfile (+coder-mcp +docker-cli/
  git/github-cli); **docker-compose.coder.yaml** (OPT-IN override — default `up`
  stays socket-free); .env.example coder section; **SECURITY.md** (trust model +
  hardening review = the ship-gate doc). **VERIFIED:** coder-mcp builds+vets;
  `coder-mcp -smoke` PASS (sandbox spawn w/ cap-drop=ALL/no-new-privs/mem-cpu-pids
  caps/non-root → Go1.26/Node24/Python3.11+LSPs → write+build → teardown);
  `bridge refresh-tools` = **3/3 OK, coder [OK] 16 tools** (was [FAIL] in M0).
  Hardening confirmed: token never in sandbox (bridge-side one-shot cred helper);
  deny-all allow-list; protected-branch refusal; reaper. **★ HINGE ② CLOSED —
  Michael's ship decisions (2026-06-13):** (1) socket off-by-default public +
  **gitignored `docker-compose.override.yaml` = on for us this machine** (Compose
  auto-merges; verified socket in merged config); (2) egress on-by-default +
  **`CODER_SANDBOX_NETWORK=off` kill-switch** added+documented (forces every
  sandbox `--network=none`; -smoke PASS both modes); (3) coder row stays enabled.
  cv4 minimax-m3 model seed → overlay (still carry-forward). **Task #158 DONE.
  MCP-packaging M2 (fetch-md+git into cmd/ + Exa re-point + archive search-mcp) +
  M3 (BYO-MCP docs) remain.**
- **★ GOSPEL-ENGINE RESOLVER GENERALIZED 2026-06-13 — core `4bb80ab` + overlay
  `90906f7`, both pushed** (Michael: "generalize as much as possible; shouldn't be
  project/workspace aware but configurable to pull external resources"). The
  "resolver" was a whole scripture-citation subsystem in `schema.rs` (the file the
  SQL-file audit missed; unused by core pipelines; overlay already owned the
  consumption). **CORE:** GospelEngineConfig→ResolverConfig + STEWARDS_RESOLVER_URL
  ({ref} template)/TOKEN; resolve_ref config-driven (boot log "resolver url=…" not
  gospel-engine); parse_gospel_links→parse_doc_links (ALL md links, external|doc);
  normalize_book+parse_reference REMOVED; refresh_doc_refs/doc_citations_resolved
  generic; provider→'resolver'; example agents/skills + stray comments genericized
  (scripture-linking→reference-linking, doc_citations kind enum, summarize prompt,
  01-graph prior-art); verify-2-1/2-2 removed. **★ BEHAVIOR CHANGE flagged: core
  import_doc now cites ALL links generically.** Both smokes green (genresolver image,
  tests/virgin-smoke.sql passes); genresolver→pg18 retag (compose default).
  **OVERLAY:** `scripture-resolver.sql` restores the scripture funcs + import_doc/
  refresh/doc_citations_resolved overrides (scripture CITES + verse decomposition);
  doc_citations_resolved keeps core signature (extension-owned, can't DROP/retype →
  `resolved` carries the verse array); STEWARDS_RESOLVER_URL in .env; manifest+
  classification entries; replay-proven (6 funcs, "Mosiah 18:8-9"→2 verses, 'scripture'
  kind). **HARNESS CLEANUP DONE (OSS `b6ec106`, pushed):** verify-1-6-1/loop/4a-steward
  scripture fixtures→water-cycle/web_lookup; verify-3e2-2 mcp_proxy test re-pointed
  gospel-engine-v2/gospel_search→core fs-read/fs_search; init brain smoke Moroni/charity
  →water-cycle (category 'study'→'ideas'; full init runs clean, fts=1); bridge.Dockerfile
  example neutralized. **Extension-wide sweep now scripture-FREE.** Task #159 fully closed.
- **★ M0 — RUNTIME STACK SHIPPED + VIRGIN BOOT PROVEN 2026-06-13 (OSS `8287967`,
  pushed; Michael: "this probably doesn't need [a loop], push through as normal").**
  The OSS repo had no runtime image/compose — only the extension Dockerfile. Added
  `extension/bridge.Dockerfile` + `extension/persona-host.Dockerfile` +
  `extension/bridge-entrypoint.sh` + root `docker-compose.yaml` + `.env.example`.
  **Clean-room single-module win:** no go.work, no sibling-stub COPYs, no personal
  MCP — the bridge image COPYs `go.mod`+`cmd/` and builds 3 binaries (stewards-mcp/
  fs-read-mcp/stewards-cli) in **~6s** (vs the workspace's multi-min go.work build).
  persona-host behind compose `--profile personas` (needs ai-chattermax+key; idles
  without). Core installs via `CREATE EXTENSION` (pg init), so the entrypoint just
  starts the bridge — no core migrations. Ports offset (pg 55434) for side-by-side.
  /workspace mounted RO (autonomous materializer opt-in; the boot warning is the
  safe default, documented in compose). **Virgin boot GREEN** (scratch
  `pg-ai-stewards-oss` project, live untouched, torn down after): CREATE EXTENSION
  → pg_ai_stewards 0.2.0 + pgvector; **4 bgworker dispatchers alive** (recovered from
  the bootstrap-phase "db does not exist" FATALs — transient, matches live); bridge
  connects + `LISTEN`s on stewards_mcp_proxy; **`bridge refresh-tools` spawns the 2
  real stdio MCP servers e2e** — fs-read [OK] 3 tools, pg-ai-stewards [OK] **31 tools
  all doc_*** (no study_* leak), coder [FAIL] = the ONLY failure = expected M1 gap
  (binary not built). **★ TWO CLEAN-ROOM FINDINGS (Michael's call, NOT acted):**
  ① **the Rust core still carries a `gospel-engine` resolver subsystem** —
  `GOSPEL_ENGINE_URL`/`TOKEN` env in `bgworker.rs` (prints `stewards: gospel-engine
  url=…` every boot) + `GospelEngineConfig`/`GOSPEL_ENGINE_CONFIG` OnceLock in
  `providers.rs` (Phase 2.2). Personal-domain name in the public core; the src audit
  missed it. **Design question** (genericize the resolver vs move to overlay), not
  act-and-report. ② `stewards-cli migrate` hardcodes the workspace path
  `<repo-root>/projects/pg-ai-stewards/extension` (`migrate.go:54`) — wrong for OSS
  layout; belongs to the two-tier runner work (M0 doesn't need it — core is
  CREATE EXTENSION). **M1 NEXT = coder-mcp port + hardening review (Hinge ②).**
- **★ MCP PACKAGING PLAN RATIFIED + committed/pushed 2026-06-13 (OSS `f603e34`,
  `.spec/proposals/mcp-packaging.md`).** Where the workspace MCP servers ship
  relative to the substrate, decided on Go-module coupling. **No separate
  `pg-ai-stewards-mcp` repo** (daemon leg already collapsed `cmd/*` into one
  module; a split regresses it). **T1 substrate-intrinsic** (cmd/): stewards-mcp✓
  / fs-read-mcp✓ / persona-host✓ shipped; coder-mcp pulled in M1 behind Hinge ②.
  **T2 generic utilities** (cmd/): ship fetch-md-mcp + git-mcp; **archive
  search-mcp** (verified = the 2026-02-03 DuckDuckGo server, throttle-unreliable,
  predates substrate, NOT a custom substrate search) + **re-point core
  `web_search` tool_def → `web_search_exa`** (remote mcp.exa.ai, operator-keyed,
  no search binary ships). **T3 domain** (gospel/webster/strongs/byu/becoming/
  yt/brain/md) stay own repos — "bring your own MCP," referenced not absorbed.
  **Phased M0** (runtime/bridge Dockerfile + docker-compose.yml — OSS has NONE
  yet, the prerequisite gap; cross-compiles cmd/*-mcp → /usr/local/bin) → **M1**
  coder-mcp port + HARDENING REVIEW = Hinge ② → **M2** fetch/git + Exa re-point +
  archive search → **M3** bring-your-own-MCP docs + example overlay. M1 = the
  coder-wave Go half (task #158); awaiting Michael's go before executing M0.
- **★ CODER WAVE — SQL SURFACE SHIPPED 2026-06-13 (OSS `a943a95`, pushed; Michael: "do the SQL surface first").** `20-coder.sql` consolidates cc2-6/cv2-2/cv3-12/r10/r12: a GENERIC clean-room `dev` agent (the workspace's 17K personal dev/debug prompts stay overlay) + the `coder` MCP server (★ **INERT** — points at /usr/local/bin/coder-mcp, not built yet) + code-write / code-pr (7-stage final clone→plan→plan_review→implement→verify→review→pr, taken from the live final per l13) / code-deploy (prepare = always-escalate Hinge) / subagent-research-codebase pipelines + stage_models + maturity + research_codebase (clean, active) + scoped `dev` coder grants + the read-only research-codebase deny-list (study_*→doc_*). Two GRAFTS onto core finals (not pastes): work_item_advance (08 body + cv6 review + cv11 plan_review loop-backs, maturity hook preserved) + work_item_dispatch_stage (19 r3 body + cv7/cv10 review model-immunity). lib.rs: create_coder requires create_models. Virgin smoke FULLY GREEN incl. both grafts e2e (review REVISE→implement / PASSES→pr; deploy prepare→awaiting_review Hinge; dispatch critic uses input.review_model not the override), deploy escalate-gated, research-codebase read-only (8 denies/0 allows), no token value, repos genericized. **CODER REMAINING = Hinge ②: the coder-mcp Go server extraction (cmd/coder-mcp → OSS module + Dockerfile cross-compile to /usr/local/bin/coder-mcp) + the HARDENING REVIEW** (sandbox isolation, bridge-side token, repo allow-list, resource caps) — the public-ship gate, a fresh focused pass. cv4 minimax-m3 → overlay model seeds. Then the **CUT** (Hinge ①+③; live idle → soak can relax).
- **B6 tests/ + CI SHIPPED + CI GREEN 2026-06-13 (OSS `8509d26`→`9812d3f`, pushed):**
  `tests/virgin-smoke.sql` = ASSERT-based virgin-boot regression gate
  (vector-only / no-pgcrypto / no-AGE; doc_* complete; a representative object per
  subsystem 00-19 + the 4-layer dispatch FINAL; **no operator/personal seeds incl.
  no personal MCP** — only fs-read + pg-ai-stewards core daemons; spine e2e with
  capability-substitution). `.github/workflows/ci.yml` runs it on push/PR
  (extension build+virgin-smoke + go build/vet) — **full run GREEN 4m54s**, actions
  on checkout@v6/setup-go@v6 (Node-24, deprecation resolved). README CI badge;
  `tests/README.md`. **seed_harness genericize VERIFIED** (virgin boot = all-generic
  agents/intents=0/core-MCP-only); **anatomy doc clean**. .gitattributes already eol=lf.
  **B6 cutover-prep DONE this session (workspace `6bdeef9`+`0cb5cd3`):** rename-map
  finalized through B5; **overlay re-author + OVERLAY-REPLAY PROOF GREEN** (35/35
  overlays apply on a virgin core — h1-1/h3-2 scripture_anchor→values_anchor, init-01
  AGE→relational import_workstream, pe7-seed-ai-news-7am filed [the B5/18 orphan];
  the ~15 other study_*-grep overlays apply clean as-is — 'study-write' is a valid
  operator pipeline name, not a renamed-object ref; both scheduled pipelines land;
  harness `parity/overlay-replay.sh`). **★ B6 / CUTOVER-PREP COMPLETE — 20 live↔repo
  mismatches CLASSIFIED, GREEN, ZERO DRIFT** (workspace `9566517`,
  `parity/mismatch-classification.md`; OSS blueprint `b474bb4`). Live
  (`pg-ai-stewards-dev`, read-only) vs rebuilt core+overlay: 101 raw body-diffs →
  30 genuine after normalizing comments/whitespace/renames; ALL accounted —
  deliberate clean-room (AGE→relational, config genericization, consolidation
  finals, doc_* renames, todos lowercase), false-positives (formatting / END vs
  END;), one rebuilt-fixes-live bug (provider_cap_refill RAISE %.2f), and ONE
  deferred-P2 gap (work_item_advance code-pr revise loop → 20-coder). Rebuilt P1 ≡
  live minus deferred P2. bgworker `_kind` enum = deferrable Rust refactor. **ONLY
  Hinge-gated work remains: the CUT** (Hinge ①+③; Michael not using live →
  low-risk, soak can relax) + the **coder wave** 20-coder.sql (Hinge ②; must
  re-add the work_item_advance code-pr arm). Cut-planning: the
  work_item_promote_trigger unwrapped-PERFORM sabbath tension.
- **★ AUTHORING LEG COMPLETE 2026-06-13 — B5 SHIPPED, chain runs 00→19, migration manifest = ZERO migration entries (verify/test harness only).** All 189 historical migrations consolidated into 20 authored subsystem files. B5 commits (all pushed, virgin-smoke green each):
  - **17 (`35d66a6`)** personas — `17-personas.sql`: persona agent + persona-turn pipeline (r7) + lmstudio/gemini example pipelines (r8) + ct2-7c persona/room facets (dispatch_facets/remember/forget FINAL) + persona_outbox + room_say (r16/r20) + room_react (r21). compose_tools('persona')=[room_react,room_say]; **16's on_one_shot persona-% arm auto-verifies a persona-turn (cross-batch proof, on_one_shot NOT re-authored — the B5/17 note honored)**. r18/19 max_tokens→16000 folded; overlay = librarian/codewright/gamemaster room_react grants; persona deny study_*→doc_*.
  - **18 (`9d9a0f4`)** scheduler — `18-scheduler.sql`: cron scheduled_pipelines (pe6 engine + pe7 fire/watchman-tick FINAL). cron parse + e2e dispatch + D-PE4 missed-window all green. ai-news-7am operator seed → overlay.
  - **19 (`addeee8`)** models — `19-models.sql`: model_capability + model_usable + auto-probe (m1/m4/m5/an1) + **work_item_dispatch_stage FINAL** (r3 = J.8.a 4-layer + M.2 capability-substitute + J.11 spend-cap + R.3 max_tokens). Dispatch capability-substitution e2e + max_tokens green. ALL model seeds incl zen1 Claude catalog → overlay; core defaults usable+openai.
  **NEXT = B6** (tests/ re-author + CI day-one + .gitattributes + rename-map.tsv finalize + overlay re-author against doc_*/relational/config-keys + anatomy-doc update) + classify the 20 live↔repo mismatches (verify-suite) + **B5-tail** (seed_harness genericize + bgworker `_kind` enum — schema.rs/Rust-side, NOT authored-SQL). Then the **CUT** (Hinge ① stop live stack + move personas, ③ data-import confirmation) + the **coder wave** `20-coder.sql` (Hinge ② public-ship nod after hardening review).
- **AUTHORING LEG B4/16 SHIPPED 2026-06-13 (OSS `4ba752d`, pushed) — B4 COMPLETE; the consolidated chain runs 00→16:**
  `16-subagents.sql` = sub-agent delegation + the §7.3 self-editable base prompt.
  l9 depth-cap(≤2) + k4 spawn_subagent (**'scripture-study' fallback → config
  default_intent_slug**) + es8 consult + es10 grant + r11 on_one_shot FINAL + ct2-5
  autotag/context_resolve_handle FINAL + ct2-7e (self_prompt_on → propose→critic→ratify
  surface + **compose_tools FINAL**, deferred from 15b). lib.rs: create_subagents
  requires create_context_surface. 7 files retired; manifest 46→39; ext dir 57 .sql;
  secret-scan clean; Go unchanged. Virgin smoke FULLY GREEN (pgcrypto absent; no
  scripture-study hardcode; **depth cap raises@3 / allows≤2**; spawn at root
  origin=agent_planning/cap=500000; **INERT** — propose hidden non-flagged, shown
  w/both-flags, context_* gated; **propose happy-path** session→smoke16-sp→proposal
  pending + prompt-critic work_item; ct2-5 id resolution; es10 22 families minus
  prompt-critic w/ deny-* intact). **Deviations (act+report):** ① **es10 placed BEFORE
  ct2-7e** → prompt-critic (tools-disabled) stays tool-free (★FLAG 20-mismatch: core
  coverage = pipelines-thru-15b, benign superset; live may differ). ② **r11 = on_one_shot
  FINAL here** (manifest line 42, chronological last, true superset of r7/r8) → ★**B5/17
  must NOT re-author on_one_shot — r7/r8's versions are DEAD; 17 only authors the persona
  agent/pipelines/deny-***. ③ context_resolve_handle FINAL = ct2-5 (re-author over 15b's
  ct2-3, +tags fallback). ④ compose_tools FINAL authored here (self_prompt_on first per
  LANGUAGE-sql CREATE-time validation; no later redef — grep-confirmed). Blueprint
  `<pending-16>`→`4ba752d` rides the B5 commit.
  **NEXT = B5** (17-personas: r7/r8/ct2-7c/r16-r21 · 18-scheduler: pe6/pe7 · 19-models:
  j8a/j11/m1/m2/m4/m5/r3/an1/zen1 + dispatch-final j8a+j11 + j7-dispatch + seed_harness
  genericize + bgworker _kind enum), then **B6** (tests/+CI+rename-map finalize+overlay
  re-author). Leg-close: classify the 20 live↔repo mismatches.
- **AUTHORING LEG B4/15b SHIPPED 2026-06-13 (OSS `13cb0f5`, pushed):**
  `15b-context-surface.sql` = the context-engine RUNTIME surface.
  compose_messages FINAL (ct2-7a2, self-contained — ct2-2 base folds
  k2→l13, +§7 self-notes) + CT2 state model(ct2-1)/levers/self-notes(ct2-7a)/
  working tags(ct2-7d, FINAL context_pressure_line w/ tag echo) + judge-brief
  path (es7 minus extract_engrams[15a-owned]: dispatch/render/apply + trigger +
  intercept FINAL + l23 trigger + tool_dispatch_complete_waiting FINAL) +
  intercept_threshold_chars(l22) + read_overflow_raw(l23) + l8 tool_name+wrap +
  l7 suspect-sources + l6 wrappers + deep_research(k5) + chat_post_internal
  FINAL + caps(l30/l31/l32) + 5-arg dry_run(l25) + work_item_cancel cascade(es1).
  24 files retired; manifest 70→46; ext dir 63 .sql; secret-scan clean. Virgin
  smoke FULLY GREEN (pgcrypto ABSENT; 38 kept/0 dead/5 triggers; compose
  system-first; self-note{global}; tag stamp+echo; **judge intercept e2e** —
  62.4k msg→built-in-sha256→overflow parent→judge wq→[JUDGE-PENDING]→K.1 skip);
  GOWORK=off build+vet green. **Deviations (act+report, all in blueprint):**
  ① **es7 sha256 swap** = correctness fix (pgcrypto digest()→built-in sha256();
  ONLY pgcrypto use, dropped; vector-only virgin would've errored at runtime).
  ② **compose_tools FINAL deferred to 16** — true final is ct2-7e (calls
  self_prompt_on, a CREATE-time sql dep born there); schema.rs base carries;
  tool ROWS registered in 15b. ③ OMIT dead judge_templates+render_judge_surface
  + l23 [CORPUS-INDEXED] trigger guard → ★FLAG 20-mismatch (live may carry).
  ④ 3 within-chain finals re-authored (tool_dispatch_complete_waiting 05→es7,
  work_item_cancel 04→es1, chat_post_internal 04→l32). ⑤ doc_* wrapper renames
  (FIRST rename-map rows; Go handlers in lockstep; workspace `45cc5fd`).
  **NEXT = B4/16** (`16-subagents.sql`: k4[slug→config]/l9/es8/es10/r11/ct2-5/
  **ct2-7e — incl compose_tools FINAL + self_prompt_on**), then B5(17-19)/B6.
  Blueprint `<pending-15b>`→`13cb0f5` rides the 16 commit.
- **★ P1 EXTRACTION UNDERWAY (kicked off 2026-06-12, Michael's "Lets kick off P1!"):**
  (1) `github.com/cpuchip/pg-ai-stewards-workspace` (PRIVATE) created at
  `projects/pg-ai-stewards-workspace/` — skeleton + covenant/intent overlay
  copies + 241-file classification (`overlays/classification.tsv`: 191 core /
  17 core-p2 / 27+1 overlay / 5 mixed / 1 scratch) + 33-entry overlay manifest
  + all overlay migrations populated. (2) OSS extension layer extracted
  (`3d8229d`): src/*.rs audited, lib.rs chain reworked (4 seed embeds removed),
  189 core + 5 SPLIT migrations, 193-entry core manifest, bundle = build
  artifact (never checked in). **Build GREEN + virgin CREATE EXTENSION proven**
  (scratch container, 0 workspace seeds leaked) → OSS pushed through journal.
  **COUNCIL (same evening, all ratified):** ct2 RETIRED live · ledger
  leave-and-map · seed pack one-lineage (jumpstart kit canonical) ·
  **doc_*** (study_* tools → doc_*, studies → docs, scripture_anchor →
  values_anchor) · **cutover = FRESH REBUILD** (no shims; selective import;
  live volume archived; rename map at workspace parity/rename-map.tsv).
  **EVENING COUNCIL (all Michael-ratified):** doc_* · fresh-rebuild cutover ·
  six rebuild lessons (early mismatch classification, verify→tests/, _kind
  enum, stewards.config, CI day-one, backup+offsite WAL tiers) ·
  compact_context PULLED IN (hold lifted) · **drop AGE** (relational edges;
  N-depth + BUILDS_ON lineage; fast-at-scale + tenancy conditions; prior art
  verified incl. gospel-engine itself) · **consolidated authored chain**
  ("dave wins"). All in extraction-plan.md.
  **DAEMON LEG SHIPPED (`3561cec`):** five binaries (bridge, stewards-cli,
  persona-host, fs-read-mcp, stewards cockpit) → ONE module
  github.com/cpuchip/pg-ai-stewards; go.work knot dead; build+vet+smoke
  green. Local builds need GOWORK=off (nested clone; strangers unaffected).
  **★ STEWARDSHIP GRANT (Michael, 2026-06-12 night, recorded in
  extraction-plan §Stewardship grant):** full P1-P2 build + migration under
  agent stewardship (act/act+report). Hinge list (still his): ① the CUT
  itself (live stack stop + persona moves) ② coder-mcp public-ship nod
  after hardening review ③ 30-sec data-import confirmation at cut
  (default: corpus/covenant/intent/yt import, histories archive).
  compact_context defaults = his sketch (between-turn, judge-pattern,
  cheap compactor). OSS persona keys: self-service attempt, ping if gated.
  **AUTHORING LEG ACTIVE:** blueprint at
  `pg-ai-stewards-oss/.spec/proposals/authoring-blueprint.md` (consolidation
  map, rename rules, batch plan B1-B6, core=100%-bundle decision).
  **B1a SHIPPED (`3602500`):** 00-config.sql (stewards.config + seeds) +
  01-graph.sql (nodes/edges + recursive-CTE walks) in the bundle chain;
  virgin boot + CYCLE-TERMINATION + bidirectional/lineage walks all proven
  on scratch. rename-map.tsv seeded in workspace repo (parity/).
  **B1b SHIPPED (`ed0da94` + workspace `22e5ea1`) — B1 COMPLETE, AGE IS
  OUT OF THE IMAGE:** create_studies→create_docs (6a + h3-1-docs-half
  ABSORBED into the table: file_path nullable, tags/source_type/
  project_association; kind default 'doc'); 02-workstreams.sql re-authors
  2-6a/b/c relational (context_for = ONE recursive CTE; context_for_hop +
  ensure_studies_graph DELETED; todos parent kinds lowercased
  workstream|doc|todo, 'Phase' retired); resolver/similarity/doc_show
  renamed + relational (doc_similar pure SQL); Dockerfile stage-2 AGE
  build DELETED (runtime = plain pgvector); doc_* swept through ALL chain
  + replay files AND Go daemons (MCP tools study_search/get/similar/
  citations→doc_*; doc_history found by virgin assertion sweep);
  rename-map grew ~27 rows. VERIFIED: virgin CREATE EXTENSION with age
  NOT AVAILABLE (0 in pg_available_extensions), 0 study% functions,
  import/citations/declared-edges/todos/phases/context_for walk/doc_show/
  doc_search/doc_get all smoke green; go build+vet green (GOWORK=off).
  Blueprint gaps fixed: h3-1 mapped (work_items half → 04), 6a removed
  from 04 sources; audit notes in blueprint (parse_gospel_links
  genericization, embed-config at B5, watchman study_id cols at B2,
  l6 wrapper names at B4).
  **B2 IN FLIGHT (2026-06-12 evening):** 03-watchman SHIPPED (`80c9f4c`):
  six files → one, verdicts/findings study_id→doc_id (+related_doc_ids,
  3 index renames, MCP field doc_id), tables born complete,
  estimate_chat_tokens reads config chars_per_token_default, harvest
  trigger e2e on scratch. 04-work-items SHIPPED (`d1d74ef`): ten files →
  one (3c1/3c2/3c2-5/3c3/3c3-1/3c3-3/3c3-5+5e4§1/i1/i2/i5);
  work_item_promote_to_STUDY→_to_DOC, flag-driven
  (pipelines.promote_to_doc — overlay must set it on study-write*),
  last-stage generic, back through import_doc (CITES sync restored);
  chat_post_internal marker fix + tool_defs budget cols +
  agent_tool_perms.source born in schema.rs; i3+h3-followup-2
  REASSIGNED→B3 (08/10 per blueprint); i5 pulled forward; lib.rs had
  NON-LINEAR requires edges (4b, 5a) — sweep for them on every chain
  cut. Full lifecycle smoke green on virgin scratch (template render →
  auto-advance → auto-dispatch → promote w/ graph sync → sabbath gate
  refusal). Gotcha: virgin work_item_create needs a seeded intent
  (hardcoded 'scripture-study' fallback — B3 09-intents wires
  config.default_intent_slug).
  **B2 COMPLETE (2026-06-13 early am):** 05-mcp-bridge `c4ed606`
  (3e2-1/2/3 + h1-5a soft-fail final + h1-7a self-surface seeds w/
  DO NOTHING; waiting_for_tools born in schema.rs work_queue CHECK;
  fan-out completion e2e on scratch). 06-cost `e49ec38` (machinery
  only — ALL operator seeds → workspace overlay
  seed-4a-cost-escalation-models.sql; record_cost_event single 11-arg;
  cost/escalation cols born in 04; j11-dispatch + j12-brainstorm
  halves trimmed in place for B4-14). 07-steward `4d7a715` (steward_tick
  6c-final w/ lessons + atonement-on-quarantine, 6c pulled forward;
  dispatch born 3-arg in 04; provider fallback de-hardcoded to NULL;
  4d stage_models seeds → overlay; live-fire tick smoke green). Final
  sweep: 0 study% fns, 0 study_id cols, AGE absent, Go green. 28
  historical files dead this batch; manifest 189→155 effective.
  LESSON: lib.rs requires-graph is NOT linear — sweep every chain cut
  (4b/5a edges bit once).
  **B3 COMPLETE (2026-06-13, OSS `737443e` + workspace `9a4456d`; root
  lane NOT pushed):** 08-gates/09-intents-covenants/10-sabbath-atonement/
  11-trust/12-council authored; virgin scratch smoke FULLY GREEN (AGE
  absent · 0 study% fns/cols · values_anchor + file_enqueued_at renames
  clean · 15 tables/9 gate_prompts/5 triggers · gate ladder + trust gate
  (trainee surface→journeyman advance) + l28 veto + verify-fail + the
  **08→10 on_maturity_verified materialize path e2e** (sabbath wrapped→
  NOTICE, enqueue_work_item_file real pwid=1, REVIEW-strip extracted body,
  pending_file_writes landed) + sabbath gate refusal + bishop_eligible).
  GOWORK=off go build+vet green. 32 historical files retired; manifest
  155→123. **Deviations (act+report, in blueprint):** apply_gate_decision
  authored ONCE in 11 (its trust SELECT needs trust_scores — a plpgsql
  SELECT-from-later-table is NOT a safe CREATE forward ref; only NEW.<field>
  + wrapped fn-calls are, per the 04 precedent); maybe_enqueue_atonement +
  sabbath/atonement dispatch finals → 10; **h1-0 FULLY consumed at B3**
  (maturity_ladder→08, overrides→10) — drop from B4's 13; 6e SPLIT (lesson
  producer→10, resolution producer→12 — %ROWTYPE/trigger on a not-yet-born
  table fails at CREATE); 5d5 gate tools_disabled finals folded into 08;
  sessions.kind union + gate_prompts CHECK born in schema.rs/08; yaml.rs
  slug-from-YAML(default "default") + values_anchor.
  **★ SURFACED TENSION (Michael's call, NOT fixed):**
  `work_item_promote_trigger` (04, B2) calls work_item_promote_to_doc
  UNWRAPPED → on a sabbath-enabled pipeline a status→completed transition
  ABORTS until sabbath_completed_at is set (the gate RAISEs check_violation).
  Conflates "defer promotion" with "block completion"; likely wants the
  PERFORM wrapped (mirror on_maturity_verified). Faithful to historical
  authoring, not introduced by B3 (smoke confirmed the abort).
  **B4 IN FLIGHT.** **B4/13 SHIPPED 2026-06-13 (OSS `97f42db`):**
  research-write (4-stage, h2 final) / planning (5-stage) / agent-proposal /
  revise-proposal / research-summary seeds + enqueue_proposed_work_items +
  apply_agent_proposal (i7 final, i6 gate folded) + apply_revision. Virgin
  smoke fully green; go build+vet green; 13 files retired, manifest 123→110.
  Deviations: h1-0+h3-1 already consumed (dropped); h-ledger-1 schema_migrations
  table → **00-config** (bundle births it; empty runtime manifest); on_maturity_verified
  NOT touched (08 single final; agent-proposal+fanout branches fold into 08 at
  B4 close — its TRUE final is j7); apply_agent_proposal single i7; dispatch
  tools_disabled deferred to 19; genericized gospel/personal-project text.
  **B4/14 SHIPPED 2026-06-13 (OSS `b1a9b01`):** fan-out machinery + 12-lens
  brainstorm + catalog_default_* helpers + one-shot/child-terminal triggers;
  on_maturity_verified TRUE final (j7) folded into 08 (late-bound forward
  refs to 13/14); dispatch-final (j8a 4-layer + j11 cap) DEFERS to 19 (j8a/j11
  KEPT in manifest); j8b→lens defs; j6 supersedes j2; start_brainstorm
  scripture-study→config. ★ spawn_children = UNION of j3+j4+j8c — **j8c (last
  live redefinition) dropped j3 aggregator + j4 per-child file_destination**
  while adding override propagation; restored here, FLAG for 20-mismatch
  classification. Virgin smoke green; go build+vet green; manifest 110→97.
  **NEXT = B4/15** (context-engine: k1-k9/l1-l32/es1-es9/ct2 — the biggest;
  engrams/rendering/judges/circuit-breakers; may split 15a/b; watch es7
  judge-gate, es1 cancel-cascade, l6 investigate_study→doc_* renames) + B4/16
  (k4/l9/es8/es10/r11/ct2-5/ct2-7e). Then B5(17-19)/B6.
  [archived 14-source detail; see blueprint] j1-j9c incl j8a-dispatch + j11-dispatch-
  gate + j12-brainstorm TRIMMED HALVES (left in place at B2); 15 = k1-k9/
  l1-l32/es1-es9/ct2-1/2/3/7a/7a2/7b/7d (may split 15a/b); 16 = k4/l9/es8/
  es10/r11/ct2-5/ct2-7e. Same loop (sweep lib.rs non-linear requires +
  forward-ref shapes on every cut). Then B5 (17-19 + seed_harness genericize
  + bgworker _kind enum + embed provider/model→config) + B6 (tests/ + CI +
  rename-map finalize + overlay re-author).
  (3) Private manifest REPAIRED (root `e5ccc0c3`): 9 live-applied migrations
  (r11-r17, ct2-5, ct2-7e) restored from ledger order; found the runner is
  LEXICAL + manifest-blind (replayed scratch-ct2-run2 into live 06-10 —
  codewright-ct2 rows; disposition = Michael's call).
- **pg-ai-stewards OSS extraction** (continues the `pg-ai-stewards-oss` lane —
  same session, retitled): spec RATIFIED, Apache-2.0 FINAL (`3c43d4e`).
  **"Anatomy of a Turn" SHIPPED (`0e8c3c9`)** + order-research update +
  2026 regrounding (`1a604af`). **Cutover gate AMENDED (`8662448`,
  ratified 06-12): FULL PARITY before the cut** — coder-mcp + UI become
  pre-cutover (P2 before cut), 20 mismatches + ledger wart now on the
  cutover critical path. Next: P1 extraction (task #151), side-by-side
  compose (`stewards-oss-*`, 55434/8081/8091). Overlay design ratified
  (`0e01a04`): private repo pg-ai-stewards-workspace, created at P1
  kickoff. jumpstart-crossover reflection seeded (`48864a47`, no build).
- **PR.1 SHIPPED + live-verified 2026-06-12** (inbox assignment, Michael's
  "best of your judgement" grant): covenants.extensions catch-all +
  presiding render + Watch echo; reseed through the real path; smoke
  `600f6673` ACK with presiding terms in the dispatched payload. Journal:
  `projects/pg-ai-stewards/.spec/journal/2026-06-12-pr1-covenant-extensions.md`.
  Carry-forwards there: walls-vs-compulsion audit (§V), trailing-reminder
  proposal, verify-suite full run, ledger naming wart.
- **compact_context SEED captured** (Michael's sketch, 2026-06-12 — HOLD,
  no build until council): commissioned-curation side quest; seed at
  `projects/pg-ai-stewards/.spec/proposals/substrate-compact-context-sidequest.md`
  with parked council questions. 2026 research also on hold per Michael.

## Claims
- NONE live. (PR.1 window CLOSED 2026-06-12: pg+bridge rebuilt/restarted,
  watchman resumed, queue clean, live smoke verified. Persona-host
  container was never touched.)
- The general-workspase lane owns the containerized persona-host
  (acknowledged; will not restart it).

## Handoffs / notes
- 2026-06-12: Anatomy doc is public — sibling sessions citing substrate
  architecture can link github.com/cpuchip/pg-ai-stewards/blob/main/docs/anatomy-of-a-turn.md.
- Supersedes lane file `pg-ai-stewards-oss.md` (same session_id; hook
  re-claimed under the new title).
- 2026-06-19 PM: doc-construction rollout R1+foundation+R2a (book-digest) done +
  proven e2e on local. **Presiding accounting:** paused `autonomy_paused`
  ~20:33–20:43 UTC as a recast wall, ran ONE controlled book-digest via
  work_item_create+dispatch (not autonomy-gated), then resumed. All scheduled
  loops back on. OSS commits `d38b336`+`b1fd72a` HELD local (public push gated on
  the batched virgin-smoke); ws `80aea6a` pushed. See journal
  `2026-06-19-doc-construction-r2a.md`. Inbox "digesters read our repos" council
  item still OPEN/unacted (left in inbox).
- 2026-06-19 PM (cont., "keep going with the carry-forwards"): pool junk purged
  (20 docs) + critique tuning + doc_finalize project-fallback + **R2b/c research
  recast (`35`)** + Dockerfile-COPY bug fix + **rebuild+virgin-smoke 20/20 (00→35)
  + clobber PASS** + doc-build-test swept + canonical digests backfilled
  (33 yt→ai, 28 book→books). **OSS PUSHED PUBLIC** `50b5ea5`; ws `558bedf`. Also
  fixed: playlist-digester pools_via_tool/project-tag weren't live (applied).
  NEW finding: research GATHER wedges on gemma w/ big multi-search web ctx
  (unchanged stage, not the recast; reaper self-heals). REMAINING: R3 (reaper
  config + judges→local, next rebuild) + gather-wedge follow.

## 2026-06-22 — local MoE default + substrate-on-llama-chip + stage taxonomy

- Substrate now runs on **llama-chip** (our native Go rig, replaced dockerized FlexLLama).
  `flexllama` provider → `host.docker.internal:8090` (name kept, zero churn; proven e2e).
- **reason/critic → MoE `qwen3.6-35b-a3b`** (was dense 27b; DATA-ONLY change —
  model_capability + role-aliases overlays, no bgworker code; ~4× faster). ingest stays
  gemma-26b-a4b. llama-chip preset = **`dance-moe`** (qwen-35b @120k/slot par2 + gemma
  @213k/slot par2). UI **rig control** on the substrate dashboard (pause/start brain · free GPUs).
- **★ HEADS UP, siblings: llama-chip is a MANUAL host process.** It dies when the launching
  session/terminal closes or on reboot — and the substrate depends on it + `dance-moe` loaded.
  If dispatches start failing (404/timeout → watchman pause), llama-chip is probably down.
  Restart: `cd projects/llama-chip && ./llama-chip.exe serve --config config.json` then UI
  "Start brain". After a rig restart, expect stale-failure watchman pauses (cancel failed
  autonomous runs newer than the last success → consec=0 → resume).
- Fixed **ctx_size = TOTAL** (per-slot = ctx_size/parallel; the port had silently halved gemma).
- **★ STAGE TAXONOMY: the engine is ALREADY data-driven** (no stage-name branching in the
  bgworker) → a new pipeline is just a row. **Vivint reframed = a knowledge assembly line:
  gather → organize(graph) → analyze; gather+organize domain-agnostic ("info brain", shared
  with book/video/article digesters), analyze domain-specific.** Spec + Michael design intent
  at workspace `docs/stage-taxonomy-and-vivint-pipelines.md`. NEXT BUILD: organize-as-a-stage
  keystone (test on a book) → vivint analyze → backfill digesters.
- Journal: `.spec/journal/2026-06-22-llama-chip-substrate-moe-and-vivint-pipelines.md`.

## 2026-06-22 (PM) — llama-chip FEDERATION shipped (Michael: "I'll need it today")

- **llama-chip can now pool GPUs across machines behind one `:8090`** (`cpuchip/llama-chip`
  `181ae8d`, pushed). LOCAL-FIRST peer mesh, no head/worker: each node serves its own GPUs +
  routes a model it doesn't serve to a peer that does; peer models evicted the instant the mesh
  drops. New `internal/fed` (registry + gossip poller) + router fallback + `/api/fed/local`
  gossip + aggregated `/v1/models` + optional bearer middleware (non-loopback only). Oracle:
  `internal/fed`+`internal/router` httptest suites green. **LIVE-PROVEN against the running rig.**
- **★ HEADS UP siblings: I did NOT touch the live rig.** Tested with a SCRATCH binary
  (`llama-chip-fed.exe`, since removed) + a scratch head on **:18090** peering the live `:8090`
  read-only (GET /v1/models + one tiny completion). Live `dance-moe` rig on :8090 confirmed
  healthy + untouched throughout. The production `llama-chip.exe` is UNCHANGED — Michael rebuilds
  + restarts it himself when he adds a `federation` block (a Windows running-exe can't be
  overwritten in place, and restarting the rig = force on the substrate that depends on it).
- **HIS HANDS next (runbook = `projects/llama-chip/docs/federation.md`):** NetBird control plane
  on NOCIX `server.ibeco.me` + enroll home+laptop; install llama-chip + a fresh substrate on the
  laptop (Ubuntu 24, 3500 Ada); migrate vivint to the laptop (enable cron there, disable on home).
- Journal: `.spec/journal/2026-06-22-llama-chip-federation.md`. Memory `project_llama_chip`.

### + the HUB (same day, ratified in council): a shared GPU-pool coordinator at llama.cpuchip.net

- **`cpuchip/llama-chip` `3b82a32`, pushed.** Control plane for the federation: an admin mints
  node/user join tokens + a live roster (who has what model / free GPU); nodes set
  `federation.hub_url`+`hub_token` instead of static peers and still route p2p over the mesh.
  `internal/hub` (token store + roster + server + embedded admin UI), `cmd/llama-hub` (+ Dockerfile
  + compose), `internal/hubclient` (node heartbeat → roster → fed). Oracle + live-binary proven.
- **Council decisions (his):** data plane = **NetBird self-host** (sovereign mesh, p2p, hub carries
  no inference); hosting = **llama.cpuchip.net, tokens-now / Google-later**. Auth primitive = API
  tokens (nodes are headless). Private-intent scoping (vivint → own nodes only) RESERVED, not yet
  enforced — doc says keep private work on an own-nodes-only group meanwhile.
- **HIS HANDS:** deploy llama.cpuchip.net on NOCIX Dokploy (`cmd/llama-hub/docker-compose.yml`,
  `LLAMA_HUB_ADMIN_KEY` secret, domain→8088, `/data` volume); then NetBird mesh. Runbooks
  `docs/hub.md` + `docs/federation.md`.
- **Filed to general-workspace inbox:** shared transactional email/SMS service (pairs with the hub
  for token delivery).
- Journal: `.spec/journal/2026-06-22-llama-chip-hub.md`.
- **★ HUB DEPLOYED + LIVE 2026-06-22: https://llama.cpuchip.net** (Michael granted deploy; wildcard
  DNS + Dokploy GH-app-all-repos). NOCIX Dokploy (`server.ibeco.me`, `DOKPLOY_NOCIX_API_KEY`):
  compose `wuyySSck8j0lbp9MKUd7g` under the **cpuchip.net** project (env `rIQcSedbPGvdfZ1YJoZg-`),
  github source cpuchip/llama-chip@main path `cmd/llama-hub/docker-compose.yml` (githubId reused
  `F3xRIOFkUcjxB6DWVkPKW`), domain→svc `llama-hub`:8088 letsencrypt, autoDeploy ON. Built+healthy in
  ~10s; `/healthz` 200, UI + admin login + mint + 401-inverse all verified live. Admin key set as
  `LLAMA_HUB_ADMIN_KEY` Dokploy env (relayed to Michael; rotate via env+redeploy). Minted a
  `home-4090s` node token (relayed). Join docs kept GENERIC (`<HUB_URL>` placeholder, `docs/joining.md`
  + UI "how to join" panel fills `location.origin`) per his ask. NEXT (his hands): NetBird mesh +
  wire home box's config `federation.hub_url`+node-token.
- **★ home box config.json WIRED + proven** (`172.17.1.228`, gitignored so token local): a scratch
  node registered `home-4090s` in the live hub roster w/ real GPU stats. Work-laptop node token
  minted too (relayed). Joins for real on the next rig restart (see below).
- **★ llama-chip UI STATS added** (`8ab5d4d`, pushed): model table now shows `120k/slot ×2` (not raw
  total); new **Live activity panel** (`/api/live` reads backend `/slots`) = in-flight requests
  (M/N active), per-slot ctx bars, in/out tokens (+cached), tok/s in/out from poll deltas — shows
  ALL traffic incl. the substrate's. Oracle: parseSlots unit test on real probed JSON + headless
  browser render check (0 console errors, screenshot). **One rig restart now bundles: hub-join +
  parallel-visible table + live panel.** qwen confirmed already parallel-2/120k-slot; data says
  120k is right (p99 prompt 56k, max 65k) — don't push to parallel-3. (`07669d5` gitignored stray
  `.playwright-cli/`.)
- **★ COST GAP FOUND + FIXED** (ws `ee23474`, live+overlay): Michael saw deepseek-v4-pro burning
  ~$4/day. Root cause: summarize subagents (subagent-url/doc-summary) configured for qwen3.7-plus,
  which **429'd on its weekly go-quota** → capability fallback chose **deepseek-v4-pro**, which was
  **mispriced $0** ("go subscription; rate unpublished"). **go=PAID, zen=FREE** (Michael's correction)
  → $0 made paid spend INVISIBLE to provider_spend_caps+watchman guard AND made the cost-fallback
  prefer the expensive model (cheapest-looking). FIX #1: priced the 3 unpublished paid-go models
  (deepseek-v4-pro $1.30/$5 [input grounded in the ~$4/day], mimo-v2.5-pro + mimo-v2-omni $0.50/$2 est).
  FIX #2: summarize → `ingest` (local gemma) = free+private+uses idle GPU1. Repricing also redirects
  OTHER qwen3.7-plus stages' fallback to genuinely-cheaper models (qwen3.5-plus/minimax) not deepseek.
  zen reviewed=correct. FLAGGED for Michael: deepseek-v4-flash + mimo-v2.5 "claimed free on the paid go
  side" — confirm. NOT done (offered): provider_spend_caps row for opencode_go.
- **★ MESH LIVE + fermion JOINED 2026-06-22 (Michael stood up `mesh.cpuchip.net` NetBird):** peers
  `fermion 100.110.60.2` (home 2×4090, this box) + `workchip 100.110.207.32` (work laptop). Wired
  fermion: `config.json` node_name→fermion + advertise→mesh IP; **graceful rig restart** (unload→kill→
  rebuild federation binary→serve→reload dance-moe — done while autonomy already paused, low-risk);
  **fermion now in the hub roster serving qwen+gemma over the mesh.** workchip also in roster (client,
  []models). **★ REMAINING (his hands, needs admin):** inbound firewall rule on fermion for workchip→
  fermion routing: `New-NetFirewallRule -DisplayName "llama-chip mesh 8090" -Direction Inbound -Action
  Allow -Protocol TCP -LocalPort 8090 -RemoteAddress 100.64.0.0/10 -Profile Any` (mesh-scoped). **★
  PERSISTENCE:** the rig runs in MY session's background — Michael must relaunch in his own terminal
  for persistence (`./llama-chip.exe serve --config config.json`). **Autonomy STILL PAUSED** (was
  already, pre-existing; left it — offered to investigate why + resume). **★ gemma alias RENAMED
  gemma-12b→gemma-4-26b-a4b** (it's the 26B-A4B MoE; consistent w/ qwen3.6-35b-a3b): llama-chip
  (`4c825a7`) + workspace overlays (`5eb8f1d`) + live substrate (model_aliases ingest / capability /
  pricing / judge_dispatch_model) + rig slot reswap; tests green, zero stale refs.
- **★ linux-port MERGED + federated-Pool UI 2026-06-23:** reviewed+merged `origin/linux-port`
  (`23d76db`) — OS-conditional backend discovery + launch (Windows untouched; Linux: libggml-base.so,
  llama.cpp-linux-x86_64- dir match, LD_LIBRARY_PATH; macOS too), validated e2e on the Ubuntu laptop
  incl. routing a substrate chat to fermion's 4090s over the mesh (`docs/linux.md`). Win+Linux both
  cross-compile, tests green. **+ Pool panel (`c26e47f`):** local loader UI now shows EVERY node's
  GPUs+models (`/api/pool` from the hub roster the node holds; `fed.Roster()` + RosterEntry.GPUs;
  self freshened locally; hidden when solo). Answered Michael's "do local clients see other GPUs"
  (yes now). **NEXT (write half, his go):** remote management — `/api/remote` proxies load/unload/
  apply-profile to a peer over the mesh + per-node UI controls. **★ rig still on the PRE-pool/PRE-linux
  binary** (my earlier surgical restart) — pool panel + linux fixes land on his next relaunch
  (`./llama-chip.exe serve --config config.json` in his own terminal, for persistence).
