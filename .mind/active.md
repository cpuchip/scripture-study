# Active Context

*Updated: 2026-05-14 build mode (autonomous shepherd, all K phases) — **BATCH K SHIPPED — engram-based context disclosure (K.1 through K.7) + K.8 fix.** Michael went on the road mid-K and asked me to shepherd the rest with commits at good boundaries. 8 commits across the day landed all phases + the reaper bump + the J.3 retest validation. **Substrate gained**: messages.engrams jsonb column populated by automatic INSERT-trigger-driven extraction via DeepSeek V4 Flash (1M context, json_object mode); compose_messages rewrites the messages array with head/torso/tail compaction emitting HOT engrams for compressed tool messages while preserving verbatim URLs/quotes/dates/names; expand_message MCP tool lets agents retrieve any tier or raw content on demand; spawn_subagent MCP tool delegates verbose work to child work_items with sync wait + cost cap; deep_research wrapper ships as proof-of-pattern (6 more wrappers deferred but build-ready); injection defense L1 via regex screen + extractor prompt + raw-retrieval gate; reaper bumped 10min→15min for outlier extractions. **K.7 retest validated K's core contribution**: crystal-radio's gather chat went from "failed with 376K-token request" to "succeeded in 38s" after engram-extracting the 426K poison message — the original 262K limit problem K was ratified to solve is FIXED. K.8 then fixed a related compose_messages bug (reasoning_content dropping breaks providers requiring it alongside tool_calls). Full retry-to-verified blocked by SEPARATE provider-quirk class (different gateways have different requirements for reasoning_content/reasoning_details) — provider-aware composition is a carry-forward. **Bugs surfaced + fixed during K** (Anthropic 4.7 stewardship pattern): sessions FK violation (K.1), bgworker response wrapping (K.1), json_schema unavailable on OpenCode Go (K.1, fallback to json_object), engram schema drift items/engrams/bare-array (K.1 normalizer), tail rule re-poisons retry (K.7), reasoning_content dropped on tool_call assistants (K.8). **Bacteriopolis surfaced extra normalizer gap**: model emitted memory_engrams/title/engram field shape — carry-forward. **Day cost**: ~$0.55 total LLM (engram extractions + crystal-radio gather chats). Budget was $5-10; well under. **10 commits today**: K.1 (1bb03e9), reaper (61e1154), K.2 (4beee4d), K.3 (e4a62d1), K.4 (ccee8a1), K.5 (3a7da7b), K.6 (261cd51), K.7 (da91c2b), K.8 (127f74d) + this memory update. **Substrate state**: 117 migrations applied, 0 drift, all green. **What's NEXT (carry-forward)**: provider-aware reasoning_content composition (blocks full J.3 retry-to-verified); engram normalizer accepts more shapes; 6 remaining heavyweight wrappers; bridge-side per-tool injection screen (K.6 ships pure-SQL trigger; bridge wrapping deferred); 14 SC work_items pending ratification (unchanged); yaml.rs gated on 3rd YAML shape (unchanged). Soak resumed at session close.

* Previous: 2026-05-13 plan + ratify mode — **BATCH K RATIFIED, K.1 BUILDING NOW.** After Batch J shipped, J.3's 4-of-6 failures (Moonshot 262K token limit on single 426KB tool result) drove a council on context management. 5 rounds of design discussion produced the ratified Batch K: **engram-based progressive context disclosure**. Three shapes: (1) **REACTIVE engram extraction** — deepseek-v4-flash (1M context, structured output) extracts HOT/MEDIUM/COLD engram array at INSERT time for tool messages >60K chars; compose_messages emits HOT engrams (~1500 tokens) instead of raw. (2) **PROACTIVE sub-agent delegation** — heavyweight tools (deep_research, summarize_url, audit_files, investigate_session, summarize_study, investigate_study, audit_studies) spawn child work_items via qwen3.6-plus; return prose digest to parent. (3) **INJECTION DEFENSE L1** — extractor prompt treats content as data not instructions; structured output includes injection_suspected + evidence; banner in compose_messages when suspected. **Ratified decisions** (20+ across 5 rounds): sync sub-agents; explicit sub-agent triggering; implicit/size-based engram extraction; document-intrinsic engrams; tier sizes HOT 1500 / MEDIUM 500 / COLD 100; multiple engrams per document (jsonb array); deepseek-v4-flash for extraction (corrected my earlier 'qwen3.6-air' invention — it doesn't exist on OpenCode Go; real tiers are GLM-5/5.1, Kimi K2.5/K2.6, MiMo-V2.5/Pro, MiniMax M2.5/M2.7, Qwen3.5/3.6 Plus, DeepSeek V4 Pro/Flash); qwen3.6-plus for sub-agent orchestration; strict structured output; sub-agent depth cap 2; sub-agent failure returns error to parent; web_search trusts provider preprocessing + lightweight regex injection screen; fetch_url full engram pipeline; sub-agent tool subsets enumerated per type. **8 sub-phases** (K.5 split into K.5a external + K.5b internal-study): K.1 schema + extractor → K.2 compose_messages rewrite → K.3 expand_message tool → K.4 spawn_subagent tool → K.5a external heavyweight (4 tools) → K.5b substrate-internal heavyweight (3 tools) → K.6 injection defense L1 + tool capability audit → K.7 replay 4 failed J.3 children. **Estimated cost: $5-10 across the arc** (budget $15). **Three scratch files + one proposal doc**: batch-k-context-management.md (full design, ~860 lines), batch-k-payload-structure.md (LLM payload breakdown), batch-k-research-compaction-and-subagents.md (prior art: Anthropic context editing, LangChain Deep Agents 85%/20K thresholds, MemGPT virtual paging, Hermes head/torso/tail, PicoClaw expandable summaries). Proposal: `projects/pg-ai-stewards/.spec/proposals/substrate-batch-k-engram-context.md` (status: ratified). **v2 deferrals**: graduated resolution under pressure, marked-important anchoring, cross-message engram search (Batch L foundation), injection L3 source blocklist. **Why K matters:** J.3's deliverables waiting on this. Tuesday Science Center day's exhibit retries become feasible. Substrate gains true context discipline — sessions of arbitrary length viable. K.1 building NOW.

* Previous: 2026-05-13 build mode (Batch J shipped) — **BATCH J FULLY SHIPPED + j7 BUG FIX.** Five sub-phases all built today in one continuous session (~7 hrs build + smoke time). 7 SQL migrations applied + UI changes + 4 commits + memory updates. **Latest commit b85d335** ships j7-failed-sibling-also-triggers-aggregator.sql — bug surfaced during J.3 where the LAST child failing (rather than verifying) left the aggregator pending forever because on_maturity_verified early-returns on non-verified maturity. Fix: extracted `check_and_dispatch_fanout_aggregator(parent_id)` helper + added `on_child_status_terminal` trigger that fires on status IN ('failed','cancelled'). Now the chain converges whether the last sibling succeeds or fails. **J.3 final result: 2 of 6 exhibit briefs verified + aggregator wrote 4KB index** — rural-electrification-webster-coop.md (17KB, $0.81, includes Webster Electric 1946 charter + 2010 smart meter + partnership concept note) and symmetry-polyhedra.md (16KB, $0.49, GENUINELY PUBLISHABLE — Marcus Hagler narrative from Versailles MO + Osage/Gasconade River bridges + Klein's 1872 Erlangen Program with arXiv citation + California Math Show 1995 with named directors). 4 children failed due to Moonshot 262K token-limit errors — research-write gather stage with 5 rounds of web search produces too much context on rich topics. Substrate-level constraint, not fan-out primitive issue. **Issues surfaced during J.3** (all carry-forward, none blocking): (1) research-write token-limit failures need substrate-level fix (3-round gather cap or 1M-context model); (2) aggregator README links to failed children when it should gate on maturity=verified; (3) slow `fetch_url_raw` hung the chain for 11 min — mcp_proxy is excluded from periodic reaper by design but consider per-tool bridge-layer timeouts. **Day cost summary: ~$3.10 total** (J.2 smoke $0.05 + J.5 brainstorm $0.09 + J.3 fanout $2.95 + aggregator $0.05). Well under the $15 budget for the arc. **Substrate primitives shipped (6 SQL migrations + j7 bug fix)**: fan-out (decompose-fanout + aggregate-children + spawn_children + 2 on_maturity_verified branches + auto-verify trigger + per-child file_destination + failed-sibling trigger), brainstorm (4 lens agents + 4 lens pipelines + start_brainstorm fn + generalized one-shot auto-verify trigger), UI (status group dropdown + tree render + parent-link badge + open/done virtual statuses). **What this means for Tuesday Science Center day:** brainstorm + fanout primitives are usable end-to-end. `SELECT stewards.start_brainstorm(...)` spins a 4-lens brainstorm; hand-crafted decompose-fanout manifests produce N briefs in parallel. The 2 verified exhibit briefs (rural-electrification, symmetry-polyhedra) are real artifacts the science center can use. Soak resumed (`schedule_enabled = t`).

* Previous: 2026-05-13 build mode (mid-session pre-J.3-complete) — **BATCH J SHIPPED — fan-out + brainstorm primitives + UI hierarchy.** Five sub-phases completed today across four commits. Ratification was 3 AskUserQuestion batches (12 decisions), build was J.2 → J.1 → J.4 → J.5 → J.3 (with J.3 last because the 6 children take longest). **J.2 substrate (commit 377b12f):** `j1-fanout-machinery.sql` + `j2-aggregate-auto-verify.sql` + `j3-spawn-sets-aggregator-destination.sql` — decompose-fanout + aggregate-children pipelines, spawn_children() SQL fn, two new on_maturity_verified branches (spawn-on-parent-verified + aggregator-dispatch-on-siblings-done), aggregator auto-verify trigger. **J.1 UI (commit c6ce6e0):** `?status=open|done` virtual groups in API, status-group dropdown + tree render with indent + expand/collapse + parent-link badge in WorkItems.vue. Default landing filter now 'open'. **J.4 brainstorm (commit f6e81b9):** 4 lens agents (SCAMPER qwen, Six Hats kimi, Crazy 8s qwen, Reverse kimi) + 4 single-stage pipelines + start_brainstorm() SQL fn. Generalized auto-verify trigger to all one-shot pipelines (j6-brainstorm-lens-auto-verify.sql). Brainstorm is implemented as a SPECIAL CASE of decompose-fanout — no new pipeline shape, ~250 lines of SQL. **J.3 substrate enabler (commit 23ccfd0):** `j4-spawn-supports-child-file-destination.sql` adds per-child file_destination from manifest + the science-center exhibits fanout script. **Smokes verified:** J.2 echo-test 2-child fanout end-to-end (~$0.05); J.5 biology-exhibits brainstorm with synthesis aggregator ($0.09, output at `projects/space-center/brainstorm/biology-exhibits-candidates.md` — quality output detected Winogradsky-column convergence across 3 of 4 lenses, surfaced rural-Missouri-maintenance constraint independently from Black-hat and Reverse). **J.3 partial:** 6 science-center exhibit briefs fanned out via research-write; 2 children failed in gather stage with Kimi 262K token-limit errors (research-write 5-round web search produces too much context on rich topics). Remaining 4 children continued; aggregator dispatches when they verify. Final J.3 result: 4-5 exhibit briefs landing at `projects/space-center/exhibits/<slug>.md` + README index; bacteriopolis-winogradsky + crystal-radio missing. ~$2-4 J.3 cost. **Substrate primitives gained today:** fan-out (one binding question → N parallel children → roll-up), brainstorm (special-case fan-out with 4 lens children + synthesis), composed chain (two work_items linked by parent_work_item_id — no third pipeline needed). **Total day's commits:** 6 new SQL files (j1-j6) + UI changes; all migrations recorded in stewards.schema_migrations. **What this enables next:** Tuesday Science Center day — `SELECT stewards.start_brainstorm(...)` to spin a 4-lens brainstorm on any question; hand-craft fanout manifests to produce N briefs in parallel; tree view shows parent → children directly. **What's NEXT (carry-forward):** (1) research-write token-limit failures — substrate-level fix needed (3-round gather cap or 1M-context model); (2) per-lens file_destination so each lens output is its own artifact; (3) local-model brainstorm lens integration; (4) friend's 8-9 brainstorming modes pending Michael's async share; (5) migrate idempotency discipline — always DROP TRIGGER IF EXISTS before CREATE TRIGGER; (6) yaml.rs gated on 3rd YAML shape (unchanged); (7) 14 SC work_items pending ratification (unchanged). Soak still paused; will resume at session close.

* Previous: 2026-05-13 plan + fix mode — **BATCH J RATIFIED + DOCKER COMPOSE PG RESTART POLICY FIXED.** Two distinct pieces of work today. **(288b089) Docker compose fix:** `pg-ai-stewards-dev` exited 06:30 UTC (clean SIGTERM, likely Docker Desktop restart) and stayed down because `pg` service had no `restart` policy (Docker default = `no`) while bridge and ui had `restart: unless-stopped`. Bridge + ui flapped (RestartCount=13) unable to reach pg:5432. Fix: added `restart: unless-stopped` to pg service in `projects/pg-ai-stewards/extension/docker-compose.yaml`. Stack now comes up as a unit on host reboots while still respecting an intentional `docker compose stop pg`. Recovery this session: `docker compose up -d` recreated pg; bridge/ui had stale DNS resolution (`lookup pg on 127.0.0.11:53: no such host`) and needed `docker compose restart bridge ui` to clear it. All three healthy. Soak survived the recreate (`schedule_enabled = t` persisted in pgdata volume). **Batch J ratified:** triggered by work_item 8218aa77 (`everyday-science-to-exhibits`) — `research-write` produced a good survey but couldn't fan out to the 6-field-per-exhibit deliverables Michael asked for. Michael's framing: "we need more legs here" → Batch J adds two new pipeline shapes (legs) + UI affordances. **Three pipeline shapes added:** (1) **Fan-out (`decompose-fanout`):** context_gather → decompose → spawn → aggregate. Spawn = deterministic SQL fn reading manifest; aggregate = event-driven via `on_maturity_verified` (council pattern); children get per-child pipeline assignment in manifest; aggregate output = index always + optional digest via `aggregate_synthesis=true` metadata flag. (2) **Brainstorm:** context_gather → divergent (N parallel lens dispatches) → converge. Lenses stored as `stewards.agents` rows with `role='brainstorm-lens'`; divergent provider mix (some local LM Studio/Ollama, some cloud); start with 4 lenses (SCAMPER, Six Hats, Crazy 8s, reverse-brainstorm). (3) **Composed chain:** just two work_items linked by `parent_work_item_id`. No third pipeline. **12 decisions ratified via 3 AskUserQuestion batches:** A1-A4 (architecture), B1-B4 (brainstorm), C1-C3 + schedule (UI/sequencing). **Sub-phase schedule:** J.2 first (fan-out machinery, building NOW), then J.1 (UI tree + open filter) + J.3 (apply fan-out to 8218aa77 → 6 exhibit briefs) alongside, then J.4 (brainstorm + lens library), then J.5 (compose on new category). Total estimate: ~7 pulses, ~$15 LLM across the arc. **Proposal saved:** `projects/pg-ai-stewards/.spec/proposals/substrate-batch-j-fanout-brainstorm.md` (status: ratified). Soak paused (`schedule_enabled = f`) for J.2 build pulse. **Why this matters:** today's Batch I (yesterday's six pulses) opened the substrate for agents to write *files*; Batch J opens it for agents to spawn *work* — one binding question → many deliverables. First concrete payoff is the science center exhibits library (J.3). Deeper payoff is the primitive itself: fan-out + brainstorm apply to study generation, lesson prep, talk prep, journal aggregation. **What's NEXT:** J.2 build pulse beginning. Carry-forward unchanged: yaml.rs gated on 3rd YAML SHAPE; Projects B deferred; 14 SC work_items pending; comprehensive bgworker SPI audit; materialize-writes /workspace RO. New carry-forward: friend's 8-9 brainstorming modes (ask Michael async; reconcile when received).

* Previous: 2026-05-12 build mode (sixth pulse) — **BATCH I.2 + PHASE A SHIPPED.** Two commits closing the substrate carry-forward. **(fdb80d1) I.2 — agent-proposal HTTP endpoint + UI filter:** POST /api/agent-proposals/create accepts the structured proposal payload directly (sibling to /api/work-items/create); validates source_type/slug/title/body/rationale at boundary; 403 with kimi-trust diagnostic for schema-migration without claude_attested. Creates work_item with pipeline_family='agent-proposal' + origin='agent_proposal' + input.draft={payload}. Optional dispatch=true. UI: origin dropdown extended with agent_proposal value; badge renders '🤖 agent write-back' (emerald) distinct from agent_planning's '✨ proposed' (purple). Smoke: valid → 200; no-attest schema-migration → 403; filter returns row. **(4d8bd37) Phase A — PgTryBuilder wraps + 60s periodic reaper:** Three AskUserQuestion ratifications: (Q1) top-4 high-risk SPI sites wrapped (startup reaper, complete_waiting_tool_dispatches, check_watchman_schedule, check_steward_tick); (Q2) 10min freshness threshold (user note: agent calls can take several minutes); (Q3) reaper action matches startup exactly (synthesize tool_dispatch failures + mark errored). Honest framing surfaced first: PgTryBuilder doesn't reliably catch every longjmp in BGW context (pgrx 0.18 limitation per tools.rs:398-402); real defense remains pre-flight checks (H.1.5a sidestep pattern); these wraps are belt-and-suspenders. Comprehensive pre-flight audit deferred. New run_periodic_reaper() function: leader-only, 60s tick, reaps work_queue rows in_progress >10min, mirrors startup reaper logic, wrapped in PgTryBuilder itself. SMOKE VERIFIED REAL: injected work_queue row 1832 with claimed_at=now()-15min; waited 65s; row reaped with status='error', error='periodic reaper: stale in_progress >10min'; log line 'stewards: periodic reaper reaped 1 stale in_progress row(s)'. Cleaned up. **Today's six pulses:** morning ledger+Projects A (6 commits), midday FK+materialized_at (3), pulse 3 I.1 agent-proposal pipeline (3), pulse 4 I.3 schema-migration+i7 bug fix+yaml.rs correction (3), pulse 5 I.2 endpoint+UI (1), pulse 6 Phase A (1) = **17 commits total**, $0.00 LLM. Substrate transformed across six dimensions: durability, project entity, gate honesty, agent write-back path, agent submission interface, worker resilience. **Full kimi-creates-tables chain (all six layers built today):** POST endpoint → work_item w/ origin → bgworker dispatches (PgTryBuilder protected) → apply_agent_proposal queues body (i7 fix) → materialize-writes + validate-sql → file lands → ledger applies → reaper catches stragglers. **What's NEXT (carry-forward, substantially narrowed):** (1) yaml.rs gated on 3rd YAML SHAPE (not three callers — corrected); (2) Projects B deferred; (3) 14 SC work_items pending ratification; (4) Comprehensive bgworker SPI pre-flight audit (multi-session, low-urgency); (5) materialize-writes /workspace RO bridge ops concern. Soak resumed at session close.

* Previous: 2026-05-12 build mode (4th pulse) — **BATCH I.3 SHIPPED: SCHEMA-MIGRATION + SQL VALIDATION + I.1 BUG FIX.** Three commits this pulse (fourth build pulse today). Closes the kimi-creates-tables-that-survive-restart loop. Five AskUserQuestion ratifications all option-A: (Q1) stewards-cli validate-sql via BEGIN/ROLLBACK against live DB; (Q2) materialized_by='error:syntax:<diag>' on validation failure; (Q3) hard gate via input.draft.claude_attested=true (honor-system for kimi-trust). **(a2fd2e5) i6+i7+validate-sql:** i6 — apply_agent_proposal redefined with schema-migration claude_attested gate; validate stage prompt updated with KIMI-TRUST GATE doc. **i7 (BUG FIX surfaced by I.3 smoke):** I.1 pipeline.file_content_jsonpath='stage_results.validate.output' returned the FULL JSON wrapper, so pending_file_writes.content was the whole JSON not the body — every materialized exhibit/study/note/lesson .md would have been JSON, schema-migration .sql wrapped in JSON. Never reached disk because bridge /workspace is RO. Fix: apply_agent_proposal INSERTs into pending_file_writes directly with body; sets file_enqueued_at so on_maturity_verified enqueue path is no-op. Scoped to agent-proposal. **stewards-cli validate-sql:** new subcommand, BEGIN/ROLLBACK via pgx.Tx, real Postgres parser, exit 0 ok / 1 with diag. **materialize-writes hook:** for .sql files in extension/, validate-sql runs before write; failure records error:syntax in materialized_by; file not written; other rows proceed. Runs in dry-run too. **Smoke verified all 3 paths:** (1) no claude_attested → kimi-trust rejection; (2) attested + valid SQL → body queued (75 bytes vs 359 of old JSON); (3) attested + bad SQL → validate-sql catches "ERROR: syntax error at or near 'TABL'". **yaml.rs CORRECTION:** I.1 journal claim was wrong — three intents using ONE parser is not rule-of-three; rule-of-three needs 3 distinct YAML SHAPES. Refactor stays deferred. Journal + active.md corrected. **Full kimi-creates-tables loop now closed:** agent proposes .sql with claude_attested → validate stage normalizes → human ratifies → apply_agent_proposal queues body → materialize-writes runs validate-sql → file lands → bridge restart → ledger applies. **Substrate state:** 102 migrations in ledger now (i6+i7 new), 7 pipeline families incl. agent-proposal, 5 source_types fully wired (study/lesson/note/exhibit/schema-migration). Cost: **$0.00** LLM. **What's NEXT (carry-forward):** (1) Batch I.2 — HTTP endpoint + origin filter (~1h); (2) yaml.rs refactor — gated on 3rd YAML shape; (3) Phase A pgrx longjmp + reaper (Claude-only); (4) Projects B deferred; (5) 14 SC work_items pending. Soak resumed.

* Previous: 2026-05-12 build mode (3rd pulse) — **BATCH I.1 SHIPPED: AGENT-PROPOSAL PIPELINE.** Three commits this pulse. Re-opens Batch I after morning cancellation per ratification this session. Closes kimi-trust + chicken-and-egg concerns: Opus does substrate-internal work; migration ledger (shipped this morning) is the durable base case so agent-authored .sql survives restart. **Today's three pulses form a coherent arc:** (1) morning: migration ledger + Projects A (durable file mechanism, project entity); (2) midday: FK + materialized_at rename (clean-up, honest names); (3) this pulse: agent-proposal pipeline (agents can now write back through gates). **Ratified scope** via AskUserQuestion: routing through work_items, 5 source_types (study, lesson, note, **exhibit** new — for SC knowledge artifacts, schema-migration Claude-only deferred to I.3), syntax-only SQL validation for I.3, extend Proposed-work panel for UI. **(3a382be)** proposal substrate-batch-i-agent-write-back.md captures scope + phasing + non-goals + stewardship decisions. **(b5cd0e5) i4+i5:** Pipeline `agent-proposal` single-stage validate (qwen3.6-plus, tools off, ~$0.005/call); `apply_agent_proposal(uuid)` SQL fn called from `on_maturity_verified` BEFORE existing enqueue path, reads stage_results.validate.output JSON, INSERTs into studies (kind=source_type) for 4 doc types, sets file_destination dynamically (study→study/<slug>.md, lesson→lessons/<slug>.md, note→becoming/notes/<slug>.md, exhibit→exhibits/<slug>.md, schema-migration→projects/pg-ai-stewards/extension/<slug>.sql), augments frontmatter with origin='agent_proposal' + proposed_by_work_item_id + rationale. New column `agent_proposal_applied_at` (idempotency, mirrors revision_applied_at). on_maturity_verified extended with agent-proposal branch ahead of enqueue. i5 adds 'agent_proposal' to work_items.origin CHECK constraint (surfaced during smoke). **Smoke verified — 3 synthetic + 1 real e2e:** exhibit (SC bias classifier slug → studies row + pending_file_writes + frontmatter origin), study, rejected (validator error in output → apply returns false → clean failure), real e2e committed and cleaned up (pending_file_writes #22 created with 550 bytes content for `exhibits/i4-real-e2e-smoke-marker.md`). **Operational gotcha:** materialize-writes from inside bridge container hits read-only /workspace mount — separate ops concern, existing G.4 machinery handles this on hosts with write access. **Bonus discovery (CORRECTED in I.3 session):** stewards.intents has 3 rows (scripture-study, general-research, planning-partner) — but this does NOT meet yaml.rs rule-of-three. Three intents using ONE parser is one parser doing its job; rule-of-three requires three distinct YAML SHAPES (e.g., agent.yaml, skill.yaml, principles.yaml). yaml.rs refactor stays deferred until a 3rd YAML shape lands. **Substrate state:** 99 migrations in ledger (i4+i5 new), 7 pipeline families (now incl. agent-proposal), 2 projects with FK, studies kind values: study/proposal/journal/doc/phase-doc + new lesson/note/exhibit support. Cost: **$0.00** LLM. **What's NEXT (carry-forward):** (1) Batch I.2 — HTTP endpoint POST /api/agent-proposals/create + origin filter chip on /work-items (~1h); (2) Batch I.3 — schema-migration source_type + SQL syntax validator (Claude-only, ~1.5h); (3) yaml.rs refactor (rule-of-three now met!); (4) Phase A pgrx longjmp catch + 60s reaper; (5) Projects B deferred; (6) 14 SC work_items still pending Michael's ratification. Soak resumed at session close.

* Previous: 2026-05-12 build mode (earlier pulse) — **SUBSTRATE CLEANUP PULSE: FK + materialized_at RENAME.** Two commits. Burned down Lane A from the carry-forward ratification (FK on project_association + materialized_at semantics drift). **(a2ffe8a) i2 — FK constraint:** ALTER TABLE work_items ADD CONSTRAINT work_items_project_association_fkey FOREIGN KEY → projects(slug) ON UPDATE CASCADE ON DELETE RESTRICT. Pre-flight verified zero orphans (27 NULL, 20 'space-center', 5 'pg-ai-stewards'). Defensive DO-block surfaces orphans cleanly if data is dirty elsewhere. Smoke (i2-fk-smoke.sql): bogus UPDATE blocked; NULL accepted; known slug accepted; DELETE on project with work_items RESTRICTed; slug rename CASCADE verified. Second migration through new ledger. **(62dbe1b) i3 — materialized_at rename:** work_items.materialized_at → work_items.file_enqueued_at. The drift: name reads "file materialized to disk" but value was set inside enqueue_work_item_file() at QUEUE time (before any file write). Matching column on pending_file_writes (also materialized_at) is the actual write timestamp set later by stewards-cli — same name, different lifecycles on two tables. Path A (rename) chosen over Path B (band-aid comment fix). Atomic migration: ALTER RENAME + CREATE OR REPLACE enqueue_work_item_file + CREATE OR REPLACE on_maturity_verified (full H.1.6 + H.3-followup-2 logic re-applied with corrected column) + verify DO-block. Paired UI: Go struct MaterializedAt → FileEnqueuedAt + json tag; TS type updated; Vue label "✓ materialized {date}" → "✓ queued {date}" (honest verb). Data preserved (9 rows still populated). E2E: API returns file_enqueued_at; old field absent. **Substrate state:** 97 migrations in ledger now (i1+i2+i3 new-style; 94 backfilled), 7 pipeline families, 2 projects with FK, 20 sabbath lessons. Cost: **$0.00** LLM (substrate + UI only). Discipline confirmed: don't edit applied migration files — references to materialized_at in 6d/h1-6-2/h3-followup-2 left untouched (drift detection would catch edits; CREATE OR REPLACE supersedes). **What's NEXT (carry-forward):** (1) yaml.rs Rust parser refactor (rule-of-three; Claude-only; ~1 session); (2) Phase A pgrx longjmp catch + 60s reaper (Claude-only; ~1 session); (3) Projects B deferred (root_directory column is the hook); (4) 14 SC work_items still pending Michael's ratification — 4 parent plans at completed/review_plan + 14 child planning proposals at pending/context_gather. Soak resumed at session close.

* Previous: 2026-05-12 build mode (earlier) — **MIGRATION LEDGER + PROJECTS A SHIPPED.** Six commits. Closes two concerns: (1) the h3-5 regression class needs tooling not just discipline; (2) projects need to be first-class entities, not freeform strings. Michael ratified: ledger pattern (ibeco.me-style "DB on startup ensures migrations applied") + Projects A (Brain v3 shape B deferred to future + maybe sandboxed git). **(e27bfd2)** proposal substrate-migration-ledger-and-projects.md captures 9-step plan + ratifications + stewardship decisions (up-only, lexical ordering, sha256 drift tracking). **(53268df) migration ledger:** stewards.schema_migrations table + stewards-cli migrate subcommand (--dry-run/--list/--target/--backfill flags). Drift detection: if recorded file's sha256 differs from disk, WARN + skip. 94 existing .sql files backfilled with notes='backfilled'; h-ledger-1 itself recorded manually. 95 total. Re-running migrate: 'substrate is current (95 files; 0 applied; 95 skipped; 0 drift)' — idempotent. **(852f89a)** bridge.Dockerfile COPYs cmd/stewards-cli/ in full (was only go.mod stub), builds it, ships to /usr/local/bin/. New ENTRYPOINT is bridge-entrypoint.sh which runs `stewards-cli migrate --repo-root /workspace` BEFORE `exec stewards-mcp bridge run`. Substrate auto-current on every restart. Failure mode: migrate fails → entrypoint exits non-zero → bridge doesn't start (operator fixes). **(40cfd9b) i1-projects-table:** first new-style migration applied through the ledger end-to-end. stewards.projects (slug PK, name, description, root_directory, archived, timestamps). Backfilled from DISTINCT project_association values: 2 rows (space-center, pg-ai-stewards). NO FK initially — soft reference. **(f69ab45) Projects A backend + UI:** 5 endpoints (/api/projects/list, /get, /create, /update, /archive) with slug regex validation, txn-wrapped partial update, work_item_count subquery. new_work.go gained project_association field. Frontend: new /projects route + Projects.vue (~250 lines: create form, inline edit, archive toggle, work_item_count link), NewWork.vue project picker dropdown with "(no project)" default + "manage ↗" link, App.vue sidebar entry. api.ts: 5 new methods + 2 types. E2E smoke verified live (list shows 2 backfilled projects + counts; create/archive round-trip). **Regression class closed:** the h3-5 "multiple files CREATE OR REPLACE same trigger" pattern is now caught two ways — sha256 drift WARN + up-only semantics make re-runs no-ops. **Substrate state:** 7 pipeline families (study-write, study-write-qwen, echo-test, research-write, planning, revise-proposal — i1 doesn't add a pipeline), 95 migrations in ledger, 2 projects, 20 sabbath lessons. Cost this build pulse: $0.00 LLM (entirely substrate + UI). **What's NEXT (carry-forward):** (1) yaml.rs Rust parser refactor (rule-of-three; Claude-only); (2) Phase A pgrx longjmp catch + 60s reaper; (3) materialized_at semantics; (4) Projects B (deferred); (5) FK harden on work_items.project_association once stable; (6) 14 SC work_items still pending Michael's ratification (plus their grandchildren). Soak still paused — resume when Michael is ready.

* Previous: 2026-05-12 build mode (later) — **THIRD PROPOSAL MODE SHIPPED: DIRECT EDIT + AI-REVISE WITH DIFF/ACCEPT.** Five commits. Michael surfaced the gap right after H.3-followup shipped: "How do I update or edit a proposed work? If I need to tweak it?" Three ratifications via AskUserQuestion (Q1 both direct edit + AI-revise textarea; Q2 new revise-proposal pipeline; Q3 side-by-side diff with Accept/Reject). **(863a085)** proposal edit-proposal-and-ai-revise.md captures architecture + 5 endpoints + non-goals (multi-turn chat deferred to substrate-aware-chat). **(f4485b2)** new pipeline family `revise-proposal` (1 stage, qwen3.6-plus, tools off, structured JSON output, cost_cap_default $0.10, maturity ladder ["raw","verified"]). Stage reads original proposal + parent plan excerpt + user feedback, emits partial JSON revision. New SQL function stewards.apply_revision(uuid) validates schema (slug regex + uniqueness; binding ≥20 chars) and COALESCE-merges only the present revision fields into the original. New column work_items.revision_applied_at timestamp (NULL=pending, set=accepted; rejected reuse status='cancelled'). Stewardship: one new column instead of two. **(8b25c83)** 5 backend endpoints: edit-proposal (direct UPDATE, txn-wrapped, origin=agent_planning guarded), revise-with-feedback (creates+dispatches revise-proposal work_item with parent linkage + cost cap), pending-revisions (lists completed revise work_items + returns raw revision JSON for diff), apply-revision (calls apply_revision SQL fn), reject-revision (UPDATE status=cancelled, family=revise-proposal guarded). **(27d20cb)** WorkItemDetail.vue extends Proposed-work panel with: "Edit fields directly" collapsible 5-field form (slug, binding_question, rationale, pipeline_family, project_association — submits only changed fields); "Revise with AI feedback" collapsible textarea with cost note; Pending revisions section auto-renders w/4s polling (auto-stops when terminal); each diff card shows feedback + cost + side-by-side red/green diff + Accept/Reject buttons. api.ts: 5 new methods + 3 new types. Bundle grew 25.5kB→34.9kB (9kB gzipped). **E2E SMOKE PASSED** on a real SC work_item: dispatched revise against space-center-laptop-webcam-ml-validation with feedback "Scope this tighter — focus only on validating that the laptop webcams are accessible to Chrome under a kiosk-mode user profile; defer ML stack validation to a separate work_item." Revise completed in <30s for **$0.005**. Output JSON: tighter slug (space-center-laptop-kiosk-webcam-validation), focused binding_question, explanatory rationale. Applied via endpoint. Original work_item updated cleanly (slug + binding + rationale all changed). revision_applied_at set. pending-revisions queue empty after apply. Full loop validated. **Substrate state:** 6 pipeline families now (study-write, study-write-qwen, echo-test, research-write, planning, **revise-proposal**), 6 real artifacts on disk, 20 sabbath lessons, 3 live intents. **Three modes for proposed work_items:** (1) Ratify/Dispatch/Cancel for terminal actions; (2) direct edit for fast no-AI tweaks; (3) AI-revise with feedback for "this is close but help me think." Mode 3 is the conversational pattern Michael asked for, scoped to single revision rather than multi-turn. **What's NEXT (carry-forward):** (1) Michael ratifies SC work_items via new buttons — one of them is now tighter scope thanks to this session's revise e2e; (2) yaml.rs Rust parser refactor (rule-of-three still triggered; this session didn't add a 4th intent); (3) Phase A pgrx longjmp catch + 60s reaper; (4) materialized_at naming drift; (5) Multi-turn chat in work_item context deferred to substrate-aware-chat workstream. Soak still paused. Bridge running clean.

* Previous: 2026-05-12 build mode — **H.3-FOLLOWUP SHIPPED + FIRST SUBSTRATE-USE PIVOT TO SCIENCE CENTER.** Seven commits. Three Michael observations after H.3 landed (kimi not trusted for substrate Rust/bg, chicken-and-egg between substrate + Claude, UI ratification gap) reshaped this session: substrate-internal Rust work stays Claude-only; substrate USE pivots to Marsfield. Four ratifications via AskUserQuestion (Q1 internal order file_template→yaml.rs→Phase A; Q2 per-item Ratify button; Q3 cancel 4 Batch I work_items; Q4 SC AI-literacy MVP with $500/5-laptops/ESP32-P4 constraints). **(ac71f83)** proposal substrate-h3-followup-small-items-and-sc-pivot.md captures all ratifications + reasoning + deferred items (yaml.rs + Phase A explicitly named next-session work). **Housekeeping (direct UPDATE):** soak paused; 4 substrate-batch-i-* work_items marked cancelled with quarantine_reason explaining the pivot. **(a6f0878 #1 fs-read SC scope)** UPDATEd mcp_servers.fs-read args to include projects/space-center/*.md + docs/** + .spec/** so context_gather can read Michael's 15+ existing planning notes. Bridge restarted; sandbox log confirmed expanded scope. h3-followup-1-fs-read-sc-scope.sql preserves the change for re-seed. **(a6f0878 #2 render_file_destination)** new SQL function reads pipeline.file_destination_template + substitutes <slug>/<project>/<id>; on_maturity_verified trigger extended to auto-render when file_destination is NULL but template exists. Closes the H.3.6 e2e gap where SQL-bypass work_item_create needed manual UPDATE. UI flow unaffected. **(7841dca UI Ratify)** three new POST endpoints (/api/work-items/{ratify,dispatch,cancel-proposal}) each origin=agent_planning-guarded so the buttons can't be repurposed. WorkItemDetail.vue gains a purple "Proposed work" panel for agent_planning items showing parent back-link + rationale_from_planning + Ratify (maturity raw→researched) + Dispatch + Cancel buttons. State-gated visibility. Panel reloads after each action. **(a1ad6c5 SC dispatch)** binding question: "What is the minimum-viable AI-literacy exhibit for Marsfield in 8 weeks with one staffer, $500 NEW materials + existing 5 Bridge Sim laptops + 10" ESP32-P4 panel? Consult projects/space-center/docs/." cost_cap=$0.75, project_association=space-center. **(ed5f498 SC plan artifact)** pipeline completed at $0.31 — context_gather read EXACTLY the right 8 files (im-seriously-looking-at-what-it-would-take-to-do-a-science/main.md + diy-science-exhibits + marshfield + opening-timeline + financial-model + waveshare-esp32-s3-specs + exhibits/README + bridge-simulator-research). **Plan "Teach the Machine"**: software-first 4-station exhibit using browser-based ML (Teachable Machine + TensorFlow.js offline). Image classification with 3D-printed space props; pose classification with toddler-friendly rocket launch; "Fix It" bias-demo HTML; "Spot It" scavenger hunt. ESP32 panel as wayfinding kiosk with LVGL+SquareLine. Hard $250-$350 target, $500 ceiling. 8-week timeline, single-staffer 10-15hrs/wk. Specific risks: Windows 11 webcam permissions, ESP-IDF v5.4 bleeding-edge, toddler attention loss, single-staffer recovery. Agent caught the Bambu X1C 3D printer from docs + incorporated into plan. **Auto-render fix from h3-followup-2 WORKED** — file_destination rendered to plans/h3-followup-sc-ai-literacy-mvp.md without manual intervention; CLI materialize-writes landed the 5.5KB file at plans/h3-followup-sc-ai-literacy-mvp.md. **5 SC proposed work_items inserted** with origin=agent_planning, project_association=space-center, parent_work_item_id pointing back, each ~2hr scope gated on a real assumption from the plan: space-center-laptop-webcam-ml-validation, space-center-esp32-lvgl-kiosk-flash, space-center-fix-it-bias-page-prototype, space-center-3d-prop-tray-cad-test, space-center-exhibit-material-budget-lock. **Pre-existing naming drift noted:** work_items.materialized_at is set by enqueue_work_item_file at ENQUEUE time, not actual-write time. Confusing but not breaking. Future tweak. **Substrate state:** 5 pipeline families (study-write, study-write-qwen, echo-test, research-write, planning), 6 real artifacts on disk (4 research + 2 plans), 20 sabbath lessons total, 3 live intents. **What's NEXT (carry-forward):** (1) Michael ratifies SC work_items via new UI buttons (recommended start: laptop-webcam-ml-validation — hardware gate); (2) yaml.rs Rust parser refactor (rule-of-three triggered, Claude-only, ~1 session); (3) Phase A pgrx BGW SPI longjmp catch + 60s periodic reaper (Claude-only, ~1 session; H.1.5a soft-fail still stable); (4) work_items.materialized_at semantics fix (small, ~30min); (5) bulk ratify + Proposals tab if queue gets noisy. Soak still paused — Michael may want to resume manually next session OR I can resume at his go-ahead. Bridge running clean.

* Previous: 2026-05-11 build mode (very late, autonomous while Michael slept) — **SUBSTRATE H.3 PLANNING PIPELINE SHIPPED. THE SUBSTRATE HAS HANDS NOW.** Eight commits autonomous overnight. Pipeline: `context_gather → explore → synthesize → propose_work → review_plan`. Produces plan documents + JSON arrays of proposed follow-up work_items, validated by its own review_plan gate, inserted via on_maturity_verified trigger extension. **H.3.1 (bc1c7af)** schema migrations: studies + tags/source_type/project_association (D-H3-B); studies.file_path nullable (open-items §1.1 blocker cleared after months); work_items.origin (D-H7 ratified — human|scheduled|watchman|steward|council|agent_planning) + project_association + parent_work_item_id self-FK. 371 studies backfilled. **H.3.2 (9fb07ba)** planning-partner intent — 5 values (surface assumptions first, ask back on underspecified, small finishable work ≤2hr, one strong plan over five branches, name risks) + 3 non_goals. .spec/intents/planning-partner.yaml is the third intent, **rule-of-three triggered for yaml.rs Rust parser refactor**. **H.3.4 (7b61eb2)** 5-stage planning pipeline. context_gather qwen3.6-plus tools-on no-advance; explore kimi-k2.6 tools-on →researched; synthesize kimi-k2.6 tools-off →planned; propose_work qwen3.6-plus tools-off strict JSON →planned; review_plan qwen3.6-plus tools-off JSON verdict →verified. Q-H3.3 $0.75 cap in metadata. file_content_jsonpath explicitly stage_results.synthesize.output. **H.3.5 (f049686)** enqueue_proposed_work_items SQL function + trigger extension. Parses propose_work output JSON (with markdown fence stripping), validates schema (slug regex ^[a-z0-9-]+$, binding ≥20 chars, rationale ≥10), resolves pipeline_family_hint, inserts each valid item with origin=agent_planning + parent_work_item_id + intent inherited + project inherited unless overridden. Unknown hint → __proposal_only stage awaiting reassignment. Smoke caught real threshold bug (length<10 let "Too short." through); bumped to ≥20. Trigger on_maturity_verified gained a 4th section calling this for planning-family only, BEGIN/EXCEPTION wrapped per D-H6.5 non-throwing discipline. **H.3.6 (a3eaee1) — FIRST REAL E2E** dispatched the binding question "What is the right shipping order for Batch I write-back + yaml.rs refactor + Phase A reaper?" Pipeline ran clean through all 5 stages, **$0.57 ($0.18 under $0.75 cap)**, review_plan returned `pass` verdict with all 5 plan_quality checks=true and zero JSON validation issues. **Trigger fired sabbath dispatch + enqueue_proposed_work_items — 4 proposed work_items now live in stewards.work_items pending Michael's ratification:** `substrate-batch-i-studies-generalization`, `substrate-batch-i-agent-gate-sql`, `substrate-batch-i-agent-proposal-endpoint`, `substrate-batch-i-vue-review-queue` — each ~2hr scoped, all `origin=agent_planning`, all `project_association=pg-ai-stewards` inherited, all `parent_work_item_id` pointing back, all pending at planning/context_gather. **Plan file landed at plans/substrate-next-three.md (8.4KB).** Materialization required one manual step — SQL-bypass work_item_create didn't render file_destination_template (UI-side only per D-G1), so file_destination stayed NULL and trigger skipped enqueue_work_item_file. Manual UPDATE file_destination + enqueue_work_item_file recovered. Future tweak: SQL-side render helper. **H.3.7 (0488279)** UI surface for origin badge. API work_items/list + get return origin + project_association + parent_work_item_id; list endpoint accepts ?origin= and ?project_association= filter params. WorkItems.vue gets origin dropdown filter + purple ✨ badge for agent_planning rows + quiet zinc chip for project_association. Q-H3.2 ratification (inline + badge) live. **Sabbath lesson #19 written**: "definitive shipping sequence that prioritizes Batch I write-back as the compounding 'substrate has hands' milestone, unblocked by pulling the H.3 studies generalization migration forward ahead of full ratification." **THE BRIDGE BUGS FROM EARLIER ARE GONE** — writeError detached-context fix + fs-read walk-allowedFiltered fix held through 8 stage sessions in this e2e without a single recurrence. **What's NEXT (carry-forward):** (1) Michael walks in and ratifies (advance maturity from 'raw') on the 4 proposed work_items he wants to run; (2) yaml.rs Rust parser refactor (rule-of-three triggered; agent's own plan ranks it second); (3) Phase A pgrx longjmp catch (agent's own plan ranks last; H.1.5a soft-fail still stable); (4) Original H.1.5/H.1.6 carry-forwards: YT pipelines, scheduled pipelines, UI intent/covenant authoring, substrate-aware chat. Soak resumed at session end. Bridge running. Substrate quiet.

* Previous: 2026-05-11 build mode (late) — **SUBSTRATE NOW READS ITS OWN STATE. H.1.7 + H.2 + H.3-DESIGN SHIPPED.** Five more commits on top of H.1.5/H.1.6 build the foundation for compounding knowledge: the research agent now consults prior journals/proposals/mind-files/studies before any external search. **H.1.7 (substrate self-awareness):** **(6695cd0)** scope proposal substrate-batch-h-context-gather-and-planning.md — 4 ratifications + H.3 design with 5 §IV.6 open questions for next-session ratification. **(94950d3)** new Go MCP server `projects/pg-ai-stewards/cmd/fs-read-mcp/` with `fs_list/fs_read/fs_search` over a path-scoped repo view (`.spec/journal/*`, `.spec/proposals/*`, `.mind/*`, `docs/**`). Sandbox uses `path.Match` not `filepath.Match` — unit tests caught a real Windows-only sandbox bypass (`filepath.Match` treats `\` as separator, `*` matches across `/`). Bridge.Dockerfile gets the fs-read build + previously-missing `scripts/stewards-ui/go.mod` stub (e99b67b regression). Compose mount: repo root read-only at `/workspace`. **(227fba4)** h1-7a registers fs-read + pg-ai-stewards (re-using stewards-mcp in inbound stdio mode) as bridge-spawnable MCPs. h1-7b grants 7 new tools to research agent (`fs_read/fs_list/fs_search` + `work_item_list/show` + `watchman_pass_show/passes_list`) and rewrites research-write's gather input_template with "CONSULT PRIOR WORK FIRST" + bumps round budget 4→8. **(1f91d41)** validation against D-H1.7-A binding question (substrate-reflective AI-tooling): kimi-k2.6 autonomously read the 11-cycle guide, memory-research-bundle, lightrag-investigation, ai-tools-weekly-2026-05-11, H.1.5/H.1.6 completion journal — exactly the pattern designed for. 4 rounds, 14+ tool calls. **H.2 (context_gather as first stage):** **(545b542)** prepended context_gather to research-write's stages. New pipeline: `context_gather → gather → synthesize → review`. context_gather uses cheaper qwen3.6-plus for structured retrieval; gather stays on kimi-k2.6 for synthesis-shaping external search. Briefing flows via `{{stage_results.context_gather.output}}`. Gather template drops H.1.7 prior-work section + lowers budget 8→5. `pipeline_first_stage_name` reads `stages->0` so prepending auto-makes context_gather the first stage; no new substrate machinery. Validation: qwen3.6-plus correctly issued 3 substrate tool calls (study_search + 2x fs_search) before hitting the bridge bug. **The bridge session-staleness bug (LOAD-BEARING carry-forward):** after ~14 intensive fs-read calls, the bridge's persistent fs-read session is invalidated by 60s call-timeout but the work_queue mcp_proxy row STAYS in_progress instead of flipping to error. Manual queue cleanup unblocked both runs. Fix: bridge CallTool deadline-handler needs to UPDATE work_queue status='error' on timeout — same shape as H.1.5a soft-fail, different code path. Should be short patch in `cmd/stewards-mcp/bridge_run.go`. Not done this session — wanted to ship H.1.7/H.2/H.3-design first per "as much of H as you have clear enough." **H.3 (planning pipeline family) DESIGN ONLY:** scope proposal §IV captures shape (context_gather → explore → synthesize → propose_work → review; dual output plan-file + proposed-work_items; new planning-partner intent; studies table generalization with tags/source_type/project_association). Five open §IV.6 questions await ratification before build: Q-H3.1 propose_work mechanism (JSON / pattern-extract / direct tool); Q-H3.2 UI surface for proposed work_items; Q-H3.3 cost cap for 5-stage pipelines; Q-H3.4 review_plan gate prompt vs reusing review; Q-H3.5 project_association enum vs freeform. **Session cost: <$0.10** (two cancelled validation runs after design proof). **Soak resumed at session end.** **What's NEXT (carry-forward):** (1) bridge deadline-handler fix in bridge_run.go to make fs-read robust under load; (2) H.3 ratification of 5 open §IV.6 questions + build; (3) Batch I — agent write-back rung on trust ladder (agents propose studies/notes/lessons through gate machinery); (4) the original H.1.5/H.1.6 carry-forwards still apply: UI rebuild for NewWork.vue, Phase A pgrx longjmp catch + periodic reaper tick, YT pipelines. **H.1.5 (gap fix):** (8ee83b4) mcp_proxy_enqueue NOTICE+NULL on disabled server instead of RAISE EXCEPTION — sidesteps the pgrx SPI longjmp that was crashing bgworker dispatchers (deeper Phase A fix deferred); (9a62a96) tightened gather input_template with explicit hard stop ("Find 8 strong sources. Then STOP. Max 4 rounds of tool calls"); (bfc9b20+127e856) cost_cap_micro $0.40 + retry on scenario #1 — converged in 4 rounds, $0.26 cost, first real research piece landed at research/ai-tools-weekly-2026-05-11.md (manual materialize). **H.1.6 (substrate completion):** the H.1.4 e2e exposed 3 latent gaps in Phase A/B/D/G — auto_advance pipelines never advanced maturity (gate-only path), sabbath never fired on terminal completion, file materialization was manual-only. Ratified D-H6.1-7 (4 decisions, all per recommendation). **(5f713b6 H.1.6.1)** work_item_advance now reads pipeline_stage_maturity and UPDATEs maturity FORWARD-ONLY (re-running an earlier stage doesn't downgrade high-water mark). **(4f3446d H.1.6.2/3/4)** AFTER UPDATE OF maturity trigger fires sabbath_dispatch + enqueue_work_item_file when transitioning TO verified. New columns: pipelines.auto_materialize_on_verified bool DEFAULT false + work_items.auto_materialize_enabled bool NULL (override pattern mirroring D-H5). Sabbath REMOVED from apply_gate_decision + apply_verify_result (single source of truth at trigger). **(a9a6b5d H.1.6.5)** real e2e on Postgres extension distribution survey: maturity advanced raw→researched→planned→verified through 3 stages, trigger fired both sabbath + auto-mat, lesson 16 written, file materialized at research/pg-ext-distribution-2026-05-11.md after stewards-cli land. 8 chats, $0.19. **(220cf35 NewWork.vue path bug)** physics-news run materialized as research/P.md instead of full slug — root cause: renderTemplate substituted <slug> on first keystroke then the watcher's `.includes('<slug>')` guard stopped firing on subsequent slug edits. Fixed by tracking lastAutoRendered alongside fileDestination so watcher only re-renders when field still matches prior auto-render (user manual edits preserved). UI rebuild needed to deploy. **(c0163cb H.1.6.6)** extract_work_item_file_content now strips leading "REVIEW: <verdict>\n+" sentinel that was leaking from review stage template into 3 real files. regexp_replace, no-op when pattern doesn't match. Trailing "Notes on revisions" footer preserved. **Three real research artifacts live in research/:** ai-tools-weekly-2026-05-11.md (AI tooling roundup, kimi-k2.6 gather/synth + qwen3.6-plus review), pg-ext-distribution-2026-05-11.md (pgrx-relevant — Feng Ruohang Pigsty, EDB extension_control_path lineage, Citus/TimescaleDB/ParadeDB/pgvector 0.8.2 CVE, Wheeler PGConf.dev 2025, CloudNativePG v1.27 OCI ImageVolume), physics-news-20260503-science-center-roundup.md (MIT atom-resolved microscopy → flash-freeze video wall + optical-tweezer beads; MADMAX null result → tunable disk-stack resonator with honest framing; neutron Airy beams → ripple tank; ALICE Pb→Au transmutation → "Philosopher's Stone, 99.999993% c" wall; Princeton chiral KV₃Sb₅ → polarization-discovery station). **Substrate handled real-world flakiness gracefully** during physics-news run — chat 1337 hit an OpenCode Zen HTTP 500, bgworker absorbed it and re-dispatched chat 1338 cleanly, no manual intervention. **Decisions ratified:** D-H6.1-7 all per recommendation (forward-only maturity, single-source sabbath trigger, pipeline+work_item override for auto-mat, fire on maturity→verified, ship origin column in H.3, defer H.5 Bridge Sim NPCs to substrate-aware-chat). Also: search/exa-search/yt MCP servers STAY ENABLED for research agent (no revert to deny-by-default — they're research-domain infrastructure now). **Session cost: ~$1.20** for substantial hardening + 3 real research artifacts. **What's NEXT (carry-forward):** UI rebuild to deploy NewWork.vue fix (`docker compose build ui && up -d ui`); Phase A deeper fix on pgrx BGW SPI longjmp + 60s reaper tick (deferred); H.2 YouTube pipelines (same skeleton, swap tool grants to yt-*); first real pre-commit-hook materialization; the three larger proposals still on runway (UI intent/covenant authoring, substrate-aware chat, scheduled pipelines for AI news). Soak running, bridge running.

* Previous: 2026-05-11 build mode — **SUBSTRATE H.1 + H.1.5 + H.1.6 COMPLETE. AUTONOMOUS RESEARCH PRODUCTION VALIDATED ON 3 REAL PIECES.** Twelve commits closed the gap-fix + substrate-completion-pass arc (see journal 2026-05-11-substrate-h-1-5-1-6-completion.md). Three real research artifacts live in research/. **Earlier 2026-05-11:** Batch H.1 shipped (with caveats). First non-gospel pipeline live; first real e2e surfaced 2 real issues. Same session as Batch G ship + Batch H planning + ratification. Five commits land H.1: **(7d4e89f) H.1.0** — substrate primitives per D-H2 + D-H5. `pipelines.maturity_ladder jsonb NOT NULL DEFAULT '["raw","researched","planned","specced","executing","verified"]'` (additive; no code refactor needed — rung names aren't hardcoded anywhere). `work_items.sabbath_enabled + atonement_enabled boolean NULL` with COALESCE(work_item, pipeline) override resolution. Smoke caught one real bug: sabbath/atonement gates AND dispatches both had independent pipeline checks, so override needed propagating to both. Fixed in same commit. **(e5f9181) H.1.1** — general-research intent seeded via direct SQL (Rust YAML parser at src/yaml.rs hardcodes slug="scripture-study" and reads old shape; .spec/intents/general-research.yaml documents new shape, will become live source on H.3's rule-of-three Rust refactor). scripture_anchor IS NULL correctly classifies it as low-stakes per D-F2. **(3605e3f) H.1.2 + H.1.3** — research-write pipeline + tools_disabled wiring. Three stages mirroring study-write's shape with research-flavored input_templates: gather (kimi-k2.6, tools enabled, gather 6-12 credible sources), synthesize (kimi-k2.6, tools enabled lightly, draft from sources brief), review (qwen3.6-plus, tools_disabled=true, structured pass/revise against credibility + recency + binding question + honest uncertainty). First gospel-shape-vs-creation-shape fork: research-write skips the "executing" maturity rung because synthesize IS the draft (no draft+execute distinction). work_item_dispatch_stage extended to forward stage.tools_disabled into payload (one COALESCE line). **(bfc9b20) H.1.4 fixtures** — first real run (scenario #1: "What shipped in AI tooling this week"). **TWO REAL FINDINGS** surfaced from the e2e: **(1) substrate bug** — bgworker dispatcher #3 exited with code 1 mid tool_dispatch when mcp_proxy_enqueue RAISE EXCEPTION'd on disabled MCP servers (search + exa-search + yt enabled=false per deny-by-default). The pgrx SPI error path doesn't catch the longjmp cleanly. Replacement worker came up but didn't run reaper (only runs at first startup), so the in_progress row stayed stuck for 5+ minutes. Quick fix: enabled the 3 servers, retry ran cleanly through 4 mcp_proxy fan-outs per round. Real fix needed: mcp_proxy_enqueue should RAISE NOTICE + RETURN NULL instead of RAISE EXCEPTION (or harden the BGW SPI longjmp catch), AND reaper should run periodically (60s tick) not just at startup. **(2) tuning gap** — after the bug fix, retry ran 9 chat rounds + 30+ tool calls + **$0.42** without converging on a sources brief. kimi-k2.6 with tools enabled + intent values (cross-reference, recency-matters) appears to internalize those as license to keep searching past the "6-12 sources" prompt guidance. Halted manually. **Fix options for H.1.5: (A)** tighten input_template with explicit hard stop, **(B)** add cost_cap_micro to research-write pipeline (e.g., $0.30), **(C)** new substrate primitive — max_tool_rounds per stage, **(D)** switch gather to qwen3.6-plus. Lean A+B for H.1.5 before any further e2e runs. **What's validated:** dispatch + tool fan-out + intent steering + tools_disabled propagation + cost tracking + file_destination template setup. The substrate machinery works. **What's NOT validated:** auto-advance through synthesize/review, sabbath on verified piece, file materialization on real content, whether the pipeline produces a *useful* research piece (the H.1.5 work). **Session cost: ~$0.44** (mostly H.1.4 runaway). **Carry-forward to next session:** fix substrate bug, apply tuning, retry e2e, then H.2 (YouTube pipelines). Soak still running. Bridge running. Three MCP servers enabled mid-session (search, exa-search, yt) — decision deferred whether to revert to deny-by-default or keep enabled for research-domain work. **H.1 build itself: shipped. First-real-run: blocked on the two carry-forward items.**

* Previous: 2026-05-11 build mode — **BATCH G SHIPPED. SUBSTRATE NOW LANDS IN REAL FILES.** Eight commits in one session closed the file-write bridge between Postgres-as-substrate and the repo filesystem. **G.1 (9f5ce97)** dropped `studies.file_path NOT NULL` — studies can exist with file_path NULL and materialize later (or never). **G.2 (9b400bf)** swapped `retry_guidance` for `retry_guidance_with_lessons` in steward_tick — retries now pull the last 3 ratified lessons for (pipeline, stage) into prompt context, closing the Phase E lesson-loop. **G.3 (1ab531a)** wired `maybe_enqueue_atonement` into the cost-cap quarantine path — atonement no longer requires a human to remember to trigger it (surfaced follow-up: failure_count_limit quarantine doesn't currently fire atonement, left for Phase 4a follow-up). **G.4.1 (a4dae0a)** schema — pipelines.file_destination_template + file_content_jsonpath, work_items.file_destination + materialized_at, stewards.pending_file_writes table; enqueue_work_item_file returns NULL when file_destination is NULL (DB-only is the default). Seeded study-write + study-write-qwen with `'study/substrate--<slug>.md'`. **G.4.2 (89470ad)** `stewards-cli materialize-writes` — path-escape protection (rejects `/`, `..`, absolutes); create mode refuses overwrite; append mode creates if missing; for source_kind='work_item' also UPDATEs studies.file_path so the path stays consistent. **G.4.3 (2312be1)** pre-commit hook `materialize_writes()` function — locates binary at projects/pg-ai-stewards/bin/stewards-cli.exe → bin/stewards-cli → PATH; quick count check before invoking CLI. bin/*.exe gitignored so hook ships but binary is built locally. **G.4.4 (6bd41ca)** UI — /api/pipelines/list, /api/work-items/set-file-destination, /api/work-items/materialize-file endpoints; NewWork.vue gets dynamic pipeline dropdown (replaces old hardcoded list) + writeFile checkbox + fileDestination input that prefills from pipeline template with `<slug>` substitution; WorkItemDetail.vue gets file destination panel with edit mode + "Use pipeline default" + "Materialize now" button. **G.4.5 (a26b63b)** lesson + resolution producer hooks — two AFTER UPDATE OF promoted_to triggers fire enqueue_lesson_file / enqueue_resolution_file; lessons → append mode + dated header, resolutions under .mind/ → append, under study/ → create with frontmatter. Trigger bodies wrap producer calls in EXCEPTION → NOTICE so the original UPDATE always succeeds (file-write failure can't block ratification). Mid-G.4, Michael's `flag_when_wrong` intervention reshaped the design from study-specific shortcut into three-layer generalized pattern (pipelines suggest, work_items decide or defer, humans materialize on demand) — recording the pattern: when a "first time substrate gets X capability" question comes up, ask whether the capability is generalizable, configurable, and settable post-hoc BEFORE building the specific case. Substrate proposal at full-agentic-substrate.md is fully reality. **Cost: ~$0** — entirely smoke-test and schema work. **Next moves are about USE, not BUILD** — first real materialization run, UI surface check, and the three new proposals Michael added (more pipelines: research/yt-gospel/yt-secular/scheduled news; UI authoring for intents+covenants; substrate-aware chat with read-only/write modes — all three unblocked by Batch G). Soak re-enabled, bridge restarted at session end.

* Previous: 2026-05-11 build mode — **PHASE F SHIPPED. SUBSTRATE FEATURE-COMPLETE THROUGH ALL SIX PHASES.** Seven Phase F commits closed the agentic substrate at the gospel framework's culmination — Zion, multi-agent council. **F.1 (19f7cfa)** schema — stewards.councils + council_members + resolutions; one_active_council partial unique index enforces D-F1; sessions_kind_check extended for 'council'. **F.2 (cc5f56f)** three council role templates seeded (proposer/critic/synthesizer) — critic template explicitly cites the covenant's surface_tensions commitment ("your function is the council's check, not its echo"); convene_council validates intent, members shape (2-5), D-F1, dispatches each member in parallel with role-specific prompt + payload markers. Synthesizer member tools_disabled=true; proposer/critic tools enabled. **F.3 (98c987f)** synthesize_council fires when all members responded, formats their responses as ### PROPOSER / --- / ### CRITIC blocks; apply_synthesize_result stores draft resolution + transitions to awaiting_bishop; resolve_council handles accept/revise/dissolve with optional D-F3 promotion (study/<id>.md or .mind/decisions.md). **F.4 (8e3d232)** bgworker auto-fire for two new markers (_council_member, _council_synthesize). Council member chats DON'T have _work_item_id so the council branch processes BEFORE the wi_opt check. Member completion → UPDATE council_members + count remaining + auto-fire synthesize when all done. Synthesize completion → apply_synthesize_result. Bgworker now handles 7 marker variants. **F.5 (22327fd)** bishop_eligible (humans always; agents need master-tier on at least one (agent, pipeline, model) cell AND the intent must be low-stakes per the 2026-05-11 ratification — scripture_anchor IS NULL AND values_hierarchy lacks doctrinal/spiritual/discernment); suggest_councils scans ratified lessons for clusters by (pipeline, stage) with default min_lessons=5 + heuristic dedupe. **F.6 (d36f10d)** five backend endpoints — /api/councils/list /get (with members + resolution joined) /convene /resolve /suggestions. **F.7 (885edcb)** Vue surfaces — /councils with active-council badge + watchman-suggested banner + 2-5 member convene modal; /councils/:id is the room — auto-refreshing every 5s while in flight, role-tinted member cards (proposer emerald, critic amber, synthesizer purple), synthesizer's draft resolution panel, bishop's accept/revise/dissolve form with destination dropdown. **D-F1 enforcement verified live during F.3 smoke:** synthetic council resolved cleanly; second council allowed (resolved excluded from index); third active council correctly rejected with "duplicate key value violates unique constraint one_active_council". **D-F2 / bishop_eligible verified:** scripture-study intent classified as high-stakes (trust-the-discernment value matched the regex) → master-tier agent correctly returned false; humans always pass. F2 future evolution path (council_authority as separate trust dimension; debug agent as candidate first cultivator per Michael's 2026-05-11 nuance) documented in the function comment. **Cost: $0** for the session — all smoke synthetic, no LLM dispatches yet (the first real council convene + run end-to-end is for next session). **Substrate feature-complete through all 6 phases of the agentic creation cycle:** Watch→Diagnose→Act→Account (A) + maturity ladder + gates (B) + intent/covenant first-class (C) + post-completion ritual (D) + trust ladder + line-upon-line (E) + multi-agent council (F). All operational on dev. The proposal at full-agentic-substrate.md is now reality. Soak re-enabled, bridge restarted at session end. **Next moves are about USE, not BUILD** — convene a real council on a real intent, run a real study through the full ladder with intent + covenant + sabbath + atonement, watch the trust matrix populate, see whether the substrate's value proposition holds in practice.

* Previous: 2026-05-11 build mode — **Phase E SHIPPED end-to-end on dev in one session (7 commits). Trust ladder + line-upon-line operational.** **E.1 (25f8599)** schema — `stewards.trust_scores` keyed (agent_family, pipeline_family, model) per the 2026-05-11 keying ratification; `trust_transitions` audit ledger; `gate_overrides` (FK to gate_decisions) for human disagreement records; `trust_thresholds` config seeded with 5/15 promotion rules. **E.2 (06efcb5)** five SQL functions: trust_record_success / _failure / _override (counter helpers that auto-call evaluate_trust); evaluate_trust applies promotion (5 successes 0 overrides → journeyman; 15 more clean → master) + demotion (counter-driven: human_overrides exceeds the snapshot stored in metrics jsonb of the most recent promotion → drop one level); trust_adjust for manual changes with required ≥10-char justification (D-E2). Smoke: 5 successes promoted trainee→journeyman; 1 override demoted back to trainee; manual master with justification worked; short justification correctly rejected. **E.3 (d0a0b92)** apply_gate_decision extended with trust gate — work_item_stage_actor helper resolves (agent_family, model) from pipeline_stage_lookup honoring model_override; on action='advance', if no trust row OR trainee → surface (status='awaiting_review'), maturity unchanged; on real advance to verified → trust_record_success fires alongside sabbath_dispatch. Smoke: trainee gate correctly redirected advance to surface. **E.4 (c7e6404)** retry_guidance_with_lessons — wraps Phase A's retry_guidance, appends "Recent lessons from this pipeline + stage:" section pulling last 3 from lessons_recent_ratified view. Stage-specific keying. Smoke: empty pool returns base text; ratified lesson appears in retry context. **E.5 (b5ecd25)** apply_gate_override atomic flow — INSERT gate_overrides row (≥10 char justification + non-self-override required); trust_record_override on the actor cell (auto-demotes per D-E3); re-apply gate_decision with new action via apply_gate_decision (synthetic decision jsonb prepends "[human override by X]" to reasoning for audit). Smoke: decision #8 advance → revise; trust row created with overrides=1 trainee; short justification rejected. **E.6 (422b1dd)** backend api/trust.go — four endpoints: /api/trust/scores (full matrix), /api/trust/transitions (audit log with optional cell filter), POST /api/trust/adjust, POST /api/gate-overrides/apply. **E.7 (2747fb3)** Vue surfaces — Trust.vue at /trust shows matrix grouped by pipeline (rows = agent×model with tier-tinted level badges + color-coded counters), recent transitions ledger with from→to arrow + auto/manual badge + justification, per-cell adjust modal (D-E2 ≥10 char enforcement); WorkItemDetail gate_decisions panel gets per-row "Override gate decision…" button + modal with new_action select + required justification + auto-reload. Router + App.vue nav extended. **No end-to-end LLM cost this session** — all smoke tests synthetic (counters + apply functions + UI flow). Trust state lives entirely in the schema layer and is exercised by future work_items as they pass through gates. **Stewardship moment caught mid-build (E.2):** initial demotion check queried gate_overrides table (empty) instead of using the just-incremented counter — fixed by switching to a counter-driven heuristic (compare current human_overrides against the snapshot stored in metrics jsonb of the most recent promotion transition). Cleaner and works whether overrides come through gate_overrides INSERT or direct trust_record_override. **Substrate now feature-complete through Phase E.** Watch→Diagnose→Act→Account (A) + maturity ladder (B) + intent/covenant first-class (C) + post-completion ritual (D) + trust ladder + line-upon-line (E) all operational on dev. Soak re-enabled, bridge restarted at session end. **Phase F (Council) is next** — the substrate's culmination, when Michael's ready.

* Previous: 2026-05-11 build mode — **Phase D SHIPPED end-to-end on dev in one session (7 commits + Lesson #3 fix). Atonement + Sabbath + Consecration are operational.** **D.1 (2922551)** schema — `pipelines.sabbath_enabled` + `atonement_enabled` (study-write + study-write-qwen seeded sabbath ON; atonement opt-in only); `stewards.lessons` audit ledger mirroring gate_decisions shape (kind = principle | decision | lesson | sabbath_reflection); `lessons_recent_ratified` view for Phase E retry composer; `sessions_kind_check` extended for 'sabbath' + 'atonement'; `work_items.sabbath_completed_at` column. **D.2 (3410796)** Sabbath: gate_prompts.sabbath template (JSON {reflection, carry_forward, surprise}, tools-off), sabbath_dispatch validates pipeline.sabbath_enabled then enqueues with `_sabbath=true` + `tools_disabled=true`; apply_sabbath_result writes lesson row + timestamps work_item. **D.3 (50f0379)** Atonement: gate_prompts.atonement template (JSON {principles_to_record, decisions, lessons}, tools-off, "be sparse"), atonement_dispatch includes last 20 steward_actions in prompt context; apply_atonement_result writes one lesson row PER ITEM across the 3 arrays, all unratified (D-D3). **D.4 (2e642cd)** bgworker auto-fire — two new markers `_sabbath` + `_atonement` join the existing 3-marker switch; mirrors Phase 5b's gate auto-fire pattern. Bgworker now handles 5 marker variants total. **D.5 (7f054e0)** sabbath promotion gate + triggers — `work_item_promote_to_study` raises `check_violation` if pipeline.sabbath_enabled and sabbath_completed_at IS NULL; `apply_gate_decision` (advance to verified) + `apply_verify_result` (all_passed=true) both auto-fire `sabbath_dispatch` for sabbath_enabled pipelines (errors swallowed via NOTICE so the parent decision still applies); new `maybe_enqueue_atonement(uuid)` helper for the steward quarantine path. **D.6 (143ad76)** backend api/lessons.go — three endpoints: `/api/lessons/list` (kind + ratified filters), `/api/lessons/ratify` (POST sets ratified_at + by + optional promoted_to), `/api/sabbath/list` (chronological reflection list). **D.7 (56f859b)** Vue surfaces — `/sabbath` route shows reflection cards with colored carry_forward (emerald) and surprise (amber) sections; `/lessons` route groups by kind with kind-tinted badges, Approve / Approve & promote → .mind/principles.md / Approve & promote → .mind/decisions.md buttons per row. Router + App.vue nav extended. **End-to-end live verified during D.4:** sabbath_dispatch on a real work_item, qwen3.6-plus completed in ~22s single chat round (tools_disabled honored), bgworker auto-fired apply_sabbath_result, stewards.lessons row #7 written with full {reflection, carry_forward, surprise}, work_items.sabbath_completed_at timestamped. Cost ~$0.005. The model's reflection was genuinely useful — correctly diagnosed the failed-outline state from the C.6 test ("missing context parameter silently derailed the entire outline stage"). **Lesson #3 fix (9c1ae8d) shipped before Phase D**: scripts/bump-extension.sh extracts pgrx CREATE FUNCTION blocks from bundled SQL, rewrites as CREATE OR REPLACE with MODULE_PATHNAME → $libdir/pg_ai_stewards, applies in BEGIN/COMMIT. PostToolUse hook in .claude/settings.json auto-runs after `docker compose build pg` from the extension dir. Hook fired correctly during D.4's pg rebuild — 7 functions refreshed cleanly. **Substrate now feature-complete through Phase D.** Watch→Diagnose→Act→Account (A) + maturity ladder (B) + intent/covenant first-class (C) + post-completion ritual (D) all autonomous on dev. Soak re-enabled, bridge restarted at session end. **Phase E is next** — trust ladder + line upon line.

* Previous: 2026-05-11 build mode — **Phase C SHIPPED end-to-end on dev in one session (8 commits, 1 build session). Intent + covenant are first-class substrate state.** Eight commits in order: **C.1 (f3ab677)** schema — stewards.intents (slug-keyed) + stewards.covenants (scope-keyed with active partial unique index) + work_items.intent_id FK (nullable initially). **C.2 (c47d6a5)** YAML parser helper in new src/yaml.rs (serde_yaml + sha2): pg_extern parse_yaml_intent + parse_yaml_covenant + yaml_sha256; SQL composers seed_intents_from_yaml + seed_covenant_from_yaml (idempotent via sha-comparison). **C.3 (e9e0cb9)** git pre-commit hook at scripts/git-hooks/pre-commit re-seeds substrate when YAMLs change; skips gracefully if container down. **C.4 (12ae5ed)** compose_system_prompt extension prepends === Active Covenant === + === Intent === blocks before the agent prompt; sessions without work_item get covenant only. **C.5 (e0efdb7)** backfilled 17 existing work_items with default scripture-study intent + ALTER COLUMN SET NOT NULL; stewardship moment caught: extended work_item_create signature with optional p_intent_id (default NULL → resolves to scripture-study) so legacy callers (watchman) keep working. **C.6 (c7f5253)** tools_disabled flag — bgworker strips `tools` from body when payload.tools_disabled=true; revised gate_prompts.evaluate references intent + covenant; new gate_prompts.covenant_check template; evaluate_gate + generate_scenarios + verify_work_item all set tools_disabled=true. **End-to-end verified:** previous gate-eval cost 11 chats (~$0.04) → new tools-off gate-eval cost 1 chat ($0.0056) → **~7x cost reduction, slightly better than the 5x estimate**. Auto-fire still works (action='surface' applied; gate_decisions row #7 written). **C.7 (0f54bbe)** backend api/intents.go (list/get/create-inline) + api/covenants.go (active/list); both wired into Register. **C.8 (3082332)** Vue surfaces — NewWork.vue gains required intent picker + inline create-new modal; Intents.vue list view with expandable detail; Covenants.vue side-by-side commitments + when_broken/recovery/council_moment cards; router + App.vue nav extended. **Lesson #3 (extension-version skipped CREATE EXTENSION) bit again twice this session:** (1) after C.2 image rebuild the new pg_extern functions weren't auto-installed; manually CREATE FUNCTION'd them pointing at $libdir/pg_ai_stewards. (2) Same for any subsequent rebuild. Per the substrate's standard pattern, live-migration via docker cp + psql is the right move for dev iteration; fresh containers will get everything via the auto-generated bundled SQL. Soak re-enabled, bridge restarted at session end. **Phase D is next** when Michael wants to keep going.

* Previous: 2026-05-11 planning mode — **Phases C–F re-validated and sub-specced. Build-ready for next code session.** Walked all 22 §VI ratifications phase-by-phase against A+B lived experience. Hybrid revise approach ratified (revise #1 same model + feedback prepended; revise #2 escalates + keeps feedback; cap at 2 → surface). 13 amendments to §VI captured: D-C4 tools-disabled, sabbath blocks promotion, atonement tools-off, lessons mirror gate_decisions, trust keying gains model dimension, promotion signal = maturity reached verified, retry pulls last 3 ratified lessons for (pipeline, stage), F2 hybrid path (master-on-pipeline now + council_authority/debug-agent as future cultivator), seed via SQL fn + git pre-commit hook, watchman+dashboard for system-suggested convening. Four sub-specs landed at `projects/pg-ai-stewards/.spec/proposals/phase-{c,d,e,f}-design.md` — each carries schema + functions + UI + open questions + acceptance scenarios + estimated programming time. Total roadmap: ~13 sessions for C–F (3+3+3+4-5). Phase B's tools-cost lesson (gate-eval through plan agent with tools enabled = 5× cost) propagates: every JSON-output gate prompt across C/D specs explicitly says `tools_disabled: true`. Substrate stays feature-complete through Phase B; C is build-ready and unblocked.

* Previous: 2026-05-11 build mode — **Phase B FEATURE-COMPLETE on dev. Maturity ladder + gate machinery + auto-fire + UI surfaces all live.** Five commits tonight on top of yesterday's 4i/4j: **(5a, 263dc3e last session)** maturity-gate substrate — work_items columns (maturity/scenarios/revision_count/spec/destination_maturity), pipeline_stage_maturity table seeded for study-write + study-write-qwen, gate_decisions audit, gate_prompts (3 templates), verify_results, render_template + evaluate_gate + apply_gate_decision + parse_gate_response. **(5b)** scenarios + verify — generate_scenarios + apply_scenarios_result + verify_work_item + apply_verify_result, two more prompt templates. **(5c)** tiny fix caught during e2e smoke: sessions_kind_check CHECK constraint allowed only chat|agent|tool|study|dev; evaluate_gate inserts kind='gate' for clean audit separation, so extended the constraint to include 'gate'. **(bgworker auto-fire)** after a chat completes, payload markers `_gate_eval` / `_scenarios_gen` / `_verify` trigger automatic parse_gate_response → apply_*. Errors logged but never propagated. Means human never has to manually call apply_* — the gate fires, LLM responds, substrate walks the maturity ladder on its own. **(B.4)** NewWork.vue gains destination_maturity dropdown (default empty = full Ammon-loop to verified, or pick a rung to surface earlier). new_work.go: workItemCreateReq carries DestinationMaturity, UPDATE after create since work_item_create() doesn't take it. **(B.5)** WorkItemDetail.vue gains 4 new conditional sections — maturity ladder (visual stepper), scenarios (acceptance-criteria list), spec, gate decisions audit (action-tinted rows with reasoning + collapsible feedback + raw_response). work_items.go extends the get handler with 5 new columns + new /api/work-items/gate-decisions endpoint. **End-to-end e2e verified live:** synthetic study-write outline work_item → evaluate_gate → qwen3.6-plus dispatched (5 tool-calling rounds while it researched) → final assistant message returned a 'revise' decision — the model legitimately critiqued the outline (Hebrew/Greek word study is a category error for an 1843 English revelation; missing D&C 130 immediate context; should use Webster 1828 for 'principle' + 'intelligence') → bgworker auto-fired apply_gate_decision → revision_count 0→1, status='failed' (steward then re-dispatched the outline as work_id 1195). gate_decisions audit row #6 written. UI panels render the audit cleanly. Cost ~$0.04 for the e2e test. **What remains for Phase B:** none from the original ratification. The gate-eval prompt currently runs through the plan agent which has tools — could be tightened to a tools-disabled prompt for direct JSON return (would skip the 5-round research loop, drop cost ~5x), but that's tuning, not substrate. Phases C-F (Council, Steward of Stewards, Spiritual Creation, Consecration) remain deferred until A+B lived with. **Substrate is now feature-complete through Phase B.** The Watch→Diagnose→Act→Account loop (Phase A) and the maturity ladder + gate ratification (Phase B) both run autonomously on dev.

* Previous: 2026-05-10 build mode — **Phases 4i + 4j SHIPPED. Phase A is END-TO-END operational including UI visibility.** **(4i)** Three MCP tools in `cmd/stewards-mcp/escalation.go` for CLI/UI consumption of the escalation queue (D-EC3): `work_item_escalation_list` (read queued items), `work_item_escalation_claim` (atomic claim with full pipeline+stage+rendered-input context, returns agent_family/model/provider derived via pipeline_stage_lookup + render_stage_input), `work_item_escalation_resolve` (success → store output in stage_results, transition to resolved+pending, clear override; failure → quarantine with reason='escalation_failed'). All 3 tool SQL paths verified end-to-end on dev: synthetic queued work_item, list returned it, claim atomically transitioned state with full rendered context (input_chars=1351), resolve(success=true) stored output and transitioned to pending. Built bin/stewards-mcp.exe — Claude Code session restart needed to load new tools. **(4j)** Stewards-UI cost + steward visibility surfaces. Backend `scripts/stewards-ui/api/work_items.go`: extended `/api/work-items/get` to include 9 new steward+cost columns (failure_count, last_failure_reason, last_failure_diagnosis, quarantined_at, quarantine_reason, model_override, provider_override, escalation_state, escalation_claimed_by, escalation_attempts, cost_micro_dollars, cost_cap_micro, cost_capped_at); added `/api/work-items/cost` (returns cost_events array + work_item summary + cap status) and `/api/work-items/actions` (returns recent steward_actions, limit 50, newest first). Frontend `WorkItemDetail.vue`: 3 new conditional panels — **Steward status panel** (escalation badge with state-tinted color, failure_count/3 counter, diagnosis, override, claimed_by, quarantine reason, expandable last_failure_reason); **Cost panel** (cumulative micro→USD, cap with percent-used, event count, expandable per-attempt table with attempt_seq/model/in/out/cache/cost/timestamp); **Steward actions audit** (action-tinted log entries with diagnosis, model_used, expandable details JSON). All endpoints verified live with real data: $0.42 cumulative across 16 events for one test work_item. Built+restarted ui container. Browser at http://127.0.0.1:8080/work-items/{id} now shows all three panels. **Phase A IS COMPLETE end-to-end on dev.** All Watch→Diagnose→Act→Account loop layers operational + visible: schema (4a), dispatch override (4b), steward dispatch+realign (4c+4d), bgworker tick (4e), cost recording (4f), ad-hoc+cache (4g+4h), escalation MCP tools (4i), UI surfaces (4j). Tonight: 8 commits, ~2200 lines.
* Previous: 2026-05-10 build mode — **Phase 4g + 4h SHIPPED. Ad-hoc cost tracking + cache-token plumbing.** Two parallel improvements: **(4g)** SQL: `cost_events.work_item_id` was already nullable but added `session_id text` column + index; replaced `record_cost_event` with 10-arg signature including `p_session_id` (between cache_read_tokens and notes). bgworker.rs: dropped the `if let Some(_work_item_id)` wrapper around the cost-recording call so EVERY chat dispatch records a cost_event (work-item-tied or ad-hoc/watchman). The CASE in the call computes attempt_seq from session_id when work_item_id is NULL. **(4h)** types.rs: added `cache_creation_tokens` + `cache_read_tokens` to WorkOutcome::Chatted. bgworker.rs chat parser: parses `usage.cache_creation_input_tokens` + `usage.cache_read_input_tokens` (Anthropic-style fields, mostly absent from OpenCode Go Chinese models but present for Anthropic via OpenCode Zen). Both passed to record_cost_event; compute_cost gates each on the per-model cache rate column being non-NULL so providers without cache distinction silently skip those contributions. **Lesson #3 (extension-version skipped CREATE EXTENSION):** the database volume persists across container restarts; the extension is already installed at version 0.2.0, so a container restart does NOT re-run the bundled SQL even though it's been updated. Need to either ALTER EXTENSION UPDATE TO a new version (with upgrade script) or use the substrate's standard live-migration pattern (docker cp + psql). Did the latter for 4g. Verified: 10-arg `record_cost_event` exists, session_id column added, ad-hoc `record_cost_event(NULL, ...)` writes a row with work_item_id=NULL, session_id set, computed micro_dollars=1350 ($0.00135 for 1000 in + 100 out kimi). **Phase A is now fully operational** with cost tracking covering BOTH work-item-tied chats AND ad-hoc/watchman chats. Cache token contributions enabled when providers expose them. **What remains for Phase A:** Stewards-UI surfaces (cost panel, escalation queue, force-model picker, steward_actions audit), 3 MCP tools for CLI-mediated escalation queue.
* Previous: 2026-05-10 build mode — **Phase 4f SHIPPED. Cost tracking is FULLY OPERATIONAL end-to-end.** Wired `stewards.record_cost_event(...)` into `bgworker.rs` chat dispatch path — fires after the assistant message INSERT when payload contains `_work_item_id`. **Caught a bug during smoke**: provider responses echo back full versioned model identifiers like `moonshotai/kimi-k2.6-20260420`, but model_pricing is keyed on the canonical short name `kimi-k2.6` (set by the substrate's stage definitions). Initial code passed `model` (response field) → no pricing match → `micro_dollars=0` for every event. Fixed by passing `requested_model` (the substrate's canonical short name) instead, with the response's `model` value preserved in `notes` for audit. Restart sequence: paused soak, stopped bridge, `compose down + compose up -d pg ui` (bridge stays down), test, restart bridge via `compose up -d bridge`, re-enable soak. **End-to-end live verification:** synthetic `study-write` work_item dispatched → 12 chats fired through kimi-k2.6 over ~65s → 12 cost_events recorded with correct micro_dollars (event #34: 5470 in + 186 out = $0.00594; sum across all 12 events = $0.28). Daily bucket at 0.44% of $12 cap, monthly at 0.088% of $60. work_items.cost_micro_dollars trigger maintained correctly. **Lesson #2** (after the cost-cascade lesson from Push C): bgworker has 4 parallel workers, so cancellation isn't immediate — chats already dispatched during the wait between trigger and cancel still complete. Future synthetic tests should pause the bgworker too, or use `work_item_fail` (terminates immediately), or TRUNCATE work_queue pending rows before cleanup. ~$0.28 burned on this test (acceptable for verification but worth tightening). **Phase A is now fully operational on dev:** schema layer (4a 3 files), dispatch override (4b), steward dispatch (4c+4d), bgworker tick (4e Rust), cost recording (4f Rust). The Watch→Diagnose→Act→Account loop runs autonomously and cost data flows through to model_pricing → cost_events → cost_buckets → work_items. **What remains for Phase A:** Stewards-UI surfaces (cost panel, escalation queue, force-model picker, steward_actions audit), 3 new MCP tools for CLI-mediated escalation queue, cache-token parsing for Anthropic-style usage shapes, ad-hoc chat cost recording (currently skipped because cost_events.work_item_id is NOT NULL).
* Previous: 2026-05-10 build mode — **Push C SHIPPED. Steward loop FULLY AUTOMATED via bgworker tick.** Added `check_steward_tick()` to `extension/src/bgworker.rs` mirroring the watchman scheduler tick pattern; wired into the main 500ms poll loop with 30s tick interval, leader-only. Built, paused soak, restarted container with new image, re-enabled soak. **Inverse hypothesis test:** synthetic failed work_item created → bgworker fired its first steward_tick within ~1.4s (`last_steward.map_or(true, ...)` forces an immediate first tick) → result: status='in_progress', failure_count=1, model_override='kimi-k2.6', provider_override='opencode_go', dispatched as work_id=1041. Bgworker log line confirmed: `LOG: stewards: steward_tick processed 1 action(s)` at 23:11:07.999 UTC. **Cost-cascade caveat (lesson):** the dispatched work_id=1041 was a REAL kimi-k2.6 chat through the bridge → made tool calls → 14 chats fired before I could cancel. The bridge isn't yet wired to `record_cost_event`, so cost_events shows 0 events for the test work_item, but real tokens were spent on opencode_go. For future synthetic dispatch tests on live: pause the bridge container first, or use a fake provider. **Phase A is now fully operational on dev:** schema (4a 3 files), dispatch override (4b), steward dispatch (4c+4d realign), bgworker tick (Rust). The Watch→Diagnose→Act→Account loop runs autonomously every 30s. **What remains for Phase A:** Stewards-UI surfaces (cost panel, escalation queue, force-model picker, steward_actions audit), 3 new MCP tools for CLI-mediated escalation queue, bridge integration with `record_cost_event` so cost_events actually populates. Watchman + existing dispatch paths confirmed unaffected post-restart.
* Previous: 2026-05-10 build mode — **Push B SHIPPED. Watch→Diagnose→Act→Account loop is operational end-to-end on dev.** After Push A, did Push B in two SQL files: **`4c-steward-dispatch.sql`** replaced steward_tick to actually dispatch (set model_override + provider_override + failure_count++ + call work_item_dispatch_stage(allow_failed=true) + log dispatched_work_id). **`4d-steward-realign.sql`** caught two bugs during 4c smoke: (1) my stage_models seeded pipeline_family='study'/'lesson'/'dev' but actual pipelines are 'study-write', 'study-write-qwen', 'echo-test' (zero overlap — pick_model raised "no stage_models row"); added rows for the real pipelines. (2) per-item exception isolation in 4c only wrapped dispatch, but pick_model raises BEFORE dispatch — when it fired the entire steward_tick transaction rolled back losing all prior actions. Wrapped the WHOLE per-item body in BEGIN/EXCEPTION; one bad item now logs 'tick_error' and the loop continues. 4d also derives provider from model_pricing instead of hardcoding 'opencode_go' so cross-provider chains work correctly. **End-to-end live test:** created failed work_item, called `SELECT stewards.steward_tick()`, confirmed status='in_progress', failure_count=1, model_override='kimi-k2.6', dispatched_work_id=1040 in steward_actions.details. The full loop runs. Manual SELECT works; only Push C (bgworker tick) is missing for full automation. Stage_models now has 7 pipeline families (4 original placeholders + 3 real). Substrate has 6 4* SQL files live on dev (4a-cost-tracking, 4a-escalation-chain, 4a-steward, 4b-dispatch-override, 4c-steward-dispatch, 4d-steward-realign).
* Previous: 2026-05-10 build mode — **Phase 4b dispatch override SHIPPED + non-regression verified on live.** After the 4a schema layer landed, knocked out Push A from `steward-bgworker-integration.md`: **`extension/4b-dispatch-override.sql`** (~150 lines) does THREE things in one migration. (1) **Provider rename:** discovered while reading the canonical work_item_dispatch_stage that my 4a seed used `provider='opencode-zen'` but the substrate's registered provider is `opencode_go` (via `stewards.providers_loaded()` and existing pipelines, base_url=https://opencode.ai/zen/go/v1). Migrated 9 model_pricing + 4 cost_buckets rows via UPDATE; also edited `4a-cost-tracking.sql` at-rest so fresh containers get opencode_go. After fix, `compute_cost('opencode_go','kimi-k2.6',1M,500K)` returns the expected $2.95 (was returning 0 before). (2) **Added work_items.provider_override column** (sibling of model_override). (3) **Replaced work_item_dispatch_stage with override-aware version** — honors COALESCE(model_override, stage->>'model') + COALESCE(provider_override, stage->>'provider'); added optional p_allow_failed_status DEFAULT false param so existing call sites (NewWork, watchman) stay safe; steward retries pass true to enable re-dispatch from status='failed'. **Bug caught during smoke**: `CREATE OR REPLACE FUNCTION` doesn't replace when signature changes — added `DROP FUNCTION IF EXISTS stewards.work_item_dispatch_stage(uuid, text)` before CREATE OR REPLACE, otherwise both signatures coexist and ANY 1-or-2-arg call becomes ambiguous. Smoke test would have caught this if I'd live-applied without re-smoking. **End-to-end live verification:** synthetic `work_item_create('study-write', ...)` + `work_item_dispatch_stage(...)` returned work_id=1039 with provider='opencode_go' + model='kimi-k2.6' (the cleanup-status-typo caused the DO block to roll back, leaving zero artifacts — accidental but confirms transaction discipline). Watchman pass at 19:50 today completed normally; soak still schedule_enabled=t; recent work_items completed with no model_override/provider_override (correct — my code hasn't been invoked yet by anything). **Status:** 4 SQL files live on dev (4a-cost-tracking, 4a-escalation-chain, 4a-steward, 4b-dispatch-override). Push A of bgworker integration plan complete. Push B (extend steward_tick to actually call dispatch) is next; Push C (bgworker tick handler) needs container restart with soak pause.
* Previous: 2026-05-10 build mode (steward layer added) — **Phase 4a schema layer COMPLETE: 3 SQL files live (cost-tracking + escalation-chain + steward).** After the cost+escalation push, added third file **`extension/4a-steward.sql`** (~430 lines) with: work_items columns (failure_count, last_failure_reason, last_failure_diagnosis, quarantined_at, quarantine_reason), `stewards.steward_actions` append-only audit ledger, `stewards.diagnose_failure(reason, failure_count)` 5-type classifier (port of brain v3 diagnosis.go), `stewards.retry_guidance_text` seed table + `stewards.retry_guidance(diagnosis, attempt)` template composer (5 brain-v3 templates seeded), `stewards.pipeline_breakers` table + 3 breaker functions (check / record_failure / record_success — 3-state machine with 5-failure threshold, 10-min cooldown), `stewards.steward_tick()` orchestration that walks failed work_items applying cost-cap + breaker + diagnosis + escalation logic and writing steward_actions, plus `stewards.work_items_steward_status` view for UI status. Wired via 3rd extension_sql_file! macro (`create_phase_4a_steward` requires escalation chain). Updated Dockerfile COPY list. Built + smoke-tested (all 17 sections passed: diagnose timeout/transient/tool_error/model_limit fallback; retry_guidance text + attempt substitution; breaker walks closed→4-failures-still-closed→5th-trips-open→cooldown-blocks→success-closes; steward_tick on empty queue returns 0; status view has 18 columns). Live-applied to pg-ai-stewards-dev (no restart needed). **Discovery during code reading:** `work_item_dispatch_stage` reads model+provider from `pipeline_stage_lookup`, NOT from `pick_model` or `model_override`. So the Watch+Diagnose+Account loop is operational on dev today, but the Act layer (actually re-dispatching with escalated model) requires a surgical modification of the dispatch function + a bgworker tick. Wrote integration plan at `projects/pg-ai-stewards/.spec/proposals/steward-bgworker-integration.md` defining 3 sequenced sub-pushes: Push A (modify work_item_dispatch_stage to honor model_override + provider_override + status='failed' re-dispatch — SQL only, no restart), Push B (extend steward_tick to set overrides + call dispatch — SQL only, no restart), Push C (bgworker tick handler calling steward_tick every 30s — Rust change requiring container restart with soak pause). ~30 min per push, ~90 min total for full Act layer. Pushes A+B don't interrupt soak; Push C is a deliberate restart that needs Michael's awareness. **Schema-layer work for Phase A is done.** 3 SQL files live; integration is one focused next session.
* Previous: 2026-05-10 build mode — **Phase 4a schema layer shipped + live on pg-ai-stewards-dev.** After Michael ratified bucket pricing model (OpenCode Go = monthly sub ~$10 with $12/day, $60/month soft caps using same per-token rates as Zen, with Zen overage available), wrote and applied two SQL files: **`extension/4a-cost-tracking.sql`** (model_pricing + cost_events + cost_buckets tables, work_items cost columns, compute_cost / record_cost_event / cost_cap_exceeded SQL functions, bucket_period_for / bucket_current / bucket_record helpers, AFTER INSERT trigger maintaining work_items cumulative + bucket roll, seeded 9 model rates from opencode.ai/docs/zen and 4 buckets with $12/$60 caps) + **`extension/4a-escalation-chain.sql`** (stage_models + model_escalation tables, work_items.model_override + 5 escalation queue columns + CHECK constraint, pick_model SQL function, seeded 14 stage_models rows for study/lesson/dev/_gate pipelines and full 20-row escalation matrix Qwen→MiniMax→Kimi→GLM→__queue_for_opus__). Wired both into lib.rs via extension_sql_file! macros (`create_phase_4a_cost_tracking` requires `create_git_mcp_seed`; `create_phase_4a_escalation_chain` requires the cost layer). Updated Dockerfile COPY list to include the new files. Built docker image successfully (one false start: forgot the Dockerfile COPY update). Smoke-tested on ephemeral container — **all 16 sections of verify-4a.sql passed**: schema presence, work_items columns, both pricing seeds, bucket caps, compute_cost integer math (Kimi $2.95, MiniMax cache-aware $0.121, nonexistent provider returns 0), pick_model walking the chain through queue sentinel, transient stays on same model, dev/plan starts at GLM, missing-stage_models raises, matrix coverage. Live-applied both files via docker cp + psql to pg-ai-stewards-dev (no restart needed, idempotent CREATE TABLE IF NOT EXISTS / ON CONFLICT). Live container now has Phase 4a operational. Soak undisturbed (still schedule_enabled=t). **What's NOT shipped:** bgworker integration (Phase A.cost.2 + A.escalation.2 — wire steward_tick to use new functions), UI surfaces (A.cost.3 + A.escalation.3 + A.escalation.4 escalation queue), 3 new MCP tools for CLI-mediated escalation. Schema-layer work is the foundation; bgworker + Vue + Go MCP work is the next push. Journal at `.spec/journal/2026-05-10-substrate-phase-4a-schema-live.md`.
* Previous: 2026-05-10 post-iron-rod — substrate Phase A specs flushed out for autonomous coding. After Michael validated Tier 1+2 on three real iron-rod studies (see entries below), walked through all 22 substrate proposal §VI decisions in 6 AskUserQuestion batches. 16/22 went with recommendations; 6 had nuance: D-A4 (cost cap as token-multiplier with input/cache-write/cache-read/output rates per model, NOT flat dollars), D-B1 (model substitution to **OpenCode Zen Chinese models** — Kimi K2.6 / GLM-5.1 / MiniMax M2.7 / Qwen3.6 Plus — NOT Anthropic Zen direct), D-C4 (free-form covenant gate, not checklist), D-F2 (master-tier agents can bishop low-stakes councils), D-F4 (manual + system-suggested council convening). All ratified inline in the parent proposal §VI. Then drafted two design sub-specs at `projects/pg-ai-stewards/.spec/proposals/`: **cost-tracking.md** (model_pricing + cost_events + cost_buckets schema, compute_cost / record_cost_event / cost_cap_exceeded SQL functions, 3-bucket tracking — 5h/weekly/monthly without enforcement) and **escalation-chain.md** (stage_models + model_escalation tables, pick_model SQL function, escalation matrix Qwen→MiniMax→Kimi→GLM, **NEW: human-mediated escalation queue** with `__queue_for_opus__` sentinel state machine and two consumer paths — Stewards-UI "Boost via Zen Opus" button OR Claude Code CLI via 3 new MCP tools using Michael's Pro subscription). Real pricing pulled from opencode.ai/docs/zen via WebFetch — Kimi K2.6 $0.95/$4 per MTok, GLM-5.1 $1.40/$4.40, MiniMax M2.7 $0.30/$1.20, Qwen3.6 Plus $0.50/$3.00, plus Anthropic via OpenCode Zen for the boost queue. **One unresolved tension surfaced (covenant: surface_tensions):** OpenCode Zen docs show only per-token pricing, no session-bucket tiers; Michael described 3 concentric buckets in D-A4 (5h/weekly/monthly). Bucket schema ships either way (additive, harmless if buckets aren't real). Phase A is now fully spec'd-out; Phase A.cost.1 + Phase A.escalation.1 can be coded autonomously next programming session.*

* Previous: 2026-05-10 late-night — **Iron-rod Part 3 shipped, harness fix validated post-restart.** Third study run on the iron-rod arc, after Claude Code session restart that loaded the subagent frontmatter fix. **Harness gate passed:** ToolSearch returned all three MCP tool schemas (gospel-engine-v2, webster, byu-citations); test call to `gospel_search(query: "tree of life love of God", mode: "semantic")` returned 10 real results. Subagents now inherit parent MCP tools as documented. Anthropic's "subagents load at session start" rule confirmed. Full phased study executed with MCP tools live: semantic search (3 queries) → footnote-chase → BYU citations (4 verses) → Webster 1828 (3 words) → conference talk reads (Uchtdorf 2009, Corbridge 2008). Three genuine Part-3 discoveries the prior parts lacked: **(1)** Jeremiah 2:13 — the Lord identifies HIMSELF in the first person as "the fountain of living waters," giving the OT root of the 1 Ne 11:25 fountain; **(2)** Moroni 8:26 footnote 26d on "filleth" cross-references 1 Ne 11:22-25 — the Church's footnote editors had already sealed the Comforter-filling-with-hope to the tree's fruit; **(3)** Corbridge "The Way" (Oct 2008) is the apostolic stack of Christ's I-AM titles — exactly the synthesis Part 3 needed for the closing move. Arc closes: rod, chain, line, anchor, way, tree, fountain, fruit = one Person in seven aspects (with the necessary contrast-set caveat — mist/building/river are anti-Christology). Study at [iron-rod-anchor-and-the-four-groups.md](../study/iron-rod-anchor-and-the-four-groups.md). Scratch at [study/.scratch/iron-rod-anchor-and-the-four-groups.md](../study/.scratch/iron-rod-anchor-and-the-four-groups.md). The MCP-subagent harness fix is now confirmed working end-to-end via a real study, not just a gate check.*

* Previous: 2026-05-10 evening — first WS6 study run on the new Claude Code cycle harness: [iron-rod-anchor-and-the-four-groups.md](../study/iron-rod-anchor-and-the-four-groups.md). Binding question: is the iron rod the chain that tethers the soul to the anchor of hope? Part 1 + Part 2 both shipped. Phases 1-7 executed in order; gap & critical analysis section forced into the scratch file before drafting (the `precondition` at the top of the study agent worked as designed — agent did not skip 3a). Major finds: (1) **Helaman 3:29-30** is the keystone verse — explicitly uses chain-functional language ("lay hold upon the word... lead... across the gulf... land their souls at the right hand of God"); (2) Hebrews 6:4-6 is the NT parallel to Lehi's Group 2 (tasted, fell away, open shame) — and sits in the same chapter as the anchor verses (vv. 18-19); (3) Bednar's 2022 "But We Heeded Them Not" already names the cling-vs-hold-fast distinction doctrinally — apostolic warrant for the Group 2/Group 3 reading; (4) **Uchtdorf's hope-IN vs hope-FOR distinction** (Oct 2008) — hope takes two prepositions and they're the two grips on the chain, hope *in* Christ = present grip on the line, hope *for* the celestial kingdom = grip on the far end; this diagnoses Group 2's failure cleanly (their "hope for" shifted to the building after they tasted, chain went slack); (5) Faust 1999 fuses Hebrews' anchor with Helaman's Rock — both Christ; (6) Holland 2009 reads the mist as falling on the *faithful* travelers too. Study commits to grip discipline, attention discipline, two-preposition hope, and the chain-is-Christ reframe. Scratch at `study/.scratch/iron-rod-anchor-and-the-four-groups.md`.

* Previous: 2026-05-10 Claude Code cycle harness ship (Tier 1 + Tier 2 ports). **Tier 1 (47 files):** 18 subagents ported from `.github/agents/*.agent.md` → `.claude/agents/*.md`, 6 slash commands from `.github/prompts/*.prompt.md` → `.claude/commands/*.md`, 21 skills from `.github/skills/*/SKILL.md` → `.claude/skills/*/SKILL.md` with Claude-Code-specific tool name updates throughout. Bug fix: `.claude/settings.json` SessionStart + PostToolUse hooks were referencing `.mind/intent.yaml` (nonexistent); corrected to `intent.yaml` (repo root). **Tier 2 (5 files):** Stop hook (gates on `git status --porcelain` — if uncommitted changes exist, reminds to write journal + update active.md per covenant `update_memory`) + UserPromptSubmit destructive-intent guard (greps stdin for `rm -rf` / `force push` / `drop table` / etc., injects covenant reminder when triggered). Three new skills: `council-moment` (three-minute pre-work connection/tension/blind-spot scan), `intent-check` (four-question intent articulation — purpose/beneficiary/success/non-goals), `sabbath-close` (lighter daily-rhythm sabbath for non-cycle work endings). All ratified by Michael in two AskUserQuestion batches as "Both tiers, in order — do all of Tier 1 in one push, all 21 skills, port Claude-Code-specific (not symlinked)." Carry-forward: validate hook behavior + skill auto-loading next session; programming-time work still pending (move stewards-ui to projects/, dynamize NewWork pipeline list, ratify substrate proposal §VI). Journal at `.spec/journal/2026-05-10-claude-code-cycle-harness.md`.
* Previous: 2026-05-09 deep-research session (after stewards-ui ship + UI feedback bug-fix wave). Substantive: produced **`projects/pg-ai-stewards/.spec/proposals/full-agentic-substrate.md`** — decision-ready 6-phase ladder porting brain v3's six load-bearing inventions (Watch→Diagnose→Act→Account, retry-with-feedback, circuit breaker, model escalation, commission Ammon-loop, gate+scenarios+verify) into substrate-native shape, each phase anchored in a step of the 11-cycle creation pattern (Intent / Covenant / Stewardship / Spiritual creation / Line upon line / Physical creation / Review / Atonement / Sabbath / Consecration / Zion). Phases A (Steward loop) + B (Spec + Gate) recommended as immediate next; C-F deferred until A+B lived with. ~22 explicit decisions for Michael to ratify. Process journal at `projects/pg-ai-stewards/.spec/journal/2026-05-09-full-agentic-substrate-research.md`. Also-ratified earlier same session: stewards-ui will move from `scripts/` to `projects/stewards-ui/`; NewWork's hardcoded pipeline list will become dynamic + need real second/third pipelines; brain v3 is legacy reference (may revive on cheaper models).
* Previous: 2026-05-09 night (stewards-ui v1 shipped END-TO-END all 7 phases. Backend: 8 API handler files (dashboard, studies, work_items, sessions, watchman, bridge, new_work, graph) calling substrate SQL functions directly. Frontend: 9 Vue views (Dashboard with 5s auto-refresh cards, Studies + StudyDetail with markdown-it + Tailwind typography + FTS search, WorkItems + WorkItemDetail, Sessions message timeline, Watchman passes with verdict badges, BridgeState 9-server table with expandable tool catalog, NewWork form with pipeline + binding question + dispatch checkbox, Graph using Cytoscape cose-layout). All 10 endpoints + SPA-fallback verified post image-rebuild. Local-only at http://127.0.0.1:8080. Now substrate has full visualization + interaction surface. Earlier same evening: Michael ratified the 3f proposal direction in two AskUserQuestion batches and asked me to kick off the stewards-ui build. **Phase 1 foundation shipped**: `scripts/stewards-ui/` Go binary with embed.FS + Vue 3 + Vite + Tailwind 4 + vue-router scaffold, multi-stage `ui.Dockerfile` (node-build → go-embed → alpine runtime), new `ui` compose service alongside `pg`+`bridge` (depends on pg.healthy, binds 127.0.0.1:8080). Verified live: container healthy, /healthz pings db, / serves real Vite HTML, /studies SPA-fallback, /api/* returns 501 as designed for phase 1. Phases 2-7 (real endpoints + view content + Cytoscape graph) follow over future sessions per the build plan at .spec/scratch/stewards-ui-build-plan-2026-05-09.md. Earlier same evening: massive autonomous run while Michael was tired from a family move — three more pieces shipped: **3d v1 sandboxed git-mcp** Go wrapper with allow-list discipline (8 tools, branch namespace lock, env-only token, Co-Authored-By trailer), built into bridge image; **fetch-md v2** chromedp + alpine chromium adds optional `js: true` to all 4 tools; **3f local UI proposal** captures Michael's pivot from cloud-hosted a.ibeco.me to local-first single-user UI alongside docker stack. Earlier same day: **bridge-in-docker** — `stewards-mcp bridge run` now a compose service (no more host shell), 9/9 MCP servers responding from inside container. Continued the 3e wave: 3e.2.d/e v1 — auto-promote `mcp_tool_cache` → `tool_defs` via SQL trigger + bridge session crash recovery; **fetch-md-mcp v1** — Go MCP for web fetching (Readability + html-to-markdown), granted to research; **first organic substrate-bridge use** — mysteries-of-god study run on kimi-k2.6 made dozens of `gospel_get` calls during review-stage quote verification, produced 16K-char study at `study/mysteries-of-god-text-vs-spirit.md`; QoL sweep — fixed search-mcp JSON-RPC notification bug + audited same defensive fix across 5 hand-rolled MCP servers, broadened agent grants (45 new manual grants across 7 agent families), built `scripts/study-export/` Go helper to auto-resolve `[slug](#)` placeholders in substrate studies. Soak still healthy through all of it. 9 MCP servers now refreshing cleanly with ~50 cached tools.) · Previous: 2026-05-08 evening (pg-ai-stewards Phase 3e MCP work landed end-to-end. **Five 3e sub-phases shipped today**: 3e.1 v1 (MCP server sidecar — Claude Code reads substrate via `pg-ai-stewards` stdio server), 3e.1.1 (jsonschema fix + study_similar/study_citations), 3e.4 v1 (read-only inbound substrate inspection — work_item_list/show, watchman_passes_list/pass_show), **3e.2.a v1** (outbound MCP-client substrate — `mcp_servers` registry, `mcp_tool_cache`, `bridge refresh-tools`; 40 tools cached across 6/7 external servers; multi-bgworker registration with leader gating), **3e.2.b/c v1** (async fan-out tool dispatch — `mcp_proxy_enqueue` + `tool_dispatch_complete_waiting` + `bridge run` long-running daemon; synthetic e2e verified gospel_search round-trip in 258ms). Substrate now has both directions of MCP — Claude Code reads in, substrate agents call out. Three example mcp_proxy tool_defs (gospel_search, gospel_get, webster_define) land **deny-by-default** — soak surface preserved. Earlier in the day: kimi-tuned + qwen-tuned study prompts both validated end-to-end via five-run multi-model voice experiment, including run #4 (kimi-tuned, corpus) catching+removing two fabricated quotes from its own draft. Both `.stewards/kimi-k2.6/` and `.stewards/qwen-3.6/` variants stable v1. Soak ran continuously through both the voice work and the 3e build.) · Previous: [archive/active-2026-04-23.md](archive/active-2026-04-23.md)*
*Hardware: Dual 4090s desktop. NOCIX server live.*

> **Edit rule:** Rewrite this file directly. Do NOT cat/append the old content first — its archive snapshot lives under `.mind/archive/`. Appending duplicates the document and doubles every memory load. (Bug: 2026-04-20.)

> **Workstream taxonomy:** [.mind/workstreams.md](workstreams.md) is canonical: WS1–WS9, status enum, frontmatter convention. Every active proposal carries `workstream:` frontmatter. Read that file before tagging anything new.

> **Model:** GitHub Copilot now runs Claude Opus 4.7 (was 4.6 through April). 4.7 is more literal — see Foresight & Adjacent Surfaces section in [.github/copilot-instructions.md](../.github/copilot-instructions.md). Adjacent Surface Audit is now standard before declaring dev/debug work complete.

---

## Priorities

1. ★ **[WS6] Study** — "It keeps me in the spirit." Latest: [broken-heart-and-contrite-spirit.md](../study/broken-heart-and-contrite-spirit.md) (May 1, fourth in arc: art-of-presidency + art-of-delegation + zion-in-a-presidency + broken-heart). Next: TBD → [study-workstream.md](../.spec/proposals/study-workstream.md)
2. ★ **[WS7] Teaching** — 11-episode arc (Option C). Agent + repo scaffolded. Content not started → [teaching-workstream.md](../.spec/proposals/teaching-workstream.md)
3. **[WS5] Token efficiency** — Apr 16 proposal awaiting refresh review → [token-efficiency.md](../.spec/proposals/token-efficiency.md)
4. **[WS2] Brain Inline Panel + Kanban 4c** — next active build → [brain-inline-panel.md](../.spec/proposals/brain-inline-panel.md) · [brain-project-kanban.md](../.spec/proposals/brain-project-kanban.md)
5. **[WS5] pg-ai-stewards** — Postgres-as-AI-substrate. **Phases 2.5 + 2.6a/b/c + 2.7a + 3a + 3b + 2.7b.1 + 2.7b.2 + 2.7b.3 + 2.7b.4 shipped May 4–6.** **v0.2.0:** nine SQL files via `extension_sql_file!`. **2.7 stack complete:** trigger-driven harvest + scheduler tick (60s) + per-pass token budget + soak prep (frontmatter watchman-skip exemption + `regenerate_active_md()` markdown status report). docs/architecture.md (466-line reading map) shipped. Container has all SQL live-applied; image rebuild deferred. **Open:** soak start (bulk-tag journals + flip `schedule_enabled=true` for 7 days), agent corpus import (option A — Copilot/Claude agent defs into substrate), ws6 AGE upstream PRs, 2.8 (LLM-inferred edges), 2.9 (self-modification, gated on soak), 3c–3g. → [proposal](../.spec/proposals/pg-ai-stewards-phase-2-5-generic-substrate.md) · [phases.md](../projects/pg-ai-stewards/phases.md) · [docs/architecture.md](../projects/pg-ai-stewards/docs/architecture.md) · [AGE-QUIRKS.md](../projects/pg-ai-stewards/docs/AGE-QUIRKS.md)

## Key Facts

- Copilot: 1500 premium/mo ($40 Pro+). Haiku 4.5=0.33x, Sonnet 4.6=1.0x, Opus 4.7=7.5x, GPT-5/5-mini/4.1/4o=0
- Brain default model: gpt-5-mini (0x). Pipeline big = claude-opus-4.7 (7.5x)
- **Brain stage defaults (Apr 23 cost discipline):** research=sonnet, plan=opus, spec=sonnet, execute=sonnet, verify=haiku (hard-pinned), revise=sonnet. Commission `Model` field = steward judgment only (gate eval). Revise loop capped at 2 → surface.
- Claude Code: Pro $20/mo. 200K context. Project caching
- Pipeline cost: research=0.33 + plan=1.0 = 1.33/entry  *(stale — see stage defaults above)*
- Active MCP servers: gospel-engine-v2 (engine.ibeco.me), webster, yt, byu-citations, becoming, exa-search
- gospel-engine v2 hosted is the single canonical search backend; gospel-mcp + gospel-vec retired as fallback

---

## In Flight

| WS | Item | Status | Ref |
|----|------|--------|-----|
| WS2 | Brain status-aware-views ecosystem parity (ibeco.me + brain-app) | ✅ Phases 1+2 shipped Apr 23; Phase 3 verify post-deploy | [brain-status-aware-views-ecosystem-parity.md](../.spec/proposals/brain-status-aware-views-ecosystem-parity.md) |
| WS2 | Brain non-pipeline kanban flow | ✅ archived Apr 23 (all phases shipped or scoped out) | [archive/brain-non-pipeline-kanban-flow.md](../.spec/proposals/archive/brain-non-pipeline-kanban-flow.md) |
| WS2 | Brain Inline Panel + Nudge Bot Controls | ▶ P1 next | [brain-inline-panel.md](../.spec/proposals/brain-inline-panel.md) |
| WS2 | Brain Project-Kanban Phase 4c | ▶ next | [brain-project-kanban.md](../.spec/proposals/brain-project-kanban.md) |
| WS5 | Token Efficiency & Memory v2 | ⏸ awaiting refresh | [token-efficiency.md](../.spec/proposals/token-efficiency.md) |
| WS5 | Brain ↔ VS Code Bridge | � building (Phase 0 shipped Apr 22) | [brain-vscode-bridge/main.md](../.spec/proposals/brain-vscode-bridge/main.md) |
| WS2 | Brain non-pipeline projects | ✅ archived Apr 23 | [archive/brain-non-pipeline-projects.md](../.spec/proposals/archive/brain-non-pipeline-projects.md) |
| WS2 | Brain manual stage transitions | ✅ archived Apr 23 | [archive/brain-manual-stage-transitions.md](../.spec/proposals/archive/brain-manual-stage-transitions.md) |
| WS2 | Johari window agent mode | 📝 proposed Apr 22 | [johari-window-agent-mode.md](../.spec/proposals/johari-window-agent-mode.md) |
| WS5 | pg-ai-stewards (Postgres substrate for agent state, memory, work, model calls) | 🔨 **Phase 3e shipped May 8** — bidirectional MCP wiring: inbound (`stewards-mcp` server lets Claude Code read substrate state) and outbound (`stewards-mcp bridge run` daemon lets substrate agents call external MCP servers via async fan-out tool dispatch). 16 SQL files in foldback chain. Soak still on the existing tool surface — new mcp_proxy tools deny-by-default. Latest commit `bf4cb7c`. Findings: [docs/3e-mcp-findings.md](../projects/pg-ai-stewards/docs/3e-mcp-findings.md). | [proposal](../.spec/proposals/pg-ai-stewards-phase-2-5-generic-substrate.md) |
| WS2 | Motivation coach agent mode | 📝 proposed Apr 22 | [motivation-coach-agent-mode.md](../.spec/proposals/motivation-coach-agent-mode.md) |
| WS3 | LightRAG investigation | 📝 proposed Apr 22 | [lightrag-investigation.md](../.spec/proposals/lightrag-investigation.md) |
| WS3 | Gospel engine v3 proxy-pointer | 📝 proposed Apr 22 | [gospel-engine-v3-proxy-pointer.md](../.spec/proposals/gospel-engine-v3-proxy-pointer.md) |
| WS5 | VS Code agent hooks integration | 📝 proposed Apr 22 | [vscode-agent-hooks-integration.md](../.spec/proposals/vscode-agent-hooks-integration.md) |
| WS5 | Memory & context research bundle | 📝 proposed Apr 22 | [memory-research-bundle.md](../.spec/proposals/memory-research-bundle.md) |
| WS6 | Study: Nate B Jones on delegation | 📝 proposed Apr 22 | [study-nate-jones-delegation.md](../.spec/proposals/study-nate-jones-delegation.md) |
| WS7 | AI presentation site tool | 📝 proposed Apr 22 | [ai-presentation-site-tool.md](../.spec/proposals/ai-presentation-site-tool.md) |
| WS7 | Launch YouTube channel | 📝 proposed Apr 22 | [launch-youtube-channel.md](../.spec/proposals/launch-youtube-channel.md) |
| WS5 | Sabbath agent | ready to build | [sabbath-agent.md](../.spec/proposals/sabbath-agent.md) |
| WS5 | Debug agent: layer triage | proposed | [debug-layer-triage.md](../.spec/proposals/debug-layer-triage.md) |
| WS5 | Claude Code Integration | researched | [claude-code-integration.md](../.spec/proposals/claude-code-integration.md) |
| WS5 | tokenomics-2026 (research) | placeholder | [tokenomics-2026/main.md](../.spec/proposals/tokenomics-2026/main.md) |
| WS1 | Classifier qwen fix | ready to build | [classifier-qwen-fix.md](../.spec/proposals/classifier-qwen-fix.md) |
| WS1 | Classification quality benchmark | proposed | [classify-bench.md](../.spec/proposals/classify-bench.md) |
| WS1 | Data safety: dev hardening + audit log | proposed | [data-safety/main.md](../.spec/proposals/data-safety/main.md) |
| WS2 | Brain Windows Service (systray) | proposed | [brain-windows-service.md](../.spec/proposals/brain-windows-service.md) |
| WS3 | Gospel Engine v1.5 + research rollup | ratified 2026-05-13 — 6 phase files at `scripts/gospel-engine-v2/.spec/proposals/` (1.5a docs → 1.5b–1.5e code+indexer → single reindex → 3-research). Phase 2 TITSW deferred. | [rollup README](../scripts/gospel-engine-v2/.spec/proposals/README.md) · [parent (superseded)](../.spec/proposals/gospel-engine/phase1.5-ergonomics.md) |
| WS3 | Gospel Graph Visualization | proposed (blocked on AGE/PG18) | [gospel-graph/main.md](../.spec/proposals/gospel-graph/main.md) |
| WS4 | study.ibeco.me UI | proposed | [study-ibeco-me/main.md](../.spec/proposals/study-ibeco-me/main.md) |
| WS4 | ibeco.me Security Audit | not started | — |

---

## Recently Shipped (last ~30 days)

| WS | Item | Shipped | Notes |
|----|------|---------|-------|
| WS2 | brain-steward-cost-discipline | Apr 23 | Three-defect fix: (1) commission `Model` no longer overrides every stage — `modelForStage` helper routes through `config.StageDefaults`; `c.Model` reserved for `EvaluateGate` only. (2) Verify hard-pinned to haiku regardless of catalog. (3) Revise loop capped at 2 → surface `loop_limit_exceeded`. `RevisionCount` field on Commission with DB migration; "Revised X/2" badge on EntryDetailView. Same fix applied to `commissionWaitForExecution` (the loop that actually burned 105 credits). Best-case opus commission ~25 credits (was ~52); worst case ~28-35 (was unbounded, hit 105). Followup Apr 23: research bumped haiku→sonnet per Michael ("stronger model researching is good") — chain still escalates from sonnet, just one fewer step before quarantine. |
| WS2 | brain-model-catalog-sot | Apr 23 | Single source of truth at `internal/config/models.go`: `Catalog` slice + `StageDefaults` map. Two pre-existing drifted maps (`modelCosts`, `AvailableModels`) and `steward.EscalationChain` now derive from it. New `GET /api/models` endpoint; frontend composable + dynamic dropdowns in CommissionDialog and ProjectDetailView inline commission. Default is now Claude Opus 4.7 (7.5×) instead of stale Opus 4.6 (3.0×). Stewardship sweep: same-bug-same-fix on `feedbackDialog` and `executeDialog` modals (UA-stylesheet dark-theme bug from doneDialog fix earlier same day). Note: `/api/models` path collided with legacy LM Studio profiles handler — moved to `/api/models/profiles` (no consumers found). |
| WS2 | brain non-pipeline kanban flow (Phases 1-3 + classify gate) | Apr 23 | Status vocab gained `working` (in-progress lane). `boardColumns` branches on `pipeline_enabled`: manual path uses literal status keys (active/working/done) instead of route-status pipeline. 5-button manual row (▶ Start / ✓ Done / ↩ Reopen / ⏸ Someday / 🗄 Archive) on board + list views. Optional reason dialog on ✓ Done appends a `_Closed YYYY-MM-DD: reason_` line to body. Native HTML5 drag-and-drop between columns (no library dep). Auto-classify gated for non-pipeline projects in relay client. Done-dialog theming fix (centered, larger, dark-theme textarea). `handleCreateEntry` now accepts `project_id` — single-call POST round-trips correctly. brain-app P4 mirror skipped per user direction (no Project surface). |
| WS2 | brain non-pipeline projects (Phases 1+2) | Apr 23 | `pipeline_enabled` column on `projects` (default true). Notebook auto-flipped to false. Single primary gate in `routeEntry`; defense-in-depth in `BuildProjectContext`. UI checkbox + 📓 badge on web. Inverse-hypothesis verified via PUT-set project_id + explicit `/api/agent/route`. |
| WS2 | brain manual stage transitions (Phase 1 mobile) | Apr 23 | brain-app: status dropdown in edit screen, long-press quick-actions sheet (done/park/waiting/archive/reactivate) + undo, semantic chip colors. Phase 2 web: pre-existing — `EntryDetailView.vue` already had full STATUS_OPTIONS dropdown. |
| WS2 | brain status-aware-views ecosystem parity (Phases 1+2) | Apr 23 | ibeco.me `ListBrainEntries` honors `?include_parked=1` (default off). brain-app history `_showParked` covers both someday+archived. Phase 3 cross-surface check pending Dokploy deploy. |
| WS5 | Opus 4.7 harness tuning | Apr 23 | Foresight & Adjacent Surfaces section in copilot-instructions.md, dev.agent.md update, Council Moment extended to dev/debug/ux. Diagnosis + fix for literalism failures. |
| WS2 | brain-status-aware-views (Phases 1-3 + Dashboard) | Apr 23 | All planned phases shipped. Phase 4 (Capture semantic) deferred. Server-side `/api/entries` filter (`?include_parked=1` opts in). Project board toggle relocated to visible header checkbox. Dashboard agent surfaces (routable/review/your-turn) filter parked server-side. |
| WS2 | brain-status-field-on-list-queries (full SELECT audit) | Apr 23 | All 6 list queries now expose `status`: ListAll, ListCategory, ListEntriesByProject, ListByRouteStatus, ListUnassigned, ListPipeline. Future filter UIs no longer guess data layer contracts. |
| WS2 | Brain audit 2026-04-22 | Apr 22 | 69 entries triaged. Personal merged into Notebook. inbox 39→10, status=NULL 96→5 |
| WS5 | Brain↔VS Code bridge Phase 0 | Apr 22 | Schema migration (workstream + proposal_path columns). Read-only inspector at scripts/harness/harness_inspect.py |
| WS5 | cleanup-2026-04 + part2 (all phases) | Apr 21-22 | Spec dedup, 19 proposals archived, workstream taxonomy + frontmatter convention, active.md rewritten |
| WS5 | Voice/bias harness v2 | Apr 21 | em-dash budget, three-beat pivot, refrains, stats cite-count |
| WS3 | engine.ibeco.me Phase 1-3 | Apr 20 | Hosted gospel search. Token UI. First study used it |
| WS1 | Commission UX Fixes | Apr 15 | Path mangling, link normalization (Space Center) |
| WS1 | Orchestrator Steward P1-6 | Apr 10-11 | Retry, escalation, circuit breaker, quarantine, nudge, commission. 86 tests |
| WS2 | Commission UI P1-3 | Apr 11 | Types/API, dialog, triggers, status panel, badge |
| WS2 | Brain UX QoL P1-7b | Apr 6 | Textarea, markdown, file viewer, WebSocket push, cost tracking, git status, nested repos |
| WS1 | Brain Pipeline Evolution P1-9 | Apr 6-7 | Governance, failure visibility, notebook mode, 3-col board, project-aware pipeline |
| WS2 | Brain Project-Kanban P1-4b | Apr 4-5 | Projects, kanban, auto-assignment, AI push-back. **4c still in flight** |

---

## Deferred / Paused

| Item | Revisit When |
|------|--------------|
| Brain UX QoL Phase 8 (auto-commit) | After human-in-loop signal stabilizes |
| Plan 17: Proactive Surfacing | WS2 Phase 3 |
| Plan 18: Widget Overhaul (Ph 3-4) | Agent infra proves out |
| Plan 19: Brain App Ideas | Natural pause |
