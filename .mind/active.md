# Active Context

> **2026-06-09 (evening) — ⚠ WEBSTER "1828" IS ACTUALLY 1913 — investigation queued for a dedicated session.** During the scripture-book v4 walk (Fable 5), discovered the workspace's `webster_define` serves **Webster 1913 Revised Unabridged** text under the 1828 label (the entry cites U.S. Dispensatory 1833, N.P. Willis 1830s — impossible in 1828; verified against webstersdictionary1828.com). Likely chain: `scripts/webster-mcp/data/webster1828.json.gz` ← github.com/ssvivian/WebstersDictionary ← **Project Gutenberg's Webster = the 1913 edition**; open question whether the upstream repo or our naming introduced "1828." Blast radius: webster-mcp · **1828.ibeco.me / 1828-illuminated** · the published **three-glories study** on cpuchip.net · book Ch 0/1/9 (handled in the book's v4 findings F-19/20/21) · webster-analysis skill · any past study quoting "1828." Michael feels (rightly) misled by the data source and wants a dedicated session to dig in + then walk ALL published works (study/ · cpuchip.net · book) fixing issues. **THE FULL BRIEF (evidence, blast radius, 6-step remediation plan, verification standard): [`.spec/proposals/webster-1828-data-integrity.md`](../.spec/proposals/webster-1828-data-integrity.md).** Incident log: `docs/06_tool-use-observance.md` (June 9 entry). Also this session: the book's **v4 honesty+voicing walk COMPLETED** (33 units, 38 findings, no edits — chat walk with Michael next; see `projects/scripture-book/.mind/active.md`).

> **2026-06-09 (evening) — CODEWRIGHT LIVE in ai-chattermax + repo-awareness (Layer A). Root `b92e805` UNPUSHED.** Michael minted a `chattercode` persona key + granted rooms; I wired it into the live persona-host. **Codewright is now an engineering bot in 10-forward / Engineering / Library on chat.ibeco.me** — ask it "how does X work?" about an allow-listed repo, it delegates to research_codebase → sandbox → answers with real file:line citations (or SILENCE / honest "can't find it"). Wiring: seeded `persona_host.personas` row (slug=chattercode, pipeline=**persona-turn-code**, agent_family=codewright) + appended to CHATTERMAX_PERSONAS in the extension `.env` + restarted persona-host (all 3 personas reconnected clean). **Live feedback → Layer A (b92e805):** codewright couldn't tell anyone what it could see (allow-list was an invisible bridge env) + was scoped to 1 repo. Fixed: **`list_repos` tool** (stewards-mcp; reads the SAME CODER_REPO_ALLOWLIST/DENYLIST the sandbox enforces → report==reality) + **deny-beats-allow** in coder-mcp `repoAllowed` (the bridge clones with a GITHUB_TOKEN that reaches PRIVATE repos, so a broad allow would expose them). **Scope set: allow `github.com/cpuchip/` (all his public repos), deny `private-study`** (the job search). r14 registers list_repos + grants codewright + re-prompts; bridge rebuilt (31 tools cached). **Proven live:** codewright asked "what repos can you look at?" → called list_repos → answers "any public repo under github.com/cpuchip/." ~10s, no fabrication. **★ FOLLOW-UP FIX (a671a37, r15): the FIRST version LEAKED — codewright announced "off-limits: private-study" in the room, naming the protected repo. Michael caught it.** Fixed: list_repos no longer returns deny_patterns (model can't see the denylist→can't say it; verified gone from binary) + r15 re-prompt (never name/guess repos it can't access). Inverse-verified: same question → zero private names (CLEAN). **Principle: a denylist's NAMES are sensitive metadata — enforcement and disclosure are different surfaces; never hand deny-names to a model that speaks.** **★ ROADMAP captured (`codewright-workspace-roadmap.md`, design-only): A repo-awareness (DONE) → B work-in-a-repo (Michael's want; rec = reuse code-pr cascade from chat, persistent-workspace security inversion flagged) → C codewright orchestrates `agy -p`/`opencode`/`claude -p` from a container (the crux = AUTH via a pre-provisioned secrets mount never in-image + cost across 3 billing pools).** Connects [[project_ai_chattermax]] + [[project_council_review_beats_gift_matching]] (C = many doers/one critic). **NEEDS MICHAEL:** push root (now incl. b92e805); decide B v1 (code-pr-from-chat) when ready; C is its own ratified project. Spend today $1.96/$12. Journal: `projects/pg-ai-stewards/.spec/journal/2026-06-09-codewright-live-and-repo-awareness.md`.

> **2026-06-09 (cont.) — P2 RESTART-WINDOW BUNDLE shipped + CT2.4 RUN 1. Root `93d593d`+`2f48c13` UNPUSHED.** After the lunch block, Michael refreshed MCP (research_codebase now in Claude Code's belt) + picked ALL of a 4-item menu (A restart-bundle / B #136 A/B / C voice-bridge V0 / D Spin offload). **Built A+B; HELD C+D for a fresh session** (large; C ends in a mic test; Mosiah 4:27 strength limit — the WIP risk I'd flagged). Soak paused→resumed. **A — #135 P2 (research_codebase live + codewright):** pg image rebuilt (5d4 fix baked → verify-suite DR now shim-free); **r12** activated research_codebase (pg-ai-stewards self-catalog = 30 tools after refresh-tools); **r13 codewright** = tool-using CODE chat persona (R9-librarian twin: deny * + research_codebase/read_corpus_parents/expand_message; persona-turn-code pipeline; read-only; room-join is Michael's mint-key+grant+persona-host step). **★ Two real bugs fixed:** (1) grant≠catalog one layer deeper — research_codebase active but not in mcp_tool_cache → "unknown tool" until refresh-tools; (2) **bridge.Dockerfile never COPYed the persona-host module that go.work lists → every `--no-cache` bridge build failed the Go compile and force-recreate silently re-ran the STALE 2026-06-04 image** (why my first 2 rebuilds didn't pick up the P1.5 binary; Dokploy-stale-build cousin). Fixed; clean binary strings=3. **★ Codewright cognition proven twice:** tool-down → it REFUSED to fabricate (read-before-quoting held in a 0-history persona); tool-live → full nested chain (codewright→research_codebase→flash sandbox cloned ai-chattermax 110s/$0) → answered with REAL file:line citations, $0.0067. **B — CT2.4 RUN 1 (#136): the informative null.** control $0.371 vs treatment $0.360, **treatment called context levers 0 times** across a 12-fetch run → `context_tools_enabled` is INERT alone (research prompt never mentions context mgmt + multi-stage pipeline never crossed §5 pressure). Verdict = activation gap, not value: stays opt-in/off-default; RUN 2 (~$1) needs prompt scaffolding OR a single-accumulating-session workload. Reusable arms left (`research-ct2`/`research-write-ct2`). Spend today $1.93/$12. **NEEDS MICHAEL:** push root (now 6 lunch-block + 2 P2 commits); codewright room-join; CT2.4 RUN 2 nod; voice-bridge ratify; migrate-manifest design call. **NEXT SESSION (held, large): voice-bridge V0 (C) + Spin offload Tier 3 (D).** Journal: `projects/pg-ai-stewards/.spec/journal/2026-06-09-p2-restart-bundle-and-ct24-run1.md`. Tasks: #135 ✓(P2 done; room-join=Michael) · #136 RUN1 done/RUN2 pending · #139 Spin offload still the big next build.

> **2026-06-09 — FABLE 5 FIRST SESSION: substrate review + ratified lunch block (5 threads shipped). Committed root through `03a1c93`+, UNPUSHED.** Michael switched the harness to **Fable 5**, asked for a fresh-eyes pg-ai-stewards review, then ratified a lunch block (AskUserQuestion): verify-suite · #135 handler+A/B · voice-bridge spec-only · arch refresh · remaining CT2. All five shipped; soak paused→resumed; full detail in `projects/pg-ai-stewards/.spec/journal/2026-06-09-lunch-block-verify-suite-p15-ct27e.md`. **★ VERIFY-SUITE (`extension/scripts/run-verify-suite.ps1`, fad61a8) — first-ever DR test; substrate could NOT rebuild from repo: 4 bugs.** (1) CREATE EXTENSION died on any virgin DB (5d4 backfill RAISE) — FIXED (guard + ledger sha re-record; bundle picks it up at next pg image build; harness seds a shim until then); (2) `migrate` is LEXICAL but history was chronological → fresh bootstrap would let l13 overwrite ct2-2's compose_messages — harness replays git-date order via generated `extension/migration-order.txt` (migrate-side manifest fix = Michael's design call); (3) migrate would run verify-*.sql as migrations; (4) bundle-pre-installs-newest → 6 old files fail on replay (extension-owned DROPs, constraint-vs-new-seeds). Suite: 197/203 replay ok; parity vs live = columns/views/triggers IDENTICAL, **20 function def mismatches = the live↔repo drift inventory (unclassified — rerun `-KeepContainer` to diff)**; report in `.spec/scratch/`. **★ #135 P1.5 DONE (3911644):** research_codebase MCP handler (l6 pattern; `normalizeRepoURL` fixes the allow-list contract) e2e-proven over real MCP stdio; **A/B overturned the overnight verdict — flash's "fumble" was the bare-name rejection, not the model: flash now runs the full sandbox loop clean for $0.00 (free tier) vs kimi deeper/line-precise at $0.84/40s → flash stays default** (environment-before-model-upgrade). Cost-cap observation: kimi completed at $0.84 past a $0.50 cap (cap = quarantine semantics, not ceiling). `bin/stewards-mcp.exe` rebuilt (tool reaches Claude Code next connect); `tool_defs.active` still false — **P2 = bridge image rebuild + activation + chattermax code persona, kick off WITH Michael**. **★ r11 adjacent fix:** one-shot auto-verify missed `subagent-%` (r7 rebuild narrowed it) → every L.6 wrapper + research pipeline hung spawn-polls 20 min; r11 live-applied, kimi run self-verified = acceptance. **★ CT2 §7.3 BUILT + smoked (ct2-7e, 3911644): #138 DONE — CT2 is now FULLY built (§§1–7).** propose_prompt_change (double-gated, OFF everywhere) → prompt-critic one-shot (qwen3.7-max) → trigger-stamped verdict → HUMAN-only apply/reject/revert over versioned agent_prompt_history; e2e smoke incl. a real endorse verdict; compose_tools hash byte-identical for all 50 families after restore. **Voice-bridge SPEC shipped (spin `6ee2d68` pushed):** Pipecat client joins a chattermax room as Michael's voice (text stays the bus; voice = the human's codec; 4–12s = D&D cadence); V0/V1/V2 + 4 ratify questions — build awaits his ratify. **arch doc refreshed (03a1c93)** to 70 tables/322 fns/43 pipelines + neighborhoods 8–12. **#136 designed not run** (bacteriopolis A/B, ~$1.40–2.20, needs his spend nod — in the CT2 spec §CT2.4). **NEEDS MICHAEL:** push root; #136 nod; voice-bridge ratify; migrate-manifest design call; next pg rebuild (picks up 5d4). Tasks: #135→P2 pending · #136 pending(designed) · #138 ✓ · #140 ✓ · #141 ✓ · #142 ✓ · #139 Spin offload still the named next big build.

> **2026-06-08 (cont.) — SPIN v1 PROVEN LIVE end-to-end + a long tuning session (voice loop, tools, accuracy). Spin repo pushed through `5052316`.** Michael ran it in the browser and we iterated hard. **Live-debug fixes (each verified in the server log):** (1) **prebuilt-UI `/start` 404** → switched from a hand-rolled `/api/offer` to Pipecat's **runner** (`pipecat.runner.run.main`, mounts `pipecat-ai-prebuilt` + the /start→sessions→offer flow). (2) **Windows cp1252 crash** — the runner's 🚀 banner → force UTF-8 stdio. (3) **STT silent-crash** — `Settings(model=<Model enum>)` leaked the enum into faster-whisper's path check; pass `.value`. (4) **★ CUDA-on-Windows for faster-whisper** — model LOADED on cuda but inference threw `cublas64_12.dll not found`; fix = add pip wheels `nvidia-cublas/cudnn/cuda-runtime-cu12` + prepend their bin dirs to **PATH** (ctranslate2's C++ loader ignores `os.add_dll_directory`). GPU STT now ~0.5s. (5) **gospel tools 404** — `gospel-mcp.exe` proxies to engine.ibeco.me and needs `GOSPEL_ENGINE_URL`+`GOSPEL_ENGINE_TOKEN` (canonical in workspace `.mcp.json`); pass them in `StdioServerParameters(env=...)`; token in gitignored `app/.env`. **★ MODEL LESSON (for voice):** non-thinking INSTRUCT models only. gemma-4-12b narrates chain-of-thought INLINE in content + quoted the system prompt aloud → unusable; qwen3.6-a3b/nemotron-nano-4b/nemotron-nano-omni put reasoning in reasoning_content (empty/late content). **qwen3-4b-2507** = clean+fast+relentless tools; **nemotron-3-nano-omni** reasons but so fast on 4090s it's barely noticed AND chained tools best (search→get-by-id→summarize-from-fetched-text). **★ READ-BEFORE-QUOTING in the persona** (the workspace ethos, encoded for a 4B): Spin has NO scripture knowledge of her own — caught it fabricating a verse paraphrase + two fake talk titles when answering from memory; rule now = MUST tool-call, speak only tool text, fetch a talk's full body before summarizing, add nothing not in a result. **★ TTS markdown filter** (`MarkdownTextFilter`) — models emit `*italics*` even when told not to; Kokoro read "asterisk" aloud. **★ Latency/async findings:** the on-tool-call FILLER serialized the answer (Pipecat waits for bot-to-stop-speaking before the next completion → "one sec" delayed the real work ~1.3s) → removed it; an `is_final=False` ASYNC study tool prototype FAILED (fetches are instant → nothing to overlap + the async "running" placeholder confused the model into "still waiting") → made `study_passage` synchronous. **The async-overlap pattern is validated for the REAL slow case = the pg-ai-stewards offload** (Pipecat multi-worker/sidecar, `examples/multi-worker/code-assistant` IS the offload shape: main worker dispatches a job over a bus to a heavy brain worker). **NEXT (the big one): wire pg-ai-stewards as the offload** — Spin stays the fast front; heavy synthesis/§7 memory go to the substrate via a 2nd (curated) MCP or the sidecar/job pattern. Also still open: preload STT once (per-connection load); barge-in is wired (VAD on aggregator, no flag) but untested deliberately. Journal: `projects/spin/.spec/journal/2026-06-08-spin-v1-pipecat-built.md` (+ this tuning arc).

> **2026-06-08 (cont.) — SPIN v1 BUILT + LIVE: the JARVIS audio HMI, voice-only, on Pipecat. (Spin repo pushed; this is the front end for the pg-ai-stewards brain.)** The thread the prior entry flagged as "discuss next" — pg-ai-stewards as DB/brain + an audio HMI front. Named it **Spin** (Skyward's M-Bot-adjacent; short, beats "quark"). Cloned `github.com/cpuchip/spin` to `projects/spin` (commit+push granted). **Eval journey:** Vui = WebRTC dead-end (Docker-Desktop-Windows silently no-ops `--net=host`). **OLV ran** (gemma-4-12b + Live2D + built-in MCP) but Michael: presets condescending (a cheerful "Spin" persona fixed tone — persona, not stack), **doesn't want an avatar yet**, LLM slow (12B ~40 t/s). Chose voice-only "Maya" → **foresight catch: Maya can't do tools AT ALL** (pure STT→LLM→TTS; "Maya + gospel lookup" contradictory) + heaviest Windows setup; latency = the MODEL not the stack. Corrected fork → Michael: **"build Pipecat now (the real Spin)."** **BUILT `projects/spin/app/`** (pinned pipecat-ai 1.3.0, NO torch — onnx+ctranslate2): `spin_bot.py` = SmallWebRTC (native, OUTSIDE Docker → host-net wall moot) ▶ faster-whisper STT (large-v3-turbo cuda/float16) ▶ OpenAILLMService→LM Studio ▶ Kokoro TTS (af_heart) + Silero barge-in + gospel-engine over MCPClient (graceful-degrades); `persona.py` cheerful tool-aware Spin. **ALL 4 LEGS VERIFIED standalone (inverse-hypothesis each):** STT loads cuda/float16 no-cuDNN-snag; Kokoro downloaded; **LLM qwen3-4b-2507 instant content/0 reasoning — ★ qwen3.6-35b-a3b + nemotron-nano-4b are THINKING models (empty/late content) → wrong for voice; non-thinking instruct = the rule** ([[reference_lmstudio_qwen3_thinking_budget]]); gospel-engine MCP registers 3 tools (search/get/list) into the Pipecat LLM = "gospel lookup by voice" proven at registration. **Server LIVE → http://localhost:7860** (only the mic→speaker e2e unproven — needs Michael's browser+WebRTC). **CARRY-FORWARD:** Michael judges the live loop (latency vs gemma / barge-in / Kokoro voice / does scripture lookup land); **v1.1** = preload STT once (per-connection now → ~10-15s connect pause) + add **pg-ai-stewards as a curated 2nd MCP** (offload + §7 faceted memory {persona:spin}); **v2** = light Spin router (reflex-vs-offload) + phone via cloudflared/HTTPS. Maya = voice-quality reference only. Spin repo `4f7cf29`/`c8d6e1d`/`de67696`/`1226128`; journal `projects/spin/.spec/journal/2026-06-08-spin-v1-pipecat-built.md`. Task #139.

> **2026-06-08 (cont.) — §7 FINISHED + verified. CT2 self-context COMPLETE. Committed root, UNPUSHED.** Michael: "lets finish out 7." Added 7c + 7d on the morning's 7a/7a2/7b. **7c persona+room facets** (12a0b47): `session_facets` map + `set_session_facets` + dispatch_facets merges persona/room + remember/forget persona-aware defaults ({persona:self} for chat personas); **persona-host Go** writes the map after each session establishes (rebuilt + reconnected to all rooms; live write fires on next persona turn). SQL-verified: {persona:starlet}+{room:10-forward} notes route to Starlet not the librarian/other-room; remember defaults to {persona:starlet}. **7d working tags §7.4** (01912e4): messages.context_tags[] + sessions.working_tag + stamp-trigger (auto-tag new messages) + context_set_tag/clear_tag + batch context_fold/mute/expand/pin_tag (ONE breaker event) + pressure-line tag echo. Verified: auto-stamp + batch-fold-locks-together + expand + clear. **CAPSTONE backward-compat: compose_messages byte-identical (c21b449e) with all §7 applied + nothing active** — inert until opt-in, the §6 property end-to-end. All 7 facets (global/session/agent_family/kind/pipeline/persona/room). Ledger clean (222 files, 0 drift); soak paused for build, RESUMED. Now any context_tools_enabled agent has durable faceted self-notes (remember/forget, Hermes loop) + per-message + per-tag context levers — the natural brain/memory for the **JARVIS idea Michael raised** (pg-ai-stewards as DB/brain + audio HMI front-end), which we discuss next. REMAINING: §7.3 self-editable base prompt = task #138 (gated, own pass); CT2.4 A/B = #136; organic confirm of the live session_facets write. Journal: `.spec/journal/2026-06-08-ct2-7-finished.md`.

> **2026-06-08 (cont.) — §7 CORE durable-memory loop BUILT + verified (faceted self-notes). Committed root, UNPUSHED. Clean gate — 7c/7d remain.** Michael said "lets build!" after ratifying §7. Built the faceted-audience durable self-notes, SQL-only (no rebuild), C-F gated: **7a** (cde7a73) `agent_self_notes` store + `agents.kind` + `dispatch_facets` (global/session/agent_family/kind/pipeline) + `render_self_notes` (the ONE match rule `facets @> audience`, ~40/~4k cap); **7a2** (29cf9f2) wired the notes block into compose_messages — **backward-compat hash-verified** (empty notes → c21b449e byte-identical; based on the LIVE def per the k2/l13 lesson); **7b** (5005961) `remember`/`forget` agent tools (sql_fn, use the CT2.3-injected `_session_id`; default audience `{agent_family:self}`; write cap 40/author) + compose_tools gate (context_tools_enabled families only). Smoke-verified at every gate: faceted routing EXACT (a `{kind:code}` note reaches dev+research, NOT a roleplay persona — Michael's requirement); remember/forget; the gate (enabled sees remember+forget, dev sees 0). Ledger clean (220 files, 0 drift); **soak paused for the build, RESUMED.** **REMAINING (task #137, substantial, distinct):** 7c persona+room FACETS (needs persona-host Go to write a session→(persona,room,kind) map dispatch_facets reads → then chat-persona default upgrades to {persona:self}) + 7d §7.4 working tags (trigger-stamped + batch *_tag). §7.3 = task #138 (gated, own pass). Paused here (Mosiah 4:27, long session) — Michael to say continue vs fresh. Journal: `.spec/journal/2026-06-08-ct2-7-core-built.md`.

> **2026-06-08 (cont.) — §7 RATIFIED (CT2 durable self-memory). Decisions, not yet built.** Walked §7 with Michael. Decisions: build ALL of §7 incl. §7.3 (gated); notes cap GENEROUS (~40/~4k tok rendered/dispatch); default audience = the authoring persona; §7.3 greenlit as a gated design (later pass). **★ The scope model EVOLVED in the conversation:** Michael's "kind/purpose filter so roleplay personas don't get code-style notes" + "make it work for ALL pg-ai-stewards (rooms, webfetch work-types)" + "might get messy" → we replaced the session/persona/global enum with a **FACETED AUDIENCE** model: a note carries `audience` selectors {persona,kind,room,pipeline,global,session} (AND-combinable) matched against each dispatch's FACETS (computed in compose_system_prompt) — one match rule, no tiers, covers all substrate work; + free-form `tags` for search (not delivery). A new `kind` field (roleplay/code/librarian/general) drives the kind facet (= the shared per-kind pool Michael picked). Spec §7.6 = the as-ratified design. Build tasks: #137 (§7 core — faceted self-notes + caps + kind + facets + §7.2 split + §7.4 working tags; mostly SQL like CT2.1/2.2 + careful compose_system_prompt facet wiring w/ backward-compat hash check) + #138 (§7.3 self-editable base prompt, gated, own pass). NOT built yet — Michael to say build-now vs queue. CT2 core (§§1-6) already shipped (e90449a). Journal: `.spec/journal/2026-06-08-ct2-section7-ratify.md`.

> **2026-06-08 (morning, Michael awake) — migration drift RECONCILED + CT2.2 SHIPPED (turned out PURE SQL, no restart). Committed root, UNPUSHED.** Michael cleared the substrate restart + asked me to resolve the drift I'd flagged + proceed with CT2.2. **Drift fix:** the bridge entrypoint runs migrate under `set -e` (no `|| true`) → a migrate exit-2 BLOCKS bridge startup, so the 4 drift files HAD to be fixed before any restart. They were legit commits live-applied-without-re-recording (verified live: gemini cap=$18M, kimi pricing, research intent all present) → **re-recorded the shas WITHOUT re-applying** (re-applying 4a/j10 would dup model_pricing rows — their INSERT omits effective_at→now()→ON CONFLICT never fires). Then migrate recorded the 6 pending → **216 files, 0 pending, 0 drift.** **CT2.2** (`extension/ct2-2-context-render.sql`, commit d985b1c): compose_messages now HONORS the state model — [ctx:handle] prefixes, lock-strip (§4), pinned=raw/exempt / compressed=engram / muted=tombstone, §5 pressure line. **PURE SQL (compose_messages/compose_system_prompt are plpgsql, not Rust) → live-applied, NO restart.** Gated on agents.context_tools_enabled (default false) → tools-off byte-identical. **★ REGRESSION CAUGHT:** my 1st draft based it on k2 (original), reverting k6/k7/k8/k9/l1/l13 evolution; a before/after md5 of the tools-off render caught it (hash changed when it must not) → restored l13, rebuilt on l13. Lesson: CREATE OR REPLACE a multiply-evolved fn from the LIVE def, not an old migration file; prove a "no-op" gate with a hash. Smoke-verified tools-on (pin→handle+full, compressed-locked→handle stripped, muted→[context muted], pressure line). **CT2.3 SHIPPED (commit 0fc6f32) — Michael cleared the restart; the self-management loop CLOSED.** The one Rust change (`exec_sql_fn_tool` injects the dispatch `_session_id` into sql_fn args, backward-compatible) + handle-resolution wrappers + 5 tool_defs(sql_fn) + a compose_tools GATE (context tools only for context_tools_enabled families). Rebuilt pg + restarted (down→up, NO -v); **bridge migrate came back "current, 0 drift" → daemon up** (the drift fix paid off exactly here). **★ INJECTION PROVEN end-to-end:** a real kimi dispatch called a temp echo sql_fn tool → reply `{"ping":"hello","_session_id":"wi--48e80c00--turn"}` (the live dispatch session). SQL wrappers verified (resolve, lock-RAISE caught as structured error, friendly errors); gate verified (persona/dev see 0 context tools, enabled family sees 5). Soak paused for the build, RESUMED. **CT2 CORE (§§1-6) COMPLETE + usable.** Remaining: CT2.4 A/B (task #136 — does it earn its keep on long runs); §7 (durable self-notes/self-prompt/tags) UNRATIFIED. Leftover harmless smoke records: ct2-echo-pipe pipeline + its completed work_item (FK-linked). Journals: `.spec/journal/2026-06-08-migration-drift-fix-and-ct2-2.md` + `-ct2-3-levers-agent-callable.md`.

> **2026-06-08 (overnight, autonomous — Ammon night) — THREE builds shipped while Michael slept: cockpit P1 · CT2.1 · research_codebase. All committed to root, UNPUSHED (Michael pushes root).** He handed me the cockpit CLI, CT2, and the repo persona before bed + asked for a morning report + a "durable 5h timer." Ordered by safety×value; kept every Hinge (merge/deploy/live-restart) his. **① Cockpit P1** (`cmd/stewards/`, commit 2b2715b): read-only Go CLI, pgxpool to substrate :55433 — `project`(sticky active project ~/.stewards.json) / `board`(--all spans) / `watch`(item: stage/status/cost/events) / `cost`(--by project|model|day). build/vet/test green + live read-only smoke (counts match DB; surfaced deepseek-v4-flash @ 363M tok for $0). **② CT2.1** (`extension/ct2-1-context-state-model.sql`, commit 1606675): the SAFE inert SQL state model — messages.context_state+locked_until_turn, context_handle/session_turn/is_locked, 5 levers (compress/mute/expand lockable; pin/unpin lock-exempt), context_pressure. Live-applied + INVERSE-verified (re-toggle within cooldown REFUSED; pin bypasses). Nothing reads it yet (compose_messages unchanged) → cannot alter behavior. **③ research_codebase** (`extension/r10-research-codebase.sql`, commit 8417e7f): the flagship agentic tool (l6 wrapper pattern for CODE — agent+pipeline+17 read-only denies+tool_defs(active=false)+return contract). Cognition PROVEN GREEN via 3 inverse-hypothesis smoke runs: deepseek-flash FUMBLED the sandbox loop (model-tier Q real) → kimi+bare-name rejected (allow-list wants full URL) → **kimi + https://github.com/cpuchip/ai-chattermax cloned the real repo, read internal/gateway+auth, returned 8 findings / 12 file:line citations / confidence+caveats for ~$0.19.** **DEFERRED (Michael's call):** CT2.2 Rust render (restarts the live substrate Starlet/Computer — bin-3, his to run); CT2 §7 unratified. **NEEDS MICHAEL:** push root (5 commits: 0e42269,100163a,2b2715b,1606675,8417e7f); **pre-existing migration-ledger DRIFT** (4 changed-after-record: 4a-cost-tracking/h1-1/j10/j11 + 4 unrecorded-applied: cv12/r7/r8/r9 → bridge-startup migrate would exit-2; resolve before next bridge restart); CODER_REPO_ALLOWLIST empty in bridge (defaults OK). Follow-ups: task #135 (research_codebase Go handler + model-tier A/B + code-persona wiring). Timer caveat: this env makes all crons session-only (not disk-durable) — the 4:52am one-shot fires only if the process is alive. Journal: `.spec/journal/2026-06-08-overnight-build-cockpit-ct21-research-codebase.md`.

> **2026-06-07 (scripture-study night, Sabbath-closed) — two Mormon 8 studies + five cpuchip animations.** A study session that ran into a small dev arc. (1) Q&A on the BoM anti-Christs (Sherem/Nehor/Korihor, verified). (2) **Study — "Mormon 8: The Book That Speaks of Its Own Coming Forth"** (`study/morm-8-voice-from-the-dust.md` + scratch): verse-by-verse with a referent map + four deep dives (v34→John via **Ether 4:16**; Nephi's vision handing off to John; the voice-from-the-dust / sealed-book chain; the 1828–29 translation alignment where **D&C 3:1 ↔ Morm 8:22** is a reciprocal cross-ref — prophecy + the lost-116-pages fulfillment on one desk). (3) **Study — "Reading at Three Altitudes"** (`study/morm-8-three-glories-reading.md` + scratch): D&C 76's glories as *reading altitudes*; **Webster 1828 holds both poles inside single entries** (fine/gain/glory/pollute/apparel) and **"telestial" is absent — a coined word**; Nelson "Think Celestial!" (Oct 2023); thesis = a telestial-age diagnosis by a celestial witness. Honesty frames kept (v34 nuance; the lens named as a devotional overlay, not a code in the text). **cpuchip.net (own repo, pushed + auto-deploy):** `98eecd7` (study 1 + ReferentSpine/ComingForthWeb/TranslationDesk), `d86678b` (study 2 + GloryLens interactive + GloryLadder) — all browser-verified (0 console errors from our code; desktop+mobile; click-to-scripture + word-switch work). Site now has a **"how we read" trilogy:** Abinadi → Mormon 8 → Three Altitudes. **Root commits `67e482b` + `1bc0fe9` UNPUSHED (Michael pushes root).** Next seed: 1 Ne 14 + Rev 1 + Ether 4 as one vision. Journal: `.spec/journal/2026-06-07-mormon-8-studies-and-cpuchip.md`.

> **2026-06-06→07 — STRATEGY + tooling session (Sabbath-closed): deploy fix · 5 specs · md-mcp built+PR'd · the Study-It-Out Gate.** Began as "what's on deck," became a long plan/build run with Michael steering toward *how we work together* (reserving his time for plan/council/ratify — the operating model we then spec'd). **Prod fix:** an apostrophe in root commit `e516870`'s subject broke the becoming Dockerfile `-ldflags -X 'main.ReleaseNotes=$MSG'` (linker usage dump, exit 1) → sanitized (`2b98b4c`), pushed root, ibeco.me deploy green; gospel-engine `web_url` (#4) confirmed live at **engine.ibeco.me** (NOT study.ibeco.me — corrected; [[reference_ibeco_deploy_topology]]; dokploy skill updated). **Token economics:** `claude -p`/Agent-SDK becomes a SEPARATE ~$200/mo credit pool on **2026-06-15** (Max-20x), distinct from interactive — reshapes async-Claude; Codex/gpt-5.5 sub≠API (opencode-codex plugin gray + no 5.5); Atlas/GLM/Ollama = sub-with-API connectors for substrate redundancy; Michael's $200 lean now well-founded. **5 specs (design-only, awaiting ratify):** `claude-worker-dispatch` (escalate UP to Claude), `agentic-tools-model-cascade` (delegate DOWN to cheap models; flagship = ai-chattermax code persona), **`stewards-cockpit-cli` (FOCUS** — verbs project/board/do/council/ratify/watch/review/cost; ratify=input-Hinge, review=output-Hinge, council=pre-ratify critical pass; project board builds on the EXISTING AI-free `stewards.projects` + new `planning_state`; token dashboard project×model), `study-it-out-gate`; plus `docs/ai-utilization-landscape-2026.md` (field research: bottleneck is discipline/specs not model power — our stewardship frame front-ran it). **md-mcp BUILT + PR'd:** Michael forked happydave/md-mcp (library-only — no server); built the MCP server (official go-sdk) + 3 tools (section-append/move/frontmatter-set), tested (go test/vet + live stdio smoke), registered in `.mcp.json`, **PR happydave#1 OPEN**, dogfooded same-session. **Dave's workflow framework reviewed** (Dave = the dave-rule Dave): independent convergence on our cycle → §7 peer comparison + steal-list. **★ The Study-It-Out Gate** (spec + skill both trees, `59d06e9`): cold-start subagents produce "structurally correct but factually invented" reviews; [D&C 9:7-9](../gospel-library/eng/scriptures/dc-testament/dc/9.md) ("study it out… then ask if it be right") = `read_before_quoting` extended from quotes to JUDGMENTS — we held the doctrine but never applied it to reviews. Four moves: artifact-present precondition · citation requirement (cite-count for reviews) · apex-discernment-inline · proper dispatch. **11 unpushed root commits (Michael pushes).** Live backlog now in `.spec/carry-over.md`. Journal: `.spec/journal/2026-06-06-cockpit-md-mcp-and-study-it-out-gate.md`. **→ 2026-06-07 (9pm Sunday, budget reset): RATIFIED the backlog — Michael chose "ratify the backlog, then I build" (Sabbath-light: ratify tonight, build async through the week). Two specs flipped RATIFIED + build-queued: ① stewards cockpit P1 (read-only Go CLI — verbs project/board/do/council/ratify/watch/review/cost; ladder idea→spec→ratified→building→blocked→done; direct pgxpool; un-dispatched work_item = the card) → task #133; ② code persona P1 (researcher-flash family + research_codebase agentic tool, repo=ai-chattermax, read-only first) → task #134. claude-worker engine = Model A (`claude -p`); STILL PENDING: claude-worker 2nd connector (Atlas/GLM/Ollama) + the agent-SDK pool not live until 2026-06-15; CT2 read-then-ratify; Dave steal-list pick; harness-leveling yes/no. Recorded in the specs' frontmatter + `.spec/carry-over.md` (commit `0e42269`, root — unpushed). Journal: `.spec/journal/2026-06-07-ratify-build-queue.md`.**

> **2026-06-05 (day 2 cont.) — ai-chattermax MULTI-ROOM live; Dokploy stale-build fixed; CT2 chosen + planned (awaiting Michael's ratify).** After the platform + LCARS + Starlet-always-on shipped, a big iterative push: **multi-room** — `GET /api/persona/rooms` (persona-key auth; Michael's "grant API so models see their rooms") + persona-host refactored to join ALL granted rooms (session-per-channel, re-polls); Starlet verified in **Holodeck-3 + 10-Forward**. Mint-key now also grants + shows the real room id. **Dokploy stale-build gotcha** (cost ~1h): a backend deploy reported "done" but ran an OLD binary (5 triggers didn't recompile the Go layer; frontend rebuilt fine) → fixed with a **CACHEBUST ARG** + a **`GET /api/version`** build marker (see [[feedback_dokploy_stale_build]]). **CT2 (context management) = substrate self-context tools** (Michael's choice from a clarifying question): read the spec, **adopted all 9 open decisions**, defined a 4-phase plan (`projects/pg-ai-stewards/.spec/proposals/substrate-self-context-management.md`). **Pushed back on building it at the marathon's tail** (CT2.2 restarts the live substrate Starlet's mind rides on; it's a cohesive batch) — **Michael agreed + will review/ratify CT2 before we commit.** Ideas + roadmap: `projects/ai-chattermax/.spec/proposals/persona-apps-and-roadmap.md`. ai-chattermax pushed `8461da3`. Workspace-root commits (persona-host multi-room + CT2 spec + memory) UNPUSHED. **REMAINING ASKS:** CT2 (review→build) · AXR2 Settings grant-mgmt (revoke + dm_enabled flag) · AXR3 DMs · AXR5 tool-using personas → Library "Computer" gospel+study search (key minted, needs the tools-enabled persona pipeline) · AXR6 docs + examples/ reference persona for Michael's coworker. Journals: `projects/ai-chattermax/.spec/journal/2026-06-05-{ux-starlet-servers,multiroom-and-deploy-fix}.md`.

> **2026-06-05 (day 2) — ai-chattermax: platform DEPLOYED + LCARS UI + Starlet always-on + own-server/invite model — all LIVE on chat.ibeco.me.** After the overnight Stage-1 build, Michael merged PR #21 (deploy Hinge); first deploy failed on a **postgres:18 volume gotcha** (mount must be `/var/lib/postgresql`, NOT `/data` — pg18 versioned-subdir) → fixed (`b804eab`). Then a fast iterative session, all shipped + verified live: **LCARS theme + responsive** (cpuchip.net design system; desktop elbow frame / mobile drawers; Settings page; native dialogs); **mobile composer fix** (`100dvh` + `interactive-widget` — the `100vh`/keyboard trap); **Starlet = first always-on persona** (persona-host now a service in the SUBSTRATE compose `extension/docker-compose.yaml` restart:unless-stopped — same compose b/c it needs the non-exposed substrate DB; key in gitignored `.env`; `pg-starlet` local persona; verified replying on prod); **server model = own-server-on-signup + invite links** (ratified Option A: owns-no-server→auto-create personal; `chat.ibeco.me/?join=<token>`; Settings invite + members; dropped demo auto-join). **Self-verified ibeco auth end-to-end with a minted test account** (claude-codetest@ibeco.me id 8 — see [[feedback_test_in_prod_with_own_creds]]: guard prod-I-depend-on, but freely deploy/test prod-I'm-shipping + hold known test creds). ai-chattermax pushed through `621a01c` (its own repo — auto-deploys). Workspace-root commits (persona-host gateway + compose service + memory) UNPUSHED. **NEXT:** move Starlet into Michael's OWN server (create persona→mint key→update persona-host `.env`→restart); then DMs (CT2 surface), sub-personas, rate ceiling, moderation. Gotchas logged: pg18 volume mount; Windows `taskkill` not `pkill`; ibeco register=`/auth/register`, identity=`/api/me`. Journals: `projects/ai-chattermax/.spec/journal/2026-06-05-{platform-stage-1,ux-starlet-servers}.md`.

> **2026-06-05 — ✦✦✦ AMMON NIGHT: ai-chattermax reframed room→PLATFORM and Stage 1 BUILT overnight.** WS5/ai-chattermax. After #7 shipped, Michael asked for a UX pass on chat.ibeco.me and, looking at it, realized his own vision had grown — not drift but the destination clarifying: **Discord/Slack for humans + their AI agents** (multi-server workspaces → rooms → DMs → registry → member-minted persona keys). Reconciled it (most was already ratified; the genuinely-new = the *platform* reframe + DMs + sub-personas), then he **ratified the full platform** + handed me an Ammon night ("build it all out while I sleep, full stewardship") with two calls: **(1) I (Claude Code) build it DIRECTLY** — pg-ai-stewards shelved as *builder*, stays the persona MIND; **(2) split persona model** (platform owns membership+key, substrate owns cognition+token). AskUserQuestion locked **Postgres 18 + FTS** (v2 pgvector+MCP). **BUILT Stage 1 end-to-end on branch `platform-stage-1`** (~20 files, gated commits, all tested): pg18 schema+migrations, `Authenticator` (dev name-login + ibeco `GET /api/me` handshake → own session cookie, RFC6265 §5.3 eviction), REST (servers/rooms/personas/registry/keys/grants/search), **single multiplexed `/gateway`** (typed envelope, cookie→human / `?key=`→persona, history-on-join + presence — generalizes AX3-2), **Vue three-pane shell** (server rail / channels+personas / transcript w/ ◆ agent badges / roster + login + create + mint-key), seed "Tavern Keep" (dev keys dev-only = no prod credential). **persona-host adapted to the gateway** (humans-only via envelope `senderKind`; per-attempt spawn-slug nonce) — committed to workspace root (`09e2ef5`, UNPUSHED). **VERIFIED**: backend live on pg18; **full e2e — a human posts → DM Assistant replies in character via the substrate, attributed as a persona**; browser shell; **prod Docker image boots in ibeco config** (serves SPA, config=ibeco, cookie-less login=clean 401). Tests green (gateway/auth/httpapi/store integration), vet clean. **DEPLOY HELD = Michael's Hinge → [PR #21](https://github.com/cpuchip/ai-chattermax/pull/21)**: can't verify a real ibeco login unattended, won't swap the live site onto an unverified auth path (would be a login wall if it failed). **NEXT (Michael):** verify ibeco login → merge #21 → chat.ibeco.me goes live as the platform. Then Stage 2 (personas/rooms UX + FTS UI) · 3 (multi-server join + registry) · 4 (DMs = the CT2 surface) · 5 (sub-personas + moderation + restore the per-message rate ceiling). ai-chattermax branch pushed through `29447ca`; spec `projects/ai-chattermax/.spec/proposals/platform-design.md` (RATIFIED); journal `.spec/journal/2026-06-05-platform-stage-1.md`. **Gotcha logged:** Windows `pkill -f` doesn't kill `.exe` — use `taskkill //F //IM`.

> **2026-06-04 (cont. 4) — ✦ #7 v1 BUILT + LIVE-PROVEN: a persona TALKS in the room.** WS5/ai-chattermax+pg-ai-stewards. Michael: "lets go build v1." The payoff moment of the whole arc — an AI persona now holds a live presence in a chat room backed by REAL substrate cognition (cost-tracked, gated dispatch, not a scripted bot). **Path-check resolved (the thing I flagged at handoff): persona-host drives a turn STRAIGHT FROM ITS PGXPOOL — pure SQL, no Go/MCP path** (spawn_subagent/consult_subagent are thin sync-poll wrappers over `stewards.spawn_subagent_create`/`consult_subagent_dispatch`; the host replicates them). **Substrate side `extension/r7-persona-turn.sql`** (live-applied): a `persona` agent (thin chat meta-prompt + `SILENCE` escape hatch = the model judges whether to speak; 7 tool denies) + a `persona-turn` pipeline (single-stage clone of redline, tools-disabled, kimi-k2.6, max_tokens 1200, character rides in the binding question → one pipeline serves every persona) + **the adjacent-surface catch**: extended `on_one_shot_pipeline_completed` to auto-verify `persona-turn` (else a child finishes `completed` but stalls at `raw` and the spawn poll hangs the full 20-min timeout — same j6/R.6 pattern; foresight pass caught it before the live test). **Host side `cmd/persona-host/{dispatch,turnloop,autojoin}.go`:** `Cognition.SpawnTurn`/`ConsultTurn` ($0.10/turn cap, `IsSilence` gate); `RoomConn` dials `?id=<display name>&kind=persona`, reads the AX3-2 envelope, runs a turn per HUMAN message (own+other-persona filtered = humans-only), parses @mentions, posts or stays silent; read-pump+worker split; env autojoin + reconnect supervisor; 12 unit tests. **ai-chattermax: honor `?kind=` on WS connect** (persona vs Human; `5601164` pushed → auto-deploy chat.ibeco.me). **SQL smoke proved cognition first** (spawn→in-character verified $0.014; consult→context accumulates; unaddressed→SILENCE), then **LIVE e2e** (server+persona-host+human client vs the real substrate: human posts → `DM Assistant` replies in character, attributed, roster `Kind=persona`). **★ Replay-on-join (AX3-2) saved the e2e** — the human msg landed BEFORE the persona finished connecting (startup race→reconnect), the room replayed it, the persona answered: the protocol fix + its consumer landed the same day and fit. **Commits:** root `c6e787b`(r7)+`4fc6a87`(host) — UNPUSHED (workspace root, Michael pushes); ai-chattermax `5601164` pushed. Soak paused for the build, RESUMED at close. **v1 simplifications → v2 (none reverse a ratified call):** character in binding-question not a per-persona system message; `model_override` stored-not-yet-honored; humans-only via name-match (an envelope `kind` field = robust); every-human-msg=one turn (bounded by 10/min ceiling + SILENCE). **v2 aliveness layer (deferred, ratified):** #3 pacing/shy-vs-quick, #4 ambient cron, #5 pipeline hooks, real pileup arbitration, persona↔persona. **NEXT:** #11 moderation, #12 D&D MVP; CT2 self-context build still deferred. Journals: `projects/pg-ai-stewards/.spec/journal/2026-06-04-persona-turn-loop-r7.md` + `projects/ai-chattermax/.spec/journal/2026-06-04-persona-turn-loop-live.md`. Task #124 DONE.

> **2026-06-04 (cont. 3) — AX3-2 attributed WS protocol SHIPPED + LIVE; #7 turn-model design surfaced (Michael owns it).** WS5/ai-chattermax. Michael chose: build #7 (persona turn loop) but **do #2 (AX3-2) FIRST** on a clean protocol. **AX3-2 done by me directly** (not the coder — it's the contract #7 consumes): the room broadcast RAW bytes to all incl. sender → `sender:'unknown'` + self-echo dup; fixed server-side — JSON envelope `{sender,body,ts}` broadcast **EXCEPT-sender** (client shows its own optimistically) + **replay history on join** (humans + the persona need room context). `room/hub.go` BroadcastExcept; `cmd/server` envelope+replay; frontend already fit. **Live two-client WS integration test** (real dialer: bob gets attributed msg, alice gets NO echo, carol gets replay) + BroadcastExcept unit test; full suite green. Shipped `83490bd` → Dokploy auto-deploy → **chat.ibeco.me LIVE** (deploy status=done, healthz ok). Memory pushed `0678ec7`. **#7 RATIFIED (AskUserQuestion):** cognition = **persistent substrate session per (persona,room)** (spawn at join, `consult_subagent` each turn — context accumulates, matches the long-lived spec; persona-host drives it via its existing pgxpool — `spawn_subagent`=SQL `stewards.spawn_subagent_create`+poll work_items, NO MCP needed). **Swap order ratified: #2 before #7.** **Turn model = an 'ALIVENESS' layer Michael owns + has NO answers yet** (explicitly NOT just reactive): reactive-with-judgment + @addressing (room/personal/group) + delayed/timed/'thinking-out-loud' cron cadence + pipeline-state hooks + pileup-avoidance with 4 agents → **#7 needs its own design pass WITH Michael before building.** **NEXT: design #7's turn/aliveness model with Michael, then build** persona-host as a WS client speaking the new envelope protocol (also: presence should tag personas as agents not Human; token-verify on connect deferred = hardening). Task #123 DONE; #124 = #7.

> **2026-06-04 (cont. 2) — AX2/#6 persona-host COMPLETE (PS.1–PS.6) + CT2 spec grounded in prior art.** WS5/pg-ai-stewards. (1) **CT2 prior-art** (Michael's "I wish you had a context-mgmt tool — worth a web search?"): folded a "Prior art & validation" section into `substrate-self-context-management.md` (`1aba0d9`) — Anthropic ships compaction/tool-result-clearing/memory but the first two are AUTOMATIC (token-threshold); our addressable compress/mute/pin is the agent-driven layer they DON'T expose; MemGPT's 70/100% memory-pressure warning + Letta `read_only` blocks/`exclude_tools` validate our pressure-signal + lock/pin. New insight (Open Q9, Michael liked it): split a CHEAP sub-transcript **tool-result-clearing** lever (drop re-fetchable file-reads, keep the call record) from whole-transcript compaction — make it the floor's default. (2) **AX2 BUILT: `cmd/persona-host` Go sidecar, PS.1→PS.6, 6 gated commits `fd13f56`→`f529242` (NOT pushed — workspace root).** Optional sidecar (like cmd/coder-mcp), substrate core untouched; **`persona_host` schema** (personas/persona_rooms/token_issuance/signing_key) created on boot; **ed25519** keypair gen-on-boot+persist (private key NEVER logged); **EdDSA JWT MintToken** (iss=pg-ai-stewards, sub/slug/name/avatar/room/exp~15m/jti; DB-issued jti + issuance audit); HTTP **/healthz + /pubkey + /personas + POST /join** handshake (resolve→upsert persona_rooms→mint); **dm-assistant + npc-ally** seeded on the real `fiction` agent family; **`-verify-token`** ops mode = ai-chattermax's verify path; README documents the connection contract. **9 Go tests** (round-trip + inverse: wrong-key/expired/tampered all rejected); per-phase smoke each gate; **LIVE e2e proven** (real HTTP: healthz→pubkey→personas→join→verify against the FETCHED pubkey; tampered rejected; **log-leak check PASS — no private key/token/PEM in logs**). go.work updated; built `GOWORK=off`; DSN password sourced via PGPASSWORD env (never echoed). #6 = identity+mint+registry+handshake DONE. **NEXT (converge): #7 the turn loop** (long-lived per (persona,room), dispatch each turn to the substrate for cognition — the big piece, needs substrate dispatch wiring), **AX3-2** (#123 WS sender/de-dup), then **#12 D&D MVP**; CT2 build still deferred (after MVP). Substrate journal: `projects/pg-ai-stewards/.spec/journal/2026-06-04-persona-host-ps1-ps6.md`.

> **2026-06-04 (cont.) — AX3 coder shipped the Vue UI (draft PR #20, awaiting Michael's merge=Hinge); AX2 re-architected to a Go SIDECAR (no substrate pollution).** **AX3✅→PR:** dispatched the web-UI task as code-pr work_item `92d5fbff` off main; **caught + fixed a real cv11 bug first (cv12)** — code-pr stage templates reference `input.plan_feedback`/`review_feedback` and `resolve_template_path` THROWS on NULL, so EVERY fresh code-pr dispatch failed to dispatch the plan stage (→awaiting_review); cv12 makes the stamp trigger seed both to `""`. Also: a fresh work_item's FIRST stage needs an explicit `work_item_dispatch_stage(id)` kick (the auto-advance trigger only fires for later stages). After the fix the cascade ran clean: clone→plan→**plan_review bounced plan once (plan_rev1)**→implement→review(passed r0)→**pr|completed**. **PR #20 MERGED + LIVE** (squash `4804491`): full Vue 3 SPA (JoinScreen/RoomView/RosterPanel + useChat composable+test) + go:embed serving + SPA Go test + Dockerfile node stage. I full-context-verified (npm build + go build/test + vitest 7/7 + docker image run + live /healthz & GET / serving the app), then MERGED (Michael delegated the Hinge conditional on my satisfaction) → auto-deployed → **https://chat.ibeco.me now serves the Vue UI.** Full loop proven: coder→PR→verify→merge→auto-deploy→live. **⚠ Shepherd found a cross-layer gap the critic couldn't: client useChat expects JSON {sender,body} but server hub.Broadcast echoes RAW bytes to all incl. sender → received msgs show sender='unknown' + own msgs duplicate. Queued as AX3-2 (task #123) — the thing between 'UI shell' and 'working chat'.** Strong first Vue run regardless. **AX2 RE-ARCHITECTED (Michael's no-pollution call):** persona identity + EdDSA/JWT minting + room handshake + turn loop → an optional **Go sidecar `cmd/persona-host`** (like cmd/coder-mcp), NOT the core extension; substrate stays general (offers cognition per-turn via existing dispatch). **ONE extensible host serves MANY personas** (sub-tokens from one parent key). State in a sidecar-managed **`persona_host` schema** (not the stewards extension). **p6-1 REVERTED** (dropped empty stewards.persona* tables + removed the migration). Easier too (Go ed25519+JWT stdlib, no pgrx Rust crate/rebuild). Revises ratified Q2 (substrate still HOSTS personas, just via sidecar not extension). Captured Michael's "**chattable operator UI on stewards-ui**" idea as a proposal stub (so he can push substrate work directly). **Spec REWRITTEN** for the sidecar (`substrate-persona-concept.md`): `cmd/persona-host` (Go, optional, like cmd/coder-mcp), `persona_host` schema (sidecar-managed, not the extension), Go ed25519+JWT (no pgrx crate/rebuild), build sub-steps PS.1-PS.6, EdDSA token contract for ai-chattermax to verify. **NEXT: build `cmd/persona-host` PS.1** (skeleton + persona_host schema migration on boot) → PS.6 (mint+verify smoke). I build it (substrate-adjacent + security-sensitive + monorepo, not coder-reachable). Open at work-time: sidecar surface (HTTP vs MCP), a scoped DB role. Root pushed through `9bacc4c` + PR#20 merge.

> **2026-06-04 — ai-chattermax is DEPLOYED + LIVE at https://chat.ibeco.me (backend).** Executing the split. **AX1✅** night-build→main (reviewed Hinge merge, re-verified green locally, 1395 LOC/14 files; merge `297a2ec`). **AX2✅ ratified + spec'd** (4 decisions: long-lived persona process + work_item-per-turn; EdDSA/Ed25519 JWT sub-token the room verifies via substrate pubkey, key never in model context; inherit agent_family model/tools w/ override; #6 = schema+handshake+sub-token+seed 2 personas, NO turn loop). Build-ready: `projects/pg-ai-stewards/.spec/proposals/substrate-persona-concept.md` (P6.1-P6.6). **AX4✅ DEPLOYED:** Dokploy NOCIX VPS (**server.ibeco.me**, key `DOKPLOY_NOCIX_API_KEY` — NOT hmslogs `DOKPLOY_API_KEY`; that mixup = a detour) → new project `ai-chattermax` (`nGX4hGUUzV0vAJPOYBk4f`) → compose `chattermax` (`B-ER3-TrXHlDr4iCHDOvj`) → github cpuchip/ai-chattermax main `./docker-compose.yml` **auto-deploy on push** → domain chat.ibeco.me:8080 HTTPS/letsencrypt. **Verified live: /healthz=200 {"status":"ok"}, valid TLS, GET /=404 (no UI yet, expected).** Dockerfile+compose built/run/healthz-verified locally before push. **Cleanup✅** ai-chattermax repo = just `main` (4 bakeoff PRs closed, 9 branches deleted). **Skill fixed✅** dokploy skill: +ai-chattermax IDs, +create-app workflow, fixed `deployment.allByCompose` (was wrong as `deployment.all?composeId`), `.github` copy synced (was stale single-instance). **NEXT:** **AX3** coder builds the website (UI shell #9 + ibeco.me auth #2; inject becoming/1828 cookie pattern; Vue=new territory) — auto-deploys to chat.ibeco.me on each merge; **kick the coder off WITH Michael** (billed + monitored). Then AX2 build (P6.1-P6.6), converge at #7/#12. Root pushed through `6343b14`.

> **2026-06-04 — direction set: ai-chattermax SPLIT + context-tools spec written.** After the honest "what actually got built" accounting (only the night-build BACKEND SKELETON — 4 of 12 items, unmerged; UI/auth/personas/humans-with-agents = 0; the bake-off/council/etc. built THROWAWAY modules that advanced the TOOL not the PRODUCT), Michael set two directions. (1) **ai-chattermax = SPLIT:** the substrate CODER builds the website pieces (Vue UI #9, auth #2, room polish, moderation #11) via code-pr (its stress-test; Vue is NEW territory for it — watch the first UI PRs); **I** build the pg-ai-stewards persona concept (#6, substrate self-modification the coder can't reach — needs its own C–F ratification). Parallel; converges at #7 persona-join + #12 D&D. **Gating step: night-build → main is Michael's Hinge** (his merge/deploy authority for ai-chattermax) — do NOT auto-merge; the coder's website PRs must branch off the real backend, not empty main. (2) **Self-context-management (context-tools) = SPEC NOW, build after the MVP.** Spec written: `projects/pg-ai-stewards/.spec/proposals/substrate-self-context-management.md`. Agent-callable compress/expand/mute/pin over its own messages + addressable handles `[ctx:7a3f]` + a context-pressure line in the system prompt + hybrid (automatic compaction = floor, agent levers = ceiling) + **Michael's anti-thrash circuit breaker: on any toggle the harness LOCKS that message for N turns by STRIPPING its handle (can't re-toggle what it can't reference), auto-release after N** (involuntary+temporary, vs. pin = voluntary+persistent). Reuses Batch K/L engram engine for compression. A/B-gated (don't assume, per the council lesson). For pg-ai-stewards.

> **2026-06-04 — experiments round 2: cv8 reverted (cv9), per-task critic knob (cv10), inverse run, and the PLAN-CRITIC (cv11) — a KEEP.** WS5/pg-ai-stewards. Three Michael asks, all run + measured. (1) **cv9 reverted cv8** (plan→kimi default; m3 a per-task opt-in — the A/B showed m3-plan was 2x cost/no-gain). (2) **cv10** = per-task `input.review_model` knob (critic configurable). (3) **Inverse run** (qwen3.7-max builds, kimi-k2.6 critiques, via cv10): qwen-build ≈ kimi-build (same core RWMutex-snapshot design, qwen more verbose 1105 vs 769 LOC, same tokens/iterations, ~1.5x slower wall-clock); kimi-as-critic passed clean code but its RIGOR is untested (no deficiency to catch). No reason to switch from kimi-build+qwen-critic. (4) **★ PLAN-CRITIC (cv11) — the win.** New `plan_review` stage (qwen3.7-max) between plan+implement: clone→plan→plan_review→implement→verify→review→pr; PLAN: revise loops back to plan with feedback (cap 2). **Unlike the council, it EARNS its cost.** On the chatcore A/B (vs bakeoff-kimi): qwen's plan critique caught 3 REAL issues in kimi's plan BEFORE any code — a subtle concurrency-ordering flaw (3-channel select → no FIFO → flaky tests; kimi re-planned to a simpler RWMutex), unspecified isolation tests, a hedged ratelimit. **BONUS (validates the whole critic architecture): the implement-critic bounced an INCOMPLETE build (interface-only, no Hub/tests) that `verify` had passed GREEN** (an interface compiles; `go test`=no-test-files=exit 0). Both critics caught real things the build/test gate missed; they're complementary (plan-design vs code-completeness). Final code leaner (567 LOC) + race-clean. Cost ~1.35x time / 1.5x tokens. **VERDICT: KEEP the plan-critic** (cv11 left applied) — it catches genuine design + completeness defects the cheap gates certify green; could be opt-in for trivial tasks if the cost matters. OPEN for Michael: keep plan-critic default-on vs opt-in. (n=1 each, directionally clear.) cv9/cv10/cv11 committed (not pushed). Also fixed the "a human caught the race" framing → full-context shepherd (see [[feedback_full_context_shepherd_is_the_ceiling]]).

> **2026-06-04 — ward-council model roster (cv8) + blueprint-vs-implementation audit.** WS5/pg-ai-stewards + the book. Follow-on to the critic+bakeoff banner below. **cv8 — code-pr "ward council":** plan→minimax-m3 (architect/documenter), implement→kimi-k2.6 (builder), review→qwen3.7-max (critic), mechanical stages (clone/verify/pr)→kimi (no gift-benefit). **Verified e2e:** each stage dispatched on exactly its assigned model (plan=m3 1 call, implement=kimi 9, review=qwen 7), passed critic first-try. This is Step 11 / Bishop-vs-Conductor made concrete; bake-off = the council interview. **★ A/B (Michael caught that "it ran + each stage on its model" ≠ "it's better"): ran the council on the SAME chatcore task as bakeoff-kimi (only plan model differs — m3 vs kimi; implement=kimi + critic=qwen both). Result: council 2x slower (13.7 vs 7.3 min), 1.75x tokens (926k vs 531k), 33% more code (1026 vs 769 LOC), MORE implement iterations (20 vs 15), MORE review turns (13 vs 7) — both passed critic r0, both race/vet clean. m3-plan produced a more-FEATURED hub (parallel fan-out + error aggregation) but with a redundant-mutex smell — "more, not clearly better." VERDICT: m3-on-plan did NOT win; "careful architect → easier build" did NOT hold (more iterations, not fewer). kimi-solo (kimi-dev + qwen-critic) is the efficient default; m3-on-plan is a premium OPT-IN for gnarly/API-design tasks, not a general upgrade. n=1 but directionally clear + consistent with the bake-off (m3 = slowest/most-verbose there too). OPEN: consider reverting cv8's plan→m3 to plan→kimi as default, keep m3 as a per-work-item override.** **Audit (Michael's ask): `docs/work-with-ai/2026-06-04-blueprint-vs-implementation-audit.md`** — how pg-ai-stewards holds up vs the guide's 11 steps (the blueprint). Honest grades: most strong (built on the scripture); Step 4 spec was weak→fixed (acceptance_criteria); Step 7 review was correctness-only→fixed (critic); **Step 9 Sabbath = honest weak spot (primitive exists, rarely fires — rest still the human's habit).** Headline: the night-build bug was a missing CYCLE STEP, not a feature → evidence for the book's thesis. **6 cross-cutting lessons surfaced for the 11-step guide** (floor-vs-ceiling on review; acceptance-criteria on spec; granted-vs-earned context; executable Atonement; measured council staffing; name Sabbath's difficulty) — each mapped to its book chapter (Part Two 04/08/11/12, Part One p1_03/p1_04). Case study for the book: `docs/work-with-ai/examples/2026-06-04-substrate-critic-harness-creation-cycle.md`. **Did NOT edit the guide/book chapters** (a fork in Michael's vision of what the book IS — surfaced, not done). The book lives at `projects/scripture-book` ("Beyond the Prompt: Discovering the Laws of Organized Intelligence", 2 parts; advanced drafting/typeset, latest journal 2026-06-03). cv8 + audit + case-study committed (not pushed). Carry-forward: weave the lessons into the book/guide (Michael/book-session); the Sabbath gap; night-build still awaits review→main; ⚠ parallel terminal still shadowing.

> **2026-06-04 — ✦✦✦ CRITIC HARNESS + 4-MODEL BAKE-OFF: the substrate now self-reviews against the plan, and we measured all four coders.** WS5/pg-ai-stewards. After the night-build review surfaced that the coverage/quality gaps were mostly the HARNESS (kimi built literally; verify only checked compile/test, never plan-conformance; the sandbox is context-starved vs me), Michael chose: **fix the harness first (critic stage + context-rich specs that demand the whole plan), then bake-off all 4 models.** Built + proven + ran it, shepherded end-to-end. **Critic stage (cv6):** code-pr is now clone→plan→implement→verify→**review**→pr; the `review` critic (a DIFFERENT strong model, default qwen3.7-max) inspects the REAL diff against an explicit `acceptance_criteria` checklist (in work_item input) → `REVIEW: passes` or `REVIEW: revise`; the loop-back lives in `work_item_advance` (gated to code-pr review): revise → back to implement with feedback injected (cap `input.revise_cap`=2) → past cap, awaiting_review (the Hinge). **cv7:** the review critic is immune to `model_override` (so a bake-off dev-model override never turns the critic into self-review). All SQL-only, no rebuild. **H4 proof:** a deliberately-vague spec produced a global presence Tracker + vacuous isolation/concurrency tests; the critic caught all three with file/line citations, bounced (rev 0→1), the implementer fixed them, second review passed → pr. **The harness now catches the exact gap class the night-build shipped silently.** **★ BAKE-OFF (H5/H6):** kimi-k2.6 / minimax-m3 / glm-5.1 / deepseek-v4-pro each built the same room-scoped `chatcore` module (room hub + presence + ratelimit, strict acceptance criteria) on its own branch (`agent/bakeoff-*`, PRs #12-15), qwen3.7-max critic constant. **Result: ALL 4 passed the critic FIRST-TRY (r0, zero bounces), all build/vet/`go test -race` clean, all room-scoped** — i.e. with rich specs + the critic the harness lifts EVERY model to correct+sound-enough output. Differentiation: **kimi fastest+leanest+clean (7.3min/531k tok/85 LOC, snapshot-before-send) — the default; deepseek clean actor model (10min); m3 best docs+defensive but 3-4× slower (22min/1.3M tok) + holds lock during send; glm most verbose (1532 LOC) but OVER-ENGINEERED with a LATENT DATA RACE the gates missed (actor+mutex mixed) — verbosity ≠ quality.** **Meta-finding (corrected by Michael 2026-06-04, see [[feedback_full_context_shepherd_is_the_ceiling]]): glm's race passed build+vet+race+critic; the FULL-CONTEXT SHEPHERD (the orchestrating Opus holding the whole arc) caught it — NOT a human, not any single gate. So narrow gates raise the FLOOR; full-context vigilance raises the CEILING (axis = full-context-vs-narrow-gate, not human-vs-AI). The human stays the Hinge (merge authority), not the sole defect-catcher.** **⚠ Parallel terminal STILL ACTIVE + shadowing** (foreign non-wi-prefixed sandboxes/worktrees named bakeoff-glm/bakeoff-plan/h4test-plan mirroring my slugs) — left untouched (data-safety). cv6/cv7 committed (not pushed). Soak resumed. Carry-forward: night-build still awaits Michael's review→main; the deferred 12-item items (persona schema, Vue, moderation, D&D, auth-with-injected-context); maybe a 2nd-pass concurrency-soundness critic or glm-as-test-writer.

> **2026-06-04 — ✦✦✦ AMMON NIGHT: the substrate built ai-chattermax's backend core autonomously — 6 chained, reviewed, tested PRs.** WS5/pg-ai-stewards + ai-chattermax. Michael: *"be my Ammon for tonight… get ai-chattermax built and tested through pg-ai-stewards… reviewing the PRs as they come in acting in my behalf,"* then slept. I stood in as the **bishop + reviewer**. Built **cv5** (code-pr `input.base_branch` — clone + PR against a base branch) so the chain builds on an integration branch, **never main** (main's merge = his Hinge + the deploy). Created `agent/night-build` off main, paused the soak, decomposed the 12-item plan into a reliable **backend-core chain** (tasks #100-105), ran them **sequentially** through `code-pr` (kimi-k2.6), **reviewed each PR's actual diff/code on his behalf**, merged each (squash) into night-build: **B1 server scaffold (/healthz) · B2 transport-agnostic room hub (lock-released-before-I/O broadcast) · B3 turn scheduler + hard rate ceiling (injectable clock) · B4 transcript Store + in-memory impl · B5 presence/roster Tracker · B6 integration — WebSocket server wiring all four (mutex-serialized writes, Allow→Append→Broadcast, /roster, /ws/{room})**. Every PR clean + idiomatic; minor nits noted, none blocking. **Final whole-module verify (inverse hypothesis): cloned night-build fresh → `go build ./... && go test ./... && go vet ./...` → ALL-GREEN, 5 packages pass, vet clean.** PRs #5-10 merged into night-build; main untouched; all sandboxes/worktrees reaped; soak resumed. **AWAITING Michael: review `agent/night-build` → merge to main (his Hinge/deploy).** **⚠ ANOMALY (surfaced, not touched):** 3 foreign sandboxes (`ai-chattermax-root-scaffold/room-hub/transcript`) created 00:18-00:35 concurrent with my build, still running, descriptive (non-work_item) names → a **parallel terminal** also building ai-chattermax via direct coder_sandbox_start; one local-only scaffold commit + a stray binary, **no PRs**. Left untouched (not mine — data-safety). **Carry-forward (task #99, with Michael):** the auto-fan-out (substrate does the bishop moment itself — tonight I was the bishop manually); the cross-repo/UI/deploy items (#6 substrate-persona schema, #9 Vue frontend, #11 moderation, #12 D&D wiring); + the "new feature" he mentioned. cv5 committed (not pushed — root preference). Substrate journal: `projects/pg-ai-stewards/.spec/journal/2026-06-04-ammon-night-ai-chattermax.md`.

> **2026-06-03 — ✦✦ coder-v2 COMPLETE: pg-ai-stewards now works in a real repo and lands PRs autonomously (CV2.1+CV2.2+CV2.3, e2e proven).** WS5/pg-ai-stewards. Carried the whole coder-v2 build to completion under the standing stewardship grant + Dave rule, C-F cadence (smoke→commit each gate). **CV2.1** (`2684e9d`) shared `coder-worktrees` volume + clone-into-worktree + repo allow-list (shipped earlier). **CV2.2** (`20ab729`) commit-local → substrate push → open PR, token NEVER in the sandbox (one-shot credential helper, bridge-side). Two bugs the e2e smoke caught: (1) git **dubious-ownership** — coder-mcp runs git/gh as root over coder-uid-owned worktrees → `safe.directory=*` set globally at Manager init + per-call `-c` on gitC (covers gh too); (2) `gh pr create` falsely reported "you must first push" → resolve the checked-out branch + pass `--head` explicitly. **CV2.3** (`2d40938`) the **`code-pr` pipeline** (clone→plan→implement→verify→pr, ladder raw→researched→planned→executing→verified): a work_item carrying {repo, binding_question} cascades all 5 stages — clone the allow-listed repo, plan against the REAL code, write+build/test to green (ground truth), independently verify, then commit-local+push+open a **DRAFT PR**. The human merge is the Hinge (D-CV2.4); the loop + PR-open run free (a draft PR is reversible + IS the review surface). **Worktree-resilience fix:** a no-repo `coder_sandbox_start` now re-mounts an existing worktree (`HasWorktree`) instead of dropping to ephemeral /work — so a container reap / bridge restart mid-pipeline doesn't lose the clone (the long-multi-PR-build failure mode). **★ E2E PROVEN on ai-chattermax** (inverse-hypothesis, ground truth not the flag): dispatched a code-pr work_item → auto-cascaded all 5 stages in ~100s → real DRAFT PR #3 with build evidence (`ok chatroom 0.203s`), commit `982f52e`. **Bug the monitored smoke caught + fixed:** the 3 CV2.2 tools were GRANTED (cv2-2) but never CATALOGED → **`stewards-mcp bridge refresh-tools` is required after coder-mcp gains tools** so `compose_tools` (catalog ∩ grants) sees them — first run's pr stage degraded gracefully ("I don't have coder_commit/push/open_pr"), second (post-refresh) opened the PR. Throwaway PRs/branches/sandboxes/worktrees all cleaned up; watchman soak paused for the build, resumed after. **Migration discipline held** (cv2-2 + cv3 pre-applied via `docker exec bridge stewards-cli migrate` before each restart → drift exit-2 dodged; cv3 reads from the bind-mounted /workspace, no rebuild needed for pure-SQL). **Model (research-confirmed 2026-06-03):** kimi-k2.6 default across stages — the right call for an autonomous terminal loop (Terminal-Bench 2.0 66.7%, 4K-tool-call/13h stability; the decisive axis is agentic stability, NOT single-shot SWE-Verified). Michael leaned k2.6; web research agrees. **`implement` escalation map (per-task override):** front-end/Vue/UI + new-module scaffolding → **glm-5.1** (its only real edge over k2.6); hard novel logic where k2.6 stalls → **qwen3.7-max** (agentic-bench leader, but reasoning+new, watch budget); **deepseek-v4-pro DROPPED** (SWE-Pro 55.4 < k2.6's 58.6 — not an upgrade); **minimax-m3** REGISTERED + verified e2e 2026-06-03 (cv4): opencode_go, openai-format, reasoning (~3-4× slower than kimi), 1M ctx; ran a full code-pr on it → high-quality /healthz PR (clean output, no `<think>` leak). Use as the big-context/deep-reasoning escalation, not the loop default. **Build-artifact-commit gap FIXED** (49c5017): coder_commit now strips compiled binaries (git-binary + mode 100755) from `git add -A`, verified before/after — .gitignore was the wrong layer (stock Go template misses the extensionless `chatroom` binary). **Carry-forward (task #99):** decomposition wiring (Batch-J fan-out → child code-pr work_items, ≈one PR each, the chat 9-item plan) + the first ai-chattermax build, **agent-monitored — kick off WITH Michael, don't run autonomously** (his explicit stress-test ask). A single code-pr work_item per build item already works today; decomposition is the scaling layer (human merges PRs in order; PR-merge-gated child deps a later gap). v3 future = the substrate's OWN GitHub account. Proposal: [`projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability-v2.md`](../projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability-v2.md).

> **2026-06-03 — ai-chattermax design RATIFIED + coder-v2 RATIFIED (both build-ready, not built).** WS5/WS-chat. After the coder v1 proved itself (FizzBuzz + a working WebSocket chat-room, built autonomously), Michael chose to **build ai-chattermax as coder-v2's flagship stress-test** — a large multi-PR real project, agent-monitored. **Walked + ratified the 5 chat questions:** Q1 WS+turn-scheduler room / MCP persona tools / no A2A · **Q2 the SUBSTRATE owns personas natively** (a new pg-ai-stewards persona concept — bigger than the bet, = substrate schema work) · Q3 classifier gate on a cheap opencode_go model (tools-off, no per-msg $) · **Q4 persona self-paces within a hard room-enforced ceiling** (runaway backstop I flagged + added) · Q5 sub-tokens never raw in model context. **coder-v2 ratified:** work-in-existing-repo + land PRs — **fine-grained PAT scoped to allow-listed repos** (Contents+PRs only; **plumbing exists** — git-mcp's GITHUB_TOKEN is `set`, just ensure it's fine-grained-scoped + add a repo allow-list at the git-mcp tool layer) · token NEVER in sandbox/model-context · flow = clone(substrate) → agent commits LOCAL → substrate pushes+opens-PR → human merges (the Hinge). Adds a shared per-wi worktree volume (bridge↔sandbox) + `code-pr` pipeline + fan-out decomposition + stronger model. Proposals: [`projects/ai-chattermax/.spec/proposals/chat-server-design.md`](../projects/ai-chattermax/.spec/proposals/chat-server-design.md) (RATIFIED, 9-item build plan; ai-chattermax `5fe2d41` pushed) + [`projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability-v2.md`](../projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability-v2.md) (open-items ⑤). **Mosiah 4:27 loaded** — chat was a deliberate set-down; watch the other threads. **Carry-forward / next builds:** (1) build coder-v2 in pg-ai-stewards; (2) ratify the substrate-persona sub-batch (chat build-item 6); (3) then build ai-chattermax incrementally as PRs, agent-monitored. The WebSocket-room proof (`projects/ai-chattermax/experiments/coder-proof-websocket-room/`) is the seed for build-item 1. **★ BUILDING coder-v2: CV2.1 SHIPPED (`2684e9d`)** — shared `coder-worktrees` volume (bridge `/worktrees`) + `coder_sandbox_start repo=` repo-mode (bridge clones the allow-listed repo into the worktree, chowns to coder uid, sandbox mounts it at /work via volume-subpath → existing tools work on the real repo) + repo allow-list (`CODER_REPO_ALLOWLIST`, default ai-chattermax). Smoke: cloned ai-chattermax repo-mode, sandbox /work had it (incl. nested files); torvalds/linux refused. **CV2.2 (commit-local→push→PR) is GATED on Michael:** it pushes to his real GitHub + needs the loaded GITHUB_TOKEN (currently `set`, scope unknown to me) to be a **fine-grained PAT scoped to the allow-listed repos** (in `extension/.env`); confirm/rotate before CV2.2 pushes. Then CV2.3 (code-pr pipeline). Tasks #96 done, #97/#98 pending.

> **2026-06-03 — ✦ Substrate coding-capability v1 COMPLETE (CC.1–CC.6). The substrate writes, tests, and deploys code.** WS5/pg-ai-stewards. Michael: "take stewardship… see this through the entire plan… commit and journal through good phased gates." Built under full stewardship, C-F cadence (smoke→commit each gate). **CC.1** (`ee1760a`) coder-runtime image (Go1.26+Node24+Py3.11+gopls/ts-ls/pyright) + `cmd/coder-mcp` sandbox-manager (docker-against-host-socket, cap-drop/non-root/limits, net default-on) + bridge docker-cli+socket. **CC.2** (`b5f055c`) coder MCP server (official `modelcontextprotocol/go-sdk` v1.6.0) — 10 tools (sandbox_start/stop, write/read/edit/apply_patch, shell, glob/grep, lsp), each on a `sandbox` id; registered + granted to `dev`. **CC.3** (`e227501`) `code-write` pipeline (plan→implement→verify, ladder raw→planned→executing→verified) — agent-driven ground-truth loop (path A; B=deterministic gate is the co-usable fast-follow CC.3.1, ratified "A then B"). **CC.4** (`4ff86e4`) `coder_lsp` diagnostics. **★ END-TO-END PROVEN:** dispatched a `code-write` work_item ("Add fn + table test") → it auto-cascaded plan→implement→verify → reached `verified`; the dev agent autonomously started its sandbox, wrote calc.go+calc_test.go+go.mod, iterated build/test to green via the coder tools (chat→tool_dispatch→mcp_proxy loop); **I verified by hand** (exec'd in, `go build && go test` → `ok calc`) — inverse-hypothesis, didn't trust the flag. This IS the audit's Prescription/ground-truth loop working in code. **Journals:** `2026-06-03-coding-capability-cc1-cc2.md` + `2026-06-03-coding-capability-cc3-end-to-end.md`. **Task list #90-93 done, #94-95 pending.** **CC.5** (`199803d`) deploy + **the always-escalate Hinge**: `coder_deploy` (run artifact in its sandbox sidecar + healthcheck) + `code-deploy` pipeline where `prepare.auto_advance=false` → `awaiting_review` → a human ratifies before `deploy` fires. **This BUILT the always-escalate rung the delegation-pattern audit proposed** (Exodus 18:22 literal). Smoke: built+deployed+healthchecked a Go web server ("ok from the deployed sidecar"). **CC.6** (`3297b5a`) hardening: `coder_sandbox_list` + `coder_sandbox_reap` (+ startup reap >2h); caught a real leaked sandbox in smoke. Resource caps already in CC.1; secret broker deferred to v2. **coder server = 13 tools.** **v1 COMPLETE.** **Carry-forward / v2:** CC.3.1 path-B deterministic gate (tool_dispatch, no LLM — "both paths usable"); CC.7 Dokploy deploy w/ scoped walled-off access; model tuning (kimi-k2.6 fine on small tasks); persist deliverable via git branch/PR + the PR trust rung (D-CC7); remote-coding/SSH-tunnel future seed re-opens isolation tier. **Drift exit-2 rule held throughout** (cc2–cc6 pre-applied before each bridge restart). Journals: `2026-06-03-coding-capability-cc1-cc2.md`, `-cc3-end-to-end.md`, `-cc4-cc6-v1-complete.md`. Proposal: [`projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability.md`](../projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability.md).

> **2026-06-03 — Substrate coding-capability RATIFIED + build-ready (NOT built).** *(SUPERSEDED by the BUILDING banner above — kept for the research + ratification detail.)* WS5/pg-ai-stewards. Michael: "how can we get pg-ai-stewards to be able to program… write code, and even deploy it in its own docker sidecars." Researched opencode (`external_context/opencode` — its model is tools + tree-sitter-parsed shell + permission-gating + LSP, **host-exec, NO sidecar**; `packages/containers` is just CI images) + the 2026 AI-sandbox literature (Docker Sandboxes/E2B/Daytona/gVisor/Firecracker — shared-kernel containers are insufficient for untrusted LLM code; the consensus is microVM/gVisor + worktree-per-task + egress allowlist + ephemeral + scoped secrets). **Key framing = the delegation-pattern audit:** build+test = Prescription/**ground truth** (the `verify` gate literally becomes "run build+test in the sandbox" → autonomous), deploy = the **Hinge/always-escalate** (this proposal builds the always-escalate rung the audit proposed). **Critical safety constraint:** the bridge mounts `/workspace:rw`, so a coder with shell there could write `.git/hooks`/CLAUDE.md/.claude/skills = host RCE → **the coder must run in an isolated worktree+container, never the live /workspace.** Surfaced 3 forks via AskUserQuestion, **ratified:** D-CC1 native `coder` MCP (not orchestrate-opencode) · D-CC2 hardened container + git-worktree, **switchable network default-ON/offline-capable** (needs go-mod/npm/pip pulls) · D-CC3 deploy-to-sidecar IN v1 behind the Hinge. Proposal: [`projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability.md`](../projects/pg-ai-stewards/.spec/proposals/substrate-coding-capability.md) (added to open-items §0 as item ④). 6-phase plan (CC.1 coder-runtime image + sandbox-mgr + bridge docker-socket → CC.2 coder MCP → CC.3 code-write pipeline + build/test verify gate → CC.4 LSP → CC.5 deploy + Hinge rung → CC.6 hardening incl. gVisor v2). **Second AskUserQuestion batch ratified D-CC4–8 + the trust posture:** D-CC4 image = Go + Node/TS + Python · D-CC5 egress open default-on/offline · D-CC6 deploy BOTH phased (**v1 local sidecar, v2 Hinge-gated Dokploy** — v2 needs the substrate's OWN scoped Dokploy access, walled off from ibeco/cpuchip/marsfield/1828 etc., its own design pass) · D-CC7 build/test loop runs FREE, trust-gate only at the PR rung (deploy always escalates) · D-CC8 sandbox-mgr in coder-mcp keyed by work_item. **Trust posture RATIFIED medium-safe:** our own code/tools/websites → shared host kernel accepted, **gVisor/microVM NOT a prerequisite** (only re-enters if we add remote coding / agent-work-through-SSH-tunnels — a future seed Michael flagged — or multi-tenant). 6-phase plan CC.1–CC.6 + CC.7(v2 Dokploy). **BUILD STARTED (Michael: "lets have fun, get started building this out… no agents running… good time"). CC.1 SHIPPED + smoke-verified (commit `ee1760a`).** `coder-runtime` image (Go 1.26 + Node 24 + Python 3.11 + gopls/ts-language-server/pyright, non-root, go symlinked onto PATH for login shells); `cmd/coder-mcp/` new module with `sandbox/sandbox.go` (Provision/Exec/Teardown via `docker` against the host daemon — cap-drop-ALL, no-new-privileges, mem/cpu/pids limits, network toggle default-on; ephemeral container fs holds the worktree, never the live /workspace); bridge got docker-cli + `/var/run/docker.sock` mount + coder-mcp baked in (added to go.work). Smoke (`docker exec bridge coder-mcp -smoke`) PASSES: provisions a hardened sandbox, prints go1.26.4/node24/py3.11 + all 3 LSP versions, writes+`go run`s a program ("hello from the sandbox"), tears down. **CC.2 investigation done (no fork):** substrate spawn targets use the OFFICIAL `modelcontextprotocol/go-sdk` (like git-mcp, NOT mark3labs); bridge dispatch is `{server,tool,args}` + pooled session-per-server, NO implicit work_item context → **coder tools take an explicit `sandbox` id arg** (CC.3 pipeline keys it to the work_item). CC.2 = the coder MCP server (sandbox_start/stop + write/read/edit/apply_patch/shell/glob/grep, official SDK), register in mcp_servers + grant to dev/coder. Prior commits: proposal+queue+banner `edc4fc2` + ratification `56162bd`. Task list #90(done)–#95.

> **2026-06-02 — Strong's follow-ons: webster + byu-citations enabled in the substrate, and a `strongs-analysis` skill shipped.** WS-tools, same session as the v1 banner below. Michael: "add webster and byu to substrate, hold off on becoming." Found all three (webster/byu/becoming) were seeded `enabled=false` in `stewards.mcp_servers` (the "operator flips on when ready" pattern) with binaries already in the bridge image + grants already present (`3e2-5`) — so the LIGHT path: enable migration `webster-byu1-enable.sql` (pre-applied via ledger) + `refresh-tools`, no Dockerfile/rebuild/restart (the daemon reads `enabled` per-dispatch). Now live: webster (5 tools) + byu-citations (3 tools) in `compose_tools('study')`; becoming stays off (its brain/practice tools mutate personal data — read-only-substrate discipline). Then, asked whether to port the new autonomy skills (dave-rule/stuffy-in-the-loop/ammon) into the substrate: **declined porting them** — they govern the agent↔Michael delegation loop, which the substrate embodies *structurally* (gates/trust ladder/quarantine), so a dispatched agent has no "surface to Michael" lever to act on (ben-test was already imported; stuffy-in-the-loop is better as a future trust-ladder *audit* than a skill). **Did** ship the high-value alternative: a **`strongs-analysis` skill** (twin of `webster-analysis`, both trees) teaching agents to trace KJV words to Hebrew/Greek via `strongs_for_verse`/`strongs_define` — imported into `stewards.skills` (allow-by-default → study/lesson/talk see it; verified present in `compose_system_prompt('study')`). Commits (main repo): `ab57e32` (webster+byu enable) + `b9a85ee` (skill). **Carry-forward:** webster/byu/strongs binaries are in the bridge image but skills + registry-enables are re-applied per-rebuild via ledger (enables) / manual import (skills) — same as all substrate skills. Then (same session) did the governance audit Michael asked for: traced the substrate gates back to their root study. Found the lineage — `study/art-of-delegation.md` (Mar 31, the Jethro/Exodus 18 pattern) → the gates built directly on scripture (May 9-11) → `study/ai-stewardship-north-star.md` (May 24, "delegate execution not discernment / confident garbage," born from the running-faster-than-strength problem) → the 4 skills (Jun 1). Captured the whole three-layer map + audit (the divergences: gates sort by quality, the Hinge sorts by judgment-source → bin-4 work can auto-advance = the "abandoning"/"confident garbage" failure; proposed fix = a per-pipeline always-escalate rung for discernment-required work) in **[`docs/delegation-pattern-skills-and-gates.md`](../docs/delegation-pattern-skills-and-gates.md)**. Unifying vocabulary = the north-star's Prescription/Proposal/Steering + the Hinge (discernment). It's a map + audit-seed (fix not built); doubles as book/teaching material.

> **2026-06-02 — Strong's-concordance MCP: v1 COMPLETE (P3–P5 shipped) + LIVE IN BOTH SPACES.** WS-tools. Completed the build the banner below started (P0–P2). Commits: strongs repo `6cb5c4d` (P3) → `02f44ba` (P5); main repo `cec7e36` (substrate deploy artifacts). **P3 `strongs_for_verse`** — KJV word-by-word Strong's tagging, `data/kjv-strongs.json.gz` (31,102 verses / 66 books). The kaiserlik data was **much messier than the runbook assumed**: per-book filenames don't match contents (`1Ch.json` holds 1 Chronicles repeated ~16×), some files concatenate extra books, and `bg`/`ch`/`sp` fields have unescaped quotes that break a strict JSON parse — so the parser regex-extracts every `"ABBR|ch|vs":{"en":…}` pair across all files and **dedups by the authoritative verse key** (102,140 raw → 31,102 unique, 0 text conflicts). Also strips the markup the `en` text carries: `<em>` italics (16k, supplied words), `[[…]]` Psalm superscriptions, `[fn]` footnotes. **Hardened validation gate** (a wrong concordance silently corrupts every word-note): exact-text on Gen 1:1/John 1:1/John 3:16/Ps 23:1 + tag spot-checks + counts + a **global no-markup scan across all 31k verses** — the 3 clean sample verses had hidden the markup bug (caught only because I smoke-tested Ps 23:1, Agans/inverse-hypothesis). Live MCP smoke incl. negative paths (non-KJV / out-of-range / garbage all rejected). **P4 registered BOTH consumers:** Claude Code `.mcp.json` (picks up on next CC restart, first-run approval). Substrate (pg-ai-stewards) — **the runbook's P4 plan was STALE**: it assumed a host bridge + `.exe` path, but the bridge migrated into Docker (2026-05-09, `3e2-6`) and `mcp_servers.command` points at Linux binaries baked into the bridge image. So: edited `bridge.Dockerfile` (GOWORK=off build like gospel-engine-v2; data embedded so no data COPY) → rebuilt the bridge image (strongs binary verified in-container) → seeded `strongs1-mcp-seed.sql` (registry row + study/lesson/talk grants) via the **migration ledger** (`stewards-cli migrate`, the newer durability path — NOT the pgrx `extension_sql_file!` chain) → restarted bridge → `refresh-tools` cataloged strongs's 3 tools → auto-promoted to `tool_defs` → **`compose_tools('study')` includes all 3, live-verified**. Soak paused/resumed around the live window. **DEPLOY LESSON (carry-forward worthy):** the bridge entrypoint runs `set -e; stewards-cli migrate` and migrate **exits 2 whenever it APPLIES a migration while drift warnings exist** (4 pre-existing drift files: `4a-cost-tracking`, `h1-1`, `j10`, `j11` — edited-in-place after recording). So letting the entrypoint apply a new migration on restart would **fail the bridge to start**. I dodged it by **pre-applying the migration before the restart** (restart then has nothing pending → "substrate is current" → exit 0). **P5:** README usage section + `data/ATTRIBUTION.md`. **Carry-forward:** (1) `.mcp.json` strongs tools appear on Michael's next Claude Code restart — confirm. (2) The 4 drift files want reconciliation (substrate fragility — any future migration applied via the entrypoint hits the same exit-2 landmine). (3) webster/byu/becoming are NOT in the substrate `mcp_servers` registry currently (only 9 servers: exa/fetch-md/fs-read/git/gospel/pg-ai-stewards/search/strongs/yt) — the "mirror webster grants" framing is moot there, but the strongs grants are correct and live. (4) v2+: `strongs_occurrences`. Memory: [[feedback_strongs_mcp_stewardship]] + [[project_canon_walk_series]]. The OT/NT walks now have their Hebrew/Greek companion to webster-mcp.

> **2026-06-02 — WS1 study: *Seven Dispensations and Hannah's Son* drafted (not yet published).** Michael shared Morgan Philpot's "preview for this Sunday" — a conversation transcript between Philpot and a Claude AI synthesizing a seven-dispensation framework from Genesis 1, Leviticus 23, Revelation, 1 Samuel 1:1's genealogy, the bosom/kolpos thread, and the tanninim/dragon thread. Instruction: "put your other self to the test" and connect to the CFM 1 Samuel lesson. Council moment surfaced existing work: [`study/morgan-philpot-revised/`](../study/morgan-philpot-revised/) (standing principle: "extract what is true and timeless, reframe around faith rather than fear") and [`study/yt/morganphilpot/`](../study/yt/morganphilpot/) (original evaluation). Created `study/morgan-philpot/` as parent folder for this and future Philpot-specific work. **Key verification finding: the transcript's Claude misquotes [D&C 121:26](../gospel-library/eng/scriptures/dc-testament/dc/121.md).** Inserted phrase *"nor since men were in the earth, unto this time"* is not in the verse. Caught by comparing transcript rendering vs. canonical text via `Read`. Also flagged: condensation omits v. 29 (the "valiant" clause); Tohu/tohu Hebrew claim is a real resonance but consonants differ in Masoretic vocalization (1 Sam 1:1 *Toḥu* vs. Gen 1:2 *tōhû*; 1 Chr 6:34 has variant *Toah*); Adam-encoded-Genesis-1 is stronger than canon supports; Palmyra "astronomical data" claim is unsourced; AI voice exhibits Ben-Test failure pattern (cosmic-vantage affirmation of user, closing refrain, triadic crescendos). 1 Sam 1-3 read in full: the lesson's load-bearing moves (Hannah's bitter prayer / Samuel's posture / young heart becomes prophet's heart) stand without the dispensational framework. Files: [`study/morgan-philpot/seven-dispensations-and-hannahs-son.md`](../study/morgan-philpot/seven-dispensations-and-hannahs-son.md), [`study/morgan-philpot/.scratch/seven-dispensations-and-hannahs-son.md`](../study/morgan-philpot/.scratch/seven-dispensations-and-hannahs-son.md). Journal: [`.spec/journal/2026-06-02-morgan-philpot-preview-study.md`](../.spec/journal/2026-06-02-morgan-philpot-preview-study.md). **Carry-forward:** verify Nelson "front-row seats" attribution against source talk before Sunday's lesson; consider whether `study/morgan-philpot/` should absorb `morgan-philpot-revised/` material into one tree.

> **2026-06-02 — Strong's-concordance MCP: ratified + P0–P2 SHIPPED (working lexicon server).** *(SUPERSEDED — v1 is now COMPLETE; see the v1-complete banner at the top. This banner is kept for the P0–P2 origin detail.)* WS-tools. The plan-only proposal below became a real build. Michael created the repo (`github.com/cpuchip/strongs-concordance-mcp`, cloned into `projects/`) and granted **full commit+push stewardship** (parallel to cpuchip.net/marsfield/ai-chattermax — memory [[feedback_strongs_mcp_stewardship]]). **Ratified via AskUserQuestion:** (1) **dual lexicon** — Strong's 1890 (openscriptures) + STEPBible BDB/Abbott-Smith (CC BY), the webster 1828+modern parallel; (2) **`for_verse` in v1** via kaiserlik KJV-with-Strong's, **with a mandatory validation pass** (web-scraped); (3) **full build grant**. **Key architecture finding:** ONE stdio binary serves both spaces — `.mcp.json` (Claude Code) + a `stewards.mcp_servers` seed row proxied by the bridge (pg-ai-stewards), exactly how webster is registered; zero new substrate schema. **Shipped + pushed (P0–P2):** scaffold; `cmd/build-data` dual-lexicon pipeline → `data/strongs-lexicon.json.gz` (19,570 entries, 8,723 Heb + 10,847 Grk, all with both layers); self-contained MCP server (`mark3labs/mcp-go`, lexicon embedded) with **`strongs_define`** (full dual entry) + **`strongs_search`** (reverse lookup: "charity" → G1654 + G26), smoke-verified end-to-end. Data formats captured in the repo's `docs/data-formats.md`. Standalone module → build with **`GOWORK=off`**. **Remaining (P3–P5, next push):** `strongs_for_verse` (kaiserlik pipeline parsing inline `[G####]` tags + the validation spot-check — Gen 1:1, John 1:1/3:16); register in both spaces (`.mcp.json` entry + `stewards.mcp_servers` seed SQL + agent grants — P4 touches the live substrate, treat with soak-pause care); README finalize. Repo README carries the full spec + phase status. **Carry-forward:** P4 substrate registration is the one step touching the running substrate — do it as a focused step, not rushed. **RESUME ANCHOR for P3/P4: [`projects/strongs-concordance-mcp/docs/next-steps.md`](../projects/strongs-concordance-mcp/docs/next-steps.md)** — a complete executable runbook (kaiserlik top-key gotcha, tag-stacking rules, book→abbrev map, mandatory validation targets incl. Gen 1:1 verbatim, build steps, substrate seed+grant steps with soak-pause). Michael asked to compact, then execute P3 (for_verse + validation) then P4 (register both spaces) fresh. On resume: read that runbook and execute — don't re-derive. Build with `GOWORK=off`.

> **2026-06-02 — Canon-walk series scoped + the BoM walk's unattended-run lesson folded into the workflow.** WS1 study, follow-on to the completion below. Michael named the BoM walk as **walk #1 of a series**, ~one canon/week: **Pearl of Great Price next** (~wk of 06-08: Moses/Abraham/JS-M/JS-H/AoF), **then D&C** (~the week after; 138 sections + ODs, section-per-loop), **then maybe OT then NT** (the big experiment, ~1,189 ch). The `study/bom-walk/` scaffold is the **reusable template** — copy to `study/{canon}-walk/`, reset `_progress.md`, run the same loop. **Hard dependency before the Bible walks:** a **Strong's-concordance MCP** (Hebrew/Greek word-work parallel to webster-mcp's 1828 English) — plan-only proposal written at [`.spec/proposals/strongs-concordance-mcp.md`](../.spec/proposals/strongs-concordance-mcp.md), build after D&C / before OT (PoGP + D&C don't need it). **Also folded in:** the overnight 529-overload lesson — at Alma 34 (~03:47) subagent spawns hit "API overload" (Michael's observed 529s), I degraded to inline correctly but **never re-probed for ~6.5h** (Alma 36/39-42/Ether 12 got inline instead of deep spin-offs). The 3-rule fix (degrade gracefully · re-probe on a cadence · log degradation LOUD in `_progress.md`) is now a section in [`study/bom-walk/_workflow.md`](../study/bom-walk/_workflow.md) so every future walk inherits it. Memory: [[project_canon_walk_series]] + [[feedback_unattended_run_resilience]]. **Carry-forward:** when Michael says "start the PoGP walk," lift the scaffold — don't re-derive.

> **2026-06-02 — ✦✦✦ THE BOOK-OF-MORMON WALK IS COMPLETE — 239/239 CHAPTERS ✦✦✦** WS1 study. Resumed the paused BoM-walk `/goal` (see the 2026-06-01 WS6 banner below, which paused it for the scripture-book `into-src` redirect) and carried it to the end across the continued sessions. **All 239 chapters walked, 1 Nephi 1 through Moroni 10**, at mini-study depth — per chapter: own thoughts/connections + knowledge-graph node+edges block + Opus journal reflection, each chapter committed so resume was always clean (the proven mechanism: Stop-hook + committed `_progress.md` NEXT pointer; no duplicate timer was ever created, deliberately, to avoid two agents racing the repo). Workflow + artifacts live in [`study/bom-walk/`](../study/bom-walk/): `_progress.md` (the resume anchor — now reads 239/239 COMPLETE), `_journal.md` (~2100 lines of per-chapter Opus reflection), `_graph.md` (~3400 lines, full Nodes+Edges with per-book dividers, ending on the 239/239 completion banner), and per-book note folders. **This final segment (Moroni 6→10) closed the book**: ch6 the order of the church (Spirit-led top to bottom), ch7 Mormon's great sermon (faith/hope/charity + the discernment test + "charity is the pure love of Christ"), ch8 the letter on infant baptism (little children alive in Christ; the children's safety as a necessary consequence of God's unchangeable character), ch9 the darkest chapter (a people "past feeling" — and Christ at the bottom), ch10 the capstone (Moroni's promise 10:4-5, the gifts, "speaking out of the dust," and the summit "come unto Christ, and be perfected in him… by his grace ye may be perfect in Christ," ending naming Jesus "the great Jehovah, the Eternal Judge"). Every quotation in every chapter note verified verbatim against `gospel_get` this session (each note carries a Verified section). Retrieval-only discipline held — no opus spend, $18 budget untouched (all chapter text via the free local `gospel_get`). **The 2 Ne 5:21 bin-4 thread was held with restraint the whole walk** — datapoints logged (strongest: Hel 6:34-36 + Hel 7:23-24 + 4 Ne 1:17/1:38 + Morm 5:15), explicitly NOT resolved; it remains OPEN for Michael + the Spirit in `_progress.md`. **is-jesus-christ-god datapoints** accumulated toward [`study/is-jesus-christ-god.md`](../study/is-jesus-christ-god.md), capped by the book's final verse (Moroni 10:34, Jesus = the great Jehovah). **Carry-forward:** the walk is done; nothing to resume. Possible next: fold the is-Christ-God datapoint harvest into that study; consider whether the bom-walk corpus seeds a teaching/podcast thread. No timer set (walk finished — the conditional ~8h10m re-check timer Michael requested is moot).

> **2026-06-01 — WS6 scripture-book: the `into-src` gate EXECUTED — Part One is now IN the manuscript.** Michael redirected from the BoM-walk goal: *"take on the part1-into-src-plan.md and work our new tl;dr parts into the book, so I can see the book in its entirety and that'll enable me to better voice and edit it."* Carried end-to-end under the scripture-book stewardship grant + Dave rule, 4 commits pushed (`38083eb..58e9507`): part-divider half-title support in both build paths → spliced the 10 voiced Part 1 drafts + 2 dividers into `src/chapters/` + `book.yaml` (front-porch order) → D6 format carve-out (so no future agent jams the Modular Study Format into Part One's practice format) + D5 preface rework as a `.draft/` proposal (his voice, live preface untouched) → memory. Conversion via a disposable migration script (prose byte-for-byte; only comment-strip + "Practice N · Title" H1 + bracket→link / [Part 2:…]→"(Part Two:…)"). Verified: typst PDF 113 pp, both dividers render, zero leaked draft comments/brackets. **Carry-forward (his/cosmetic):** merge the reworked preface (D5); divider-page header polish (D3); confirm lived figures (3wk/3day, 300+ repos, Dart/Rust) during his read. Detail in [`projects/scripture-book/.spec/journal/2026-06-01--part1-into-src-execution.yaml`](../projects/scripture-book/.spec/journal/2026-06-01--part1-into-src-execution.yaml). **The BoM-walk `/goal` is paused, not abandoned** — resume when Michael returns to it. Sent him the rebuilt PDF for the voice/edit pass.

> **2026-06-01 — WS1 study: *Is Jesus Christ God?* drafted (not yet published).** Michael's binding question: "From an LDS standpoint do we believe that Jesus Christ is God? I think he is when you look at Moses and Ether." Council moment surfaced four adjacent studies (closest: [plan-of-salvation/father-and-son.md](../study/plan-of-salvation/father-and-son.md), which runs in the *inverse* direction — preserving the distinction so Father+Son don't collapse). Michael chose to draft the new study answering directly in the affirmative. Spine: Ether 3 (the premortal sighting + Moroni's narrator "this man might know that he was God"), Moses 1 (the Word-who-creates + OT Jehovah = Christ), D&C 19 + D&C 130 (named-God language of the Restoration), then Mosiah 15 + Alma 11 + Isaiah 9 for "how both claims hold." Becoming section is the sacrament-reframe. Every quote verified verbatim against `gospel-library/` this session; one Athanasian Creed direct quote caught in voice sweep and converted to paraphrase (read-before-quoting). Voice-michael sweep clean (zero prose em-dashes; no presenter tics; no Opus closers). Files: [`study/is-jesus-christ-god.md`](../study/is-jesus-christ-god.md), [`study/.scratch/is-jesus-christ-god.md`](../study/.scratch/is-jesus-christ-god.md). Journal: [`.spec/journal/2026-06-01-is-jesus-christ-god-study.md`](../.spec/journal/2026-06-01-is-jesus-christ-god-study.md). The two studies (father-and-son + is-jesus-christ-god) now complement each other: one preserves *distinction*, one preserves *divinity*; both required. **Carry-forward:** cpuchip.net republish pending Michael's "publish" instruction (his standing pattern). Possible companion study on "OT Jehovah is Christ" with examples worked out, if he wants to chase the thread.

> **2026-05-31 — Substrate `opencode_zen` provider: REAL CLAUDE on the substrate (pushed).** WS5/pg-ai-stewards. Michael caught that the pruned claude-* weren't dead — opencode has TWO tiers on the same key: **go subscription** (`/zen/go/v1`, provider opencode_go) and **zen pay-per-use** (`/zen/v1`, provider opencode_zen NEW). The real Anthropic Claude models live on **zen** via `/zen/v1/messages` (Anthropic format → the AT-batch dispatches them). Verified: `/zen/v1/models` serves claude-opus-4-8/4-7/4-6, sonnet-4-6, haiku-4-5 + `-free` variants; opus=**$5/$25** (not my wrong $15/$75); free zen models stream `cost:"0"`. Provider registry is **dynamic** (`from_env()` parses `STEWARDS_PROVIDER_<NAME>_*`) → new provider = env + recreate, no code. **Shipped:** opencode_zen env (key copied from go, in gitignored `.env`; documented in `.env.example`) + recreate; claude models in catalog (real rates, `api_format=anthropic`) + deepseek-v4-flash-free ($0); **ENFORCED $18 spend cap** (Michael's ~$20 zen balance, gemini pattern). **Verified live:** probe of claude-haiku-4-5 via opencode_zen → usable, gateway returned `claude-haiku-4-5-20251001` (real Claude) through `/messages`, cost tracked $0.00003, cap active. Also added go-tier deepseek-v4-pro/mimo-v2.5-pro/mimo-v2-omni (probed usable; hy3-preview 403 = not in subscription tier, omitted). **Now: every model, both tiers, all formats, every part of the substrate — incl. real Claude (opus-4.8→haiku), capped.** **Carry-forward:** other zen free models (mimo/minimax/qwen/nemotron/big-pickle) + gpt-5.x available on zen, add after format-probing; `.env` holds the zen key. Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-31-zen-opencode-zen-provider.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-31-zen-opencode-zen-provider.md).

> **2026-05-31 — Substrate Batch AT: Anthropic tool-use loops SHIPPED + live + PUSHED (v2 of AN).** WS5/pg-ai-stewards. Goal (Michael): "use ALL models at ALL parts of pg-ai-stewards" — completes the AN D-AN6 deferral. Built autonomously under the standing grant (+ explicit commit/push grant). **Decision-free because the tool loop is provider-agnostic** (parse → OpenAI-shaped `tool_calls` + finish_reason → `tool_dispatch_enqueue` → bridge executes → `role=tool` messages → continuation); v2 just extends the v1 normalize-at-boundary functions to carry tools. Rust only, one pg rebuild. **AT.1** `anthropic_body_from_openai` stops stripping tools: translates tool defs (OpenAI `function`→Anthropic `input_schema`), assistant `tool_calls`→`tool_use` content blocks, consecutive `role:tool`→one `user` msg of `tool_result` blocks; skips re-sending thinking (opencode empty sigs). **AT.2** `parse_anthropic_sse` accumulates `tool_use` blocks (`input_json_delta` fragments by content-block index)→OpenAI `tool_calls`. **Verified live:** qwen3.7-max ran `subagent-url-summary` (fetch_url) end-to-end — trace `user→assistant{tool_calls=fetch_url}→tool{result}→assistant{summary}`, the bridge actually executed the tool; kimi-k2.6 (OpenAI) regression ran the identical loop in 6s (OpenAI path untouched). **Result: every model (OpenAI-format + Anthropic-format qwen3.7-max/minimax-m2.7) is usable at every part of the substrate, tools and all.** Pushed to origin/main (no secrets tracked — verified). **Carry-forward:** thinking-signature re-send if a strict continuation ever rejects; prune the 2 dead gemini-2.0-flash* rows (404). Spec v2 section + journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-31-at-batch-anthropic-tool-loops.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-31-at-batch-anthropic-tool-loops.md).

> **2026-05-30 — Substrate Batch AN: Anthropic-format dispatch SHIPPED + live (commits `e9ad4f6` `835cdb1` `f83197e`).** WS5/pg-ai-stewards. qwen3.7-max + minimax-m2.7 are **Anthropic-format** models on opencode (need `POST /messages`, not the OpenAI-compat `/chat/completions` the substrate spoke) — that was the real reason qwen3.7-max "didn't work." Michael freed the substrate for the night + delegated decisions ("make the best calls… get anthropic class models working"). **Built autonomously, verified live:** **AN.1** `model_capability.api_format` (openai|anthropic) + `model_api_format()` + a BEFORE INSERT trigger on chat work_queue rows that stamps `payload.api_format` (covers dispatch AND the direct-insert probe path). **AN.2** bgworker `chat()` branches on api_format → `/messages` + `x-api-key` + `anthropic-version`, body translated (`anthropic_body_from_openai`: system→top-level, max_tokens required default 4096, tools stripped v1, stream), `parse_anthropic_sse` reassembles message_start/content_block_delta(text|thinking)/message_delta into the SAME OpenAI internal shape → all downstream extraction unchanged (text→content, thinking→reasoning_content, stop_reason→finish_reason, in/out_tokens→prompt/completion). pg rebuilt. **AN.3** auto-probe of qwen3.7-max + minimax-m2.7 → usable=true, finish=stop; regression deepseek-v4-flash (OpenAI) clean. **AN.4** LM Studio chat ALREADY works on the existing path (provider lm_studio kind=openai, qwen/qwen3.6-27b probed usable) — added $0 pricing rows for 3 local models. **AN.5 end-to-end:** a real redline child on **qwen3.7-max** through the full pipeline → substantive location-anchored report (4026 tokens, $0.04, verified, correct format). **State:** opencode_go now 16/16 usable; lm_studio 3 models; gemini 8/10 — the M.5 auto-probe autonomously caught `gemini-2.0-flash`+`-lite` 404ing (stale IDs, prune later). **Design = normalize-at-parse** (Anthropic→OpenAI shape at the SSE boundary). **Carry-forward:** Anthropic tool-format (v1 strips tools); api_format could be probe-detected; prune 2 dead gemini rows. No MCP binary change (bgworker/extension + SQL only). Spec: [`projects/pg-ai-stewards/.spec/proposals/substrate-anthropic-format-dispatch.md`](../projects/pg-ai-stewards/.spec/proposals/substrate-anthropic-format-dispatch.md). Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-30-an-batch-anthropic-format.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-30-an-batch-anthropic-format.md).

> **2026-05-30 — Substrate Batch R: multi-model document redline (panel) SHIPPED + live-verified (commits `e4b6f6f` `e163ddc` `488fa43` `0663f2c` `d63331c` `bf28814`).** WS5/pg-ai-stewards. From the scripture-book 3rd-draft attempt that FAILED: `start_brainstorm` gave a 7-model panel the manuscript but all failed to read it (fs-read allow-list miss + `fs_search` budget-burn → empties). Root cause confirmed against code (single global fs-read allow-list; `audit_files` is NOT a privileged path — same allow-list). **Fix = stop making the model fetch the doc; inject it server-side.** Six gated phases, mostly live-apply SQL (one container recreate, no Rust/extension rebuild): **R.1** `redline` pipeline + `panel-redline` agent (location-anchored mandate, verification gate baked in, `tools_disabled` so the panel can't reach a quote to "fix" it). **R.2** pg `/workspace:ro` mount + `read_workspace_doc(path|glob)` via `pg_read_file` (doc-extension-only gate refuses `.env`/secrets/traversal) — **D-RL2 revised**: ratified "bgworker reads /workspace" was impossible (bgworker is in the pg container, which had NO /workspace mount — only the bridge); pg-side `pg_read_file` is lighter (mount + recreate, no rebuild), Michael confirmed. **R.3** per-call `max_tokens` 32k + input-scoped `tools_disabled` in `work_item_dispatch_stage`. **R.4** `start_panel_redline()` + `panel_redline` MCP tool — reads doc, injects into each child binding_question, builds a `decompose-fanout` manifest (reuses `spawn_children`) with one `redline` child/model (model_override + provider from `model_pricing` so gemini→google_gemini + `input_extra{tools_disabled,max_tokens}` + auto-scaled cost cap). **R.5** `panel_redline_condense()` + `redline-condense` agent — optional substrate merge into one ranked menu (value×consensus, preserve every flag); default stays orchestrator-condenses. **R.6** auto-verify `redline%` (j6 didn't cover it → children stuck `maturity=raw`, aggregator dangled). **LIVE E2E (<$0.02):** 3-model panel (deepseek/mimo/qwen3.6-plus) on `06_bilateral_covenant.md` → all 3 produced 7–10KB location-anchored reports quoting real text; D&C 82:10 quote flagged `Touches quote/doctrine: yes` + preserved verbatim; condense merged with per-edit consensus; children auto-verified + index materialized. **The original "can't read → empty" failure is GONE.** This replaces the `agy-cli` stopgap skill. **CARRY-FORWARD:** (1) MCP binary swap pending (panel_redline/panel_redline_condense + list_models/list_connectors are in code, `bin/stewards-mcp.exe` MCP-locked — `/mcp` disconnect → `go build -o ../../bin/stewards-mcp.exe .` → reconnect; SQL path is live now). (2) **tools_disabled latent bug — 10 pipelines** (planning/research-*/yt-*/revise-proposal/thummim-define/agent-proposal) declare `stage.tools_disabled=true` on review stages but `work_item_dispatch_stage` never propagated it → they run WITH tools (cost leak). R.3 scoped around it (input-only); ratify whether to globally honor stage.tools_disabled (changes live soak output). (3) manifest doc-replication (N+1× — fine for chapters, ~1MB for whole-book×many). (4) whole-book run not yet live. Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-30-r-batch-panel-redline.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-30-r-batch-panel-redline.md). Proposal SHIPPED: [`.spec/proposals/substrate-multimodel-document-redline.md`](../.spec/proposals/substrate-multimodel-document-redline.md).

> **2026-05-29 — Substrate Batch M: model-capability awareness + auto-probe SHIPPED (commits `6e417eb` `2a2141d` `2966434` `8ac655c` `f87edde` `8a721c1`).** WS5/pg-ai-stewards. Michael: "check on qwen3.7 / what glm-5 versions / test each" → "should we add a tool that displays models + connectors?" → chose full scope (substitute+log on a dead model; build the auto-probe now). **Five gated phases, all live-apply SQL + one Go build, NO pg rebuild** (the dispatch chokepoint is one SQL fn; the bgworker already records completions to `messages`/`work_queue`, so the probe is a trigger not a Rust change): **M.1** `model_capability` table + `model_usable()` + `model_catalog` view (usable=false is the only gate; unrowed defaults usable, mirrors J.11 cap gate). **M.2** capability substitution in `work_item_dispatch_stage` — an unusable resolved model is swapped for a usable same-provider one (catalog default → cheapest usable → raise) + logged via the l29 trigger (now single writer, `reason` column); usable dispatch byte-identical. **M.3** `list_models` + `list_connectors` MCP tools (read DB state, not the bgworker-only `providers_loaded()`). **M.4** `enqueue_model_probe` (direct work_queue insert, bypasses M.2 so the real model is tested) + `trigger_resolve_model_probe` (verdict on done/error). **M.5** `enqueue_due_model_probes` + guarded `AFTER INSERT` trigger on `watchman_passes` — probing rides the watchman cadence, pauses with the soak, never aborts a pass. **THE MISDIAGNOSIS M.4 CAUGHT:** earlier this session I called glm-5/glm-5.1 "streams empty" from a **shell-grep** SSE probe — committed it, annotated `model_pricing`, wrote it into the scripture-book workflow doc, reported it as fact. M.4's first run overturned it: the substrate's real parser extracts GLM's content fine (substantive probe = 385 chars, finish=stop). **glm-5/5.1 are USABLE**; the brainstorm emptiness was per-lens budget/transient. **qwen3.7-max is the one unusable opencode model** (HTTP 401 whose body IS "not supported for format oa-compat" — same rejection my curl saw). Corrected everywhere (m1 seed, model_pricing j10+4a+live, m2 smoke, workflow doc submodule `4a820c6`+pointer `f2f0eac`). Lesson: build verification into the system + test the real path; ad-hoc diagnostic tools lie (Moroni 10:4 / Agans 9 caught a shipped false diagnosis). Live state: opencode 15/16 usable (only qwen3.7-max gated), gemini 10/10; soak `schedule_enabled=true` so auto-probe is live. **CARRY-FORWARD: confirm auto-probe fires end-to-end on the next real watchman pass** (trigger installed+guarded+enqueue verified, but not yet watched on a scheduled pass; will probe the unprobed claude-*/gemini/minimax-m2.5/qwen3.5-plus — watch first gemini probe cost, tiny + capped). Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-29-m-batch-model-capability.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-m-batch-model-capability.md).

> **2026-05-29 — Fixed `start_brainstorm` MCP `models`-param schema bug (commit `3a18247`) — binary REBUILT, fix is live.** WS5. The scripture-book audit's first real brainstorm run hit `InputValidationError` on every per-lens model override: `models` was typed `json.RawMessage` (`[]byte` → array-of-int schema). Fixed to `map[string]any` (free-form object) + a `brainstorm_test.go` regression guard using the SDK's own `jsonschema.For[]` generator (test PASSES). Workflow-doc gotcha updated (scripture-book submodule `789ad49`). **`bin/stewards-mcp.exe` rebuilt this session** (after the MCP disconnect released the lock); the rebuild also bundles the M.3 `list_models`/`list_connectors` tools — both go live on the next MCP reconnect.

> **2026-05-29 — Substrate SET DOWN; picked up 1828 (the neglected thread) → UX 1-2 punch shipped (commit `3c7b5c8`).** WS7. Of the three May-23-Sabbath threads: substrate (set down after J.8–J.12), teaching (advanced hugely via the scripture-book — reconciled as "the book IS the teaching thread in paperback form, the harvest the Sabbath named," not a 4th thread), and **1828** (untouched since the Sabbath = the gap). Picked 1828's UX punch (self-contained; Phase 6 Thummim re-opens substrate, webster-v2 is a bigger build). **Shipped `SearchBar.vue`**: always-visible header search, intent detection (reference → Verse Explorer canon params; word → word-detail), `/` + Cmd/Ctrl-K focus. Neighboring fix: VerseExplorer query-watch now re-fetches (loadedKey guard) — fixes same-page search + latent back/forward bug. **Browser-verified** per 1828's rule (rebuilt frontend container vs live backend, Playwright): "1 ne 3:7" → dropdown → Enter → `/verse?mode=canon&v=bofm&b=1-ne&c=3&r=7` → verse loaded; "charity" → "Look up" suggestion. Spec: [`projects/1828-illuminated/.spec/proposals/ux-1-2-punch.md`](../projects/1828-illuminated/.spec/proposals/ux-1-2-punch.md). **1828 carry-forwards:** full-text search results surface (backend exists, no frontend view), connective study/present actions, Phase 6 Thummim, webster-v2 MCP, + production deploy (Michael's Dokploy action — no auto-deploy on commit). Journal: [`.spec/journal/2026-05-29-1828-ux-search.md`](../.spec/journal/2026-05-29-1828-ux-search.md). **Pace: this capped a very long session — the Sabbath's "less but better" is still live.**

> **2026-05-29 — Competitive-landscape scan captured for pg-ai-stewards.** WS5. Web research (Exa) on "Postgres-as-AI-agent-substrate" — the original "only MS GraphRAG + pgai" is badly out of date. Three camps now: (A) Postgres-as-store + external orchestrator (LangGraph PostgresSaver, Tiger Data, pgcortex, pgmq+pg_cron+pgvector) — mainstream; (B) in-SQL LLM calls — **pgai archived Feb 2026 + removing in-DB LLM calls by June 30 2026** (the giants retreated); (C) Postgres-extension-as-agent-runtime with in-DB bgworker — **pg-ai-stewards' camp**: closest architectural twin **pgclaw** (Rust/pgrx + bgworker + rig + claude-agent-sdk, row-bound, governance-light), closest feature peer **NeuronDB/NeuronAgent** (proprietary). **Distinctive: the governance layer — no peer has maturity gates / trust ladder / enforced spend caps / reflective primitives.** Michael's framing: "governance + hermes/pi/openclaw in a DB with arms and legs to actually do things. Opinionated and Jesus." Captured at [`projects/pg-ai-stewards/.spec/research/2026-05-29-competitive-landscape.md`](../projects/pg-ai-stewards/.spec/research/2026-05-29-competitive-landscape.md); cross-linked from the standalone-extraction proposal as positioning input. Ben Test: "most rewarding to work on" ≠ "most likely to find users" — both true.

> **2026-05-29 — pg-ai-stewards public-OSS docs pass (standalone-repo readiness, docs layer).** WS5. Ratified public OSS + docs-only-now + gospel-as-example. Rewrote `README.md` from the stale pre-build research verdict to a real "what this is + quickstart" (old one preserved at `docs/history/2026-05-02-research-verdict.md`); refreshed `docs/architecture.md` counts (65 tables / 263 fns, was 23/67); added LICENSE (MIT), .gitignore, CONTRIBUTING.md, QUICKSTART.md, docs/history index. **Key finding: "standalone" is two layers** — docs (done) + build-decoupling (the Dockerfiles build with `context: ../../..` and pull sibling modules via the shared go.work). The Go modules have NO sibling source deps (just coexist in go.work), so extraction is plumbing not rewrites — specced in `.spec/proposals/standalone-extraction.md` (own go.work, self-contained contexts, pluggable bundled MCP servers, vendor UI in, subtree-split + CI). Extraction NOT executed — ratified as separate later work. Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-29-public-oss-docs-pass.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-public-oss-docs-pass.md).

> **2026-05-29 — Substrate J.12: budget/cap error surfacing (commit `0f535a4`, no rebuild).** WS5/pg-ai-stewards. Michael: make Gemini budget failures easy to tell. The raw error is already captured (advance trigger → work_item_fail → work_items.error), so this was read-time classification: `classify_error(text)` → category (spend_cap_reached | provider_budget | rate_limited | auth | timeout | other | none; gemini 429 quota wording matched before generic 429). `start_brainstorm` **pre-flight** RAISEs a clean refusal before spawning if a lens routes to an over-cap provider (closes the J.11 swallowed-refusal gap). stewards-ui: API returns `error_category`; WorkItemDetail shows an amber budget banner + refill hint; WorkItems list shows a 💸 badge. Smoke: classifier all-categories ✓, pre-flight clean refusal (0 parents) ✓, API returns provider_budget for a synthetic 429 ✓. Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-29-j12-budget-error-surfacing.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-j12-budget-error-surfacing.md). **NEXT (planned, not started):** standalone-repo documentation pass for pg-ai-stewards — see the plan presented 2026-05-29.

> **2026-05-29 — Substrate J.11: enforced Gemini prepaid spend cap + streamed cost-capture fix (commit `c3cc2cc`).** WS5/pg-ai-stewards. Michael flagged Gemini is pay-as-you-go against a ~$20 prepaid balance (not the opencode subscription) — make it opt-in + don't let it burn the balance. Verified Gemini was **already opt-in** (5-way check). Ratified + built real enforcement: prepaid-balance model, **$18 cap**, gemini-only, refuse-at-dispatch. **Three pieces:** (1) `bgworker.rs` — `stream_options:{include_usage:true}` so streamed responses carry usage (CLOSES the J.10 gemini cost-tracking gap; verified — gemini dispatch now records cost_events, $0.003 smoke spend tracked); (2) `j11-provider-spend-caps.sql` — `provider_spend_caps` table + `provider_cap_exceeded()` + `provider_cap_refill()` + a gate in `work_item_dispatch_stage` that RAISEs before enqueue when an enforced provider is over cap (opt-in per provider via `enforced`; opencode untouched); (3) `Dockerfile` — added `am1-pending-file-writes-notify.sql` to the COPY list (pre-existing build break: lib.rs referenced it but it wasn't in the build context, so pg couldn't rebuild — am1 was the only missing ref; resolves the build-blocking slice of the foldback debt). Smoke: gemini cost tracked ✓, gate refuses over-cap with clean message ✓, opencode not gated ✓. **Refill after topping up:** `SELECT stewards.provider_cap_refill('google_gemini'[, <new_cap_micro>]);`. Required a pg rebuild (soak paused/resumed). Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-29-j11-gemini-spend-cap.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-j11-gemini-spend-cap.md).

> **2026-05-29 — Substrate J.10: Gemini usable + expanded opencode_go models.** WS5/pg-ai-stewards. Michael's ask: get Gemini usable from his key + add more opencode_go chat models (deepseek etc.). **Gemini was mis-configured** — base_url `.../v1beta/` 404s because the substrate posts to `{base_url}/chat/completions` and Gemini's OpenAI-compat path is under `/v1beta/openai`. Fixed in `.env` (gitignored, surgical sed — keys never entered context) + documented the gotcha in `.env.example`; recreated `pg` to reload the registry. **`j10-provider-models-pricing.sql`** added 10 Gemini + 7 opencode_go chat-model pricing rows (real prices from live pricing pages 2026-05-29; tiered Gemini uses ≤200k rate + notes). `/api/models` 9→26. Live smoke verified BOTH: deepseek-v4-flash (real content + $0 cost row) and gemini-2.5-flash-lite (real SCAMPER content, completed+verified). **CARRY-FORWARD — Gemini cost-tracking gap:** bgworker records cost only when usage tokens>0 (`bgworker.rs:845`); Gemini omits the usage block on *streamed* responses unless `stream_options.include_usage=true` is sent (opencode gateway includes it, so DeepSeek tracked; Gemini didn't). model_pricing is correct ($1.55 verified) — just need usage captured. Fix = bgworker change + pg rebuild, separate batch. **NOT priced** (unpublished on opencode's table): deepseek-v4-pro, mimo-v2-pro/2.5-pro/omni, hy3-preview — dispatch fine, flagged no_pricing_row. Journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-29-j10-gemini-opencode-models.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-j10-gemini-opencode-models.md).

> **2026-05-29 — Opus 4.8 harness review.** Both harnesses (Claude Code + Copilot) moved to **Claude Opus 4.8** (`claude-opus-4-8`); Claude Code effort set to `xhigh`. Read the migration guide; the honest finding was that **almost nothing needed to change** — 4.8 builds on 4.7 with no behavioral reversals, so all existing 4.7-tuning (literalism compensation, Foresight & Adjacent Surfaces, positive-examples-for-voice) applies unchanged. Edited 3 files for accurate framing only: `copilot-instructions.md` Model-context note + 2 stale "4.7" citation refs, `active.md` Model banner + cost-table line, new `CLAUDE.md` addendum "Model & effort" section (effort default `high`, standing default `xhigh` for dev, `high` for pure prose/study). Substrate untouched (opencode_go models, no Claude effort param). Named alignment: 4.8's more-direct/less-validation-forward nature pulls the same direction as the Ben Test + "don't over-hedge." **Carry-forward:** confirm the 4.8 Copilot premium multiplier (cost table shows last-known 4.7 `7.5x`, flagged). Journal: [`.spec/journal/2026-05-29-opus-4-8-harness-review.md`](../.spec/journal/2026-05-29-opus-4-8-harness-review.md).

> **2026-05-29 — Models catalog + 3 new projects registered (commit `a5a6ae8`).** WS5/pg-ai-stewards. Two enhancements on top of today's brainstorm surfacing: (1) registered 3 workspace projects in `stewards.projects` via `/api/projects/create` — `scripture-book`, `cpuchip-net` (name=cpuchip.net), `marsfield-org` (name=marsfield.org). Slug regex disallows dots so dotted forms preserved in name + root_directory. 5 projects total now. (2) Surfaced AI model catalog at two locations: `<datalist>` autocomplete on Brainstorm form's per-lens model inputs (still allows free-text fallback) + dedicated `/models` view with provider grouping, filter input, click-to-copy model names, prices in $/Mtok, provider-default star, provider load status. New `GET /api/models` endpoint returns model_pricing rows enriched with `is_provider_default` joined against `providers_loaded()`. Smoke: 9 opencode_go models returned correctly; kimi-k2.6 flagged as default; Vue SPAs serve on /models and /brainstorm.

> **2026-05-29 — Brainstorm surfacing layer SHIPPED (M + U batches).** WS5/pg-ai-stewards. Both J.8/J.9 carry-forwards from earlier today closed: **M batch (commit `0c1926c`)** — new `start_brainstorm` MCP tool in `cmd/stewards-mcp/brainstorm.go`, wraps the SQL function with pre-validation of lens names and parent_id+children return shape; bridge rebuilt + restarted + Windows `bin/stewards-mcp.exe` rebuilt (reachable from Claude Code on next MCP refresh). **U batch (commit `2fe9b33`)** — new `/brainstorm` route in stewards-ui at http://127.0.0.1:8080/brainstorm; `GET /api/brainstorm/lenses` + `POST /api/brainstorm/start` API endpoints; `Brainstorm.vue` view with originals pre-checked + 8 J.9 lenses collapsed under "More lenses" expandable section + per-lens model override input; nav link in App.vue between New work and the section separator. **Stewardship sweep:** ui.Dockerfile was missing the `1828-illuminated/backend` go.mod COPY (same shape as bridge.Dockerfile's 2026-05-22 fix that didn't sweep ui.Dockerfile) — fixed inline per `exercise_stewardship`. Smoke: lens endpoint returns 12 with originals correctly tagged; start endpoint validates binding_question; Vue SPA serves on /brainstorm; bridge entrypoint auto-applied today's 6 J.8/J.9 SQL files via `stewards-cli migrate --repo-root /workspace`. **CORRECTION to earlier carry-forward claim:** the bridge log revealed a working migration ledger system I missed in my earlier investigation — `stewards-cli migrate` tracks 176 SQL files and applies them on fresh rebuild. The "J-series lib.rs/Dockerfile foldback debt" I named in the J.8 + J.9 commit messages was based on incomplete investigation: the lib.rs `extension_sql_file!` path is for the embedded extension binary, but the migration ledger covers fresh rebuilds via `stewards-cli`. The earlier-named "foldback debt" is real only for callers who BOTH bypass the migration ledger AND need fresh extension-binary state — not the substrate's normal rebuild path. Substrate journal addendum at [`projects/pg-ai-stewards/.spec/journal/2026-05-29-substrate-j8-j9-brainstorm-finish.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-substrate-j8-j9-brainstorm-finish.md).

> **2026-05-29 — Substrate brainstorm-finish (J.8 + J.9) SHIPPED.** WS5/pg-ai-stewards. Michael's stewards push for the week — sub-batch inside the substrate Sabbath thread, not displacing Council ② substrate-scheduled-workflows. **J.8 (commit `7753424`)** = model generalization: 4-layer dispatch fallback chain (`work_items.{model,provider}_override → stages → pipelines.metadata.default_* → catalog_default_*()`), `start_brainstorm()` accepts `p_models jsonb` (string shorthand OR `{model, provider}` object); existing 4 lenses NULL'd in stages with today's defaults preserved in `metadata.default_model` (behavior preservation byte-exact via fallback chain). **J.9 (commit `23ce243`)** = lens library 4 → 12: added Mind Mapping, Brainwriting, Starbursting (5W1H), Disney Method, Storyboarding, TRIZ, Forced Analogy, Worst Possible Idea — each prompt designed for a DIFFERENT output shape, not just a different topic on the same shape; `start_brainstorm()` accepts `p_lenses text[]` (default = existing 4 for backward compat); unknown lens names raise at function entry with helpful list. 6 SQL migrations + 5 smoke files (~1.9k lines). **NEW carry-forward:** J-series lib.rs + Dockerfile foldback debt — j1-j9 all live-only since 2026-05-13; substrate convention (never `docker compose down -v`) makes this debt not breakage but it IS real foldback debt that should batch before any rebuild. **Mosiah 4:27 evidence-test reading at session close:** substrate work happened; teaching (Episode 2) and 1828 (UX 1-2 punch + webster-v2) untouched today — no slip detected, brainstorm-finish was the right scope for an "alongside Council ②" pass. Substrate journal: [`projects/pg-ai-stewards/.spec/journal/2026-05-29-substrate-j8-j9-brainstorm-finish.md`](../projects/pg-ai-stewards/.spec/journal/2026-05-29-substrate-j8-j9-brainstorm-finish.md). Workspace journal: [`.spec/journal/2026-05-29-substrate-brainstorm-finish.md`](../.spec/journal/2026-05-29-substrate-brainstorm-finish.md).

> **2026-05-29 — *Beyond the Prompt* book completion pass.** WS6. Marathon session in `projects/scripture-book/`: ten of twelve Tier 1 / Tier 1+ council items closed; all five Tier 3 verified factual fixes shipped. Five chapter engineering-parallel rebuilds (Ch 0 arrow flip + Ch 8/9/10/12 full rebuilds under the hybrid directional flow — gospel as lens, AI as the lit thing). **One new chapter — Ch 11: *The Seventh Time*** — closing the missing Sabbath step of the eleven-step creation cycle. Anchored on Abraham 5:2 (the seventh time decided in council before the work began). **One new backmatter chapter — Ch 14: *Afterword: How I Got Here*** — the 3.5-year journey relocated from the preface so the warmest opening sentence (Ch 1 line 13) lands by page 15 instead of page 26. **Introduction deleted entirely**: its three pieces were duplicating Ch 1 (Confluence), prescribing navigation the chapter tags now make self-evident (How to Read), or duplicating the new preface (opening paragraph). Front matter dropped from 28% to ~5%, in line with Lewis, Lanier, Peterson, Newport, Sapiens, Bushman per the editing-research audit. **Two new workspace studies, both live on cpuchip.net** with full provenance discipline (every scripture verified verbatim via `gospel_get`; apostolic quotes verified against local talk files): [`study/four-groups-and-the-engineer.md`](study/four-groups-and-the-engineer.md) (Bednar Oct 2024's "In the Space of Not Many Years" reading of 1 Nephi 8:28 as a pride failure, applied to engineers who learned the discipline — Lamanite virtue as corrective) and [`study/the-seventh-time.md`](study/the-seventh-time.md) (what the Sabbath does that work alone cannot — the two reasons, Nelson's sign-reframe, Hebrews 4:10's precision on "own works"). **Eight chapters tagged** with their cycle-step word in single-line italics under each H1 — *Specification, Watching, Covenant, Stewardship, Atonement, Layering, Atonement, Sabbath, Consecration · Zion* — readers navigate either by reading order or by step. RQ1.0b directional editing pass + RQ1.0c hybrid flow embodied in every chapter rebuild. Tier 1.4 imago Dei boundary closed via the per-chapter EP close pattern (each rebuilt chapter ends with "The shape rhymes. The substances do not... only one of them..."). **Tier 1.1 front-loading fix** landed in one structural sweep: preface trimmed to ~35 lines with two-audience hospitality notes (*"A note to the Saint who is wary of AI" / "A note to the engineer who is wary of religion"* — Michael's distinction between notes-to and how-to-read framing was the key reframe). Bacteriopolis "ran for ten hours" replaced with verified "burned through 230 million input tokens." Moses 7:68 framing fixed (Zion stood 365 years, not Enoch walked 365 years). Oaks 2000 + Bednar 2007 inline attributions added to Ch 8. D&C 131:7-8 capitalization aligned between Ch 0 and Ch 8. Two backmatter renumber operations preserved history via git mv (Ch 11-14 → 12-15 when Sabbath chapter inserted; Ch 14-15 → 15-16 when Afterword inserted). Ten scripture-book commits + four workspace commits across the day. Full session detail at [`projects/scripture-book/.spec/journal/2026-05-29--book-completion-pass.yaml`](projects/scripture-book/.spec/journal/2026-05-29--book-completion-pass.yaml). Project active memory refreshed at [`projects/scripture-book/.mind/active.md`](projects/scripture-book/.mind/active.md). **What's left for the book**: Tier 1.2 (Ch 6 production-note prominence — both alt-readers called it the most credible page), Tier 1.6 (Section II label mismatch), a handful of Tier 2 polish items, dist/ artifact rebuild, optional voice pass.

> **2026-05-28 — ai-chattermax project SCAFFOLDED (design-only, NOT yet a ratified cycle thread).** WS5-adjacent. Michael revived the chat-with-repos seed (set down at the 2026-05-23 Sabbath as work-scope-adjacent) after a coworker conversation, created `projects/ai-chattermax/` (own repo at `github.com/cpuchip/ai-chattermax`, MIT, Go-flavored .gitignore), and granted commit+push stewardship parallel to marsfield.org. Today's design session captured the A2A-vs-MCP-vs-chat-protocol distinction, persona-vs-server-identity question, MVP binding question (D&D, 1 human DM + 2 pg-ai-stewards personas), classifier-as-prompt-gate pattern (reuses Brain's `tools_disabled=true` gate-eval), rate-cap-with-quiet-periods architecture. Scaffolded: `CLAUDE.md`, `.mind/active.md`, `.spec/journal/2026-05-28-genesis-and-design-session.md`, `.spec/proposals/chat-server-design.md` (stub with five open questions). Auto-memory entries written ([project_ai_chattermax](../../../.claude/projects/...memory/project_ai_chattermax.md), [feedback_ai_chattermax_stewardship](../../../.claude/projects/...memory/feedback_ai_chattermax_stewardship.md)). **Mosiah 4:27 evidence-test:** the seed was set down 5 days ago; rapid revival warrants watching whether discovery-mode pulls or "say yes to everything" reasserts. At the next Sabbath, decide: (a) fourth thread, (b) displaces substrate Council ② / teaching Episode 2 / 1828 finish, or (c) stays design-only another week. **No code shipped to ai-chattermax until ratified.** Journal: [`.spec/journal/2026-05-28-ai-chattermax-genesis.md`](../.spec/journal/2026-05-28-ai-chattermax-genesis.md).

> **2026-05-27 — Full manuscript of *Beyond the Prompt* (Chapters 0–14) completed, verified, and compiled.** WS6. Completed drafting Section III (Chapters 8–11) and the Backmatter: Chapter 12 (Epilogue: The Silent Loop), Chapter 13 (Glossary of Fused Terms), and Chapter 14 (Recommended Study). Created quote provenance files for all new sections, verifying citations verbatim against the local `gospel-library/` source files (the cite-count rule). Successfully compiled the print-ready PDF and EPUB artifacts locally using Python and Docker, producing clean builds in `projects/scripture-book/dist/`. Journal: [`2026-05-28--section-iii-execution.yaml`](file:///c:/Users/cpuch/Documents/code/stuffleberry/scripture-study/projects/scripture-book/.spec/journal/2026-05-28--section-iii-execution.yaml).

> **2026-05-27 — Antigravity IDE 2.0 and Gemini 3.5 Flash harness solidified.** WS6/WS7. Initialized `GEMINI.md`, `AGENTS.md`, and `.agents/skills/` directories at the workspace root and within `projects/scripture-book`. Successfully integrated standard workspace covenants, intent files, and the `source-verification` skill into the Antigravity agent flow. Journal: [`.spec/journal/2026-05-27--antigravity-harness-solidification.yaml`](../.spec/journal/2026-05-27--antigravity-harness-solidification.yaml).

> **2026-05-27 — ibeco.me login loop diagnosed + four-fix sweep SHIPPED (`074e769`).** WS4. Symptom: existing users redirected back to home page on login, never staying authenticated. Root cause: commit `643574d` (May 24) added `COOKIE_DOMAIN=.ibeco.me` so 1828 subdomain logins could share session, but per RFC 6265 §5.3 a host-only and a Domain-scoped cookie with the same name are TWO DIFFERENT cookies — the new one did not evict the old one. Browser sent both; Go's `r.Cookie()` returned the stale host-only one first; /api/me returned 401; frontend loop. Clean browsers + local dev (no COOKIE_DOMAIN) worked; only existing users with pre-May-24 cookies were trapped. Four fixes in one commit: (1) `setSessionCookie`/`clearSessionCookie` emit an extra host-only `Max-Age=-1` eviction when CookieDomain is set — the loop fix; (2) LandingView's Sign In `<router-link>` now forwards `?redirect=` query via computed `loginTo`/`registerTo`; (3) auth guard honors `?redirect=` for already-authed users hitting /login (was unconditionally sending to /today); (4) shared open-redirect allowlist (Go + Vue) — relative paths + `ibeco.me`/`*.ibeco.me` only, 14 test cases including suffix-confusion attack. Verified live by Michael. **Secret-leak failure mode also surfaced and named:** mid-investigation Dokploy `application.one` query exposed `POSTGRES_PASSWORD` (embedded in `BECOMING_DB` DSN) into model context despite the dokploy skill's WARNING. Reactive skill-level filtering proved insufficient. **Carry-forward:** design a Dokploy-secret bridge that returns env-var-NAMES for inspection or routes values through a side-channel that never enters model context. Rotation request issued to Michael. Journal: [`.spec/journal/2026-05-27-ibeco-login-loop-and-secret-leak.md`](../.spec/journal/2026-05-27-ibeco-login-loop-and-secret-leak.md).

> **2026-05-26 — Study on Emad Mostaque's YouTube interview completed.** Grabbed the transcript for the Tom Bilyeu interview of Stability AI founder Emad Mostaque ([study/yt/Se91Pn3xxSs-you-have-3-years-left.md](../study/yt/Se91Pn3xxSs-you-have-3-years-left.md)) and completed the evaluation and study notes. Explored the connection between his "infinite grads" thesis, the deflationary nature of AI intelligence scaling, and our book chapters (particularly the Value Shift and Delegation as Stewardship), mapping these concepts directly to the pg-ai-stewards substrate and covenant-based human-AI collaboration. Journal: [`.spec/journal/2026-05-26--emad-mostaque-youtube-evaluation.yaml`](../.spec/journal/2026-05-26--emad-mostaque-youtube-evaluation.yaml).

> **2026-05-25 — "Beyond the Prompt" Book Repository Initialized & First Chapters Drafted.** Initialized the `projects/scripture-book` repository as the home of our compiled book studying the gospel with AI and examining how gospel principles apply to working with AI. Created CLAUDE.md, README.md, book.yaml, src/style.css, and scripts/build.py. Drafted the Introduction and the first four chapters (The Value Shift, The Four Disciplines, Spiritual Before Temporal, and Watched Until They Obeyed) in our modular study format. Established outline skeletons for Chapters 5 through 11. The build script compiles the manuscript to a print-ready PDF-ready HTML structure and a standard valid EPUB, converting all local relative scripture links to absolute online Church URLs. Pushed the initial build commit to the GitHub remote main branch. Journal: [`.spec/journal/2026-05-25--scripture-book-initialization.yaml`](../.spec/journal/2026-05-25--scripture-book-initialization.yaml).

> **2026-05-25 — Study on "Softening What I Cannot Soften" completed.** Graduated the Send Me / Todd interview mapping ([study/yt/V40tBshkMnE-send-me-covenants.md](../study/yt/V40tBshkMnE-send-me-covenants.md)) into a full standalone study: [softening-what-i-cannot-soften.md](../study/softening-what-i-cannot-soften.md), provenance at [study/.scratch/softening-what-i-cannot-soften.md](../study/.scratch/softening-what-i-cannot-soften.md). Binding question: how does a person receive a softened heart when scripture says they cannot soften their own? Central reframe: Alma 12:10's agency-bearing clause is "will not harden" — humans bring/yield/stop; the Lord visits/softens/sprinkles/gives the new heart. Two discoveries: Alma 22:18's conjunction is "AND" not "then" (simultaneous trade, not sequenced bargain), and Helaman 3:35's graduated "stronger and stronger" sits alongside the flash-moment. Landed on morning-hour test — two independent witnesses (Sabbath agent + Todd interview) in the same week pointed at the Marshfield-data-center-Facebook-scroll as the hardening pathway. Extends the [give-away-all-my-sins](../study/give-away-all-my-sins.md) arc. Journal: [`.spec/journal/2026-05-25--softening-what-i-cannot-soften.yaml`](../.spec/journal/2026-05-25--softening-what-i-cannot-soften.yaml).

> **2026-05-24 — Three-Way Review on "Refinement, Stewardship, and Hope" completed.** Synthesized [refinement-stewardship-and-hope.md](../study/refinement-stewardship-and-hope.md) and populated source log in [study/.scratch/refinement-stewardship-and-hope.md](../study/.scratch/refinement-stewardship-and-hope.md). Connected the metaphysics of D&C 93, the three stewardship patterns of Jared's barges, and hope's attentional geometry within the veil. Verified and incorporated quotes from Ether 2-3, 12, Moroni 7, Hebrews 6, Matthew 6, and D&C 88, 131.

> **2026-05-23 — Study on "The Mechanics of Refinement" completed.** Drafted [mechanics-of-refinement.md](../study/mechanics-of-refinement.md) and populated source log in [study/.scratch/mechanics-of-refinement.md](../study/.scratch/mechanics-of-refinement.md). Verified and incorporated quotes from D&C 88, 93, 122, 131, 3 Nephi 24, Alma 7, and talks by Nelson, Oaks, Eyring, Bednar, and Pingree.

> **2026-05-23 — SABBATH OBSERVED. Cycle closed: "The Arc That Said Yes to Everything" (May 13–23, ~173 commits, zero rollbacks).** Record at [`.spec/sabbath/2026-05-23-the-arc-that-said-yes-to-everything.md`](../.spec/sabbath/2026-05-23-the-arc-that-said-yes-to-everything.md). **Declaration: It was good.** Three learnings written: [floating-docker-tags](../.spec/learnings/2026-05-23-floating-docker-tags.yaml), [go-work-build-context](../.spec/learnings/2026-05-23-go-work-build-context.yaml), [subproject-memory-bubble](../.spec/learnings/2026-05-23-subproject-memory-bubble.yaml). **Ratifications recorded same day (2026-05-23):** (1) Next cycle holds **three threads simultaneously** (Michael's call, declining the one-binding-question recommendation): **substrate** (Council ② scheduled-workflows + silent-stall + auto-dispatch carry-forwards), **teaching** (Episode 2 + supporting studies), **1828 finish** (Phase 6 Thummim cache sync + UX 1-2 punch + **webster-v2 MCP server** — successor to webster-mcp, managed like gospel-engine-v2 → engine.ibeco.me, with the dictionary backend built/updated/downloadable from 1828.ibeco.me). Mosiah 4:27 stays loaded as evidence-test — if teaching gets crowded out by substrate again, that's the evidence. (2) Memory-architecture principle ratified **pull-based, not push-based**: workspace memory READS from `projects/*/.spec/journal/` at session start. Written to `.mind/principles.md` → "Read Subproject Journals, Don't Bubble Them"; session-start checklist updated in `.github/copilot-instructions.md` (new step 7). (3) A2A / per-repo-steward ecosystem is **work-scope** (Michael's employer), not workspace ambition. A small Slack-like chat-with-repos prototype against these repos is held as a seed only — explicitly NOT one of the three threads. Set down: `scripts/brain` as active workstream, Marshfield-data-center-as-Facebook-scroll, maximalist A2A/MCP vision as thing-to-start-now.


> **2026-05-23 — 1828-illuminated UX round + becoming engine cutover both LIVE.** WS7+WS2. Three deploys went green this session: 1828 UX, becoming, and the Dockerfile Go bumps that unblocked both. **1828 UX additions (5 commits):** `51fc263` pinnable study tree (📌 toggle anchors panel below header so nav/settings stay reachable); `727d8cc` sidebar integrates INTO the page (max-w-7xl + flex columns when pinned, no cream-zone gap to viewport edge) + class-E words clickable inside definitions (new `GET /api/dict/headwords` endpoint streams 98,828 words → frontend lazy-loads into reactive Set → tokenize() recognizes any 1828 entry, subtler stone underline so the page doesn't vibrate) + E tier toggle with all 6 tiers ON by default; `6c4c7ac` definition vs scripture click mode (📚/📖 toggle pill in header, persists localStorage, routes word clicks to /word/X or new /word-study/X showing every canonical occurrence grouped by book); `bf85d97`+`6d5999b` Dockerfile Go 1.23→1.25→1.26 bumps after the floating `golang:1.25-alpine` tag drifted to a broken patch (`splitNamed` segfault then `ir.Node is *ir.BranchStmt` regression — Adjacent Surface Audit revealed this hit BOTH 1828 backend AND becoming's same week). **Becoming engine cutover (commit `99fc4a4`):** dropped the bundled ~20MB scripture markdown tree from becoming's Docker image. New `internal/engine/lookup.go` adds `Lookup(ctx, ref)` to the existing `engine.Client` (the same client that handles ibeco.me ↔ engine per-user token bridge — single client now also handles lookup). Soft-dep posture per `scripts/becoming/.spec/proposals/scripture-via-engine.md` §IV: engine reachable → 200 LookupResult (cached LRU-200); engine UNREACHABLE / 5xx / 401 → 503 `{available:false, error:"..."}`; books + name-search unaffected (metadata-only in-binary). One fewer build stage, ~20MB lighter image. Verified live by Michael — "works, no issues." **Next session pickup**: 1828 UX 1-2 punch with the ux agent — always-visible scripture/term search bar across surfaces, flow refinements for study/explore/present modes.

> **2026-05-22 — 1828-illuminated DEPLOYED TO 1828.ibeco.me (production).** WS7. **Phase 7 CLOSED.** Public site live behind Dokploy + Traefik TLS. Verified end-to-end: `https://1828.ibeco.me/` → frontend serves; `/api/healthz` → ok; `/api/dict/1828/gainsay` → returns the 1828 entry (class-E reach proof — that word is NOT in the curated tier list); `/api/scripture/dc/93:36` → returns "The glory of God is intelligence, or, in other words, light and truth." with the canonical apparatus link. Dokploy service type: **Compose** (per `deployment-shape.md §V`), pointed at `projects/1828-illuminated/docker-compose.yaml`, three services (frontend/backend/db) co-managed on the compose-defined network. Migration friction surfaced this session: (1) old Dokploy panel at `dokploy.hmslogs.com` migrated to `server.ibeco.me`; updated skill at `.claude/skills/dokploy/SKILL.md` (commit `b60be33`). (2) Initial Application-project attempt failed with duplicate-path error; switched to Compose project type per the proposal. Only remaining 1828 carry-forward: Phase 6 Thummim cache sync (substrate's D-THM-7 hook + nightly snapshot to `thummim_entries_cache`).

> **2026-05-22 — substrate AUTONOMOUS MATERIALIZER (am1) SHIPPED.** WS5/pg-ai-stewards. Closes the last "autonomous-up-to-disk" gap from the 2026-05-20 bridge-stall journal: scheduled and triggered pipelines now flush files to disk without waiting for a human `git commit`. Three artifacts: SQL trigger `am1-pending-file-writes-notify.sql` fires `pg_notify(stewards_pending_file_write, NEW.id)` on every INSERT; `cmd/stewards-mcp/materializer.go` goroutine LISTENs + 60s safety poll + exec's `stewards-cli materialize-writes` (no algorithm duplication — CLI stays source of truth); `docker-compose.yaml` flipped `/workspace` mount `ro → rw`. Three decisions ratified upfront via AskUserQuestion (D-AM-1/2/3). Live smoke verified end-to-end: bridge restart drained the science-news-weekly post that had been sitting in pending_file_writes since 23:53; NOTIFY-path INSERT → drain within ~2s. **First autonomously-materialized substrate output committed in `767386a`** — `research/science-news-weekly--2026-05-22-2353.md`. **Adjacent surface caught**: 1828 backend's go.mod was added to `go.work` 2026-05-20 but never to `bridge.Dockerfile`'s COPY list — latent breakage exposed only when build was finally attempted today. Same shape as morning's go.sum sweep — `go.work` membership is a build-context dependency. Fixed inline. Pre-commit hook stays as belt-and-suspenders pending one-week soak. Journal: [`.spec/journal/2026-05-22-substrate-autonomous-materializer.md`](../.spec/journal/2026-05-22-substrate-autonomous-materializer.md).

> **2026-05-22 — substrate `science-news-weekly` scheduled pipeline SEEDED.** WS5/pg-ai-stewards. New SQL migration `projects/pg-ai-stewards/extension/pe8-seed-science-news-weekly.sql` — live-applied via `docker cp + psql -f`. Row visible in `stewards.scheduled_pipelines` + `/api/scheduled/list` API. Companion to `ai-news-7am` but uses `research-write` pipeline_family (3-stage: tools-on gather → structured synthesize → qwen review) rather than `research-summary`; this is the engine that produced the format anchor `research/physics-news-20260503-science-center-roundup.md` (commit `220cf35`, 2026-05-11). Cadence: Mondays 13:00 UTC = 7am MT (`0 13 * * 1`); first natural fire **2026-05-25 13:00 UTC**. Missed-window 72h (Mon→Thu catch-up, then skip + advance). Binding question covers physics + astronomy + chemistry + biology + materials + climate + neuroscience with "Science-center translation" required per headline, target sub-$500 exhibits. **Stewardship arc closed today**: substrate produces → research/ → manual review → marsfield.org republish, all working end-to-end across two repos in three sessions, ~16 commits, zero rollbacks. Three decisions surfaced upfront via AskUserQuestion before SQL hit disk per substrate C-F cadence. **Carry forward:** watch first Monday fire for prompt quality + cost; auto-republish bridge (research/ → marsfield.org content/) is the natural next automation but explicitly deferred until prompt shakes out. Journal: [`.spec/journal/2026-05-22-substrate-science-news-weekly-pipeline.md`](../.spec/journal/2026-05-22-substrate-science-news-weekly-pipeline.md).

> **2026-05-22 — marsfield.org Science News category LIVE.** WS7. Commit `809a7c9` pushed; Dokploy auto-redeployed; `https://marsfield.org/science-news` now serves first post — a republish of `research/physics-news-20260503-science-center-roundup.md` with frontmatter, honesty preamble (the news is May 2025 dated, generated May 2026), and h3→h2 promotion for revealSections animation. Six files: useContent + router + LcarsNav + CategoryView + lcars.css + the new content/science-news/ post. End-to-end stewardship loop confirmed: **edit → docker verify → commit → push → live in <60s.** **Substrate audit surfaced a gap:** there is no automated science-news pipeline. Only `ai-news-7am` exists (`pe7-scheduled-pipelines-fire-and-tick.sql:185`). The physics-news file was a manual NewWork.vue `research-write` dispatch from 2026-05-11 (commit `220cf35`), not a scheduled job. **Pending substrate work:** add a `science-news-7am` scheduled_pipelines row with a physics-news-tuned input_template + real web-search step (Exa) to avoid the year-old-training-data problem the existing roundup exhibits. Deferred for council moment, not built unilaterally. Journal: [`.spec/journal/2026-05-22-marsfield-science-news-category.md`](../.spec/journal/2026-05-22-marsfield-science-news-category.md).

> **2026-05-22 — marsfield.org SCAFFOLDED.** WS7. New public site for the Mars-field Science Center, mirrors `projects/cpuchip.net/` pattern (Vue 3 + Vite 8 static SPA, LCARS theme + AnimeJS animations, multi-stage Dockerfile → Dokploy). 28 files in `projects/marsfield.org/` — its own git repo, content layer with three categories (blog/learning/research), `LcarsFrame` + `LcarsNav` + `BootSequence` rebranded, brand strings swapped (`Mars-field` title, `MARSFIELD.ORG` boot, lines: `LCARS / EXHIBIT MANIFEST=STANDBY / RESEARCH ARCHIVE / HUBBLE FRONTIER=ALIGNED`). Scripture-specific surfaces dropped on purpose (ScripturePanel, CreationCycle, S.vue, study/* components, StudyView, PresentationView, WP archive scaffolding) — documented in `CLAUDE.md § Things explicitly not copied`. **The marsfield.org ↔ space-center boundary is the keystone decision**: marsfield.org = public face, space-center = workshop. Encoded as table in CLAUDE.md so it doesn't drift. Verified: `npm install` clean, `npm run build` clean (same eval/chunk warnings cpuchip inherits), `docker build` clean, container served at :18080 with SPA fallback confirmed for `/blog`, `/research/missing-post`, favicon 200/image/svg+xml. **Not yet committed/pushed** — awaits Michael setting GitHub remote + Dokploy project. Carry-forward: research category empty by design until a substrate physics/science finding (not AI-news digest) gets verified and written — the most distinctive promise of this scaffold. Journal: [`.spec/journal/2026-05-22-marsfield-org-scaffold.md`](../.spec/journal/2026-05-22-marsfield-org-scaffold.md).

> **2026-05-22 — Dokploy ibeco.me deploy unblocked.** WS4. Build had been failing on the `scriptures` stage of `scripts/becoming/Dockerfile` with `missing go.sum entry for go.mod file` errors against `golang.org/x/{sys,net}`. Root cause: commit `3ba0aef` (May 16, "Update dependencies and add new module for Bacteriopolis exhibit") refreshed deps across **14 Go modules** but landed only the `h1:` package-content hashes — the matching `/go.mod h1:` lines were omitted. Local builds tolerated this (warm module cache); Docker `-mod=readonly` refused. Latent for five days; surfaced when Dokploy's cache evicted. Reproduced in `golang:1.25-alpine` (`BUILD_FAILED` exact match), applied `go mod tidy`, verified Agans Rule 9 loop (fix → confirmed gone → reverted → confirmed returns → restored). Adjacent Surface Audit found same bug in all 14 modules from that commit; swept all on Michael's call. Commit `be823c1`: 163 insertions, 0 deletions, 0 `go.mod` changes. Both Dockerfile Go stages verified `BUILD_OK` post-fix; Dokploy picked it up and the deploy went green. Journal: [`.spec/journal/2026-05-22-dokploy-go-sum-sweep.md`](../.spec/journal/2026-05-22-dokploy-go-sum-sweep.md).

> **2026-05-21 — 1828-illuminated phase 5.5 (study tree + UX polish) SHIPPED.** WS7. Three commits: `3f5edd0` (pg18 + canon UX), `1640e40` (study tree composable + panel + cross-domain wiring), and a render-node follow-up. **pg18.4 bump** done — convention changed mount to `/var/lib/postgresql` with `PGDATA=/var/lib/postgresql/pgdata`. Volume wiped + auto-re-seeded from embedded data (98,828 1828 entries, 41,995 verses, 859 tier_words, 710 modern_defs all back). **Canon-browse UX**: route-back via `route.query` sync, new `VerseList.vue` renders proper paragraphs with per-verse copy-ref pill, verse-range input ("36" or "36-40" or empty for whole chapter), routes to `/api/scripture/:ref` vs `/chapter/:ref` accordingly. **Study tree**: `useStudyTree.ts` composable with five node kinds (word/verse/chapter/render/note), idempotent `visit()` (active=identity no-op + child-identity reuse), localStorage `study-tree-v1` persistence with 250ms debounce, markdown export. `StudyTreePanel.vue` slide-out drawer with `TreeBranch.vue` recursive child, kind glyphs + colors, click-to-jump (navigateTo + router.push), per-node remove, two-click clear. Hidden on `/present` per D-ST-10. **Cross-domain wiring**: WordDetail visits on word change; `selectWord` visits via dynamic import (clicks in any rendered passage branch the tree); VerseExplorer visits chapter nodes on load; `useLLMRender` visits render nodes on success. The path word↔verse↔render↔word is preserved as a navigable graph — "five-dimensional time-travel chess" per Michael's framing.

> **2026-05-20 — 1828-illuminated phase 5 (frontend cutover) SHIPPED same day as phases 1-4.** WS7. Six commits on `main` (6b0e98a + 36419b1 + 70ede1f + 554db8c + bbc1466 + 07bf9f3). Static-JSON-import Vue SPA is gone — useWordData reaches `/api/dict/{1828,modern,search}` async; BYOK session-key flow; class-E reach surfaced in WordSearch; canon-browse mode. See `.spec/journal/2026-05-20-frontend-phase-5-cutover.md`.

> **2026-05-19/20 — substrate: Council ① CLOSED + YT-T batch shipped same day.** WS5/pg-ai-stewards. ~15 commits across PE-A + PE-B + PE-C + end-to-end smoke + YT-T, zero rollbacks. **YT-T (substrate-yt-transcripts)** wedged in after council ① closed because PE-final's yt-gospel-evaluate against Morgan Philpot produced an honest refusal — bridge had no yt-dlp, workspace yt/ was read-only mounted. YT-T fixed all three layers: yt-dlp in bridge Dockerfile (pip-upgraded to 2026.03.17 from broken Alpine 2024.12.03 pin), workspace yt/ rw-mounted at /opt/yt/yt (bridge writes; pg also gets ro-mount for SQL function reads), and new substrate primitives `yt_transcripts` + `yt_transcript_segments` tables with `import_yt_transcript()` populating them from cues.json + metadata.json. The Morgan Philpot rerun completed at $0.46 with a real substantive 16k-char evaluation citing 8 verbatim quotes with timestamps from the 1516-segment substrate transcript, caught two factual citation errors in the talk, and pushed back theologically on the priesthood-gender segment. Soak resumed. Next: council ② substrate-scheduled-workflows. Canonical navigation: [`projects/pg-ai-stewards/.spec/open-items.md`](../projects/pg-ai-stewards/.spec/open-items.md) §0.

*Hardware: Dual 4090s desktop. NOCIX server live.*

> **Edit rule:** Rewrite this file directly. Do NOT cat/append the old content first — its archive snapshot lives under `.mind/archive/`. Appending duplicates the document and doubles every memory load. (Bug: 2026-04-20.)

> **Workstream taxonomy:** [.mind/workstreams.md](workstreams.md) is canonical: WS1–WS9, status enum, frontmatter convention. Every active proposal carries `workstream:` frontmatter. Read that file before tagging anything new.

> **Model:** Both harnesses run **Claude Opus 4.8** (`claude-opus-4-8`) as of 2026-05-29 (4.7 from late April, 4.6 before that). 4.8 builds on 4.7 — more literal than 4.6, more direct, effort-sensitive. The 4.7-era tuning in [.github/copilot-instructions.md](../.github/copilot-instructions.md) → Foresight & Adjacent Surfaces applies unchanged (4.8 sharpened those traits, didn't reverse them). Effort default `high`; Claude Code set to `xhigh` for dev/substrate work, `high` is fine for pure prose/study. Adjacent Surface Audit standard before declaring dev/debug work complete.

---

## Priorities

1. ★ **[WS6] Study** — "It keeps me in the spirit." Latest: [mechanics-of-refinement.md](../study/mechanics-of-refinement.md) (May 23 — D&C 93 and the physics of the Atonement, spirit-matter capacity governed by law, experiential descent, and the refiner's fire). Prior presidency arc: art-of-presidency → art-of-delegation → zion-in-a-presidency → broken-heart-and-contrite-spirit. Next: TBD → [study-workstream.md](../.spec/proposals/study-workstream.md)
2. ★ **[WS7] Teaching** — 11-episode arc (Option C). Agent + repo scaffolded. Content not started → [teaching-workstream.md](../.spec/proposals/teaching-workstream.md)
3. **[WS5] Token efficiency** — Apr 16 proposal awaiting refresh review → [token-efficiency.md](../.spec/proposals/token-efficiency.md)
4. **[WS2] Brain Inline Panel + Kanban 4c** — next active build → [brain-inline-panel.md](../.spec/proposals/brain-inline-panel.md) · [brain-project-kanban.md](../.spec/proposals/brain-project-kanban.md)
5. **[WS5] pg-ai-stewards** — Postgres-as-AI-substrate; an autonomous agentic creation cycle. **Phases A–F + Batches G/H/I/J/K/L/L.1.1 + ES arc + Council ① (PE-A + PE-B + PE-C + final smoke) all shipped (2026-05-04 → 2026-05-19).** Next: council ② `substrate-scheduled-workflows` (cron jobs, D-SW1–7; inherits PE-B's scheduled_pipelines machinery) → ③ `stewards-ui-evolution` (D-UI1–12; PE-C was a preview slice). Directional lens: **hybrid work-together modes** — the substrate as a workspace human + agent co-manage. Soak ALIVE — bridge worker silently stalled 2026-05-20 13:37 UTC mid-Thummim-batch (process alive, goroutine dead — `docker compose restart bridge` recovered cleanly, reaped 7 stale mcp_proxy rows). Three substrate-shape items named in the bridge-stall journal: (1) silent-stall panic-recovery [open], (2) ~~materializer-only-on-git-commit cadence~~ **CLOSED 2026-05-22 by `am1` autonomous materializer (commit 767386a)**, (3) auto-dispatch gap (14 work_items pending 8 days because nothing called `work_item_dispatch_stage` after INSERT) [open]. Deferred: pg rebuild + replay-86-post-G-SQL ritual (touched zero times in council ①). → [open-items.md](../projects/pg-ai-stewards/.spec/open-items.md) §0 · [pipelines-expansion §X+§XI](../projects/pg-ai-stewards/.spec/proposals/substrate-pipelines-expansion.md) · [bridge stall journal](../.spec/journal/2026-05-20-substrate-bridge-stall-recovery.md)

## Key Facts

- Copilot: 1500 premium/mo ($40 Pro+). Haiku 4.5=0.33x, Sonnet 4.6=1.0x, Opus 4.8=7.5x *(last-known 4.7 rate — confirm 4.8 multiplier)*, GPT-5/5-mini/4.1/4o=0
- Brain default model: gpt-5-mini (0x). Pipeline big = claude-opus-4.7 (7.5x)
- **Brain stage defaults (Apr 23 cost discipline):** research=sonnet, plan=opus, spec=sonnet, execute=sonnet, verify=haiku (hard-pinned), revise=sonnet. Commission `Model` field = steward judgment only (gate eval). Revise loop capped at 2 → surface.
- Claude Code: Pro $20/mo. 200K context. Project caching
- Pipeline cost: research=0.33 + plan=1.0 = 1.33/entry  *(stale — see stage defaults above)*
- Active MCP servers: gospel-engine-v2 (engine.ibeco.me), webster, yt, byu-citations, becoming, exa-search
- gospel-engine v2 hosted is the single canonical search backend; gospel-mcp + gospel-vec retired as fallback

---

## In Flight

| WS | Item | Status | Ref |
|----|------|--------|-----|
| WS7 | cpuchip.net — personal site revival (AI + Gospel) | 🔨 revival arc shipped — deploy, LCARS visual arc, scripture-panel component. Own repo + own `.mind`/`.spec`/`CLAUDE.md`. Next: republish two studies. | [projects/cpuchip.net/.mind/active.md](../projects/cpuchip.net/.mind/active.md) |
| WS7 | marsfield.org — public site for Mars-field Science Center | 🔨 **LIVE 2026-05-22** at https://marsfield.org. Four content categories now: blog, learning, science-news, research. First post in `/science-news` is the May 2025 physics roundup with five buildable exhibits (republished from `research/physics-news-20260503-science-center-roundup.md`). Agent has commit+push stewardship; Dokploy auto-deploys on push. Next: (a) substrate work to add a `science-news-7am` scheduled pipeline mirroring `ai-news-7am`, (b) first piece of original Learning content, (c) maybe Events category when first pop-up dates set. | [projects/marsfield.org/CLAUDE.md](../projects/marsfield.org/CLAUDE.md) · [science-news journal](../.spec/journal/2026-05-22-marsfield-science-news-category.md) · [scaffold journal](../.spec/journal/2026-05-22-marsfield-org-scaffold.md) |
| WS2 | Brain Inline Panel + Nudge Bot Controls | ▶ P1 next | [brain-inline-panel.md](../.spec/proposals/brain-inline-panel.md) |
| WS2 | Brain Project-Kanban Phase 4c | ▶ next | [brain-project-kanban.md](../.spec/proposals/brain-project-kanban.md) |
| WS5 | Token Efficiency & Memory v2 | ⏸ awaiting refresh | [token-efficiency.md](../.spec/proposals/token-efficiency.md) |
| WS5 | Brain ↔ VS Code Bridge | 🔨 building (Phase 0 shipped Apr 22) | [brain-vscode-bridge/main.md](../.spec/proposals/brain-vscode-bridge/main.md) |
| WS2 | Johari window agent mode | 📝 proposed Apr 22 | [johari-window-agent-mode.md](../.spec/proposals/johari-window-agent-mode.md) |
| WS5 | pg-ai-stewards (Postgres substrate for agent state, memory, work, model calls) | ✅ **Council ① + YT-T CLOSED 2026-05-19/20**. YT-T added yt-dlp to bridge, workspace yt/ rw mount, native `yt_transcripts` table. Morgan Philpot rerun produced a real 16k-char evaluation. **2026-05-20 bridge stall + recovery** — worker goroutine wedged silently at 13:37 UTC, 7 Thummim items orphaned in `gather`; restart reaped stale rows + workers resumed; `stewards.thummim_entries` backfilled 14/14 from materialized markdown via `projects/1828-illuminated/scripts/backfill_thummim_entries.py` (D-THM-7 workaround pending canonical hook fix). Daily-digest scheduled pipeline confirmed firing daily (2026-05-21 morning digest committed at `7288aa5`). Next: ② scheduled-workflows → ③ ui-evolution. Direction: hybrid work-together modes. | [open-items.md](../projects/pg-ai-stewards/.spec/open-items.md) §0 · [bridge stall journal](../.spec/journal/2026-05-20-substrate-bridge-stall-recovery.md) |
| WS7 | 1828-illuminated scriptures (1828.ibeco.me) | 🔨 **Phases 1-5.5 SHIPPED 2026-05-20/21.** Backend phases 1-4 + phase 5 frontend cutover landed 2026-05-20 (three-container stack, scripture corpus, dictionary backend, LLM proxy + BYOK, fully API-driven Vue SPA with class-E reach + canon-browse + session-key). 2026-05-21 morning: LLM proxy bugfix bundle — `usage` decode for nested-object detail fields + system/user prompt split (cut reasoning-leak across providers) + minimax-m2.7 default for opencode-go (the model that emits clean output from the gateway's Chinese-only lineup). 2026-05-21 afternoon: phase 5.5 polish — **pg18.4 bump** (PGDATA convention changed; mount = `/var/lib/postgresql`), **canon-browse UX** (route-back, per-verse rendering, single-verse + verse-range selector), and the **branching study tree** (`useStudyTree` composable + slide-out `StudyTreePanel` + cross-domain wiring word↔verse↔render↔word). useWordData chunk dropped from ~1.5MB to 247KB. Legacy 8082 container untouched throughout. **Phase 7 SHIPPED 2026-05-22 — live at https://1828.ibeco.me via Dokploy Compose project.** **2026-05-23 UX round shipped** (commits `51fc263`+`727d8cc`+`6c4c7ac`): pinnable study tree integrated into page layout, class-E words clickable inside definitions (new `/api/dict/headwords` endpoint), E tier toggle + all 6 default ON, **definition vs scripture click mode** (📚/📖 header pill → /word/X vs new /word-study/X view). **Next pickup: UX 1-2 punch with the ux agent** — Michael wants always-visible scripture/term search bar across surfaces, plus flow refinements for the three usage modes (study, explore, present). Phase 6 (Thummim cache sync — D-THM-7 substrate hook + nightly snapshot into `thummim_entries_cache`) is the other remaining carry-forward. | [phase 5.5 journal](../projects/1828-illuminated/.spec/journal/2026-05-21-phase-5.5-study-tree.md) · [LLM proxy fix journal](../.spec/journal/2026-05-21-llm-proxy-bugfix.md) · [phase 5 journal](../projects/1828-illuminated/.spec/journal/2026-05-20-frontend-phase-5-cutover.md) · [phase 1-4 journal](../projects/1828-illuminated/.spec/journal/2026-05-20-backend-phase-1234-build.md) · [proposals](../projects/1828-illuminated/.spec/proposals/) · [project CLAUDE.md](../projects/1828-illuminated/CLAUDE.md) |
| WS2 | Motivation coach agent mode | 📝 proposed Apr 22 | [motivation-coach-agent-mode.md](../.spec/proposals/motivation-coach-agent-mode.md) |
| WS3 | LightRAG investigation | 📝 proposed Apr 22 | [lightrag-investigation.md](../.spec/proposals/lightrag-investigation.md) |
| WS3 | Gospel engine v3 proxy-pointer | 📝 proposed Apr 22 | [gospel-engine-v3-proxy-pointer.md](../.spec/proposals/gospel-engine-v3-proxy-pointer.md) |
| WS5 | VS Code agent hooks integration | 📝 proposed Apr 22 | [vscode-agent-hooks-integration.md](../.spec/proposals/vscode-agent-hooks-integration.md) |
| WS5 | Memory & context research bundle | 📝 proposed Apr 22 | [memory-research-bundle.md](../.spec/proposals/memory-research-bundle.md) |
| WS6 | Study: Nate B Jones on delegation | 📝 proposed Apr 22 | [study-nate-jones-delegation.md](../.spec/proposals/study-nate-jones-delegation.md) |
| WS7 | AI presentation site tool | 📝 proposed Apr 22 | [ai-presentation-site-tool.md](../.spec/proposals/ai-presentation-site-tool.md) |
| WS7 | Launch YouTube channel | 📝 proposed Apr 22 | [launch-youtube-channel.md](../.spec/proposals/launch-youtube-channel.md) |
| WS5 | Sabbath agent | ready to build | [sabbath-agent.md](../.spec/proposals/sabbath-agent.md) |
| WS5 | Debug agent: layer triage | proposed | [debug-layer-triage.md](../.spec/proposals/debug-layer-triage.md) |
| WS5 | Claude Code Integration | researched | [claude-code-integration.md](../.spec/proposals/claude-code-integration.md) |
| WS5 | tokenomics-2026 (research) | placeholder | [tokenomics-2026/main.md](../.spec/proposals/tokenomics-2026/main.md) |
| WS1 | Classifier qwen fix | ready to build | [classifier-qwen-fix.md](../.spec/proposals/classifier-qwen-fix.md) |
| WS1 | Classification quality benchmark | proposed | [classify-bench.md](../.spec/proposals/classify-bench.md) |
| WS1 | Data safety: dev hardening + audit log | proposed | [data-safety/main.md](../.spec/proposals/data-safety/main.md) |
| WS2 | Brain Windows Service (systray) | proposed | [brain-windows-service.md](../.spec/proposals/brain-windows-service.md) |
| WS3 | Gospel Engine v1.5 + research rollup | ratified 2026-05-13 — 6 phase files at `scripts/gospel-engine-v2/.spec/proposals/` (1.5a docs → 1.5b–1.5e code+indexer → single reindex → 3-research). Phase 2 TITSW deferred. | [rollup README](../scripts/gospel-engine-v2/.spec/proposals/README.md) · [parent (superseded)](../.spec/proposals/gospel-engine/phase1.5-ergonomics.md) |
| WS3 | Gospel Graph Visualization | proposed (blocked on AGE/PG18) | [gospel-graph/main.md](../.spec/proposals/gospel-graph/main.md) |
| WS4 | study.ibeco.me UI | proposed | [study-ibeco-me/main.md](../.spec/proposals/study-ibeco-me/main.md) |
| WS4 | ibeco.me Security Audit | not started | — |

---

## Recently Shipped (last ~30 days)

| WS | Item | Shipped | Notes |
|----|------|---------|-------|
| WS4 | ibeco.me login loop fixed — RFC 6265 cookie shadowing + 3 adjacent fixes | May 27 | `643574d` (May 24) added `COOKIE_DOMAIN=.ibeco.me` for 1828 cross-domain auth. Existing users with pre-May-24 host-only `becoming_session` cookies got trapped because a host-only and domain-scoped cookie with the same name are TWO cookies per RFC 6265 §5.3 — browser sent both, Go read the stale one first, /api/me 401, frontend loop. Clean browsers + dev (no COOKIE_DOMAIN) worked; only existing users broke. Fix in `074e769`: (1) `setSessionCookie`/`clearSessionCookie` emit host-only eviction when CookieDomain set; (2) LandingView forwards `?redirect=` via computed loginTo/registerTo; (3) auth guard honors redirect for already-authed users hitting /login; (4) shared open-redirect allowlist Go+Vue, 14 test cases. Verified live by Michael. **Secret-leak failure also surfaced**: Dokploy env query exposed POSTGRES_PASSWORD to model context despite skill warning — reactive filtering proved insufficient. Bridge for secret-safe Dokploy access logged as carry-forward. |
| WS4 | ibeco.me Dokploy deploy unblocked — 14-module go.sum sweep | May 22 | `3ba0aef` (May 16) landed deps across 14 Go modules with only `h1:` hashes; missing `/go.mod h1:` companions. Latent five days behind Docker cache, surfaced when Dokploy evicted. Diagnosed → reproduced in `golang:1.25-alpine` → Agans Rule 9 loop closed → Adjacent Surface Audit found same bug in all 14 → swept on Michael's call. Commit `be823c1`: 163 insertions, 0 deletions, 0 `go.mod` changes. Deploy verified green in production. Lesson: docker-layer caching hides go.sum partial-fills until the cache evicts — `go mod verify` belongs in a pre-push hook. |
| WS5 | pg-ai-stewards — YT-T batch (substrate-yt-transcripts) | May 19/20 | Post-council-① wedge after PE-final's yt-gospel-evaluate produced a refusal (bridge had no yt-dlp). Five sub-steps: (YT-T.1) yt-dlp in bridge.Dockerfile via pip install --upgrade — Alpine apk pin to 2024.12.03 was broken on every YouTube video, upgraded to 2026.03.17; (YT-T.2) workspace yt/ rw-mounted into bridge + ro into pg so the SQL function can read; (YT-T.3) `yt_transcripts` + `yt_transcript_segments` schema (D-YTT3 separate FK table for time-range queries; D-YTT4 no pgvector, engrams handle that); (YT-T.4) `stewards.import_yt_transcript(video_id)` reads /opt/yt/yt cache via pg_read_file, populates both tables idempotently; (YT-T.5) Morgan Philpot rerun completed at $0.46 with substantive 16k-char evaluation — 8 verbatim quotes with timestamps, caught 2 factual errors in talk, pushed back on priesthood-gender segment citing Oaks. Bridge crash-loop sub-incident (scratch file in extension/ broke migration discovery) caught + cleaned. Soak resumed. |
| WS5 | pg-ai-stewards — Council ① CLOSED (PE-A + PE-B + PE-C + final smoke) | May 19 | One calendar day, 13+ commits, zero rollbacks. PE-A: 3 new pipelines + studies/AGE promotion + backfill. PE-B: scheduled_pipelines table + plpgsql cron parser + dispatcher wired into watchman tick + ai-news-7am seed (reframed from Rust cron crate to plpgsql to avoid the 86-file replay event). PE-C: full-CRUD /scheduled Vue route + Dashboard scheduled-runs card + NewWork.vue per-pipeline forms. PE-final: all three pipelines (research-summary, yt-secular-digest with Nate B Jones, yt-gospel-evaluate with Morgan Philpot) dispatched + completed + verified + promoted to studies + AGE. Total spend $0.36. One bug surfaced + fixed mid-smoke (yt-* missing auto_materialize_on_verified). Three reframes: D-PE1' Option B, D-PE2' reuse general-research, D-PE7' build missing promotion path. |
| WS5 | pg-ai-stewards — ES emergency-stop arc (ES.1/3/4/5/6/3.s5) | May 15–17 | Bacteriopolis runaway diagnosed + closed: bleed-stoppers, judge-compiled-brief, streaming chat dispatch, fs/PDF/consult follow-ups, gateway upstream-cost capture. ~95 commits, zero rollbacks; the substrate runs a research pipeline clean to a verified artifact (~$0.33). |
| WS5 | pg-ai-stewards — Batches G/H/I/J/K/L/L.1.1 | May 11–14 | File-write mechanism (G), research/planning pipelines (H), agent write-back (I), fan-out + brainstorm (J), engram context compaction (K), Context Engine v2 + v2.1 Judges pattern (L/L.1.1). |
| WS5 | pg-ai-stewards — Phases A–F (agentic creation cycle) | May 10–11 | Watch→Diagnose→Act→Account loop, maturity ladder + gates, intent/covenant, atonement/sabbath/consecration, trust ladder, multi-agent council. |
| WS6 | Study — last-supper-four-cups | May 17 | Passover ↔ the Lord's Supper; the four cups; the bitter cup begun in Gethsemane, finished on the cross. |
| WS2 | brain-steward-cost-discipline | Apr 23 | Three-defect fix: (1) commission `Model` no longer overrides every stage — `modelForStage` helper routes through `config.StageDefaults`; `c.Model` reserved for `EvaluateGate` only. (2) Verify hard-pinned to haiku regardless of catalog. (3) Revise loop capped at 2 → surface `loop_limit_exceeded`. `RevisionCount` field on Commission with DB migration; "Revised X/2" badge on EntryDetailView. Same fix applied to `commissionWaitForExecution` (the loop that actually burned 105 credits). Best-case opus commission ~25 credits (was ~52); worst case ~28-35 (was unbounded, hit 105). Followup Apr 23: research bumped haiku→sonnet per Michael ("stronger model researching is good") — chain still escalates from sonnet, just one fewer step before quarantine. |
| WS2 | brain-model-catalog-sot | Apr 23 | Single source of truth at `internal/config/models.go`: `Catalog` slice + `StageDefaults` map. Two pre-existing drifted maps (`modelCosts`, `AvailableModels`) and `steward.EscalationChain` now derive from it. New `GET /api/models` endpoint; frontend composable + dynamic dropdowns in CommissionDialog and ProjectDetailView inline commission. Default is now Claude Opus 4.7 (7.5×) instead of stale Opus 4.6 (3.0×). Stewardship sweep: same-bug-same-fix on `feedbackDialog` and `executeDialog` modals (UA-stylesheet dark-theme bug from doneDialog fix earlier same day). Note: `/api/models` path collided with legacy LM Studio profiles handler — moved to `/api/models/profiles` (no consumers found). |
| WS2 | brain non-pipeline kanban flow (Phases 1-3 + classify gate) | Apr 23 | Status vocab gained `working` (in-progress lane). `boardColumns` branches on `pipeline_enabled`: manual path uses literal status keys (active/working/done) instead of route-status pipeline. 5-button manual row (▶ Start / ✓ Done / ↩ Reopen / ⏸ Someday / 🗄 Archive) on board + list views. Optional reason dialog on ✓ Done appends a `_Closed YYYY-MM-DD: reason_` line to body. Native HTML5 drag-and-drop between columns (no library dep). Auto-classify gated for non-pipeline projects in relay client. Done-dialog theming fix (centered, larger, dark-theme textarea). `handleCreateEntry` now accepts `project_id` — single-call POST round-trips correctly. brain-app P4 mirror skipped per user direction (no Project surface). |
| WS2 | brain non-pipeline projects (Phases 1+2) | Apr 23 | `pipeline_enabled` column on `projects` (default true). Notebook auto-flipped to false. Single primary gate in `routeEntry`; defense-in-depth in `BuildProjectContext`. UI checkbox + 📓 badge on web. Inverse-hypothesis verified via PUT-set project_id + explicit `/api/agent/route`. |
| WS2 | brain manual stage transitions (Phase 1 mobile) | Apr 23 | brain-app: status dropdown in edit screen, long-press quick-actions sheet (done/park/waiting/archive/reactivate) + undo, semantic chip colors. Phase 2 web: pre-existing — `EntryDetailView.vue` already had full STATUS_OPTIONS dropdown. |
| WS2 | brain status-aware-views ecosystem parity (Phases 1+2) | Apr 23 | ibeco.me `ListBrainEntries` honors `?include_parked=1` (default off). brain-app history `_showParked` covers both someday+archived. Phase 3 cross-surface check pending Dokploy deploy. |
| WS5 | Opus 4.7 harness tuning | Apr 23 | Foresight & Adjacent Surfaces section in copilot-instructions.md, dev.agent.md update, Council Moment extended to dev/debug/ux. Diagnosis + fix for literalism failures. |
| WS2 | brain-status-aware-views (Phases 1-3 + Dashboard) | Apr 23 | All planned phases shipped. Phase 4 (Capture semantic) deferred. Server-side `/api/entries` filter (`?include_parked=1` opts in). Project board toggle relocated to visible header checkbox. Dashboard agent surfaces (routable/review/your-turn) filter parked server-side. |
| WS2 | brain-status-field-on-list-queries (full SELECT audit) | Apr 23 | All 6 list queries now expose `status`: ListAll, ListCategory, ListEntriesByProject, ListByRouteStatus, ListUnassigned, ListPipeline. Future filter UIs no longer guess data layer contracts. |
| WS2 | Brain audit 2026-04-22 | Apr 22 | 69 entries triaged. Personal merged into Notebook. inbox 39→10, status=NULL 96→5 |
| WS5 | Brain↔VS Code bridge Phase 0 | Apr 22 | Schema migration (workstream + proposal_path columns). Read-only inspector at scripts/harness/harness_inspect.py |
| WS5 | cleanup-2026-04 + part2 (all phases) | Apr 21-22 | Spec dedup, 19 proposals archived, workstream taxonomy + frontmatter convention, active.md rewritten |
| WS5 | Voice/bias harness v2 | Apr 21 | em-dash budget, three-beat pivot, refrains, stats cite-count |
| WS3 | engine.ibeco.me Phase 1-3 | Apr 20 | Hosted gospel search. Token UI. First study used it |

*Older items (Apr 4–15: Commission UX, Orchestrator Steward, Commission UI, Brain UX QoL, Brain Pipeline Evolution, Brain Project-Kanban P1–4b) rolled off the 30-day window — recorded in git history + `.spec/journal/`.*

---

## Deferred / Paused

| Item | Revisit When |
|------|--------------|
| Brain UX QoL Phase 8 (auto-commit) | After human-in-loop signal stabilizes |
| Plan 17: Proactive Surfacing | WS2 Phase 3 |
| Plan 18: Widget Overhaul (Ph 3-4) | Agent infra proves out |
| Plan 19: Brain App Ideas | Natural pause |
