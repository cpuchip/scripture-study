# Active Context — the in-flight board

> **2026-06-11/12 — ★ pg-ai-stewards is PUBLIC: spec ratified, Apache-2.0 FINAL, "Anatomy of a Turn" SHIPPED.** `github.com/cpuchip/pg-ai-stewards` → `projects/pg-ai-stewards-oss/` (private substrate stays at `projects/pg-ai-stewards/`). Ratified: **v0.1 = core + persona-host** · clean-room fresh history · **cutover at FULL PARITY (amended 06-12 — one clean cut, no hybrid; gate = virgin boot + coder/UI extracted + 20 mismatches classified + overlay-replay proof + soak; spec §Cutover-parity-gate, `8662448`)** · **Apache-2.0 (`3c43d4e`)** · side-by-side docker dev (`stewards-oss-*`, 55434/8081/8091, own persona keys). **Deliverable #1 landed `0e8c3c9` + order-research update:** `docs/anatomy-of-a-turn.md` — source-verified turn pipeline, 6-beat Remotion storyboard, serial-position section (don't invert; covenant first AND last). P4 = playground machine; P5 = AI-office vision. **P1 UNDERWAY 2026-06-12 (Michael: "Lets kick off P1!"):** workspace overlay repo LIVE (`cpuchip/pg-ai-stewards-workspace`, private — classification + overlays populated) · OSS extension layer EXTRACTED (`3d8229d`) · **virgin CREATE EXTENSION proven on a scratch container** (0 workspace seeds leaked). Lane: `.mind/sessions/pg-ai-stewards.md`.

> **Board discipline (ratified 2026-06-11):** this file holds ONLY what is
> genuinely in flight. The full record lives in `.spec/journal/` (and
> subproject journals); the old banner ledger is preserved verbatim at
> `.mind/archive/active-ledger-thru-2026-06-11.md`. Multi-session
> coordination lives in `.mind/sessions/` (lanes + inboxes — read the README
> there once). **When an arc closes: journal it, then delete its lines here.**
> If an open thread of yours is missing below, re-add it as ONE line with a
> journal link — do not rebuild banners. Target size: a few hundred lines max.

## Open threads

**Workspace / cross-cutting**
- **★ FIRST ORBIT — new KSP-like browser MP game, Phase 0 SHIPPED 2026-06-19 (hubble-frontier-game lane).** Michael's "crazy idea": KSP-1-like + multiplayer, original IP, Google-AI images, Dokploy-deployable; **full stewardship+presidership, hands-off ("I'm letting you make all the decisions").** Title **First Orbit**, domain `orbit.cpuchip.net` (his short-domain call), **2D faithful** (his fidelity call). Set in the **Hubble Frontier ORIGIN era** (marsfield `space-center/docs/ip-bible.md` — the dawn of the Mars Orbital Shipyards; a prequel to his existing IP). Repo `cpuchip/first-orbit` → `projects/first-orbit/` (gitignored). Built on the deadweight harness (Vite+Svelte5+canvas / Node `ws` authoritative / 1 container / Dokploy / `/version` stamp). **★ Crown jewel = `shared/` deterministic two-regime sim** (RK4 powered flight + EXACT analytic Kepler coast → cheap deterministic MP; coasting vessel = f(time), server authoritative over the persistent universe). **Oracle-first honored:** `shared/sim/smoke.ts` 11/11 (roundtrip/energy/period/Hohmann/reach-orbit + **inverse hypothesis** weak-rocket-must-fail), `server/wstest.ts` 7/7. VAB Δv/TWR (Kerbal Engineer baked in) + gravity-turn ascent autopilot (MechJeb baked in) + flight view (true-scale world auto-zoom) + orbital map + warp + shared program + chat. **★★ LIVE at orbit.cpuchip.net 2026-06-19** (Michael: "create the project and deploy now... push as far as you can... and multiplayer!") — Dokploy compose `51xwf0eCPZ1M0bME6Obqb` (proj `6-NRRFHIyHdjncEa_DLxD`, githubId reused `F3xRIOFkUcjxB6DWVkPKW`, auto-deploy on push). **Deploy fix: expose-only, NO host-8080 publish** (collided w/ deadweight's host 8080 → clean build then `compose up` bind-fail; Traefik routes via domain over internal net; empty errorMessage+unreadable log → reasoned from the known-good local run). **★ Phase 0 + Waves B–F all SHIPPED+LIVE (8 commits, `/version`=2f50f88, prod browser-verified):** B patched conics (dominantBody/referenceFrame; Terra↔Luna SOI, land Luna, true-scale bodies) · C economy (milestones/funds/science + "★ first to…" broadcasts + toasts + recovery) · D maneuver nodes (plan Δv/preview dashed orbit/warp-to-node/auto-execute) · E rocket configurator (engine **clusters**/tanks/stages/presets/localStorage; engine-count born from playtest TWR-0.9 fix) · F live MP (standings board · flight-view rendezvous presence · 10Hz interpolation; two-client Alice+Bob verified). Oracles **smoke 19/19 + wstest 9/9** green throughout; each wave browser-driven. **bugs the discipline caught:** degenerate-orbit-at-liftoff (e≈1 false ∞ apoapsis → trust apo only when e<1) · `/version`='dev' (`.dockerignore` ate `.git`) · deploy port-collision · standings board missing `position:absolute` (outside `.hud`, overlapped readouts). **★ port gotcha:** `:8090`=FlexLLama/`:1234`=LM Studio/leftover serves hold ports → a failed-bind server SILENTLY serves the WRONG app (confirm `/healthz`+page `<title>` first). **★★ v1 RUN 2 SHIPPED+LIVE (prod 8cec983, +6 commits, Michael: "keep working toward v1... and have fun!"):** **G4 art** — pg-ai-stewards Gemini key (`AIza…` AI-Studio) WORKS for native image gen; generated logo+planets+part-icons via Nano Banana (~$0.50 of $10), downscaled 14.6MB→859KB (TEMP sharp install, kept out of package.json), wired logo(menu, transparent)+planet-textures(clipped-disk map/flight)+part-icons(VAB); **★ key ALSO has Veo 2/3/3.1 video + Nano-Banana-Pro + Imagen-4 → video needs NOTHING from Michael, just a spend call (~$0.40/s Veo3)**; agy=text-only · **G1 map** scroll-zoom+drag-pan+Follow/Reset+textured marble · **G2 rooms+rejoin** — server=registry of independent universes (per-room Program+file, room-scoped broadcasts); public "frontier"=MMO default + private rooms by code; localStorage callsign+room→"Rejoin"; wstest+1 room-isolation · **G3 MCP buddy** — game read/chat API (`/api/rooms`, `/api/room/:id/state` computed positions+orbits+roster, POST `/say`) + `mcp/` standalone MCP server (list_rooms/room_state/say; smoke.mjs real-MCP-client + curl verified; AI advises via chat, doesn't fly). **smoke 19/19, wstest 10/10.** AI-buddy wire-in: `claude mcp add first-orbit -- node projects/first-orbit/mcp/server.mjs` (NOT auto-added to workspace .mcp.json — standing capability, his call). Key copied to `projects/first-orbit/.env` (gitignored). **lesson:** `const URL` shadows global URL constructor (name locals BASE). **★★★ /goal "GET TO V1" REACHED + LIVE (prod 1beca88, v1.0.0, +7 commits, Michael's punch-list):** **V1-A SAS** heading hold (prograde/retro/radial/target/node — the retrograde-burn answer) · **V1-B burn-timing FIX** (the over-burn is gone: B ARMS → ship points at burn → warps to node → auto-burns CENTRED on node time → tapers to clean stop at Δv=0; arming early just warps, can't corrupt the plan). **★ NEW `burntest` oracle (headless Game driver) caught a latent bug: the client autopilot NEVER auto-staged** → could never reach orbit alone (unnoticed — full ascent=3min, only ever watched 15s); fixed (thrust-on-dry-tank→drop stage), browser-confirmed stage 2/2 · **V1-C right-click target** (browser ctx-menu disabled; right-click body/ship on map→target reticle+dist+rel-speed+SAS-Tgt; Luna read 12,014km/542m/s=correct) · **V1-D multi-node QUEUE** (chronological; each planned on post-burn orbit, chain previewed; arm executes in order; ⇧N add/[/]cycle/Del remove; burntest+3 chained-burns-hit-predicted) · **V1-E onboarding** (Flight Manual overlay auto-on-first-launch+H/?, objectives tracker in HUD) + **v1.0.0**. Oracles **smoke 19 / wstest 10 / burntest 6** green throughout. **★ SCOPE CALL: V1 = polished Terra–Luna game; the interplanetary Mars+first-shipyard arc DEFERRED to its own goal** (real heliocentric restructure: root≠launch-body + solar-scale map + transfer planning — not /goal-rushable; ROADMAP "next major arc"). **★★★ /goal "GET TO v1.5" REACHED + LIVE (prod af1d4cb, v1.5.0, +9 commits — Michael + his SON playing & having fun):** bugs-first: **★ clock-unification fixed "flung into the solar system"** (ship flew on st.t=0 while MAP drew Moon on SERVER clock → see-vs-gravity mismatch; now ALL render on st.t + SOI rings drawn) · burn-line freeze (was recomputing off the live orbit) · MMO hardening (NaN-vessel render guard + server drops invalid vessels on load → prod self-heals + per-player vessel cap) · physics warp 1-4× on ground/atmo (on-rails 10×+ coasting) · flight-view zoom (son's ask) · SAS target-rel (T▲/T▼ rendezvous) · **★ phase-timed transfer planner** ("fly me to the Moon" button; `shared/transfer.ts` phase-angle root-find; oracle: vessel arrives INSIDE Luna SOI — the dashed transfer aims at where Luna WILL be) · economy (part costs/prominent funds/**tech tree** spend-science-to-unlock-tiers/**lunar lander**+legs+parachute; server-charged+validated) · day-night terminator (rotating sun) · game menu Esc (pause private-rooms-ONLY/recover/quit) · **competitive contracts** (comsats/lunar-survey/boots-on-Luna; first-to-claim, server-validated vs real vessels) · **Mission Control** ambient AI flight-director (greets+announces firsts/claims in chat; MCP `say` takes the voice live). Oracles **smoke 22/burntest 6/wstest 13** green throughout. **Svelte gotcha:** `game.*` isn't $state → drive UI off reactive `hud`/`nodeInfo`. **NEXT (his go):** more icons; rival-AI-agency for contracts; interplanetary Mars+first-shipyard STILL deferred (heliocentric restructure = own goal). `ROADMAP.md` = the record. Journal `2026-06-19-first-orbit-game.md`; memory `project_first_orbit_game`.
- **★ Deadweight Acquisitions MULTIPLAYER — FAITHFUL + on the v3→v7 PARITY ROADMAP 2026-06-17 (general-workspace lane).** Dave's (dave-rule namesake) browser space-miner → Michael forked it + ratified competitive last-corp-standing MP + full stewardship + **GOAL: drive to v7 (full parity), autonomous (Ammon), surface only on input-needed.** Node WS server-authoritative multi-corp sim (reuses Dave's Phaser-free worldGenerator; Phaser client = renderer; SP untouched), **LIVE at https://deadweight.cpuchip.net** (NOCIX Dokploy `deadweight` / compose `l4tkfFkX5GvAvNSlmqR3H`). **v2 FAITHFUL economy** (money-gated miners 300cr + base menu + manual sell + storage). **v2.1** right-click fix + Pause/Quit. **v3 DONE** (select→designate default + quick-claim; minimap [DOM canvas]; ship-select EntityPanel; textured planet). **v4 DONE** (named ships Hauler-NN; per-ship cargo upgrades 200/350/550/800; auto-designate toggle — idle miner-haulers auto-claim richest free rocks; browser+prod verified). **★ auto-deploy-on-push WORKS** (Michael granted GitHub-App repo access 06-17; no more manual `compose.deploy`; prod wstest = ground truth). **★ v5a DONE = THE DEEP MINING LOOP** (the biggest change): miners are now a purchased POOL; a hauler CARRIES a miner out + DEPLOYS it at the rock; the deployed miner mines + ejects nets (net-starved backpressure); the hauler SHUTTLES nets to base; miner recovered on depletion. `world.ts` rewritten (SimMiner entities + deploy/collect/shuttle state machine); MinerSnap + new ship phases; client renders deployed miners + nets. smoke+wstest+browser+prod green (deploy+shuttle+tonnage). **★ v5b-1 DONE + LIVE (5f180e2) — read the loop:** click a deployed miner → detail panel (state/nets/host-rock + RECALL = undesignate, keeps the owned miner); **beacons** (net-starved/depleted miners throb a pulsing ring in-scene + amber minimap blip); **net-starved alert** (log line + fleet "⚠ N full") the moment a miner fills. Server change minimal (updateMiner takes corp to log the beacon once); rest client (mpSelectedMiner store + nearestMiner pick + panel). smoke 21/21 (+ beacon-trip + recall) · build/typecheck · wstest 18/18 local+**prod** (53t over wire, bundle content-verified RECALL MINER live) · browser deploy→deliver loop 0 console errs. **★ /goal SET 2026-06-17 (Stop-hook active): drive to v7 DEPLOYED, each phase a tested commit+push, break phases down as needed, surface only on real input-needed; intent = faithful MP of Dave's SP + MP + ease-of-life.** SHIPPED THIS RUN (all prod-verified): **clickable minimap** (`e333810`, ease-of-life — click MP minimap flies the camera, faithful to SP; mpCameraTarget store→scene pan) · **orphaned-net recovery** (`daeb562`, v5b-2a — recall-with-nets leaves salvage [OrphanNetSnap + 'to-orphan' phase], freed hauler auto-recovers; faithful to free-orbit nets) · **company asteroid arrivals** (`d210066`, v7 — Dave's generateCompanyAsteroid; interval BASE→MIN by natural-fraction, capped; gold halo + minimap dot; AsteroidSnap.isCompany; companyArrivalsCount stat) · **Keplerian orbiting** (`9fb0977`, v7 — field drifts ω=ORBITAL_K/r^1.5; miners+docked haulers ride their rock; wstest gained an over-the-wire orbit-drift assert) · **room chat** (`03a839d`, v7 — lobby+match, collapsible bottom-left, server broadcasts color-tagged lines) · **★ build stamp** (`1a76ba2`, DEV INFRA — Michael's ask: `__BUILD_SHA__` git hash on the menu+corner badge + `GET /version` + dist/version.txt; **deploy-verify is now `curl /version`**, definitive even for server-only changes; Dockerfile installs git+un-ignores .git to read the commit then rm's it). smoke **30/30**, wstest **21/21**. **★ stale-server gotcha (kept):** a leftover `npm run serve` on :8080 made orbiting look broken over the wire (EADDRINUSE silent-fail → testing the OLD process); FREE :8080 first (netstat→taskkill) before trusting a local test; the deterministic smoke is the oracle. **★ build stamp = the verify tool (`1a76ba2`):** `curl /version` until it equals the pushed short-hash = definitive deploy-verify (esp. server-only changes); Dockerfile installs git + un-ignores .git to read the commit (rm'd after); deploy now ~2min w/ a brief Bad Gateway during swap. **★ /goal SCOPE DECISION (AskUserQuestion): "Full faithful parity — all of it" (auto-managed).** v6 resource systems = SP manual-mgmt → auto-managed/gentle in MP, but Michael wants them all + VISIBLE (bars + fee credit-sinks). SHIPPED: **(a) hauler fuel+battery** (`a495a2f` — drain thrusting, auto-refuel at base for a distance-scaled fee, never strands; bars in ship panel) · **(b) miner condition+battery+repair** (`2e82f13` — wears on-station so a FAR rock dips below grace [mining penalty] between spaced services while close rocks stay topped; hauler auto-services recharge-free+repair-for-fee; bars in miner panel). smoke **35/35**. **LESSON:** an auto-managed resource stat is invisible unless wear outpaces service cadence → tie wear to deployment-time/distance (what the loop can't hide), not the serviced action. **★★ v7 DEPLOYED 2026-06-17 (`638d953` live, /version-verified) — /goal REACHED, Stop-hook cleared.** Remaining phases all shipped+prod-verified: station-service fees (`7f2c294`) · 2-miner-bay milk-run (`0a73848`) · room persistence — matches survive restart, reconnect-by-name, GC-fix keeps running rooms on empty + TTL sweep (`dc36ec4`) · spectator/camera polish — F frame/C center/auto-frame-on-elim (`638d953`). **★ POST-SHIP FIX (`adfbcc0`): lobby froze on connect** — MpChat auto-scroll was a reactive `$: {...tick()}` reading a bind:this ref → infinite-loops INVISIBLY in prod (no dev guard); fix = subscription + requestAnimationFrame, never `$:` for post-render DOM. Debug recipe: eval→prove main-thread-block, console-marker bisect (froze after `welcome`), disable-components to isolate. Verified live. **13-phase autonomous run, each a tested commit+push; full faithful parity of Dave's SP (deep loop · orbiting · arrivals · fuel/battery · condition/repair · station fees · 2-miner bays) + MP (last-corp-standing · chat · persistence) + ease-of-life (minimap nav · build stamp · beacons · salvage).** **★ PLAYTEST ROUND 2 (`99de0e3` live) — SP-comparison fixes from Michael's feedback:** STARTING_MINERS 0→1 (SP pre-loads a starter miner; money gate now = additional miners) · idle haulers park OUTSIDE the base ring + bigger hull + white outline (were invisible dead-centre on the base disc) · 520-star starfield · SNAPSHOT_HZ 10→20 + **client interpolation** (per-id eased display positions → smooth motion vs raw-10Hz jitter; SP runs 60fps local) · chat moved bottom-left→right-edge-below-standings + collapsed-default (it covered the base menu). smoke 45/45 + wstest 21/21 + screenshot-verified. Comparison screenshots sent. Remaining SP-nicer bit = planet gradient/atmosphere (low pri). **NEXT (Michael's, not blocking): friends-playtest = balance-tuning input.** `ROADMAP.md` = the record. Journal `2026-06-17-deadweight-multiplayer.md`; memory `project_deadweight_game`.
- **★ Garrison (`garrison-cli`) — P0 RATIFIED, council CLOSED 2026-06-18 (general-workspace lane).** The lean, sovereign, local-first coding go-bag (own binary + local model + embedded SQLite ledger; Harness>Intelligence makes a weak 27B model trustworthy). The post-cut gate is now satisfied (substrate cut 06-15) → **P1 is buildable on Michael's go.** All six open questions resolved (full text in `.spec/proposals/sovereign-coding-agent.md` → "Decided in council — P0 CLOSED"): two-model split (FlexLLama-borrowed — `qwen3.6-27b`+`gemma-12b`+`nemotron-4b` on one `:8090` endpoint, **embeddings via LM Studio `text-embedding-nomic-embed-text-v1.5` `:1234`** — Ollama isn't installed) · `Ledger`+`Dispatcher` interfaces (pg/MCP adapters at P4) · terminal `garrison watch` TUI ships with spawn · Tier-2 = P5 own-council, hang-with-consent = reflect-steward approve→queue→drain · `plugin-someday` = luxury MCP client, core MCP-exposable from P1 · dogfood = greenfield Go util (drive pi+qwen baseline first). **Store: FTS5 + chromem-go (RRF) in P1** (Michael's call; not sqlite-vec). **★ P1 UNDERWAY 2026-06-18 (Michael: "create the repo... and start building") — THE FLOOR IS SHIPPED + pushed:** repo `cpuchip/garrison` (private), cloned to `projects/garrison/` (gitignored from root). One OpenAI-compatible Go client (stdlib only) + a CLI proving the model path against LIVE local endpoints — `garrison ping` (chat `:8090` up, all 4 role models served ✓; embed `:1234` up, nomic ✓), `garrison embed` (768-dim nomic vector), `garrison chat` (nemotron-4b 1s + qwen3.6-27b returned a correct iterative fib, 34s). `go build`+vet+real-round-trip = oracle green. Commit pushed to main. **★★ /goal SET 2026-06-18 ("get to the dogfood stage, full ammon loop") — REACHED + verified in one autonomous run.** Drove G1→G5, each a tested commit+push to `cpuchip/garrison`: **G1 ledger** (`Ledger` iface + modernc pure-Go SQLite — work-item hierarchy via recursive-CTE Tree, FK'd messages, cost rollup) · **G2 retrieval** (FTS5 keyword [modernc, trigger-synced] + chromem-go vectors [pure-Go, file-persisted, LM-Studio embeddings] fused by RRF; LIVE fusion test green — vector half finds a 0-keyword-overlap doc) · **G3 loop** (`internal/agent`: Dispatcher iface + LocalDispatcher; forgiving ===FILE=== parser strips `<think>`, path-safe ApplyEdits; Verify oracle [go build+test]; `Loop.Run` read→plan→edit→verify writing every step to the ledger; `garrison run` CLI; mock-dispatcher tests drive real go build/test — pass-1/fail-then-fix-2/blocked) · **G5 DOGFOOD** (qwen3.6-27b wrote package strutil [rune-correct Reverse + WordCount] + table-driven tests through Garrison's OWN loop, oracle-passed in 1 attempt / 1m29s / 4684 tok; **inverse-hypothesis independently re-ran go test → 7/7 incl CJK reversal**; `docs/dogfood-01.md`). **Harness>Intelligence demonstrated on itself.** Deps added: modernc.org/sqlite v1.52, philippgille/chromem-go v0.7. go.work landmine: garrison carries a gitignored `go.work` (use .); the loop runs targets with GOWORK=off. **★ G4 DONE → P1 COMPLETE** (Michael: "lets push and finish p1"): G4a skills-as-data (`internal/skills` — md frontmatter; Select by relevance + always; Render into plan+edit prompts; `garrison skills`; 2 seed skills) · G4b gated exec tool (`===RUN: cmd===`, off unless `--allow-exec`; oracle still gates) · G4c MCP-client stub (`internal/mcp` — JSON-RPC initialize/tools-list/tools-call over stdio; net.Pipe roundtrip test). **DOGFOOD verified TWICE** (strutil + mathx, each 1-attempt oracle-pass; 2nd run skills-active used 6k±1 prime opt; inverse-hypothesis re-ran both). **9 tested commits pushed to `cpuchip/garrison`** (floor+G1+G2+G3+G5+G4a/b/c+docs), all build+vet+test green. **P1 done; NEXT = P2 code oracle suite (operator-authored tests).** Journal `2026-06-18-garrison-council-closed-p0.md`. **Root PUSHED 2026-06-18 (Michael: "lets push") — `.spec/`+`.mind/` Garrison records carried** (ibeco.me unaffected — no `scripts/becoming/` changes). **★★ /goal SET 2026-06-18 ("set a goal to do p2 and p3") — REACHED + LIVE-VERIFIED (autonomous run, 4 tested commits to `cpuchip/garrison`):** P2.1 operator-authored acceptance tests (`--acceptance` copies+protects; model must satisfy, can't rewrite — `splitProtected`) · P2.2 detectors `internal/detect` (gofmt[auto-`-w`]/vet/exported-doc/naked-panic, precision-tuned) · P3 critic `internal/agent/critic.go` (D&C 88:122 — gemma-12b 2nd look after the oracle; VERDICT:APPROVE/REVISE, bounded by MaxCritics; blocks only on explicit REVISE). **LIVE PROOF (medianx):** acceptance test held BYTE-IDENTICAL (model couldn't weaken it), detectors+auto-format clean, critic APPROVED code that clones-before-sort (= the mutation the copy-passing acceptance test couldn't catch). **★ inverse-hypothesis caught 2 real bugs:** (a) STALE `garrison.exe` (`go build ./...` ≠ rebuild binary — need `-o garrison.exe`; cost a wasted run) → ALWAYS rebuild before a live test; (b) gofmt blocked correct code on 'no newline at EOF' (===FILE=== parser strips trailing \n; weak model can't emit byte-perfect) → **gofmt is a FORMATTER not a linter: loop now `gofmt -w` after every edit** (`detect.Format`). `docs/dogfood-02.md`. **P1–P3 COMPLETE; NEXT = P4 (substrate-backed power-up over MCP).** Root records committed, not pushed (Michael pushes root). **★ /goal P3.5 (Michael chose "spawning + garrison watch" — the presiding chain) UNDERWAY 2026-06-18, 3 tested commits to `cpuchip/garrison`:** P3.5a spawning (`spawn.go` — `RunWithSpawn`: decompose into ===TASK=== sub-tasks → child loop per sub-task under the parent work-item [sequential, shared tree] → preside [account for blocked/failed children, D&C 121] → integrate; `runItem` refactor reuses the loop body; `--spawn`/`--max-children`) · P3.5b `garrison watch` (bubbletea read-only TUI tailing the ledger → live presider→child tree w/ status glyphs; `renderTree` pure-tested; `--once` non-tty; `ledger.AllWorkItems`) · robustness (request timeout 300→600s configurable). **STRUCTURE PROVEN LIVE** (a textkit `--spawn` run decomposed into 2 sub-agents + `watch --once` rendered the real parent+2-children tree) BUT **full code-gen demo BLOCKED — FlexLLama rig ~20× slow right now** (qwen 1.4 tok/s, 115s for a 3-word reply; EOF+timeouts); children couldn't finish generating. Unit tests prove the machinery; **full live demo (P3.5c) deferred to rig recovery.** **★ COUNCIL MOMENT scope-expansion 2026-06-19: Michael's vision = Garrison as a full Claude-Code/opencode-like TUI** (modes/flow council→do→critical · live stats [context pressure · time-on-task · cost · flow] · pause&chat to step in when sidetracked · emergency-stop [tool-disable→council→resume, D&C 121] · brownfield+greenfield · tickets · multi-spawn · pg-ai-stewards context tools). Roadmap reorg: **P4 Brownfield (R1-R5: read-contents/SEARCH-REPLACE-edits/retrieval-into-loop/git/resilience)** · P5 the Garrison TUI shell (his flagship, build-TOGETHER, needs his eyes+rig) · P6 context engine (native or borrow) · P7 substrate-MCP power-up · P8 tickets+parallel-spawn+multi-lang · P9 Tier-2 WASM/package. **★★ /goal SET 2026-06-19 (Michael: "set you loose while I sleep") — AUTONOMOUS OVERNIGHT RUN = Phase 4 Brownfield** (R5 resilience FIRST so the run survives rig hiccups [nemotron lockups], then R1→R4; all rig-INDEPENDENT machinery, unit-tested w/ mock dispatcher → flaky rig can't waste the night; live demos opportunistic). Council mode SHIPPED this session (`garrison council`/`chat`-no-arg, /run synthesizes→ratify→build). TUI (P5) = next together-arc. NEXT after P4 = P5 TUI. **★★ OVERNIGHT RUN PROGRESS 2026-06-19 — Phase 4 BROWNFIELD COMPLETE + tickets (7 tested commits to `cpuchip/garrison`, all rig-independent + green):** R5 transient-resilience (`llm.IsTransient` + `Loop.dispatch` retries 5xx/EOF/timeout w/ exp backoff, MaxRetries=3 — runs survive the rig's lockups) · R1 read file CONTENTS into context (budget-capped, binary/size-skipped, re-read each edit iter) · R2 surgical `===EDIT===` SEARCH/REPLACE edits (edit existing files precisely, not whole-rewrite; protected-checked) · R3 relevance-ranked read (files ordered by task token-overlap so the relevant ones make the budget on a big repo; `ReadBudget` tunable) · R4 git `--commit` (`internal/vcs`; stages excluding `.garrison`, diff-stat, commit; off by default) · tickets `--ticket FILE`. **Garrison now edits existing repos, not just greenfield.** LIVE brownfield demo IN-FLIGHT (existing git repo w/ buggy Add returns a-b → surgical fix + --commit; rig slow but R5-resilient). Council mode also shipped earlier (`garrison council`). **REMAINING toward full coder tool:** P5 TUI (modes/stats/pause/emergency-stop — build TOGETHER, his flagship) · P6 context engine (pressure/compaction) · P7 substrate-MCP · multi-lang oracle · semantic/chromem half of R3. Journal `2026-06-18-garrison-council-closed-p0.md` Update 6. **★★ /goal P5+P6 2026-06-19 (Michael: "set a goal and finish p5 and p6, save p7 for when im up") — COMPLETE (6 more tested commits, all rig-independent + green):** **P6 context engine** (`internal/contextx`: `EstimateTokens`/`EstimateMessages` + `Pressure` gauge [used/window/pct/Bar] = the "context pressure" stat; `Compact` summarizes older turns over a token budget via a `Summarizer` — wired into council so long councils stay snappy) · **P5.1 stats** (`agent.Stats`+`StatsFromLedger`: flow[status→council/do/verify] · time · cost · children · pressure — from the ledger, no loop surgery) · **P5.2 control** (`agent.Control`+`BasicControl`: Wait[pause]/Stopped[emergency-stop→cancel+account D&C121]/Injected[steer]; loop checks at each iter boundary) · **P5.3 `garrison drive`** (bubbletea TUI that DRIVES a run: live stats bar + presiding tree + log tail + keys [space]pause/[i]inject/[s]stop/[q]quit; `renderStatsBar`/`latestRoot` pure-tested; interactive shell built-to-spec for Michael's live UX tuning). **Garrison is now ~Claude-Code-shaped: greenfield+brownfield · spawn · council · a driving TUI with modes/stats/pause/inject/emergency-stop.** ~20 tested commits this session arc; README P1–P6. **NEXT (with Michael, when up): P7 substrate-MCP power-up; then multi-language oracle.** Journal Update 7. **★ LOCAL-RIG LEARNINGS APPLIED 2026-06-22** (general-workspace, acting on the pg-ai-stewards doc-construction inbox signal; cpuchip/garrison `0e020dd`, pushed): mapped the substrate's 5 soak learnings against Garrison's real loop code — (1) **borrow the MoE the rig now serves** (default doer/planner `qwen3.6-27b`→`qwen3.6-35b-a3b`; `ping` proved the dense 27b was no longer served → a run would 404; MoE also ~4× faster) · (2) **kill the one-shot trap** (per-iter instruction + feedback strings demanded "re-output the complete files", overriding the already-built `===EDIT===` surgical-diff path → redirected to diffs; `===FILE===` for new files only) · (3) **journal-as-output** (`===JOURNAL===`+`ParseJournal`, captured as a distinct `journal`-role note; **proven e2e on the live MoE — 3 journal notes**) · (4) **honest gauge** (`DefaultWindow` 192k→120k = the `--parallel 2` per-slot reality under `dance-moe`) + README rig docs (per-slot ctx, q8-KV/dedicated-VRAM, manual-host restart). **Already satisfied:** critic reads disk (not a blob). **Surfaced, not built blind:** true source-page-in needs an on-demand `===READ===` tool loop (the naive "only re-echo changed files" breaks stateless-per-iteration dispatch) → scoped for a supervised pass in `projects/garrison/docs/local-rig-learnings.md`. Also gofmt'd Garrison's own source (it wasn't passing its own gofmt detector). Journal Update 8.
- **Root PUSHED 2026-06-12 (`e74e6e90`, 12 commits — Michael's explicit ask, so he can read the preside study before council):** carried the study + lanes + Callie + OSS/book/jumpstart docs. ibeco.me VERIFIED through the rebuild window (200 throughout, no blips; no becoming/ changes in the stack). New commits accumulate unpushed as usual; Michael pushes root.
- **Session lanes are NEW (2026-06-11):** every open session should claim its lane in `.mind/sessions/` on next reground (the UserPromptSubmit hook will prompt you with your session_id). Statusline now shows `⟨lane⟩ … 📬 N`.
- **★ TOMORROW (Michael, not Sunday) — break `becoming`/ibeco.me into its own repo.** Council 2026-06-14: the invariant for in-repo services = own GitHub repo + gitignored-from-parent + independently deployable. gospel-engine (`scripts/gospel-engine-v2`) + strongs already satisfy it; **`scripts/becoming` does NOT** (lives in root history → root push rebuilds ibeco.me + jams the single NOCIX queue, as it did 6.5h on 06-13). Plan: `git subtree split`/filter-repo → new repo (public vs private = his call; public ⇒ Dokploy plain-git-URL, no App auth) → repoint Dokploy `web` → gitignore `scripts/becoming`. Verify first: does becoming bundle gospel-library at build or fetch from gospel-engine at runtime (self-contained?). Caveat: split fixes the *coupling*, not the single-queue jam (separate fix). Webster/byu MCP-server extractions = separate lower-urgency track. **Also pending:** remove the premature `/projects/gospel-engine/` line from root `.gitignore` (gospel-engine stays at `scripts/gospel-engine-v2`, NOT moving) — fold into this topic. gospel-engine HTTP-MCP code shipped+pushed (`ad2c060`); live deploy was the only blocked piece (queue jam, since rebooted).
- **OSS docs landed 2026-06-14 (`842d8a0`, pushed):** `docs/wiring-up-mcp-servers.md` + `docs/personas-and-chattermax.md` + README Documentation table + examples/README currency. Source-verified against 05-mcp-bridge/17-personas/compose/.env.example.
- **★ REFLECT-STEWARD — autonomous stewardship toward intent (DESIGN RATIFIED + P0 DRY-RUN DONE, 2026-06-15).** The substrate's reason-for-being made concrete: "pg-ai-stewards is my LLM knowledge buddy" — one engine, many intents (Vivint first; AI-experiments/fiction-to-audio/Marsfield; **capstone = multi-agent repo sim**). Each intent = a Zion knowledge pool + a back-office reflect-steward (gather→brainstorm→rank→propose→report) + a front-desk chat persona. Ratified: episodic loop (doers hourly / steward 2-3h), 5 proposals/run, ≤$1/cycle post-turn-quarantine (kimi flat-rate $16/mo de-risks it), autonomous-vs-gated line (PR-by-repo-ownership; publish/tool-growth/real-spend gated; kill switch + Claude-watchman wake every 2-5h), governance skills (Ammon/Dave/Ben/stuffy) → substrate. Spec: `.spec/proposals/reflect-steward.md`. **P0: the `planning` pipeline IS the engine** — Vivint cold-start cycle ran e2e → 5 grounded proposals enqueued (report: `.spec/proposals/reflect-steward-p0-dryrun-report.md`). **ALL ENGINE FIXES DONE + fresh-container-verified (OSS `b116ab9`/`dd34715`/`f8ea1bb`):** core-seed generic `research` agent / unify off stewards-explore (+virgin-smoke OK 3b); yt bridge WITH_YT (7/7); **★ Finding B FIXED+PROVEN** (review_plan emitted dead JSON; the verify gate needs `REVIEW: passes` prefix → switched it → fresh cycle hit `verified` + **auto-enqueued 3 proposals, no manual call**, 0 before); research granted doc_search/get/similar + compact_context + memory tools (minus gated base-prompt editor); Finding A `work_item_create` injects `input.today`. The loop closes e2e on its own. **★ GO-LIVE BUNDLE BUILT + PROVEN + pushed (OSS `c285ff5`, new `22-reflect-steward.sql`):** kill switch global (`autonomy_paused` gates scheduled_pipelines_fire + the drain) + per-intent (`reflect_pause_intent` disables schedules + drain skips); **approval queue + capacity-gated drain** — `reflect_approve` QUEUES (no dispatch), `reflect_drain_approved` (each tick) launches oldest-first up to `reflect_max_concurrent`=2 so a batch never floods (Michael's design: approve→queue→capacity); verbs reflect_status/proposals/approve/decline/steer/pause(_intent)/resume(_intent); virgin-smoke OK 7 (fresh-container green, chain 00→22); functional proof on live (pause→0, approve→queued in_flight=0, cap0→0/cap2→1). Check-in skill `.claude/skills/reflect-checkin/`. **Vivint schedule `vivint-reflect` seeded `enabled=false`** (cron `0 */3 * * *`). **★★ GO-LIVE DONE 2026-06-15 (Michael: "set it to auto + kick an immediate run") — Vivint reflect-steward is LIVE + autonomy PROVEN e2e:** schedule `enabled=true` (auto every 3h, `0 */3 * * *`); a clean run (`vivint-reflect-autoproof-1`) climbed raw→researched→planned→**verified** and **auto-enqueued 2 proposals with ZERO manual call** (the full loop: dispatch→gather→record-sources→synthesize→propose→verify→auto-enqueue). Launch run's 4 proposals also landed (project=vivint). Watchman cron `f05ef449` every 3h (SESSION-ONLY — durable not honored; a substrate-internal watchdog is the persistent follow-up). Kill switch `reflect_pause()` live. NOTE: autonomy_paused also governs book/playlist digesters (one global switch). **★ LESSON (recorded → [[feedback_pg_ai_stewards_rebuild_discipline]]):** re-applying a SINGLE consolidated-chain file standalone to live REVERTS later-file-wins functions — re-applying `04` alone clobbered `08`'s maturity-bump `work_item_advance` (launch run stuck at raw/0-proposals); fixed by re-applying `05→22` in order. NEVER single-file-apply to live; apply changed-file→end-of-chain, or rebuild. Repo was always correct (fresh installs unaffected). **★★ LOOP CLOSED + PROVEN SELF-IMPROVING (2026-06-15 PM, OSS `60aedf9`):** two soak-surfaced gaps fixed — (A) research findings now publish to the searchable docs POOL (on_maturity_verified import_doc + project-tag; was file-only → pool never compounded; `import_doc` returns id-not-slug, tag by NEW.slug), (B) `intent_work_survey` tool = the **Council Moment** (Abraham 4:26) — survey already-proposed/in-flight/done before proposing, granted to research, schedule prompt calls it FIRST. **PROVEN:** a fresh cycle READ the pool ("two verified research docs exist: trustpilot, consumeraffairs…") + proposed 5 genuinely NEW frontiers (competitors/installation/pricing/PR-posture/regulatory) — ZERO duplication of the done BBB/Reddit work. 5 prior findings backfilled to the pool (10-14k chars, project=vivint); pool_search returns hits. The loop is whole: research→compounding-pool→survey→non-dup-proposals→synthesis. Every fix fresh-container-smoke-verified (OK7, chain 00→22). **+ KNOWLEDGE POOL (`1a24ac0`):** the intent KB = `docs` (FTS+vector, doc_search is GLOBAL → gatherers do cross-intent meta-studies); + new `intent_source_ledger` dedup (time-aware 10-day) w/ tools `intent_sources_recent` (check-before-crawl, skip-if-fresh) + `intent_source_record` (log-after) granted to research, intent derived from _session_id — builds the pool UP, no re-scrubbing (Michael's "opposite side of the persona"); Vivint schedule prompt teaches the loop. Functional-proven + virgin-smoke OK7 + fresh-container green. Journal: `.spec/journal/2026-06-15-reflect-steward-p0.md` (private). Report: `.spec/proposals/reflect-steward-p0-dryrun-report.md`. **★★ SELF-PRESIDING WATCHMAN GUARD SHIPPED + LIVE 2026-06-15 PM (OSS `082be5e`, new `23-reflect-watchman.sql`):** a deterministic guard wired into `watchman_scheduler_fire` (the bgworker heartbeat — persistent, no Claude session) that watches the steward's OWN autonomous work and auto-pauses the global kill switch on a runaway (in_flight≥8 / 5 consecutive failures / 24h-spend≥$10 / proposals≥50), logging every trip to `reflect_guard_log` (D&C 121 "account for emergency force" — never auto-resumes; a human lifts it). No LLM, no cost. `reflect_status` now surfaces `guard{would_trip,breach,…}` + `last_guard_trip`. virgin-smoke **OK 8 inverse-proven** (force breach → auto-pause + 1 log row + idempotent). Live reading at ship: nominal, but **autonomous spend $8.51/$10 (24h, 85%)** from today's heavy proof runs — Michael's dial whether to bump the cap for the soak. Applied to the live soak via psql (image rebake at cut). Michael loved it: "a presiding agent of sorts."

**D&D holodeck (chat.ibeco.me) — machinery complete, table items remain**
- At Michael's table: `/archive` live-proof (admin-gated) · `/char` panel browser eyeball · **THE FIRST REAL CAMPAIGN**. Callie (née Party, she/her) + DM (he/him) live; deference rule active; Theron Nightwind awaits adoption. Journals: `.spec/journal/2026-06-1{0,1}-*.md`, `projects/pg-ai-stewards/.spec/journal/2026-06-1{0,1}-*.md`.
- Sibling-lane follow-ons: DH-5 "character forge" (parked) · CT2 RUN3 (model-driven, after the handle-UX fix) · roster mood UI (#6) · mid-turn pivot (spec'd only).
- **D&D / storytelling CRAFT RESEARCH — Ammon stewardship run 2026-06-15** (inbox from pg-ai-stewards → personas + the `storytelling` skill group). **17 research artifacts drafted** into `projects/pg-ai-stewards-workspace/research/` (11 skills + 3 personas [gamemaster + 2 PCs] + 3 templates; ledger `research/00-LEDGER.md`); **report DELIVERED to the pg-ai-stewards inbox**. Digested: 6 Mercer GM Tips + 2 storytelling blogs (Harmon circle / Kenn-Adams story spine) + 1 voicing video (Esper/Laban). **★ Findings:** (1) the best DM **presides not compels** (= D&C 121 / Callie-DM deference / voice-michael) → gamemaster persona inherits the presiding covenant; (2) **causal-momentum (`therefore-but-not-and-then`) found in 3 places** (D&D improv spine / Pixar-Kenn-Adams story spine / Harmon circle); (3) **Laban effort system** → systematic voice that maps to text. **Honest /goal counts: G4 17✓ · G5✓ · G1 6/15 · G2 2/5 · G3 1/5** (carry: more GM Tips + actual-play CR/D20/NADDPOD; 3 blogs; 4 voicing). ⚠ **"Aunty Tauny" unconfirmed** — Michael to confirm the channel. Study `study/yt/dnd-craft-01-mercer-gm-tips.md`; journals `2026-06-15-dnd-craft-{mercer,stewardship-run}.md`. Nothing pushed.

**Substrate (pg-ai-stewards)**
- **★★ 2026-06-26 — A2A / OPEN ENGINE PHASE 1 BUILT + virgin-smoke-green + live-proven over MCP *and* HTTP → PR #12 (Michael's merge-Hinge).** The substrate turned outward: agents hand work to agents, the human stops being the hallway. `69-a2a-engine.sql` — `a2a_agents` registry (generalizes lanes) + `agent_notes` (the migrated NOTES inbox) + work_items `a2a_assignee/owner/question` + `origin='a2a'` + the inert `a2a-handoff` pipeline + verbs `a2a_register/submit/inbox/claim/needs_input/answer/receipt/note/note_clear` (the escalation claim-lock, generalized). Surfaced as **MCP tools** (`cmd/stewards-mcp/a2a.go`, with `A2A_MIRROR_DIR` file-fallback) **+ REST** (`/api/a2a/*`, so agy can curl it) + the **drive-the-engine** skill (core skill row AND `.claude/skills/`). Oracle: virgin-smoke **OK 58** (whole loop on a virgin boot; chain 00→69). Live-proven on the dev substrate both surfaces, zero copy-paste; the live-apply caught a real bug the consolidated-04 read missed (`intent_id` NOT NULL, added by 09 — resolve the default intent like work_item_create). Branch `a2a-open-engine`; spec `.spec/proposals/a2a-open-engine.md` (ratified §6); journal `2026-06-26-a2a-open-engine-phase-1.md`. **★ SET UP FOR MY SESSION + LIVE-MCP-PROVEN (post-restart):** rebuilt the local `stewards-mcp.exe` (the `.mcp.json` binary) so the `a2a_*` tools load; added `A2A_MIRROR_DIR=.mind/sessions` to `.mcp.json` (file-fallback on); registered myself as `pg-ai-stewards` (bare lane name, so the mirror lands in the existing `inbox/<lane>.md`). **★ the live MCP surface caught a real bug SQL+HTTP missed:** the SDK reflects `json.RawMessage` as array-of-bytes → a jsonb-OBJECT verb failed output-schema validation. Fixed (`5ce34be`): structured output `map[string]any`, object/array inputs typed. **Drove the FULL say-hello loop through the real fixed MCP server** (python stdio MCP client): register agy → submit → agy inbox(todo) → claim → receipt → my inbox(receipt), every call validated, zero copy-paste. Fixed binary in place; loads natively on the NEXT restart. **CARRY:** an MCP-level stdio smoke is the regression guard (the one surface SQL+curl didn't exercise). **NEXT:** real agy drives it (not a stand-in) → Phase 2 (Agent Card + JSON-RPC + token auth) → Phase 3 (external/deploy). Root skill + records committed (not pushed).
- **★ 2026-06-27 — A2A DRIVEN NATIVELY + the HINGE REVIEWER woken & drained (journal `2026-06-27-a2a-hinge-reviewer-google-convergence.md`).** A2A is live in my own session (registered `pg-ai-stewards`; drove register→submit→claim→receipt natively, mirror fired). **"Can you be woken?" → council-moment found the Hinge reviewer ~70% BUILT** (`39-hinge.sql` + `scripts/hinge-review/`, Phase H #195 — curated `claude -p`, bounds-in-SQL, ran overnight 06-21; nearly speced a duplicate). Became an **amendment**: `70-hinge-decouple.sql` (the reviewer is cloud-Max/rig-independent → `hinge_runs_during_global_pause` lets it work during the GPU pause; a watchman emergency still halts it; matrix-verified + virgin-smoke **OK 59**; chain→70 on the a2a branch). **Drained the 49 backlog at Opus caliber: 49→0 = 20 applied / 28 revised / 1 escalated** (the 1 was a PARSE-FAILURE fallback, not a judgment — the verdict-parse path wants robustness). ~$25 total. **DEFERRED:** the `notify()` transport (Michael leans own-infra brain-app web push, send-only — NO new 3rd party; Pushover/Apprise researched + agy-cross-checked but parked); the daemon running continuously (his hands); widen-to-instruction/judge kinds. Doc: OSS `.spec/proposals/hinge-reviewer-amendment.md`.
- **★ 2026-06-27 — THE FIELD CONVERGED ON OUR THESIS (studies in `study/yt/`).** Michael surfaced Google Cloud Tech's "AI-native databases" talk (w/ Anthropic's MCP creator) + its 47-video playlist (39 digested via a fan-out workflow). Verdict: **"Google is rebuilding, as managed services, what an in-DB runtime has intrinsically — their roadmap is a map of what we get for free."** They put AI *primitives* in the DB; we put the *runtime* in (the moat). **Top steals (study has the ranked 30):** identity-at-transport / **parameterized secure views** (the deterministic data wall — the in-DB answer to OpenClaw), **progressive-disclosure tool catalog** (the fix for the 159-tool gather-hang), risk-tiered action classification on dispatch (Hinge as a graded gate). **Throughline: Google independently stated our covenant** (the eval-gaming guard verbatim; read-before-quoting = HCA's citation-verifier). **OpenClaw** (the 200k★ agent gateway) = the security nightmare (arXiv+Kaspersky) Michael built pg-ai-stewards to avoid — pg-ai-stewards = OpenClaw's reach done send-only/walled. **Meta-fix:** "Search before you ask" now in `copilot-instructions.md` (I asked Michael "what's openclaw" when I had web_search). yt-mcp idea (video+slide-frames→vision) → general-workspace inbox.
- **★ 2026-06-27 — MULTI-TENANCY + THE WORKSPACE-HOST / ENTERPRISE VISION (council-pending; OSS `.spec/`).** From the convergence review, three nested arcs, all captured: **(1) Multi-tenancy spec** (`.spec/proposals/multi-tenancy-and-single-user.md`) + **web-cited Postgres research** (`.spec/notes/2026-06-27-postgres-multitenancy-research.md`) — model (a): shared-schema + `owner_id` + FORCE RLS + **non-superuser `stewards_app` dispatch role** (THE #1 line — a superuser bgworker silently voids every policy) + owner-OR-grant via SECURITY-DEFINER membership fn + `security_invoker` views. **Single-user stays first-class** ("one tenant is the default; the second is the feature" — `current_principal()` falls back to `solo_principal()`, fails CLOSED). Build-the-oracle-first (RLS-leak detector). Pooling-leak is edge-only (bgworker owns its txns). P0 = role+oracle+key (additive); P1 = RLS; P2 = grants; P3 = code-World. **(2) Workspace-Host vision** (`.spec/notes/2026-06-27-workspace-host-and-enterprise-vision.md`) — pg-ai-stewards builds **scoped ephemeral workspaces** (graph orients → clone only the 3–8 repos a task touches on the right branches → `claude -p` codes IN it as freely as Claude here; a human **tunnels in alongside** via VS Code Remote-Tunnels+Dev-Containers, confirmed standard). Resolves the 500-repo/docker problem: **graph in the DB, code in a transient workspace** (never hold 500 live). Two deployments of one engine = his-solo untouched + company-multi-tenant; **group ownership + transfer** + ai-chattermax folds in. Enterprise path = additive (groups/transfer/SSO-at-edge), not a rewrite; nested-org RBAC stays deferred. Playground = our workspace (become/i1828/cpuchip.net/mcp/gospel-engine-v2/scripture-book). **(3) ★ HYBRID RRF — REAL across the doc+world surfaces, BUILT + proven (2026-06-27, `71-hybrid-rrf.sql`, commit `4d1eba5` on a2a branch, CI validating).** `world_entity_hybrid` (57's weighted-linear `0.45·lex+0.55·sem`) → **real equal-weight RRF** (`Σ 1/(60+rank)`, union via FULL JOIN); new **`doc_search_hybrid`** (RRF over `ts_rank` + `embed_query` cosine on `docs.embedding` [vector(768), defined in `schema.rs` not SQL — the grep gotcha]) with graceful lexical-only fallback; **`doc_search_tool` repointed to hybrid** (bare `doc_search` stays FTS for internal callers); misnamed "RRF" comments fixed. **Oracle: virgin-smoke OK 60** (RRF math B,A,C discriminates rank-fusion from score-blend) + **inverse-hypothesis proven live** (real RRF picks B, weighted-linear picks A — methods disagree on the same fixture). **★ Side-quest verdict: the "borrowed weighted-linear from gospel-engine-v1" hypothesis was FALSE** — both gospel engines (v1+v2) were always real RRF; the substrate's weighted-linear was NATIVE, the "RRF" name aspirational (gospel-v2 got a test only, `9de78dc`, unpushed — Michael's call). **DEFERRED (the "full" follow-on):** engram-hybrid (15a vector-only, NO FTS leg → needs a tsvector column/schema) + the auto-chained graph-expand hop. **SURFACED (Michael's call):** `pool_search` (22) is also FTS-only over the same now-embeddable `docs` → could take identical RRF (a new-capability ranking change, so surfaced not silently done). Also: the A2A `a2a_claim(claimer=…)` self-asserted-identity regression gets fixed by the identity-at-transport work; the Hinge parse-failure wants MCP-tools-record-verdict (not parsable text). Council Q4: RLS is a one-way door — ratify the pattern before columns land.
- **★★ 2026-06-26 — THE RIGOR ARC SHIPPED TO MAIN (lane `.mind/sessions/pg-ai-stewards.md` PM(8)→PM(12) has the blow-by-blow; journals `2026-06-26-{orientation-arc, rigor-mode, rigor-v2-vetting-and-resilience}.md`).** A coworker's critique of a substrate research report became a real, shipped feature: **orientation arc** (62/63/64 — autoload skills + `orient_survey` + standing trajectory critique) → **Rigor Mode v1** (65, the chat 🔬 toggle: ground-or-flag / calibrate / observe-vs-recommend) → **v2** (66, the *fidelity* reframe: a resolving citation ≠ a supporting one; contract + trajectory-critic fidelity rubric) → the bugs the live vetting surfaced: **cap raise 12→40** + **force-final-at-cap** (67, the silent-death closed) → **model-fallback hardening** (68, a pulled local model fails over gemma↔qwen instead of 404'ing). **PR #11 (5 commits) MERGED to main, CI green** (after a flaky-rerun). **Honest null result, NOT shipped:** always-on rigor on the doc builder (the `research` builder is already grounded; before/after said it's not worth the cost) → new principle "Vet on Real Data — and Let the Null Result Stand." **CARRY-FORWARDS (none urgent, for the new work Michael named):** carry-through rigor for docs (propagate the chat's rigor flag through start_task, not always-on) · proactive liveness-aware model resolution (don't pick an offline model as primary; reactive failover covers it now) · the premise-neutrality *reflex* (run the question stripped — still a contract rule, not automated) · the deterministic fidelity *oracle* (v2 layer 3, schema-specific overlay) · #249 mid-loop alias MEMBER re-resolution.
- **★★ 2026-06-25 — STEWDIO DETAILS MODE + the RC EXPLORE-REPOS ARC SHIPPED + DEPLOYED LIVE (OSS `0c86d75`→`f61e780`; lane has the blow-by-blow).** Six tested increments in one long run (back half autonomous): **Details mode** (self-healing working-pulse fix + live Activity/token-dispatch stream + `⚙ Dev`→`⚙ Details`; `0c86d75`) · **Activity pane shows NON-LLM work** (Tools & sandboxes = doc-extract/ClamAV/coder calls; `d562b5d`) · **RC-1** explore PUBLIC repos `/explore <url>` (anonymous clone lane, no embed; `9924d9b`) · **RC-2** explore a DROPPED code archive (unpack→sandbox→research_codebase; `6901c3c`) · **RC-3** lift the ~120s import cliff (bridge per-tool `--slow-call-timeout 600`; `f61e780`). **★ the adversarial-QA workflows caught 3 REAL security holes a green oracle missed — incl. a `rm -rf /` blocker (a prompt-injected sandbox id → `/worktrees/..`) that was pre-existing in CloneRepo too** — all fixed in layers + oracle-covered (→ new principle "Every Untrusted Input, Not Just the One You're Thinking About"). DEPLOYED LIVE on dev (grant on pg, bridge rebuilt+refresh-tools'd, schemas+slow-timeout verified). **HIS HANDS:** a model-driven e2e (drop the repo zip / `/explore <url>`). **DEFERRED (named):** true-async corpus import (rare big-DOC case), prune dead opencode free models, Stewdio S6–S9 (#253, superseded). Tasks #258-261 ✅. Journals `2026-06-2{4,5}-*`; memory `project_stewdio`.
- **★★ STEWDIO — the work-item cockpit: P0→P4 ALL SHIPPED + LIVE 2026-06-23 → FEATURE-COMPLETE (Michael: "what was missing from the beginning. holy cow!" → "get p3 and p4 out the door").** A VS-Code-like cockpit in `stewards-ui` (`/stewdio`, dockview-vue): project-filtered work-item/doc browser (left) · artifact-or-live-plan viewer (center) · streaming model-switchable chat (right). The reframe (his): doc + corpus + sessions are facets of a work_item → **"chat with a work item"** + kick off pipelines + watch the plan light up. **P1** = chat-with-a-work-item (read-only `work-item-chat` agent + `dispatch_chat_turn` core migration `45` + `/api/chat/{send,stream}` SSE relay + 3 panels). **P2** = `＋New` launcher + plan=progress (✓/▸/○, polled "· live"). **P3** = book SOURCE corpus (`examples/book-corpus.sql`, sibling of yt-transcripts — chain stays 45): `book_text`/`book_chunks` + `book_search` (FTS+verbatim, granted to the chat agent) + digester persists by page-in handle; book chat now quotes the BOOK verbatim with location (proven On Liberty @ chunk 32). **P4** = layout-persist (localStorage+reset) + multi-session 💬 history + provenance chips + `/api/chat/send` 10s→45s (the compose-latency fix = the old P1 "transient 500"). Spec `projects/pg-ai-stewards-oss/.spec/proposals/stewards-studio.md`; commits `fc32c14`/`630a41e`/`3570055`/`3783333`/`127e31c`/`b2b4b69`. **Follow-ups (none blocking):** book backfill needs re-digest; compose latency; dormant chunk embeddings (no synchronous query-embed in substrate); chatty over-explore; NL-driven launcher. ui rebuilt+live; autonomy paused. Journals `2026-06-23-stewdio-{cockpit,p3-p4}.md`; memory `project_stewdio`. **★ NEW ARC (cont.2): chat Delegate + rich-docs multimodal.** **sub_work_item/start_task SHIPPED** (OSS `47046ad`, chain→46): chat spawns a parent-linked sub work_item (Delegate; parent from `stewdio-<uuid>` session id; proven agent-driven). **RICH-DOCS RATIFIED + spec** (`911d9d8`, `rich-docs-in-chat.md`): attach media as injectable subject material, chat over work_item+media+corpus-lens, render inline, spawn from the combo; P0→P4. **P0 PROVEN** (llama-chip `ab4c9de`): `--mmproj` launch support → gemma-4-26b-a4b read a game screenshot's telemetry exactly, local/$0; rig now serves dance-moe+vision (I own it for now, ~99.7% VRAM). **NEXT=P1** (`content_parts jsonb` + compose_messages passthrough + `vision` alias + image-in-a-chat-turn e2e). Journal `2026-06-23-sub-work-item-and-rich-docs-p0.md`.
- **★★ FEDERATED GPU POOL LIVE 2026-06-22/23 — `llama.cpuchip.net` (hub) + `mesh.cpuchip.net` (NetBird) UP.** llama-chip became a federation: pool GPUs across machines behind one endpoint, hub for tokens+roster, NetBird mesh for p2p data. Peers `fermion` (home 2×4090) + `workchip` (Ubuntu laptop); fermion serving qwen+gemma over the mesh; linux-port merged (laptop runs it); local UI Pool view. Also the deepseek **$0-mispricing cost gap** found+fixed (mispriced→invisible spend AND cost-optimizer prefers it; go=paid/zen=free) + gemma alias→`gemma-4-26b-a4b`. **★ 2026-06-23 REMOTE MANAGEMENT (write half) SHIPPED + LIVE-PROVEN (`7529828`):** any node's UI manages every node over the mesh (`/api/remote?node=&op=` proxy = load/unload/profile/ensure + reads; Pool cards get ＋Load/Unload-all/✕) + node-pinned chat (`?node=` pins to a chosen remote even on a name clash). Oracle green + a scratch zero-model node read+managed the live rig (`?node=fermion` chat → "pong") without touching it. Full detail: lane `.mind/sessions/pg-ai-stewards.md` + journals `2026-06-2{2,3}-llama-*`. His hands (optional): workchip cross-machine test; relaunch rig in own terminal for persistence. **Autonomy PAUSED (Michael's call — GPUs free for innovation week); Michael manages-grant on llama-chip until stabilized.** NEXT (deferred, documented): automatic federated placement (auto-pick a free-VRAM peer).
- **★ LOCAL-MODEL ENABLEMENT + PARALLELISM + DOC-CONSTRUCTION 2026-06-19 (autonomy LIVE on the local FlexLLama rig, $0).** Ran the autonomous loop (books/ai-news/videos/vivint) on local; overnight soak watched via hourly cron + 8am report (244 flexllama chats, $0, quality good). 3 findings: 15-min reaper too short for big local reads (hardcoded); gemma `--parallel 1` **WEDGES** under concurrency; qwen "peg-native format" 500 on grammar-constrained one-shot (videos leg broken). FIXES SHIPPED: **(a) gemma q8 `--parallel 4 @524288` = 4×131k slots, 248 tok/s @ 4 concurrent, fits beside nemotron** (q8 the enabler; one instance beats two — shared weights+continuous batching) — codified ws `b2eb206`. **(b) agentic doc-construction** (Michael's reframe: model BUILDS the artifact via small tool-call diffs, chat output = journal; how the coder works) — Phase 1 `34-doc-builder.sql` (doc_create/append/patch/read/finalize over self-contained doc_drafts; chained lib.rs) shipped+e2e-proven OSS `6145d51`; **thesis proven on qwen** (the broken model: 8 chats/0 errors/0 peg); **playlist-digest RECAST proven e2e on live** (read=gemma emits header-only → build=qwen yt_get+doc_*+`playlist_publish_draft` from the draft handle [never body-arg]; 14 chats+12 tool_dispatch/0 errors, real 7253c digest pooled, journal output; OSS `06282eb`). All 3 findings cleared on the real pipeline. Rig: qwen q8@192k GPU0 + gemma q8 --parallel4 + nemotron q8@512k GPU1. Also: qwen/nemotron WDDM-spill fixes (KV must be DEDICATED VRAM → q8). Plans: OSS `.spec/proposals/{agentic-doc-construction,local-throughput-experiments}.md`; journal `2026-06-19-local-soak-and-doc-construction.md`; watch+findings `.mind/sessions/pg-ai-stewards-soak-watch.md`. **CARRY:** generalize doc-construction → book-digest/research-summary (still one-shot, reaper-prone); re-add a separate critic stage (doc_patch); pgrx image rebuild to bake `34` + sweep inert `doc-build-test` pipeline (FK-pinned); the 5-family local routing is LIVE-ONLY drift → codify as overlay (or keep paid default + local overlay); config-ize the reaper + route the 3 background judges (engram-extractor/judge-brief/watchman-consolidator, hardcoded opencode_go) to local. **★ R1 + FOUNDATION + R2a DONE + PROVEN 2026-06-19 PM (Michael: "build out those plans"; OSS `d38b336`+`b1fd72a` HELD local [public push gated on virgin-smoke], ws `80aea6a` pushed).** Council-read surfaced 2 things the plan missed, both fixed: ① **per-stage sessions** (`wi--<uuid8>--<stage>`) → `doc_drafts` wouldn't cross a stage boundary so a SEPARATE critic couldn't patch build's draft → **foundation: work-item-scoped drafts** (`doc_draft_session_match` on the `wi--<uuid8>` prefix + new `doc_current` tool; keeps cross-WI+persona isolation) — unblocks the critic for all 3 recasts, PROVEN cross-stage. ② **latent double-pool**: `on_maturity_verified` auto-pooled the final-stage output (a JOURNAL for doc-construction) while the publish tool already pooled the real doc → the shipped playlist loop was pooling BOTH a clean `yt-<id>` digest AND journal junk under the work_item slug (project='ai'), real digest un-tagged → **fixed `metadata.pools_via_tool`** (skip the arm) + publish bridges project-tag the canonical doc. **R1 routing codified** (book/curate/playlist name roles in OSS examples + `models.sql` public-default aliases pri 5; overlay recasts planning/research-write/research-summary). **R2a book-digest** recast read(ingest header-only)→build(reason doc_*)→critique(critic doc_current→patch+Tensions→book_publish_draft); *A Modest Proposal* digested e2e on local (gemma+qwen): pooled ONCE `book-a-modest-proposal` project=books 6 sections incl Tensions, **0 double-pool junk**, $0/22 chats/0 errors. Presiding: paused autonomy ~20:33–20:43 UTC (recast wall), 1 controlled run, resumed — accounted. **CARRY (next, fast — foundation in place):** R2b research-summary + R2c research-write (synthesize→build/review→critique→`doc_finalize`; **NEW: doc_finalize project-from-work-item fallback** since research has no static project) + update role-aliases to new stage names; R3 reaper config + judges→local; **the pgrx rebuild + virgin-smoke** (gates the held OSS push) + sweep `doc-build-test`; critique-convergence prompt tuning (critic re-web-searches → slow but found real errors); purge historical `playlist-digest-cron--*` pool junk (Michael's call). Full log: OSS `.spec/proposals/local-learnings-rollout.md` + journal `2026-06-19-doc-construction-r2a.md`. **★★ CARRY-FORWARDS DONE + OSS PUSHED PUBLIC 2026-06-19 PM (Michael: "keep going"; OSS `e8a040c`→`50b5ea5`, ws `558bedf`).** Pool junk purged (20 double-pool docs; canonical 33 yt + 27 book intact). Critique tuning (book+research: work-from-draft, no web re-search → converge). `doc_finalize` project-from-work-item fallback + pools kind `doc` (34). **R2b research-summary + R2c research-write recast** (new `35-research-doc-construction.sql`: gather/build/critique + context_gather/gather/build/critique; doc_* build + doc_finalize critique; auto_mat off + pools_via_tool; gather stages preserved). **Dockerfile bug FIXED** (rebuild gate caught: 34 in lib.rs but never the COPY → image wouldn't build/CI red; added 34+35). **Rebuild + virgin-smoke 20/20 (chain 00→35, OK 20 = doc-construction) + overlay-clobber PASS** (caught cut3's stale on_maturity_verified → mirrored the pools_via_tool guard, ws). Swept `doc-build-test`. **NEW FINDING:** research GATHER stage (gemma, multi-round web) can WEDGE on a big accumulated context (a test run hung 13+min on one gather turn; rig healthy 41 chats done) — the UNCHANGED gather, not the doc-construction recast; self-heals via reaper requeue. **REMAINING (small):** R3 (reaper_stale_minutes Rust config + 3 judges→local, both want the next rebuild — but doc-construction MITIGATES the reaper, low urgency) + the gather-wedge follow (cap rounds / bigger gather model on local) + the live research e2e once gather unblocked (machinery proven by OK 20 + book e2e).
- **★ MODEL ALIASES + INTENT-AWARE FREE/NO-TRAIN ROUTING SHIPPED + LIVE 2026-06-17 (OSS `a0abce9`, ws `fa606b1`, both pushed; pg18 rebaked; tasks #185+#186 DONE).** Michael's teaching/design ask (teach providers → alias table → use zen free models → project/intent override) → he chose "Full." New core **`31-model-aliases.sql`** (chain 00→31): `model_aliases` (logical name → ordered (provider, provider_model) members; `pick_alias_member` = lowest-priority that is configured[`providers_loaded()`]+usable+under-cap+no-train-when-private) + `model_capability.trains_on_data` (per-(provider,model) — opencode_zen mixes free-train+paid-no-train so per-provider too coarse) + helpers. **work_item_dispatch_stage re-authored**: requested model may be an ALIAS (the provider-crossing fallback M.2 same-provider substitution couldn't do — nvidia free kimi → opencode_go paid kimi); a `file_private` intent drops train-on-data alias members + **refuses** a literal train-on-data resolution — **bypassed per-stage by `"public_io":true`** (the gather-is-public escape hatch = Michael's "gather public info on vivint = free, analysis/steward/critic = private," exactly). + re-authored `trigger_log_model_substitution` to skip alias-expansion phantom logs (caught by the live e2e). **★ THE PROBE CAUGHT REALITY:** `qwen3.6-plus-free` (the one Michael was excited about) + `minimax-m3-free` had their zen free **promos END** (HTTP 401 "subscribe to opencode_go"; `/v1/models` still lists them — listing lies, probe is truth) → seeded usable=false; 4 zen free models DO work (deepseek-v4-flash/mimo-v2.5/nemotron-3-ultra/north-mini-code). So the gather alias anchors on **nvidia free kimi-k2.6** (verified, tool-capable) instead. virgin-smoke **OK 18** (00→31), overlay-replay 47/48 (1 = pre-existing vera persona_host gap), clobber **3/0**. **LIVE-PROVEN e2e (inverse-hypothesis):** a throwaway vivint(file_private) `context_gather`(public_io) dispatched to nvidia free moonshotai/kimi-k2.6, torn down clean; vivint analysis stages → opencode_go paid no-train. Overlay (ws): `opencode-zen-free.sql`+`nvidia-provider.sql`(+trains_on_data)+`model-aliases.sql` (kimi alias + repoint book/playlist/curate gather + planning context_gather/explore to public_io aliases). docs/wiring-up-models.md got alias+private-routing sections. **Closes the NVIDIA #185 private guard rail (generalized, not an `if provider=nvidia` one-off).** **★ P1 RUNTIME FAILOVER SHIPPED SAME SESSION (Michael: "lets take that on"; OSS `58d7ac7`, pg18; chain now 00→32) — `32-alias-failover.sql`:** when a provider is UP but fails MID-CALL (Cloudflare 521/522, Anthropic 529), the **steward** walks the alias-dispatched stage to its next untried member. Found+fixed a real bug: `diagnose_failure`'s transient regex matched `5(00-04)` only → MISSED 52x/529 (the actual outage shapes) → broadened to any 5xx+408+529/overloaded+"web server is down". The hook is `steward_tick` not the per-completion trigger (pick_model RAISES for stages-jsonb pipelines w/ no stage_models row → an alias stage otherwise got NO retry); branch added BEFORE pick_model: transient/timeout + stage.model is alias → `alias_transient_failed_members` (from work_queue error history) excludes tried, `pick_alias_member(...,exclude)` next, set model_override+provider_override, re-dispatch (bounded by tried-set + failure_count<3). Mirrors the existing escalation mechanism. **Known limit (documented):** override persists → a mid-pipeline failover keeps the work_item's LATER stages on the chosen member for that run; self-heals next run; clean follow-up = clear overrides on stage advance (improves the existing escalation too). virgin-smoke **OK 19** (00→32; diagnose covers 5xx/52x/529/timeout, exclude skips a tried member, steward walks the failure + logs `alias_failover`), clobber 3/0, applied+verified live. **Gemini answer (Michael's side-Q): `google_gemini` IS wired direct** (loaded, key present, 8 models, 7 auto-probed usable; only `gemini-3-pro-preview`=404 retired-id, correctly walled). Note: Gemini's free AI-Studio tier trains-on-data too → flag `trains_on_data` if ever used for a private intent. Journal: OSS `.spec/journal/2026-06-17-model-aliases-intent-routing.md`.
- **★ PARITY ROADMAP UNDERWAY 2026-06-13 (tasks #161-168; full detail in lane `.mind/sessions/pg-ai-stewards.md`).** Michael: "keep going until parity." Revised parity = #4 + coder enablement + UI/CLI before the cut. **M1 SHIPPED+PROVEN** (OSS `3e5ef66`): yt-mcp opt-in fold (+new `yt_playlist` tool) + the #4 playlist digester — digested a real video off the AI-research playlist e2e; 6-hourly schedule live. **M1+M2+M3+M6-coder+M4-schedules ALL DONE/verified (2026-06-14, Michael fixed the PAT).** **M2 closed: DRAFT PR #1 landed e2e** (token write verified by bridge dry-run first). **M3: the Vue+Go web UI (59 files, 61 routes) is EXTRACTED + RUNNING** (OSS `08850f6`; clean-room single-module; doc_* rename schema-verified incl. the non-1:1 study_search_text→doc_search + scripture_anchor→values_anchor-is-an-intents-col gotchas; all 36 GET routes 0×500). **M6-coder: code-pr works out-of-the-box** (OSS `6078771`; review qwen3.7-max→glm-5.1 + verdict parser start-→line-anchored + pr cap=40) — **proven by a fully AUTONOMOUS no-touch run → DRAFT PR #2**. **M4-scheduler confirmed firing autonomously** (book/playlist digesters hourly). **★ M5 compact_context COUNCILLED + BUILT + PROVEN E2E** (OSS `a8d5cc5`; Michael "I still got time!" → built same session): tools-off compactor JUDGE returns {mute,compress,pin} verdict, substrate applies to caller's session, reversible [COMPACTED] marker; proven on a 14-msg session (compressed the 2 spent dumps, kept the plan, 25s @ $0); pgrx compiles it (pg image built). Judges-not-executors / presiding-recursive. **REMAINING — all need Michael or are polish:** M4-personas ⚠key-safety · M6-tail (stamp default/bgworker-surface/capability-substitution polish + a virgin-smoke assertion for compact_context) · M7 soak · CUT ⚠his Hinge. Journals: OSS `.spec/journal/2026-06-14-{m2pr-m3-ui-m6-coder, m5-compact-context}.md`. **★ 2026-06-15 PM — M4/M6/cut-prep CLOSED (Michael: "do 1-4, hold 5"):** **M4 PROVEN** — "Vera," a Vivint CX-analyst persona, fired on the virgin OSS core + answered a PM question ENTIRELY from the gathered pool (real cites: Trustpilot 3.9/59k, BBB Billing #2 ~2,134/9k, JustUseApp #400 errors) — the P0.5 architecture e2e (gatherer writes pool → host reads → tool-free persona talks); schedules already proven. **M6/parity REFRESHED GREEN + STRONGER** (workspace `a6b3731`): live frozen since 06-12; fn-name-set diff 430 rebuilt vs 325 live = zero uncaptured (all pgcrypto-dropped / study→doc rename / retired-orphan); rebuilt now a SUPERSET; overlay-replay 36/36 on 00→23; P2 coder arm closed. **Sabbath-tension RESOLVED** (OSS `2f43d3b`): `work_item_promote_trigger` unwrapped-PERFORM → WRAPPED (a promote failure no longer aborts an unattended sabbath-completion; matches 22/23 pattern). **CUT-PREP runbook written** (`parity/cut-runbook.md`): ★ gating prereq = ai-chattermax persona-host still dials LIVE → must repoint to OSS bridge before the cut (else chat.ibeco.me personas go dark); + data-carry table + cut sequence + rollback + D1-D4 for Michael. **CUT stays Michael's Hinge (outward-facing now).** Held #5 (becoming/gospel-engine → own repos). **★★★ THE CUT EXECUTED 2026-06-15 PM (Michael: "lets cut over… full send") — OSS IS NOW THE ONE SUBSTRATE; LIVE RETIRED.** Discovery that reshaped it: ai-chattermax's persona-host dials BOTH (wss→chat.ibeco.me + Postgres→substrate); the swap = its `STEWARDS_DSN`. The real blocker wasn't parity — the OSS bridge lacked Michael's domain MCPs. Built **`stewards-oss-bridge:michael`** (workspace `bridge.Dockerfile`: FROM the live bridge image — inherits gospel/strongs/webster/byu/search binaries + webster data + yt-dlp/ffmpeg — with the OSS clean-room substrate binaries swapped in; same Alpine/musl → drop-in; gospel.db via the `/workspace` mount). Sequence: archive dump (5.6M) → 36/36 overlays applied to OSS (persona families/pipelines/14 mcp_servers/intents) → `fiction` agent + missing `persona_host.personas` (chattercode etc.) COPY-transferred live→OSS (overlay `cut1`) → Michael-bridge up (refresh-tools **13/13**) → **persona-host swapped: callie/chip-assistant/dm-assistant/npc-ally/chattercode all connected to chat.ibeco.me off OSS, exactly ONE host (no native exe, no double-fire)** → gospel/search enabled (overlay `cut2`) → **librarian verified e2e (fetched + quoted Alma 32:21 via gospel-mcp)** → `.mcp.json` repointed (55434 + OSS exe; reload Claude to take effect) → **live stopped** (`pg-ai-stewards-dev/bridge/ui`, not removed — `docker start` to recover). Workspace `b57343a`; `compose.override.yml` populated. **Born-clean DEBT:** the Michael-bridge is pragmatically FROM the live image (not a from-source domain-MCP rebuild). **OPEN:** remove stopped live containers + archive volume after OSS soaks clean. The substrate is ONE. **★ POST-CUT FIXES + CAPS 2026-06-16 (the seams the cut opened, all fixed + proven):** ① **stale-overlay-clobbers-core** — overlays authored vs the OLD chronological history re-author functions the consolidated core finalizes LATER → applied core-then-overlay they REVERT core (r6→on_one_shot dropped persona/subagent arms→turns stuck raw→personas SILENT; pe5→on_maturity_verified dropped the reflect pool-publish). Fixed `cut3-restore-core-finals.sql` (manifest tail wins) + **the oracle `parity/overlay-clobber-check.sh`** (diffs fn bodies core vs core+overlays, FAILS on non-allowlisted core re-author; rule in overlays/README) — "overlay-replay clean" = no SQL error ≠ no semantic revert. Lesson → [[feedback_pg_ai_stewards_rebuild_discipline]]. ② **Vera LIVE** (`vera-persona.sql`): pool-reading `analyst` persona in the Vivint room, reads the gathered findings; prompt fixed (answer the specific Q not a canned dump) + **persona-host off-by-one consult fixed** (`dispatch.go`: tool turns span many wq; ConsultTurn read the prior reply — now baselines + waits for THIS turn's). ③ **request_research** (`request-research.sql`): a persona queues research on a pool-gap as an approve-gated proposal → drain → pool → answers next time (front-desk feeds back-office; gated v1, proven w/ Vera). ④ **e2e-turn-loop.sh** — the missing test layer (runs a real turn, asserts auto-verify + diff-Q-diff-A + consult-tracks; catches what smoke/verify can't). ⑤ **Skills proposal** (`.spec/proposals/skills.md`): instruction-modules-as-context-lever + groups + 3-tier catalog (the engram move); awaiting ratify D1-D6. OSS commits `de1fc14`→`e8c9cac`; workspace `b57343a`→`d7ff693`. **PARKED for Michael:** ratify skills D1-D6; `.stewards/` archive-vs-relocate; full code audit (rested pass, #175 sibling); persona-host intent-stamp on dispatch; live-container removal post-soak. Tasks #173/#175/#176 DONE. **★★ SKILLS #174 + CORPUS #178 RATIFIED + BUILT + DEPLOYED-TO-LIVE 2026-06-16 (Michael: "go ahead… excited to see where this goes"):** ⓐ **Skills** = the 3-tier on-demand catalog ON TOP of the pre-existing flat `stewards.skills` (council-moment: the table+body+`skill_permission`+catalog already existed; the one-shot `skill`/`load_skill_tool` fetch coexists). New core `24-skills.sql`: `skill_groups`+`group_family`+`session_skills`/`session_skill_groups`, `render_skills_block` (tier-0 group summary → tier-1 frontmatter → tier-2 loaded body), levers `skill_group_open/close`+`skill_load/unload` (sql_fn, budget-gated **refuse**-over-budget), `compose_tools` re-authored IN 24 (LANGUAGE-sql validates the skill_groups ref at CREATE → can't live in 16), `09` compose calls render (late-bound). First group `storytelling` (overlay, applies_to `fiction`, 6 skills from `.claude/skills/*` + new `therefore-but-not-and-then`). OSS `a9a5fe6`, ws `b0bac11`. ⓑ **Corpus** = pool-publish DECOUPLED from file-materialize in `on_maturity_verified` (moved out of `IF file_destination`; gate = `(auto_mat+file) OR project_association`); new core `25-corpus.sql` = `intent_project_map` + ADDITIVE FK-guarded BEFORE-INSERT fill trigger; overlay seeds map (book-study→books, video-study→ai, general-research→ai) + books↔ai neighborhood (vivint walled); `cut3` mirror updated in lockstep. OSS `225bea9`, ws `1eba541`/`26dc214`. **PROVEN:** virgin-smoke **OK 9 + OK 10** (00→25; tiers+budget; decouple+trigger+FK-safe — the smoke caught 2 wrong assumptions: core HAS 2 skills, projects.name NOT NULL), **clobber-check PASS 3/0** (cut3 matches core). **DEPLOYED LIVE** (one atomic BEGIN/COMMIT psql apply to running `stewards-oss-pg`, no recreate; 4 persona families still compose; **backfilled 9 books→books + 26 yt→ai**; pg18 rebaked). **Follow-ups (P1):** stewards-cli skill import; retire-or-keep one-shot `skill`; broaden storytelling applies_to→`gamemaster`; a persona per pool + `request_research` for loops. Journal: OSS `.spec/journal/2026-06-16-skills-and-corpus.md`. **★ SAME-SESSION REFLECT-PLANNER TIGHTENING (OSS `91e58d0`, deployed live):** Michael caught the planner re-proposing pending/done work. The council survey already surfaced the queue but it's a soft nudge + the only hard dedup was exact-slug. Fixed: ① deterministic near-dup **gate** in `enqueue_proposed_work_items` — `binding_question_overlap` (Jaccard, singular/plural-folded, no ext dep) drops a proposal overlapping a non-terminal one for the intent `>= reflect_dedup_overlap_threshold` (config 0.5; 0.6 would've missed real dups); ② propose_work `Maximum 5→3` (fewer/deeper) + a "gate rejects reworded dups" contract; ③ `intent_work_survey` gains `existing_studies` (pool docs + gists, project+neighbors) so the planner reasons over what we KNOW (11 surfaced for vivint). Honest limit: lexical not semantic (embedding cosine = future). virgin-smoke **OK 11**, clobber 3/0. Also: approved 1 proposal (ran+verified+**pooled autonomously** — live decouple proof) + declined 8 redundant (35→27). **★ SKILLS P1 + CORPUS P1 (persona-per-pool) DONE 2026-06-16:** (a) **Computer/librarian reads the books+AI study pool** (ws `47949ca`: granted doc_*/pool_search + prompt; pool_search project:'books' → books↔ai; gospel path untouched; vivint walled) — corpus decouple CONFIRMED autonomous (books pool 9→15 via the hourly digester). (b) **Storytelling skill group 6→13 + gamemaster = presiding DM** (OSS `257d8a5` + ws `932a359`): digested the general-workspace D&D-craft research (17 artifacts, Michael ratified via ask-tool); core add = skill groups target MULTIPLE families (`applies_to` comma-glob-list, `group_applies`); gamemaster prompt merged to inherit the presiding covenant + keep dnd mechanics + load storytelling skills (granted skill+skill_* — its deny-* had hidden the catalog, e2e-caught). Verified live. **OPEN council item (inbox):** "let the digesters READ our repos" (cross-reference-our-corpus stage; fs-read bind-mount oss repos; dominion_in_council). **★ 2026-06-16 PM — CONTEXT ENGINE TURNED ON + productivity plan + stuck-items diagnosed:** (a) report finding — the context engine's reactive floor ran (29 overflow intercepts) but the AGENT-DRIVEN layer was 100% dark (0/39 `context_tools_enabled`, 0 engrams, 0 self-notes); (b) **enabled context tools on 36/39 agents** (ws overlay `context-tools-on.sql`, `4c0584c`; judges excluded; deny-* agents got explicit context grants; the dangerous `propose_prompt_change` stays off — gated on a separate `self_prompt_on` I didn't touch) — now observe usage + tune (runs the CT2.4 #136 A/B in prod); core default stays conservative-false. (c) **Productivity-surface proposal** (OSS `67a61ce`, `.spec/proposals/agent-productivity-surface.md`): todos/goals as the context-mgmt front-end — a todo IS a working tag w/ a lifecycle (done→`context_mute_tag` auto-fold); reuses tags+session_facets; dominion_in_council. (d) **"stuck" research-write items = un-triaged agent_planning PROPOSALS by design** (31; 0 approved → inert until approve→drain; not a bug); cleaned 1 test artifact (m5-e2e-parent). Backlog = triage/auto-age decision, under guard cap. **★ 2026-06-16 PM — PRODUCTIVITY SURFACE P0 BUILT + DEPLOYED (Michael ratified via council/ask-tool; OSS `55afb09` `26-productivity.sql`, ws `context-tools-on` regrant):** todos+goals coupled to the working-tag lifecycle — a todo IS a tag (`todo_add`→`context_set_tag` active/auto-stamp; `todo_done`→`context_mute_tag` AUTO-FOLD to engram; `todo_reopen`→expand). 6 levers (todo_add/done/reopen/focus/list, goal_set) + `render_agenda` (AGENDA block in compose) + `session_todos`/`session_goals` + `todo_autofold_on_done` config. compose_tools re-authored (carries 24's body) to surface todo_/goal_ on `context_tools_on` agents; deny-* agents granted todo_*/goal_*. Ratified: auto-fold ON, single-active (focus to switch), separate-from-work_items, all-context-enabled. virgin-smoke **OK 12** (todo_add→working_tag; todo_done→muted; reopen→verbatim); clobber 3/0; live-verified (research/gamemaster/librarian see it, judges + propose_prompt_change off). **It's the usage-driver for the now-enabled-but-dormant context engine (#136)** — a todo's completion is the concrete reason to fold. P1=focus/reminders/done-log; P2=promote-to-work_item. Proposal `.spec/proposals/agent-productivity-surface.md`. **★ 2026-06-16 — context-tools/token investigation (Michael: "are the digesters really not using tokens?"):** digesters ARE token-heavy (playlist ~81k in/run, book 33-53k) + the context engine IS helping — all 29 overflow-intercepts are the big READ stages (`wi--*--read`), auto-folded so the fat transcript/book doesn't bloat downstream. Agent-driven levers stay 0 because digests are short one-shots (reactive intercept covers them). **★ FOUND+FIXED a waste bug:** book-digest spun HOURLY on an EMPTY shelf since 05:00 (17 runs, ~178k wasted input tokens) — the `SHELF EMPTY` no-op leaked (auto_advance ran digest/critique/recommend → pooled 17 junk "null-case report" docs into the books pool). Root: empty-shelf no-op was prompt-only (LLM can't "stop"); + the general lane's book-add went to the DOWN 55433, not live 55434. Fixed: (a) **structural guard** `book_digest_skip_empty_shelf` BEFORE-UPDATE trigger cancels a book-digest work_item when read=SHELF EMPTY (OSS `5013452` examples/book-digester.sql; tested live → cancelled, no pool); (b) deleted 17 junk docs (books pool 27→10); (c) queued Poincaré "Science and Hypothesis" on live + re-enabled the schedule. **Follow-up:** playlist-digester likely has the same empty-case bug (add the same guard); the "fold the context/productivity tool catalog for short agents (there-if-needed, ~0 cost)" idea Michael liked — future enhancement, keep tools enabled + measure for now. **playlist-digester empty-guard ADDED** (OSS `af9b0db`, examples/playlist-digester.sql) — cancels on read='NO PLAYLISTS'/'NOTHING NEW' (tested live, both→cancelled; its prompt-threading already kept pool clean, this stops the wasted stages). **+ generalization PROPOSED** (`.spec/proposals/digester-empty-source-halt.md`, Michael's "so it won't happen again"): a pipeline declares `metadata.halt_on={stage,outputs}`; ONE generic core BEFORE-UPDATE guard cancels when the named stage emits a declared sentinel — retires the 2 per-pipeline triggers; new digesters inherit it in one line. dominion_in_council (core change) — offered to build.. **★ DIGESTER-STEWARD P0 — book-study CURATOR SHIPPED + e2e-PROVEN + pushed 2026-06-16 (OSS `38f6c6d`; Michael: "lets council, I want this change… enable by default").** The missing back-office leg (the reflect-steward generalized to a queue): a scheduled tools-on `curate` stage (research agent, kimi-k2.6) that presides over the reading shelf — STEP1 runway (`book_shelf_status` new fn+tool; `queued>=3` → "SHELF STOCKED", feed only when low) · STEP2 survey (`intent_work_survey`+titles, no repeats) · STEP3 pick + **verify findability before book_add** (phantom URL wastes a digest run) · STEP4 dry → `start_brainstorm`. + `book-curate` pipeline + `book-curate-cron` (every 6h, **enabled** — capped by watchman guard + opencode-go spend cap) + dials (`book_curate_runway_threshold=3`/`_max_adds=5`) + research grants. **Proven BOTH branches on live:** feed path added 3 non-dup intent-aligned books (Nicomachean Ethics / Hume Enquiry / Bacon Novum Organum — all real Gutenberg URLs, I confirmed HTTP 200, inverse-hypothesis on the verify-gate); restraint path returned "SHELF STOCKED", added nothing. Two snags: the tool_defs INSERT was 5-col/4-val (added `active=true`); **Moonshot AI [kimi] was DOWN** (Cloudflare 521/522) so I proved the *logic* by overriding the e2e work-item only to qwen3.6-plus (opencode-go flat-rate → ~$0; production stays kimi, retries next tick). The shape is now whole — every intent loop = pool + front-desk persona + back-office steward. Journal: OSS `.spec/journal/2026-06-16-digester-steward-curator.md`. Proposal: `digester-steward.md`. **P1 (ratified, not started):** generalize curator → video-study/ai-news; the generic `digester-empty-source-halt` (retires the 2 per-pipeline triggers); cadence balanced 2026-06-17 (curator→2h, see rotation note below). **★ MODEL ROTATION 2026-06-16 (Michael: "rotate those models up… max is the most expensive on opencode"):** the cheap workhorse `qwen3.6-plus`→`qwen3.7-plus` (newer, same price) across ALL dispatch/selection paths (25 pipeline stages, 15 stage_models, 6 brainstorm metadata, 3 gate-fn hardcodes evaluate_gate/verify_work_item/sabbath_dispatch, 5 escalation rules) + the one critical-review stage `prompt-critic` `qwen3.7-max`→`qwen3.7-plus` (max = priciest opencode tier AND usable=false/gateway-rejected). **★ CAUGHT: qwen3.7-plus had NO price row** → would've blinded the watchman spend cap to the workhorse; priced it (the catch — else the watchman spend cap goes blind to the workhorse). **PRICE CONFIRMED $0.40 in / $1.60 out per Mtok** (opencode zen, Michael's link — CHEAPER than qwen3.6-plus's 0.50/3.00, output ~half; live+repo corrected, OSS `62f9a27`/ws `82ce3aa`). Catalog keeps qwen3.6-plus + qwen3.7-max (valid models); factual comments + "NOT qwen3.7-max (cost)" caveats + non-run verify-* fixtures preserved. Live rotated in lockstep (direct UPDATEs + 3 gate-fn CREATE OR REPLACE from extracted final bodies). virgin-smoke OK 1-12 + clobber 3/0; pg18 rebaked. OSS `73f7c81`, ws `51534d9` (both pushed). **★ book-curate cadence 6h→2h (`0 */2 * * *`, live+repo OSS `62f9a27`)** — book-digest eats ~1/hour so 6h drained the shelf; 2h keeps pace (STOCKED-run = one cheap no-add call). Steward cadence: reflect-steward 3h, book-curate 2h. **★ PROPOSAL WRITTEN — `context_search` (awaiting council, OSS `dc5d106` `.spec/proposals/context-search.md`; Michael's idea: "context isn't durable in a model BUT it is in our system because we curate it — give every agent grep over its context + inject into docs").** A deterministic exact-text search over an agent's OWN durable `messages` (the Ctrl-F a model structurally can't do; the cheap locator that makes `expand_message` usable; sibling of investigate_session). Council refinements captured: tool `context_search`; curated-by-default + **`include_folded`** flag to recover muted/compressed bodies for re-opening (closes the loop w/ productivity auto-fold); scope ladder = D&C 121 walls (own→self→descendants[the watch]→ancestors[private-aware]→global[gated]); per-message **`private` flag** so a parent walls privileged info from children (opt-out-vs-opt-in posture = the one council fork); provenance≠truth (self-recall, NOT a substitute for source verification); snippet+handle results that ride the context engine. **★ RATIFIED 2026-06-17 (council; OSS `ce79e60`): P0 = own (session+self) + descendants (the watch) + a manual session-level `private` flag that BEATS the watch** (a private child is invisible even to its parent — the security primitive: sensitive work on a local non-cloud model walls its context). Upward (`ancestors`) private-by-default + per-message `private` + a `sensitive` intent/agent flag (forces local-dispatch + private) = **P1**; recall-surface (unify pool+engrams) + global tier = P2. The wall is ABSOLUTE (D&C 121 — a parent can't compel it down). dominion_in_council SATISFIED. **★ P0 SHIPPED + LIVE + pushed 2026-06-17 (OSS `de52a24`/`14fce53`, pg18 rebaked): `27-context-search.sql`** — `context_search`(scope session|descendants, curated verbatim+pinned default + `include_folded` to recover muted/compressed, snippet+`[ctx:handle]`, limit) + `context_session_private` wall (sessions.private; beats the watch; absolute) + `context_descendant_sessions` watch (work_items parent_work_item_id+session_ids, excludes private). `context_*` auto-surfaced by compose_tools (no gating change); sql_fn (no refresh-tools). Tool descriptions written to TEACH (adoption) + "what you SAID not that it's true." virgin-smoke **OK 13** + clobber 3/0; live-verified (search→snippet+handle, surfaced to research). **`self` (all my sessions) folded to P1** (needs a session→agent identity map). P1 = ancestors(private-by-default)+per-msg private+`sensitive` intent/agent flag; P2 = recall-surface (unify pool+engrams)+global. **Michael's adoption idea captured (proposal): a per-tool-group usage primer + usage telemetry** (models weren't trained on these tools → if adoption low, teach them; query real usage). Pairs w/ the open "let digesters read our repos" inbox item. **★ GUARD NARROW AUTO-RESUME SHIPPED + LIVE 2026-06-17 (OSS `c1dc09f`, pg18; Michael ratified "narrow resume").** The guard (23) auto-paused tonight on the 24h spend cap (working as designed); Michael bumped cap $10→$12 + resumed, then had the better idea: the guard should RELEASE its own brake when the breach self-clears. `28-guard-autoresume.sql`: `reflect_guard_autoresume_tick()` on the heartbeat lifts a pause ONLY when — pause was guard-set (`reflect_pause_source='guard:<breach>'` marker; human `reflect_pause`='manual' stays manual), breach is SELF-CLEARING (spend|in_flight; failures/proposals stay for a human), no breach currently active, AND metric back under `reflect_guard_autoresume_pct`=75% of cap (deadband, no flap). Logs `action=auto_resumed` (account for releasing the brake too, D&C 121). Re-authors reflect_pause/reflect_watchman_tick/watchman_scheduler_fire/reflect_status (carried 22/23 bodies + marker + autoresume call; status now shows pause_source/autoresume/last_guard_resume). virgin-smoke **OK 14** (spend pause self-heals+logged; human + failure pause do NOT) + clobber 3/0; live-verified. **Spend cap LIVE at $12** (Michael's bump; auto-resume makes it less load-bearing — revert to $10 his call). Journal: OSS `.spec/journal/2026-06-17-context-search-and-guard-autoresume.md`. **★ A+B+hook 2026-06-17 (OSS `30a0991`, ws `4a9878e`, pg18):** removed the stale `.git/hooks/pre-commit` (RW mount auto-materializes). **A `29-intent-private-routing.sql`** = `intents.file_private` + a BEFORE-trigger prefixing `private/<intent>/` to a private intent's file_destination (one choke point; enqueue re-reads the col) — **vivint drops moved to `private/vivint/` + root .gitignore net (`30f384a3`, unpushed); live-verified `plans/x→private/vivint/plans/x`**; overlay `intent-private-vivint.sql` flips the flag. **B `30-tool-primers.sql`** = `tool_primers` + `render_tool_primers` (gated context/skills) injected by compose_system_prompt — the **adoption lever** (telemetry: context_search/todo/goal/skill = 0 uses, models untrained; reactive engine fine). OK15+OK16, clobber 3/0. **Tool telemetry finding (#136 directional):** agent-driven context mgmt ≈0 adoption until taught; reactive floor works. **One-shot Q:** 57 tools-ON (multi-round) vs 24 tools-OFF (one-shot judges). **★ DIGESTER EMPTY-SOURCE HALT — FIXED + LIVE 2026-06-17 (OSS `244ae38`/`6407e42`, pg18; from the morning book-shelf investigation).** Morning dig found book-digest "completed" 6× overnight but the shelf didn't advance — turned out 3 were REAL digests (Science/Nicomachean/Enquiry, no junk — content-gate holds), 3 were SHELF-EMPTY no-ops that **ran all 4 stages despite the guard**, and 2 were 13h zombies (Moonshot-outage hangs, I cancelled them). **Root cause (reproduced):** the per-pipeline BEFORE-UPDATE empty-guards set status=cancelled, but `work_item_advance` still RETURNED the next stage name and the bgworker dispatches off the return → cancel and return disagreed → all stages ran. A BEFORE-trigger can't win that race. **Fix:** moved the halt INTO `work_item_advance` (single advance choke point) — pipeline declares `metadata.halt_on={stage,outputs[]}`; on a sentinel it cancels + RETURNS NULL (no dispatch). book-digest + playlist-digest declare halt_on; 2 per-pipeline triggers retired. virgin-smoke OK17 + clobber 3/0 + live inverse-hypothesis (SHELF EMPTY → NULL/cancelled/stays-read, was: ran 4 stages). = digester-empty-source-halt SHIPPED. Tasks #182/#183/#184 DONE.
- **★ LOCAL MoE DEFAULT + SUBSTRATE-ON-LLAMA-CHIP + STAGE TAXONOMY 2026-06-22 (journal `2026-06-22-llama-chip-substrate-moe-and-vivint-pipelines.md`).** Substrate runs on **llama-chip** (our native Go rig, replaced FlexLLama; `flexllama` provider → `host.docker.internal:8090`, zero churn; proven e2e). reason/critic swapped dense qwen-27b → **MoE qwen3.6-35b-a3b** (DATA-ONLY: model_capability + role-aliases overlays, no bgworker code; ~4× faster; proven), ingest stays gemma-26b-a4b. llama-chip preset **`dance-moe`** (qwen-35b @120k/slot par2 + gemma @213k/slot par2); UI **rig control** on the substrate dashboard (pause/start brain · free-GPUs · state). Fixed **ctx_size=TOTAL** (FlexLLama→llama-chip port had silently halved gemma to 106k/slot). Bake-offs → `projects/llama-chip/docs/model-insights.md` (MoE > dense 3-4×; gemma=ingest, qwen=reason/critic; both cards par-2 = 0.94× true parallelism). **★ STAGE TAXONOMY: the engine is ALREADY data-driven — no stage-name branching in the bgworker → a new pipeline is just a row.** **Vivint reframed = a knowledge assembly line: gather → organize(graph) → analyze; gather+organize are domain-agnostic ("the info part of the brain", shared with book/video/article digesters), analyze is domain-specific.** Spec + Michael design intent (gather: all-sources/incremental/category-gap/**TIME-AWARE**; analyze: do-differently/products-fill-gaps/too-far-or-not) at workspace `docs/stage-taxonomy-and-vivint-pipelines.md`. **CARRY (next build): organize-as-a-stage keystone (test on a book) → vivint analyze pipeline (file_private) → backfill digesters onto shared gather+organize.** **★ OPERATIONAL: llama-chip is a MANUAL process — it dies when the session/terminal closes or on reboot, and the substrate depends on it + dance-moe loaded.** Restart: `cd projects/llama-chip && ./llama-chip.exe serve --config config.json` then UI "Start brain"; before gaming hit UI "Free GPUs" (pause+unload). GPU0 cushion ~478MiB @120k/slot (held under a probe; drop qwen to ~110k/slot if 2 concurrent reason dispatches OOM). Tasks #199-205 DONE. Memory: `project_llama_chip`, `reference_local_moe_for_substrate`.
- **★ PR.1 SHIPPED + live-verified 2026-06-12:** the presiding covenant now rides every dispatch — `covenants.extensions` jsonb catch-all (anti-silent-drop), parser pass-through, presiding render + `The Watch (echo)` in compose_system_prompt (covenant first AND last, per serial-position research). Live smoke `600f6673` ACK'd with presiding terms in the dispatched payload. +~820 tok/dispatch (watch on gate volume). Journal: `projects/pg-ai-stewards/.spec/journal/2026-06-12-pr1-covenant-extensions.md`. NEW gotchas: MSYS path-mangle ate a `psql -f` (use `MSYS_NO_PATHCONV=1`); ledger naming wart (bridge keys suffix-less vs manual `.sql` rows — feeds the migrate-manifest call).
- Carry-overs indexed in `projects/pg-ai-stewards/.spec/open-items.md` + recent journals; notable: 20 live↔repo function-def mismatches (verify-suite, unclassified — NOW on the cutover critical path) · migrate-manifest design call (Michael) · walls-vs-compulsion audit (preside §V) · **compact_context side-quest SEED (Michael's sketch 2026-06-12, do-not-build until council: `.spec/proposals/substrate-compact-context-sidequest.md`)** · trailing-reminder near true context end (proposal-first; provider quirks; may merge with compact_context council) · **jumpstart-eval test lab PLANNED = task #30 (build after P1 starts; absorbs the book-lane kimi/qwen handoff)** · #136 CT2.4 RUN-2 nod · #139 Spin offload = the named next big build · voice-bridge V0 ratify.
- **★ OSS-P1 UNDERWAY (2026-06-12, task #151):** workspace repo `cpuchip/pg-ai-stewards-workspace` created (skeleton + covenant/intent copies + **241-file classification**: 191 core / 17 core-p2 / 28 overlay / 5 mixed / 1 scratch + 33-entry overlay manifest, all overlay migrations populated). OSS extension layer extracted + pushed (`3d8229d`): lib.rs chain reworked (4 seed embeds out), 5 migrations SPLIT (machinery→core, seeds→overlay), 193-entry core manifest, **bundle = build artifact** (stale-bundle lesson). **Virgin boot proven**: docker build green + scratch-container `CREATE EXTENSION` with 0 workspace seeds leaked. FINDINGS: private manifest had drifted (9 entries restored from ledger, root `e5ccc0c3`); the runner is LEXICAL+manifest-blind — replayed `scratch-ct2-run2` into live 06-10. **COUNCIL RATIFIED same day (Michael):** ① codewright-ct2 RETIRED (agent+13 grants deleted live; pipeline row marked retired, kept for FK from 2 completed work items) · ② ledger = **leave-and-map** (never rewrite history; runner normalizes suffix at lookup) · ③ seed pack = **one lineage, ai-jumpstart kit canonical** (workspace layers specifics in overlay) · ④ **`doc_*` RATIFIED** ("lets migrate to doc. doc.") — study_* tools → doc_*, studies → docs, scripture_anchor → values_anchor (rec, unflagged) · ⑤ **cutover = FRESH REBUILD** ("totally okay with rebuilding… if it means its all clean"): OSS stack born clean (doc_* native, suffix-less ledger, NO shims), live stack retires + volume archived; data carries by SELECTIVE IMPORT (corpus/yt/covenant re-import clean; work-item/message/engram/cost history = archive-by-default, Michael's per-category call at cutover planning); parity diff gets a rename map (`pg-ai-stewards-workspace/parity/rename-map.tsv`). **Rebuild-lessons council (same evening): six RATIFIED** — early mismatch classification · verify files → tests/ · `_kind` enum · `stewards.config` · CI day-one (+.gitattributes) · backup story INCL. offsite (pg_dump + pgBackRest/wal-g WAL-archiving tiers) — and **compact_context PULLED IN** (hold lifted; builds in OSS core; parked questions ratify at leg start; trailing-reminder + 2026-research stay held). **RATIFIED: drop AGE** (relational edges; fast-at-scale + multi-tenancy conditions; prior art verified incl. our own gospel-engine) **+ consolidated authored chain** ("dave wins"). **DAEMON LEG SHIPPED (`3561cec`)**: 5 binaries → ONE module github.com/cpuchip/pg-ai-stewards, go.work knot dead, build+vet+smoke green. NEXT (fresh session): the AUTHORING leg — consolidated chain w/ doc_*+AGE-out designed in → runner-consumes-manifest → compose → kit seed pack → compact_context → replay proof. Journal: `projects/pg-ai-stewards-oss/.spec/journal/2026-06-12-p1-kickoff.md`.
- **pgEdge investigated 2026-06-12** (Michael's ask): `external_context/pgedge-postgres-mcp` (depth-1) + org survey. Verdict: different species — their arrow points INTO Postgres (query surface, client-side agent loop, keyword-rule compactor, SQLite chat store), ours OUT (the DB thinks). Learn: packaging (ghcr images + manifests), per-client onboarding grid, security checklist, provider-specific token estimators, LLM-proxy pattern. Full notes: `projects/pg-ai-stewards/.spec/notes/pgedge-comparison.md`; P1/P3 inputs logged in the OSS extraction plan.

**Webster 1828 remediation — tail**
- Published-works audit walk WITH Michael (legs 2+; leg 1 three-glories done + republished) · ~27 OCR-dropout tier words to hand-add. Spec: `.spec/proposals/webster-1828-data-integrity.md`; carry-over: `projects/1828-illuminated/.spec/carry-over.md`.

**Study-tooling suite (NEW 2026-06-13, from the walk retrospective)** — constructor + detector, "build the oracle first." SHIPPED: `scripts/verify-quotes/` (Webster), `scripts/quoter/` (the constructor, v1–v3+promote), `scripts/study-lint/` (scripture-verbatim + link-validate, sharing the quoter's resolver+grammar). Self-validating loop proven. Journal: `.spec/journal/2026-06-13-study-tooling-suite.md`. Spec: `.spec/proposals/study-tooling.md`. **Carry-forward:** 30 deferred verse near-misses (`study/.audit/scripture-verbatim-carryforward.md` + `accepted.tsv`); 376 wrong-depth links (low-stakes, Michael's call); MCP wrappers + talks-source + citation-depth-2 + publish-linkifier (council nod for the write-time MCP). Memory: `feedback_build_the_oracle_first`, `project_quoter_tool`, `project_verify_quotes_tool`.

**Beyond the Prompt (book)**
- **ai-jumpstart** (public: github.com/cpuchip/ai-jumpstart, `projects/ai-jumpstart/`) — the book's companion kit, v0.2.1; appendix/QR wired into the book. **Model experiments CLOSED 2026-06-12 at FIVE passing models** (Haiku/Sonnet/Opus/Gemini 3.5 Flash High/3.1 Pro High; Lap 4 found agy's model lever = settings.json `model`, instrument-verified per run; findings: `experiments/ai-jumpstart/findings.md`). kimi/qwen rounds HANDED OFF to the pg-ai-stewards session (coder-pr machinery — Michael's call).
- ★ v4 audit FULLY CLOSED · **epilogue benediction added + a7ee924 PUBLISHED 2026-06-12** ("Run the loop — endure to the end," 2 Ne 31:20 provenance-verified; 124pp, 0 QR collisions; live on cpuchip.net, byte-verified). **NEXT: voice pass (pass 3)** — walk the manuscript against `docs/voice-profile-michael.md` (8 markers / 8 tells), solo-walk → chat-walk cadence, Michael ratified. Then his manual read → KDP. Remaining gates: optional P2 Oct-5 enrichment · voice-corpora commit call. Journals: `projects/scripture-book/.spec/journal/2026-06-12--epilogue-endure-publish.yaml` · lane: `book-v4-walk`.

**Studies / series**
- **Euclid digestion 2026-06-13** (`study/yt/WGwRCw9TRyo-euclid-walk-by-definitions.md`): the "walk by definitions" — truth.md + Lectures on Faith L1 verified as quasi-Euclidean (definitions → chains → QED → cite-the-warrant catechism); honest seam = Euclidean in *form*, not epistemology (our verification is fruit/Spirit, not deductive certainty). **Euclid = the build-the-oracle-first archetype.** Substrate carry: 5 learning modes (lowest-cost = "cite-the-warrant" linter [next study-linter rule] + Postulates block); proposal-shaped, dominion_in_council → surface at a substrate council. Book → `books/Euclid/` (gitignored). Journal: `.spec/journal/2026-06-13-euclid-walk-by-definitions.md`.
- **Preside study SHIPPED + COUNCILED + ★ COVENANT RATIFIED 2026-06-12** (`study/preside.md`, pushed for Michael's read; council same day): **covenant.yaml `presiding:` extension is LIVE** — preside_under_121 (+ emergency-accounting amendment) · watch_what_you_order (UNIFORM full watching — Michael's call; ladder-depth deferred until a trust ladder is exercised) · keep_the_watch_whole · dominion_in_council · when_presiding_is_broken. First covenant evolution since the teaching extension. **Every session/lane: read the extension on next reground (copilot-instructions now teaches it).** Open follow-ons: walls-vs-compulsion audit of substrate mechanisms (§V). (The substrate side LANDED 2026-06-12 — PR.1 carries the presiding extension into every dispatch; see Substrate section.) Bonus: art-of-presidency's Webster quote audit-verified vs genuine 1828. Journal: `.spec/journal/2026-06-12-preside-study.md`.
- Canon-walk series: **✦✦ D&C walk COMPLETE 2026-06-14** (140/140 — §1→§138 + OD 1 + OD 2; autonomous, multi-context-window, resumed via committed `_progress.md`; `study/dc-walk/`; journal `.spec/journal/2026-06-14-dc-walk-complete.md`; **13 commits `cf36c0b7`…`8f88c498`, UNPUSHED — Michael pushes when he reads**). The Malachi bracket opened+closed the canon (§2→§13→§27:9→§110→§128→§138). Project-source texts landed as scripture (§82:10 covenant epigraph; §121:41 the presiding source; §130:18-19 the copilot-instructions epigraph; §93:24-25 the verification covenant; §58:26 the autonomy frame; §131:7-8 spirit-is-matter). **For Michael (bin-4, his + the Spirit's):** temple/sealing flags throughout; the plural-marriage material (§132 + OD 1) recorded-not-adjudicated; **the priesthood-lineage cross-walk thread CLOSED at OD 2** (the three-walk thread → "all are alike unto God"; restriction history/origins left unweighed). Possible follow-on at Michael's call: synthesis/podcast/teaching pass. · **✦ PoGP walk COMPLETE** (16/16; journal `.spec/journal/2026-06-14-pogp-walk-complete.md`; 2 bin-4 lineage flags → the with-Michael 2 Ne 5:21 study). · **BoM walk COMPLETE.** All three canon walks done; scaffold reusable; Strong's MCP live for the Bible walks.

**Claude Code tooling**
- Plugin-someday: package the covenant-memory + lanes patterns as a shareable plugin (community + Michael's work env). Memory: `project_claude_code_context_plugin`.
- **★ Garrison / `garrison-cli` — sovereign local-first coding agent (PROPOSED + refined 2026-06-13, awaiting council; `dominion_in_council`).** Spec: `.spec/proposals/sovereign-coding-agent.md`. **Name RATIFIED** ("the person who drives it presides" — praesidium, scales down the chain of watches). Lean Go loop Michael owns + local model (qwen3.6-27B class) + substrate principles as safety net (oracle-first verify / council-critic / gated autonomy = load-bearing on weak models, not luxury; Harness > Intelligence is the bet). Architecture: executor owned-and-LOCAL (not Claude Code — survives its loss), substrate governance as engine. **★ 06-14 REFINED — run modes:** standalone (default + the go-bag) = Go binary + local model + **embedded SQLite** (pure-Go modernc; holds the presiding ledger for one Garrison + its sub-agents; NO Docker); substrate-backed (power-up) = optional pg-ai-stewards over MCP for a shared/multi-session ledger + dispatch/council. **Isolated harness** (never depends on substrate). **LM Studio + Ollama BUILT IN** (OpenAI-compatible /v1). Extensions = MCP/JSON-RPC/HTTP/WS + WASM, **NO gRPC**, no native plugin. **Self-extension Tiers 0–3** (build-the-door / hang-with-consent gate; Tier 2 = new dominion → council). Invariant = sovereignty (owned, not rented). **★ Superpower = the presiding chain:** Michael→steward→sub-agents, the ledger (SQLite default / pg shared) tracks all = `watch_what_you_order` given EYES (Batch J work-items + K/L engrams). Post-cut, not now. Separate from stewards-cli. Ties to plugin-someday (luxury client) + ai-jumpstart/book.
